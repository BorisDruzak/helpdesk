# CODEMAP (pc_agent)

Карта кода `pc_client/pc_agent`. Используется для быстрой навигации и поиска (в т.ч. скрипт `scripts/agent_find.py` и контекст агента). Пути указаны относительно корня репозитория (например `pc_agent/ws_agent.py`).

---

## 0. Операционный старт

- Если задача начинается с diff, сначала выполните `python scripts/diff_context.py`.
- Если тема пересекает сервер и агент или неясно, где вход, откройте `docs/QUICK_LOOKUP.md`.
- Для точечного поиска по агенту используйте `python scripts/agent_find.py "<ключевое слово>" --dir pc_agent`.
- Если тема касается observer, module breadcrumbs, update trace или action-trace bridge, дополнительно откройте `server/docs/OBSERVER_LAYER.md` и `server/docs/OBSERVER_AUTHORING_RULES.md`.

### Truth baseline

- Корневой pytest-контур: `pytest.ini` (markers `unit`, `integration`, `manual`, `no_db`).
- Agent runtime/tests use package imports (`pc_agent.core.*`, `pc_agent.modules.*`, `pc_agent.ui_gui.*`) so root `python -m pytest --collect-only` can collect server, agent and scripts together without top-level `core` / `modules` collisions. Managed external modules may still use legacy `modules.base_module` / `core.registry` only through the dynamic loader path; `pc_agent/core/loader.py` snapshots and restores legacy top-level imports and `sys.path` so the compatibility boundary does not leak into mixed root collection.
- Agent baseline: `python -m pytest pc_agent/tests -m "not manual"`.
- Exploratory suite `pc_agent/tests/test_support_chat_reliability.py` помечен как `manual` и не входит в обычный CI.
- Для совместного server+agent baseline используется `scripts/run_ci_suite.py`.

| Сценарий | Открыть сначала | Затем |
|------|------------------|-------|
| Handshake / outbox / ACK | `pc_agent/ws_agent.py`, `pc_agent/ws_agent_runtime_helpers.py` | `pc_agent/core/sender.py`, `pc_agent/docs/PROTOCOL_V3.md` |
| `run_tool` / команды | `pc_agent/core/orchestrator.py`, `pc_agent/core/orchestrator_collect_helpers.py`, `pc_agent/core/orchestrator_job_helpers.py` | `pc_agent/core/registry.py`, `server/tools/service.py` |
| Auth / bootstrap | `pc_agent/core/identity.py`, `pc_agent/core/machine_identity.py`, `pc_agent/core/device_fingerprint.py` | `pc_agent/auth/token_source.py`, `pc_agent/auth/connection_request.py`, `pc_agent/docs/AUTHENTICATION.md` |
| Self-update / launcher / rollout | `pc_agent/docs/AGENT_UPDATE_WORKFLOW.md` | `pc_agent/docs/SELF_UPDATE.md`, `pc_agent/launcher/installer.py`, `server/docs/AGENT_UPDATES_API.md` |
| Always-on / tray / runtime logs | `pc_agent/docs/AGENT_RUNTIME_ALWAYS_ON.md` | `pc_agent/ws_agent.py`, `pc_agent/core/runtime_logging.py`, `pc_agent/ui_gui/main.py`, `pc_agent/ui_gui/tray_manager.py` |
| GUI / `ui_bridge` | `pc_agent/ui_gui/main_window.py` | `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_bridge/api_server.py`, `pc_agent/docs/AGENT_RUNTIME_ALWAYS_ON.md` |
| Модули / registry | `pc_agent/core/module_manager.py` | `pc_agent/core/loader.py`, `pc_agent/docs/MODULES.md` |

---

## 1. Точки входа

| Файл | Назначение |
|------|------------|
| `pc_agent/ws_agent.py` | Основной runtime: WS-соединение, handshake, команды, UI bridge; auth/connection orchestration (через state machine), Scheduler RPC + runtime loop; now runs as always-on process with sticky `connection_state`, runtime diagnostics/status/log tail callbacks для `ui_bridge`, server-driven update recommendation cache (`is_release`, `release_channel`, `recommended_version`, `update_available`) и local trigger recommended update через обычный server update flow; runtime status now also overlays `pending_update_*` and `update_request_*`, suppresses `update_available` while a request is already in flight, and keeps the GUI aligned with launcher-side pending state; GUI закрытие больше не считается автоматическим shutdown; при auth bootstrap умеет fallback lookup токена по `machine_id -> install_id -> legacy uuid`, after explicit auth rejection (`4003`, invalid-token, token-required) переводит и GUI, и headless режим в automatic reprovision, while transient WSS handshake failures such as proxy `502` stay in reconnect/backoff and must not crash the agent; long-running `run_tool` / `call_tool` dispatch теперь уходит в background tasks, чтобы агент мог принять `cancel_operation` без блокировки WS loop, `handshake_ack.server_capabilities` переключает capability-gated `outbox_items_batch` transport, `handshake_ack.payload.registration` сохраняет локальный статус регистрации пользователя без блокировки агента, successful authenticated sessions upload bounded `agent_observer_batch` telemetry from local action trace with a durable cursor, and the canonical `device_id` is passed into `AgentOrchestrator` so module/toolset lifecycle device events can be emitted in the real runtime |
| `pc_agent/ws_agent_runtime_helpers.py` | Вынесенные runtime helper-блоки `WSAgent`: restart/update-shutdown, scheduler RPC/runtime loop, auth bootstrap, reprovision/request-connection flow, форматирование uptime |
| `pc_agent/launcher/launcher_main.py` | Launcher / запускные сценарии; applies pending updates, records failed launches, prunes old version directories after successful publish, and rolls back `current.json` to `previous` after repeated immediate crash of a newly switched version |
| `pc_agent/launcher_portable_main.py` | Портативный launcher; auto-detect install/data roots рядом с exe, импорт токена из primary `%LOCALAPPDATA%\\PCClientAgent\\data` для локального Windows теста и rollback на `previous` при repeated immediate crash новой версии |
| `pc_agent/ui_gui/main.py` | Запуск Qt GUI, lifecycle окна, minimize-to-tray, start-hidden, явный exit path и cleanup локальных SSE/API ресурсов; before showing the main window, validates the stored machine token through `/api/registry/agent/account-state` and deactivates stale local tokens that return HTTP auth failures so account login is not shown on top of a broken technical agent authorization; installs the optional-by-env `GuiPerformanceProbe` field diagnostic for focused-window CPU investigations; локальные lifecycle/debug сообщения должны оставаться читаемыми русскими строками без mojibake |
| `pc_agent/build_windows_release_v2.py` | Каноническая Windows release-сборка: launcher.exe + versioned agent layout + update ZIP |

---

## 2. Структура каталогов (ключевые файлы)

### 2.1 Ядро (core/)
| Файл | Назначение |
|------|------------|
| `pc_agent/core/orchestrator.py` | Фасад orchestrator-команд: dispatch, run_tool, collect/list/update, lifecycle операций, orchestration над `ConsentService`, кэш модулей; collect/job/uptime ветки полностью делегированы в helper-модули, cancel-operation flow доведён до канонического agent-side terminal результата и ticket event publish; update path is single-flight over `pending_update.json`, writes per-operation download artifacts, and returns `"scheduled"` only after shutdown-for-update is truly armed; `run_tool`, `list_tools` и `describe_tool` теперь резолвят канонический semantic tool id и legacy aliases через текущий registry без отдельного V2-контура, а `list_tools` сохраняет registry contract fields (`output_contract`, `presentation_schema`) для server capability projection и Tool Output Presentation Builder; module lifecycle rebuild emits `tools_changed` only when the computed toolset hash changes and emits `module_state_changed` from install/activate/deactivate/remove/rollback using the canonical device id; observer drilldown получает guaranteed action-trace breakdown по `module.resolve` и `module.execute` даже для “молчащих” модулей; server `trace_id` прокидывается в orchestrator/module action trace для точной diagnostics bundle корреляции; execution path теперь lane-aware: risky/stateful tools serialise through a single execution lane, while safe read-only tools keep limited parallelism |
| `pc_agent/core/recipe_runner_bridge.py`, `pc_agent/modules_packages/agent_recipe_runner/*` | Declarative Agent Recipe Runner. `run_recipe` is a Protocol V3 command handled by orchestrator and delegated through `RecipeRunnerBridge` to the active protected managed module `agent_recipe_runner`; the core only resolves/version-checks/delegates and returns ToolResponse-compatible payloads. The managed runner exposes `describe_primitives`, `validate_recipe`, and `run_recipe` for read-only primitives; primitive descriptors now include `presentation_schema`, and recipe results use a composite presentation schema; it does not expose arbitrary shell/script execution or remediation. |
| `pc_agent/core/action_trace.py` | Локальный JSONL action trace для tech drilldown и поиска по `trace_id` / `ticket_id` / `operation_id` / `tool_name`; writes use redaction helpers and an in-memory sequence counter after startup initialization, so GUI polling does not rescan `action_trace.jsonl` on every trace record; also exports compact upload-ready `agent_observer_events` with stable sequence/event ids for startup/reconnect/update/tool/module telemetry; observer expansion rules описаны в `server/docs/OBSERVER_AUTHORING_RULES.md` |
| `pc_agent/core/orchestrator_collect_helpers.py`, `pc_agent/core/orchestrator_job_helpers.py`, `pc_agent/core/orchestrator_shared.py` | Helper-слой распила orchestrator: каноническая collect/job/uptime логика и общий mojibake-safe logger; в `orchestrator.py` не должно оставаться дублированных тел этих handler-веток |
| `pc_agent/core/consent_service.py` | Отдельный lifecycle consent (`WAITING_USER/APPROVED/REJECTED/EXPIRED`) поверх `pending_consents` |
| `pc_agent/core/database.py` | SQLite (data/storage.db), outbox, seq, idempotency, consent, scheduled_tasks, DB_SCHEMA_VERSION |
| `pc_agent/core/sender.py` | WSOutboxFlusher: доставка outbox, ACK/NACK, retries; если сервер объявил `outbox_batch_v1`, flusher может отправлять несколько `outbox_item` одним `outbox_items_batch` frame без изменения per-item ACK/NACK semantics; Protocol V3 event kind is sourced from `device_seq` XOR `agent_seq`, and local SQLite compatibility `ticket_id=device_id` for device events must not be serialized as top-level or payload `ticket_id` |
| `pc_agent/core/identity.py` | Каноническая identity агента: `machine_id` -> `device_id`, вторичный `install_id`, миграция legacy `identity.json`, handshake metadata включая `device_fingerprint` |
| `pc_agent/core/device_fingerprint.py` | Best-effort hashed hardware proof для provisioning/handshake: Windows default path avoids WMI/PowerShell by using MachineGuid, WinAPI boot volume, CPU signature and MAC hashes; optional baseboard WMI is gated by `PC_AGENT_ENABLE_WMI_FINGERPRINT=1`; raw serial/MAC не отправляются |
| `pc_agent/core/machine_identity.py` | Разрешение стабильного `machine_id` из OS/runtime (Windows MachineGuid, Linux machine-id, env override, fallback file) |
| `pc_agent/core/module_manager.py` | Установка/удаление/rollback модулей, semver, инвентарь |
| `pc_agent/core/loader.py` | load_module_from_path (modules_store), сброс кэша импорта |
| `pc_agent/core/registry.py` | Дескрипторы инструментов, canonical tool id/alias → runtime method, `call_tool()`; public tool name может быть semantic key (`dns.resolve`), а legacy `module.tool` остаётся alias; `@exposed_tool` прокидывает `output_schema`, `output_contract`, top-level `presentation_schema` и additive capability metadata (`execution`, `deployment`, `safety`, `readiness`, `evidence`, `artifacts`) в tool specs |
| `pc_agent/core/tools.py` | Agent-side tool spec helpers над shared contract layer; legacy risk aliases живут здесь только как compatibility shim |
| `pc_agent/core/policy_engine.py` | Политики выполнения |
| `pc_agent/core/artifacts.py` | Артефакты (screenshot/record) |
| `pc_agent/core/recording_controller.py` | Управление записью |
| `pc_agent/core/job_manager.py` | Джобы |
| `pc_agent/core/runtime_logging.py` | Единая настройка runtime-логирования: loguru sink, rotation/retention/compression, чтение tail для diagnostics |
| `pc_agent/core/runtime_paths.py` | Пути данных/модулей |
| `pc_agent/core/user_profile.py` | Локальное хранилище подтверждаемого пользователем профиля регистрации (`data/user_profile.json`): sanitize/max-length validation, load/save/clear, default OS login/hostname hints and registration status/claim cache without collecting extra PII silently |
| `pc_agent/core/account_session.py` | Локальная requester/account session (`data/account_session.json`) поверх технического machine-token: stores server-issued `session_id`/optional `session_token` for confirmed binding, server-issued pending registration or verified other-account mode, sanitized load/save/clear, reads confirmed person fields from both top-level server session payloads and nested `session.person`, can enrich already saved sessions from fresh account-state, matches local sessions only by server session id/current account-state instead of legacy binding-only heuristics, refuses local-only pending sessions as a valid login, preserves base registered binding context for other-account ticket marking, stores pending other-account login request state for account-gate polling, and classifies `ACCOUNT_SESSION_*` / `ACCOUNT_ACCESS_DENIED` errors for the GUI recovery path |
| `pc_agent/core/single_instance.py` | Один экземпляр приложения |
| `shared/tool_contracts.py` | Общий vocabulary для server+agent: canonical risk/lifecycle/error/artifact/dependency/execution-envelope models |

### 2.2 Модули (modules/)
| Файл/каталог | Назначение |
|--------------|------------|
| `pc_agent/modules/base_module.py` | Базовый контракт модуля + mandatory observer SDK (`bind_trace`, `trace_event`, `trace_span`) для новых tool methods; dangerous module steps обязаны следовать `server/docs/OBSERVER_AUTHORING_RULES.md` |
| `pc_agent/modules/impl/` | Встроенные реализации модулей; core enabled modules are `system`, `screen`, `diag_logs`, `inventory` and `presence`; `system.collect`, `inventory.collect`, `presence.collect`, `screen.collect`, `screen.record` and `diag.logs.collect` expose `agent_builtin` capability metadata; `screen.record` captures bounded ffmpeg stderr, retries live Windows raw-pipe encoder failures through a temporary PNG frame-sequence fallback, preserves the `screen_recording` artifact contract and cleans fallback temp frames; `system.collect` declares a Tool Output Presentation Schema v1 layout and `output_contract.device_card` hints, while `inventory.collect` returns the normalized privacy-safe `device.inventory.snapshot` used by device card slots with v2 printer details, bounded real `processes.items`, static key-app profiles kept for compatibility, optional hardware identifiers and detailed output/presentation schemas; `presence.py` returns coarse `device.presence.snapshot` state/daily counters without content capture; `inventory_profiles.py` owns the trusted key-app profile registry; declarative inventory/presence schemas live only in `shared/builtin_tool_descriptors.py`, and collectors import those constants instead of carrying local descriptor copies, so server fallback does not import collector runtime code; `diag_logs.py` collects system logs and agent runtime logs (`data/logs`) through `diag.logs.collect`, marks `logs.bundle` endpoint evidence as passport-eligible and produces `logs_zip` artifacts |
| `pc_agent/modules/dynamic/` | Legacy (загрузчик использует только пакеты из modules_store) |
| `pc_agent/modules/__init__.py` | extra_paths, поддержка test_<name> для тестов |

### 2.3 Сеть и загрузки
| Файл | Назначение |
|------|------------|
| `pc_agent/network/uploader.py` | Загрузка файлов/артефактов на сервер |

### 2.3.1 Аутентификация и bootstrap подключения
| Файл | Назначение |
|------|------------|
| `pc_agent/auth/token_source.py` | Источник токена: `AUTH_TOKEN` → `auth_tokens` → optional GUI callback; controlled migration локального токена с legacy install-based ID на canonical `machine_id` |
| `pc_agent/auth/connection_request.py` | Connection-request flow: POST/GET polling, approve/reject и события в event_bus; stores server-issued `request_id` + `poll_secret` in memory for protected manual status polling/heartbeat, recreates the request on `POLL_SECRET_REQUIRED` / `INVALID_POLL_SECRET`, различает обычный reject, `DEVICE_ARCHIVED` и `TOKEN_LIMIT_EXCEEDED`, пишет `connection_request_error.json` для GUI при блокировке сервером и не логирует raw token или poll secret |
| server observer coupling | Server-side auth/provisioning events are written to `agent_runtime_audit` and projected as operation-less observer traces with `root_kind=device_provisioning` / `agent_auth`; diagnose with `/api/admin/tech/observer/search?q=connection_request` or `q=invalid_token` |
| server explainable observer detail | Admin trace detail is now explained server-side through `/api/web/admin/observer/traces/{trace_id}` / `trace-detail/{trace_id}`. Agent-side action trace and toolset snapshots remain the source for tool/module metadata, but the UI reads normalized `explanation`, stage labels and failure-stage flags from the server boundary instead of interpreting raw agent spans directly. |
| `pc_agent/auth/gui_auth_state_machine.py` | Явные состояния GUI-авторизации (`NoToken -> RequestSent -> Polling -> TokenReady/WsConnecting`); transient blocks `DEVICE_ARCHIVED` и `TOKEN_LIMIT_EXCEEDED` не превращаются в вечный локальный `connection_rejected.flag` |
| `pc_agent/auth/rejected_flag.py` | Путь к локальному флагу `connection_rejected.flag` |

### 2.4 UI bridge (core ↔ GUI)
| Файл | Назначение |
|------|------------|
| `pc_agent/ui_bridge/api_server.py` | Локальный API для GUI (SSE/HTTP), settings + local agent control/diagnostics (`/ui/agent/status`, `/ui/agent/logs`, `/ui/agent/shutdown`, `/ui/agent/update`) |
| `pc_agent/ui_bridge/event_bus.py` | События между core и GUI; хранит sticky `connection_state` для поздних SSE-подписчиков |
| `pc_agent/ui_bridge/models.py` | Модели данных для UI |
| `pc_agent/ui_bridge/settings_service.py` | Настройки для GUI; `GET /ui/settings` отдаёт эффективные `enabled_modules`, read-only `core_enabled_modules` и исходные `configured_enabled_modules`, чтобы локальная форма не скрывала встроенные `inventory` / `presence` |

### 2.5 GUI (Qt)

2026-05-23: `pc_agent/ui_gui/dynamic_form_widget.py` is the reusable schema/field renderer for both ticket creation and agent user registration. `chat_panel.py` keeps compatibility aliases for `TicketDynamicFieldsWidget`, while `main_window.py` loads `GET /api/registry/agent/registration-form` and submits/confirm registration through the existing registration endpoints instead of the ticket create flow. Account login is a server-backed requester session, not password auth and not a replacement for the machine token: `pc_agent/ui_gui/account_gate.py` is the first non-modal page, settings stay accessible before login, and ticket list/create/detail are gated until `AccountSessionManager` has a valid server-issued session from confirmed binding or admin-approved verified other-account login. A `registration_pending` session is stored only to show/poll the pending registration gate and is not a ticket-workspace login. Before login, `main_window.py` enters account entry mode: it hides the main sidebar/footer shell, shows only the account gate, the separate registration entry page or settings, gives entry settings an explicit back action, and opens the full workspace only after confirmed/verified-other-account session selection; after login the Account page is a read-only server session summary with logout, not the legacy local profile editor.

New requester-detail slice: `pc_agent/ui_gui/ticket_detail_widgets.py` contains focused Qt Widgets for the redesigned ticket detail page, currently `TicketHeaderWidget`, compact `NextActionCard`, `TicketRightInfoPanel`, `TimelineItemWidget` and `TicketComposerWidget` wired to view models with ticket title/status/actions, resolved-ticket confirm/reject actions in the header menu, requester next action, requester facts, a single visual SLA/deadline block with `SlaProgressBar` rows, access code, optional real device facts, requester-safe system/diagnostic/attachment/user/support timeline events and terminal-status composer disabled states. Mapped requester and specialist message events render as dedicated `TimelineUserMessage` / `TimelineSupportMessage` bubbles instead of centered system cards. The old `ChatPanel` left meta panel and raw access-code/link strip remain only as hidden compatibility objects and are not added to the visible detail layout; missing device/inventory payloads must not be shown as an offline device. Create-ticket wizard result widgets live in `pc_agent/ui_gui/ticket_create_wizard_widgets.py`.

Account-session validation note: `pc_agent/ui_gui/server_api.py::validate_account_session()` sends the session token in a POST JSON body or account headers and must never place the token in URL query parameters; isolated local-agent instances must receive their own `PC_AGENT_DATA_DIR` / `PC_AGENT_INSTALL_ROOT` so GUI profile/session caches do not bleed across Live runs.
| Файл | Назначение |
|------|------------|
| `pc_agent/ui_gui/main_window.py` | Главное окно (sidebar + dashboard/tickets/account/create/settings stack), настройки и статусы, sidebar agent-status/update card as the visible online/version/update surface, auto-collapsing sidebar icon rail for focused ticket/create-ticket workspaces, create-ticket navigation switches the workspace synchronously and uses a GUI task helper for async preparation from Qt slots, protected-connection footer under the workspace, секция always-on/tray/logging diagnostics, release badge и кнопка recommended update из локального `ui_bridge`; account gate is shown first in entry mode, which hides the main sidebar/footer shell until login, settings remain available without login with an explicit back button, registration opens a separate registration entry page instead of the settings page, the post-login Account page shows the selected server-issued account session plus logout instead of the legacy profile selector/editor, refreshes missing display/full/email/phone fields from account-state for existing local sessions, validates stored server sessions through `/api/registry/agent/account-sessions/{session_id}/validate` on refresh/startup, retrieves the one-time token for approved other-account requests before saving the local session, and dashboard/tickets/create/account pages are unavailable until local account session is valid; connection/account/ticket shell state exposes stable UIA-readable accessibility names for live validation without leaking tokens; settings page shows always-on core modules separately from modules_store packages so `inventory` / `presence` are visible even when they are not installed packages; update CTA now renders explicit `requesting` / `requested` / `pending_restart` states and runs a short refresh burst after an accepted local update request; connection rejected/block events show the server-provided message, including token-limit diagnostics; update/status microcopy is localized for end users; large Qt soft shadows are opt-in through `PC_AGENT_ENABLE_GUI_SHADOWS=1` to avoid focused-window CPU spikes on weak Windows graphics stacks; recording STOP overlay stays a `Qt.Tool` window so it does not create extra `pc_agent` Taskbar/Alt-Tab entries; sidebar/header widgets must be parented or added to layouts before they are shown so blank labels do not become separate `python` top-level windows |
| `pc_agent/ui_gui/accessibility.py` | Shared helper for stable UIA metadata (`agent.connection.*`, `agent.account.*`, `agent.tickets.*`, `agent.ticket.*`), redaction of token/cookie-like text, and ticket/list description formatting used by live pywinauto probes. |
| `pc_agent/ui_gui/account_gate.py` | Non-modal first-screen account gate: displays verified server account-state, lets the user enter as confirmed registered account, continue/confirm pending registration or request admin approval for another account; approved `verified_other_account` server sessions are selectable, while unregistered/pending/unknown states do not show the other-account action; other-account login never calls registration endpoints and every ticket uses the server-issued account session instead of trusted local identity payloads |
| `pc_agent/ui_gui/performance_probe.py` | Field-only Qt GUI profiler for Windows/RDP/Proxmox CPU investigations; logs `[gui-profiler]` samples with process CPU/RSS/thread deltas, active/focus widget labels, Qt event counters and hot event receivers; default enabled for diagnostic release and disabled with `PC_AGENT_GUI_PROFILER=0` |
| `pc_agent/ui_gui/chat_panel.py` | Чат, создание обращения, reply-to, mark-read, legacy local requester accounts; ticket list/create/detail/message/read/upload/close APIs are gated by the `account_session_provider`, requester summaries and `create_ticket()` prefer the selected server account session over legacy local profiles, sending only `session_id`/optional token for requester account validation. Создание заявки использует request-template catalog plus P1/P1.1 Service Catalog cache (`service_catalog.json`) from `/api/service-catalog/current`, keeps legacy form-pack fallback, shows explicit `Раздел обращения -> Тип обращения` catalog cards before the dynamic form, binds the selected offering to `request_template_key`, and sends `service_code`/`offering_code`/`offering_full_code` in server preview/create without changing Protocol V3; if catalog is unavailable, it falls back to legacy form cards and still supports unique-template catalog enrichment. It caches server form packs with `request_template_version`, `form_schema_id`/`form_schema_version`, `*_policy_code` and `policy_refs`, and refreshes the cache when server version or normalized policy metadata changes; preserves field-level `visibility`/`process_mapping` metadata and hides requester-invisible internal process fields unless the template explicitly exposes them; renders server-driven priority fields from `field_roles`/`priority_policy`, supports extended form field types including `multi_select`, native `date`/`datetime` controls, registry-backed `user/department/location/device/service_picker` combos and replaceable/clearable `file` fields that contribute attachment paths; `service_picker` remains CMDB affected-service selection, not catalog service selection. |
| `pc_agent/ui_gui/tickets_list_model.py` | `TicketsListModel` и `TicketCardDelegate` — обновление строк без полного `clear()`, отрисовка карточек |
| `pc_agent/ui_gui/ticket_format.py` | Подписи/цвета статусов, формат дат, отпечаток строки тикета для диффа модели |
| `pc_agent/ui_gui/ticket_view_models.py` | Requester-facing view-model helpers для нового desktop helpdesk UI: человекочитаемые статусы, локальные сроки, progress/remaining-time SLA view-model fields, optional device visibility, `TicketHeaderViewModel`, `NextActionViewModel` that uses server `next_action_owner`/`first_response_at` facts for clearer requester statuses, `TicketInfoPanelViewModel` и mapping ticket events в безопасные `TimelineItem` для system/diagnostic/attachment/user/support событий без internal notes/raw tool logs |
| `pc_agent/ui_gui/ticket_create_wizard_widgets.py` | Компоненты нового мастера создания обращения; `CreateTicketProgressBar` renders the four-step requester-facing wizard progress (`Каталог`, `Описание`, `Preview`, `Готово`), `CreateTicketTypeGrid` is reused for service cards and offering cards while preserving the existing `form_key` selection contract for legacy fallback, `CreateTicketConfirmationPanel` renders the confirmation summary and required correctness checkbox before submit, and `CreateTicketSuccessPanel` renders the Step 4/Готово result screen with ticket number, subject, separate access code, requester-facing next action/deadlines and open/add-message/create-another/copy-code actions |
| `pc_agent/ui_gui/theme.py` | Централизованные light/dark theme tokens, QSS-фрагменты для `MainWindow`, `ChatPanel`, sidebar/profile/settings, requester helpdesk objectName/dynamic-property styles via `requester_helpdesk_stylesheet()` (`Card`, `SoftCard`, `NextActionCard`, `InfoCard`, `StatusBadge`, `PrimaryButton`, `SecondaryButton`, wizard cards, `wizardState`, `ticketTypeSelected`, `nextActionStyle`, `statusStyle`), topbar connection-state QSS, `refresh_qss_state()` для repolish и helper-пути для GUI assets/icons |
| `pc_agent/ui_gui/window_chrome.py` | Кастомная frameless-шапка окна для Windows/Linux: логотип/заголовок, minimize/maximize/close, ручной drag/resize без Qt native `startSystemMove()` / `startSystemResize()`, чтобы на Windows не появлялись `_q_titlebar` helper windows в Taskbar/Alt-Tab; resize cursor updates are deduplicated so active RDP/Proxmox windows do not call `setCursor`/`unsetCursor` on every mouse move |
| `pc_agent/ui_gui/assets/icons/` | SVG line-icons для Qt Widgets GUI: navigation, ticket cards, search/filter, chat CTA и update/status actions |
| `pc_agent/ui_gui/server_api.py` | Обращение к серверу из GUI, создание обращений через `/api/tickets/create` с legacy `form_key`, first-class `request_template_key`, optional P1 Service Catalog fields (`service_code`, `offering_code`, `offering_full_code`), optional `diagnostic_consent` and server-backed `requester_account` session id; GUI request headers refresh the active local auth token from `storage.db` before requester API calls so runtime reprovision/token rotation cannot leave stale Bearer headers in an already-open GUI; `get_service_catalog_current()` reads requester-safe `/api/service-catalog/current`; server-backed preview through `/api/tickets/create/preview`, отправка `reply_to`/message metadata, вызов `mark_ticket_read()` и ticket history API с `get_ticket(..., since_event_id=..., before_event_id=..., limit=...)`; ticket list/detail/message/read/upload/close methods pass account session id/token via query/body/headers so server authorization is account-scoped, not device-only; `sync_registry_profile()` keeps the legacy profile sync endpoint compatible, registration profile submission/status/confirm/account-state and server-side account sessions use `/api/registry/agent/profile`, `/api/registry/agent/registration-status`, `/api/registry/agent/claims/{claim_id}/confirm`, `/api/registry/agent/account-state`, `/api/registry/agent/account-sessions/*` and `/api/registry/agent/account-login-requests/*` with normalized `{status:"success", data}` unwrapping and friendly HTTP auth error messages for GUI callers, а `get_registry_options()` читает `/api/registry/options` для picker-полей формы |
| `pc_agent/ui_gui/sse_client.py` | SSE-клиент к ui_bridge |
| `pc_agent/ui_gui/tray_manager.py` | Тонкая cross-platform обёртка над `QSystemTrayIcon`: открыть окно, открыть логи, restart agent, exit agent |
| `pc_agent/ui_gui/tray_notifications.py` | Maps auth-block events (`TOKEN_LIMIT_EXCEEDED`, `DEVICE_FINGERPRINT_MISMATCH`, `DEVICE_ARCHIVED`) to tray/system notifications |
| `pc_agent/ui_gui/consent_dialog.py` | Диалог согласия на операцию |
| `pc_agent/ui_gui/token_dialog.py` | Диалог токена |
| `pc_agent/ui_gui/wait_for_auth_dialog.py` | Диалог ожидания авторизации; читает `connection_request_error.json` и показывает пользователю `TOKEN_LIMIT_EXCEEDED`, когда сервер отказал в выпуске третьего активного токена |

### 2.6 Конфигурация

P2 Knowledge Platform agent integration is HTTP-only and does not change Protocol V3. `pc_agent/ui_gui/server_api.py` exposes `get_knowledge_suggestions()` and `record_knowledge_feedback()` for `/api/knowledge/suggest|feedback`, and `create_ticket()` accepts safe `knowledge_attempts`. `pc_agent/ui_gui/chat_panel.py` requests requester-safe suggestions after Service Catalog offering selection, records viewed/not-helpful/deflected feedback, includes attempts when ticket creation continues, and falls back to the existing catalog/form flow if the knowledge API is unavailable. P2.2/P2.2.1 rollout policies are enforced server-side for the same `agent_gui` suggestion surface; the agent sends urgency/impact context, reads returned rollout, respects safe projection plus min/max suggestion gates, treats rollout-disabled/no-policy/API-unavailable states as non-blocking by default, honors explicit blocking no-suggestion/API-unavailable policies, and needs no new wire protocol or local policy cache.
| Файл | Назначение |
|------|------------|
| `pc_agent/config/config_loader.py` | Загрузка настроек |
| `pc_agent/config/settings.yaml`, `settings.default.yaml` | Конфиг |

### 2.7 Скрипты и тесты
| Путь | Назначение |
|------|------------|
| `pc_agent/scripts/smoke_check_module.py` | Проверка модуля (tool names, resolved methods) |
| `pc_agent/tests/` | Unit/integration тесты |

---

## 3. Быстрый поиск (ключевое слово → файлы)

Используйте поиск по документу, `docs/QUICK_LOOKUP.md` или `scripts/agent_find.py <ключевое_слово> --dir pc_agent`. Ниже — куда смотреть в первую очередь.

- **handshake, protocol_version, ws_ticket_v3** — `ws_agent.py`, `docs/PROTOCOL_V3.md`; handshake diagnostics log the current `PROTOCOL_VERSION` value and must not mention legacy `ws_mcp_v1`; raw diagnostic probes use `client_kind=diagnostic_probe`, while the real agent remains `agent_runtime`
- **outbox, outbox_ack, ACK/NACK** — `core/sender.py`, `core/database.py`, `docs/SENDER.md`
- **command_result replay / restart recovery** — `ws_agent.py`,
  `core/database.py`, `docs/PROTOCOL_V3.md`, `docs/DATABASE.md`;
  terminal command results are kept in `pending_command_results` until
  `command_result_ack`, and previous-runtime `seen_commands.in_progress`
  rows become terminal `AGENT_RESTARTED` recovery results on startup.
- **run_tool, command** — `core/orchestrator.py`, обработка command/envelope V3 в `ws_agent.py`; current contract accepts canonical semantic tool ids and legacy aliases, while runtime still binds them к текущему module registry
- **run_recipe, agent recipe** — `core/orchestrator.py` handles the Protocol V3 `run_recipe` command and delegates to `core/recipe_runner_bridge.py`; bridge results must be normalized to `ToolResponse` before common command-result post-processing.
- **schedule_task, cancel_task, list_tasks, task_run_now** — `ws_agent.py` (Scheduler RPC), `core/database.py` (scheduled_tasks storage)
- **device_seq, agent_seq** — тип события только по ним; outbox в `core/database.py`
- **модули (загрузка, registry, rollback)** — `core/module_manager.py`, `core/loader.py`, `core/registry.py`, `core/orchestrator.py` (cached context, rebuild), `modules/__init__.py`
- **инструменты (list_tools, call_tool, aliases, semantic ids)** — `core/registry.py`, `core/tools.py`, `core/orchestrator.py`
- **consent, pending_consents** — `core/consent_service.py`, `core/database.py`, `core/orchestrator.py`, `ui_gui/consent_dialog.py`
- **аутентификация, machine_id, install_id, токен** — `core/identity.py`, `core/machine_identity.py`, `auth/token_source.py`, `auth/connection_request.py`, `docs/AUTHENTICATION.md` (не логировать сырой токен; migration lookup идёт по `machine_id -> install_id -> legacy uuid`)
- **always-on, tray, runtime logs, локальный shutdown/status/logs** — `docs/AGENT_RUNTIME_ALWAYS_ON.md`, `ws_agent.py`, `core/runtime_logging.py`, `ui_gui/main.py`, `ui_gui/tray_manager.py`, `ui_bridge/api_server.py`
- **reprovision_required / invalid token** — `ws_agent.py` (`_request_token_from_console` теперь запускает auto reprovision через `connection_request` flow без ручного ввода)
- **артефакты, upload** — `core/artifacts.py`, `core/recording_controller.py`, `network/uploader.py`
- **self-update, pending_update, update_history, launcher rollback, recommended update UI** — `docs/AGENT_UPDATE_WORKFLOW.md`, `docs/SELF_UPDATE.md`, `launcher/installer.py`, `build_windows_release_v2.py`, `version.py`, `ws_agent.py`, `ui_bridge/api_server.py`, `ui_gui/main_window.py`
  - Update tracing now spans `ws_agent.trigger_recommended_update` -> `core/orchestrator._handle_update` -> `ws_agent_runtime_helpers.shutdown_for_update` -> `launcher/installer.py::apply_update`, all through the shared `core/action_trace.py` recorder / external bridge.
- **update_request_state, pending_restart, truthful scheduled** — `ws_agent.py`, `core/orchestrator.py`, `ui_gui/main_window.py`, `launcher/installer.py`, `launcher/launcher_main.py`
- **GUI, SSE, UI bridge, аккаунт обращения** — `ui_gui/*`, `ui_bridge/*`; account gate and Account page live in `ui_gui/main_window.py` + `ui_gui/account_gate.py`, while `ui_gui/server_api.py` and `ui_gui/chat_panel.py` must pass server-issued account session id/token for create-preview, ticket create/list/detail/message/read/upload/download/close and ticket-scoped tool actions.
- **agent request-template create/detail smoke** — `ui_gui/chat_panel.py`, `ui_gui/ticket_view_models.py`, `ui_gui/ticket_create_wizard_widgets.py`, `ui_gui/server_api.py`, `tests/test_chat_panel_helpers.py`, `tests/test_ticket_api_client_attachments.py`; live checks should cover server preview, dynamic required/file/picker behavior, explicit required diagnostic-consent hint, diagnostic consent payloads, attachment upload/message refs, requester-safe result labels and success panel, visible diagnostics/deadline/assignment summaries in detail, NextAction/status/timeline view-model mapping, user/support message bubbles, Russian system event text, and observer summary for the created ticket.
- **SQLite, миграции схемы** — `core/database.py` (DB_SCHEMA_VERSION), `docs/DATABASE.md`

---

## 4. Потоки выполнения

- **Boot:** ws_agent.py → IdentityManager, DatabaseManager, AgentOrchestrator, загрузка модулей → WSOutboxFlusher, UI bridge.
- **Agent → Server:** события в outbox → WSOutboxFlusher → WS → outbox_ack (удаление записи) / outbox_nack (retry).
- **Server → Agent:** command по WS → оркестратор (run_tool/module/job) → результат и события в outbox.

---

## 5. Хранилище и миграции

- SQLite: `pc_agent/data/storage.db`. Версия схемы: `core/database.py` (DB_SCHEMA_VERSION). Таблицы: outbox, seq_ticket, seq_device, seen_commands, pending_command_results, pending_consents, auth_tokens, scheduled_tasks. См. `docs/DATABASE.md`, `docs/SENDER.md`.

---

## 6. Инварианты

- Protocol version на handshake: `ws_ticket_v3`. Тип события только по device_seq/agent_seq. ACK для outbox = удаление записи. Не логировать сырой токен. Детали: корневой `AGENTS.md`, `docs/PROTOCOL_V3.md`.

---

## 7. Когда обновлять этот CODEMAP

Каноническая карта агента — этот файл: `pc_agent/docs/CODEMAP.md` (других CODEMAP для дерева `pc_agent/` нет).

При изменениях, затрагивающих структуру кода агента, его **нужно** обновить:

- добавление/удаление/перенос ключевых модулей или точек входа в `pc_agent/`;
- новые или переименованные ключевые файлы в core/, modules/, ui_gui/, ui_bridge/;
- смена назначения существующих модулей или потоков.
- если change затрагивает agent-side observer coverage, action trace bridge, module breadcrumbs или dangerous flow instrumentation — синхронно обновлять и `server/docs/OBSERVER_LAYER.md` + `server/docs/OBSERVER_AUTHORING_RULES.md`.

Проверка: ключевые термины и файлы из раздела «Быстрый поиск» и «Структура каталогов» должны соответствовать коду.
## 2026-05-09 requester timeline projection

- `pc_agent/ui_gui/ticket_view_models.py::map_ticket_event_to_user_timeline_item()` now prefers server-provided `requester_timeline_text`, `requester_timeline_kind` and `requester_timeline_payload` from `GET /api/tickets/{ticket_id}`.
- The local mapper remains only as an older-server fallback and must hide unknown/debug/internal events instead of showing generic `Обращение обновлено.` or raw `unknown` status text.
- `pc_agent/ui_gui/chat_panel.py` and requester detail widgets consume the resulting `TimelineItem` model; they should not render raw `event_type`, raw tool payload, trace ids, internal notes or worklog data in requester-facing cards.

## 2026-05-09 Remote Assist MVP

- Agent-side Remote Assist starts from `pc_agent/ws_agent.py` handling the server command `remote_assist.request`; it publishes a UI event instead of blocking the WebSocket loop.
- UI entry points are `pc_agent/ui_gui/main_window.py` and `pc_agent/ui_gui/remote_assist_dialog.py`: the user gets a non-blocking consent dialog, an active-session banner, and a user-side end-session action. The consent dialog is retained by session id while open and raised as a topmost Qt dialog so it is not garbage-collected or hidden before the user can approve/deny.
- Windows stable agent `3.1.61` supersedes `3.1.60` for GUI startup failure handling: `ui_gui/__init__.py` keeps non-GUI API imports from loading Qt, `ws_agent.py` fails fast instead of falling back to `--no-gui` when Qt/PySide6 cannot load, and both launcher entrypoints stop after repeated immediate crashes if rollback is unavailable while writing `last_failed_launch.json` diagnostics.
- Windows stable agent `3.1.60` supersedes `3.1.59` as the compiled registration/account-session release: `core/account_session.py`, `core/user_profile.py`, `ui_gui/main_window.py`, `ui_gui/chat_panel.py`, `ui_gui/main.py` and the ticket API client carry the requester account gate, dynamic registration form, pending registration sessions, other-account session handling and refreshed server registration-claim contracts.
- Windows stable agent `3.1.56` supersedes `3.1.55` for Remote Assist elevated/admin helper diagnostics: `elevated_helper.py` logs helper launch/client identity (`AGENT_VERSION`, PID, executable, port) and treats `OSError: cannot read from timed out object` as an idle timeout instead of letting the helper subprocess print a traceback. `3.1.55` handled the Remote Assist runtime-module bootstrap: `main_window.py` now creates sessions through `pc_agent/remote_assist/runtime_host.py`, which loads the active managed `remote_assist_runtime` module from `modules_store` and falls back to bundled `thread.py` when the module is absent or invalid. `3.1.54` handled elevated/admin stability: `elevated_helper.py` uses a timeout-safe raw socket line reader instead of `socket.makefile().readline()` while idle, so UAC helper pauses no longer crash with `OSError: cannot read from timed out object`. `3.1.53` handled clipboard/control coexistence: `webrtc_client.py` continues the control worker after `clipboard_enable`, so default clipboard auto-sync no longer disables mouse/keyboard input; the support viewer treats stalled video as a recoverable warning, handles file paste from `clipboardData.items` and window-level Ctrl+V, and exposes the active-session elevated/admin request for support operators. `3.1.52` handled Remote Assist default capabilities: screen-sharing requests preselect clipboard auto-sync when policy/permission allow it, every permitted mode preselects file transfer, and the active support viewer exposes a `Повысить права` action that ends the normal session and creates a separate `elevated_admin` request. `3.1.51` handled Remote Assist file transfer: approved policy-gated sessions accept the `file-transfer` WebRTC data channel, receive chunked files from the support viewer button/drag-drop/browser file-paste paths, validate size/checksum, and save them under `Downloads\Maria Remote Assist` without routing file bytes through the server. `3.1.50` handled elevated/admin control: `elevated_admin` sessions use `pc_agent/remote_assist/elevated_helper.py`, which launches the same `pc_agent.exe` through UAC with `--remote-assist-elevated-helper` and proxies validated input over a one-time-token loopback channel. `3.1.49` handled input/session lifecycle fixes: `webrtc_client.py` processes control-channel messages through a sequential queue, the viewer translates browser click/double-click/drag into explicit control messages, user-side end sends `session.end` before closing WebRTC, and the server can push HTTP-ended sessions to connected signaling peers. `3.1.48` handled stalled capture and RDP/virtual-display cursor positioning. Windows stable `3.1.47` added agent-side WSS trust for the HTTPS stand by loading Windows Trusted Root/CA stores and optional `PC_AGENT_TLS_CA_FILE` roots. Windows stable `3.1.46` superseded `3.1.45` for clipboard/control hotfixes: `clipboard_enable` / `clipboard_disable` are routed to the clipboard bridge, sanitized data-channel decisions are logged, and Windows `SendInput` attaches to the foreground input queue before injecting input. `3.1.45` carried the quality/control/clipboard slice: requested capture quality is passed into the WebRTC screen track, approved `interactive_control` sessions auto-enable control without a second consent prompt, drag/wheel/shortcut input is supported, and text clipboard auto-sync runs only when the initial session capability and server policy allow it.
- Windows stable agent `3.1.36` carries the Remote Assist connection-hardening release: browser offers use an explicit recvonly video transceiver, viewer/agent timeouts close failed WebRTC transports, failed sessions are audited, the Maria Agent banner stays in connecting text until ICE is connected, and screen capture reuses its `mss` context to reduce failed-session CPU. Windows stable `3.1.35` remains the interactive-control test release (`SendInput` on Windows, `pynput` on Linux); `3.1.34` remains the prior WebRTC packaging hotfix for bundled `aiortc`/ICE/media dependencies and startup-failure reporting.
- WebRTC/capture/runtime lives under `pc_agent/remote_assist/`: `runtime_host.py` is the bootstrap boundary that loads `remote_assist_runtime` from `modules_store` and falls back to bundled code; `thread.py` owns a dedicated `QThread` + asyncio loop, `webrtc_client.py` owns aiortc signaling/peer connection plus the `control` and `file-transfer` data channels, `tls.py` prepares the HTTPS/WSS trust context for stand/prod certificates, `screen_track.py` captures one primary monitor through `mss`, `input_controller.py` validates interactive-control mouse/keyboard messages before dispatching to the platform input backend (`SendInput` on Windows, `pynput` on Linux), `elevated_helper.py` owns the Windows UAC helper/proxy for `elevated_admin`, `clipboard.py` owns text clipboard auto-sync, and `file_transfer.py` owns operator-to-device chunk validation and local saving for sessions that requested and received the file-transfer capability. First managed module source is `pc_agent/modules_packages/remote_assist_runtime` and publishes `remote_assist_runtime@1.0.0`. Control is active only when the approved session mode is `interactive_control` or `elevated_admin`; the viewer auto-enables it on data-channel open and can still pause/resume locally.
- View-only remains the default and all capture/WebRTC/input/clipboard/file-transfer work must stay outside the GUI thread. No audio, recording, UAC bypass, or hidden unattended access is active unless a later policy-backed layer explicitly implements and verifies it.
