# Шаблон настроек агента. При первом запуске копируется в data_root/settings.yaml.
# Фактические пути (data_dir, logs, modules) задаются через runtime_paths (data_root).

server:
  ws_url: "ws://192.168.100.17:8666/ws"
  api_url: "http://192.168.100.17:8666/api"
  http_port: 12345
  reconnect_interval: 5

security:
  allow_remote_code: false

paths:
  data_dir: "."
  identity_file: "identity.json"
  temp_dir: "temp"

logging:
  level: "INFO"
  file: "logs/agent.log"

enabled_modules:
  - "system"
  - "screen"

ui:
  enabled: True
  host: "127.0.0.1"
  port: 8765
  autostart_gui: True

modules:
  extra_paths: []
