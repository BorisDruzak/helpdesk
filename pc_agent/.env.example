# PC Agent - переменные окружения
# Скопируйте в .env и заполните значения

# Токен аутентификации (Bearer/JWT). Используется при подключении к серверу.
# Приоритет: ENV AUTH_TOKEN > auth_tokens (БД) > identity.json (legacy)
# export AUTH_TOKEN="your_bearer_token_here"
