## 2026-04-19 agent runtime and launcher flow

- 2026-06-15/16 Web-first Registration R12: start with `server/config.py`, `server/requester/identity_service.py`, `server/web_api/requester_handlers.py`, `server/registry/effective_identity_service.py`, `server/registry/browser_pairing_service.py`, `server/registry/account_session_service.py`, `server/tests/test_config_feature_flags.py`, `server/tests/test_requester_workspace_api.py`, `server/tests/test_registration_api.py`, `server/tests/test_account_session_service.py`, `webapp/src/features/requester/types.ts`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`, `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md` and `PLANS.md`. R12 adds the `PROFILE_COMPLETION_REQUIRED=true` default with policy-aware requester `profile_completion.blocks`, keeps old pending agent registration claims visible in requester/admin UI, resolves verified `ui_login` identities for self-reported requester profiles so KB audience works before admin person activation, preserves confirmed bindings and verified other-account sessions through current validation rules, and adds explicit stale browser-pairing/account-session expiry cleanup services. Rollback uses `WEB_SELF_REGISTRATION_ENABLED=false` and `PROFILE_COMPLETION_REQUIRED=false`; normal agent GUI releases stay browser-only while legacy backend endpoints remain compatibility surface for older agents.

- 2026-06-15 Web-first Registration R11: start with `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `webapp/src/features/admin/registry/registry-requests-tab.tsx`, `webapp/src/pages/admin/registry-page.test.tsx`, `pc_agent/ui_gui/main_window.py`, `scripts/test_web_first_registration_localization.py`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. R11 localizes Knowledge Ask draft context, requester device agent/version/status/activity labels, requester accessible labels and admin registration blocker text; it repairs a mojibake account-session error in the Windows agent and adds a static guard for touched-file mojibake/raw normal-UI snippets. Browser evidence is under `artifacts/browser_live_validation/web-first-registration-r11-20260615/`.

- 2026-06-15 Web-first Registration R10: start with `server/registry/service.py`, `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_registry_web_api.py`, `server/tests/test_registry_people_admin.py`, `server/tests/test_registry_admin_previews.py`, `server/tests/test_registry_timeline_admin.py`, `server/tests/test_registration_api.py`, `webapp/src/features/admin/api.ts`, `webapp/src/features/admin/registry/registry-people-tab.tsx`, `webapp/src/pages/admin/registry-page.test.tsx`, `server/docs/REGISTRY_MANAGEMENT_CENTER.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. R10 keeps admin registry as the moderation center for pending link claims, person/UI-account identities, duplicate people, department/location policy suggestions, device transfer preview, account-session revoke and timeline audit; `GET /api/web/admin/registry` now projects schema-aware `people[].profile_completion`, and the People tab shows Russian profile-completion status and missing field labels without exposing requester storage internals.

- 2026-06-15 Web-first Registration R9: start with `server/knowledge/suggestion_service.py`, `server/tests/test_knowledge_suggestions.py`, `server/tests/test_knowledge_api.py`, `server/tests/test_knowledge_ask.py`, `server/tests/test_knowledge_hybrid_retrieval.py`, `webapp/src/features/requester/api.ts`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `server/docs/KNOWLEDGE_PLATFORM.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. R9 keeps Registry audience enforcement as the source of truth for requester search/suggestions/portal/Ask/RAG, adds safe R8 `form_payload` / `requester_context` / selected `device_metadata` as pre-submit suggestion query signals, skips raw ids/secrets/session/contact fields in suggestion query extraction, and persists requester `knowledge_attempts` on ticket create for support visibility.

- 2026-06-15 Web-first Registration R8: start with `server/requester/identity_service.py`, `server/web_api/requester_handlers.py`, `server/tests/test_requester_workspace_api.py`, `webapp/src/features/requester/types.ts`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. R8 adds server-owned requester context v1 for request forms: preview/create recompute profile/device/account/routing facts from the authenticated web session, completed `RegistryPerson` and active binding; ticket create stores `custom_fields.requester_context_snapshot` plus flat aliases like `requester_department_id`, `requester_location_id`, `requester_device_id`, `requester_asset_id` and `requester_account_mode`; `/app/requester` pre-fills empty form fields from safe context, renders registry-backed department/location/device/service picker options and shows preview context explanation.

- 2026-06-15 Web-first Registration R7: start with `server/registry/service.py`, `server/web_api/registry_handlers.py`, `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_registry_quality_remediation.py`, `server/tests/test_registry_web_api.py`, `webapp/src/features/admin/api.ts`, `webapp/src/features/admin/registry/registry-people-tab.tsx`, `webapp/src/features/admin/registry/registry-person-edit-dialog.tsx`, `webapp/src/features/admin/registry/registry-utils.ts`, `webapp/src/pages/admin/registry-page.test.tsx`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. R7 adds focused production context without a DB migration: the admin registry snapshot projects person position/workplace/internal extension/manager, department manager, service owner/criticality/audience and asset responsible-person context from existing rows/metadata; admin person create/update accepts the controlled work-context fields; quality issues now include missing person department/location, missing asset owner/responsible and pending department confirmation.

- 2026-06-15 Web-first Registration R6: start with `server/registry/profile_schema_service.py`, `server/requester/identity_service.py`, `server/web_api/registry_handlers.py`, `server/web_api/requester_handlers.py`, `server/routes.py`, `server/tests/test_requester_workspace_api.py`, `webapp/src/features/admin/registry/registry-profile-schema-tab.tsx`, `webapp/src/features/admin/api.ts`, `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md` and `PLANS.md`. Admin profile schema is controlled config over `registry_admin_policies(policy_key=requester_profile_schema)`: system fields cannot be hidden/deleted, optional built-ins can be hidden/required, custom fields must store under `registry_people.metadata_json.profile_custom_fields.<key>` and write audited `profile_schema_updated` events. Requester APIs return a safe schema projection without storage/`metadata_json` internals; required custom fields participate in `profile_completion` and requester setup submits `custom_fields`.

- 2026-06-15/16 Web-first Registration R5: start with `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/tests/test_account_gate.py`, `pc_agent/tests/test_main_window_runtime_windows.py`, `pc_agent/docs/CODEMAP.md` and `PLANS.md`. The GUI agent no longer exposes or builds the legacy full-profile registration flow: there are no local registration/confirm buttons, no `registration_entry_page`, and no `default_agent_registration_form()` in normal GUI code. Unlinked/pending account gate states show browser device linking, copyable pairing code and status refresh instead.

- 2026-06-15 Web-first Registration R4: start with `PLANS.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `webapp/src/pages/device-pairing/index.tsx`, `webapp/src/pages/device-pairing/device-pairing-page.test.tsx`, `server/web_api/registry_handlers.py`, `server/registry/browser_pairing_service.py` and `server/tests/test_registration_api.py`. `/app/requester/devices` is now the requester cabinet entry for manual device-link code lookup and direct `pairing_id` preview; registration confirmation requires a completed web profile before creating a `device_registration_claim`, returns `REQUESTER_PROFILE_INCOMPLETE` with `/app/requester/profile/setup` otherwise, and builds the claim profile snapshot from the resolved `RegistryPerson` instead of browser-submitted profile fields.

- 2026-06-15 Web-first Registration R3: start with `PLANS.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`, `server/requester/identity_service.py`, `server/web_api/requester_handlers.py`, `server/routes.py`, `server/tests/test_requester_workspace_api.py`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `webapp/src/features/requester/api.ts`, `webapp/src/features/requester/types.ts`, `webapp/src/app/router.tsx`, plus the R2 account files `webapp/src/features/auth/register-page.tsx`, `webapp/src/features/auth/api.ts`, `server/web_api/session_handlers.py`, `server/web_api/dto/session.py`, `server/config.py` and `server/tests/test_web_session_api.py`. R3 makes profile completion web-first and registry-backed: `GET /api/web/requester/bootstrap` returns `profile_completion`, `PUT /api/web/requester/profile` writes the caller-owned `RegistryPerson` and verified `ui_login` identity, required full name/department/location/phone are enforced, department/location must come from registry pickers, and normal requester ticket preview/create returns `REQUESTER_PROFILE_INCOMPLETE` until the gate is complete. Browser evidence is under `artifacts/browser_live_validation/web-first-registration-r3-20260615/`.

- 2026-06-15 Knowledge Section Constructor K1: start with `server/app/repos/knowledge_repo.py`, `server/knowledge/portal_service.py`, `webapp/src/features/knowledge/sections-page.tsx`, `webapp/src/features/knowledge/support-workspace-page.tsx`, `webapp/src/features/knowledge/authoring/EditorMetadataStep.tsx`, `webapp/src/pages/admin/knowledge-sections-page.tsx`, `webapp/src/pages/admin/knowledge-sections-page.test.tsx`, `webapp/src/features/knowledge/article-visibility-model.ts`, `webapp/src/features/knowledge/api.ts`, `webapp/src/app/router.tsx`, `webapp/src/app/navigation.tsx`, `server/docs/KNOWLEDGE_PLATFORM.md` and `server/docs/KNOWLEDGE_VNEXT_ARCHITECTURE.md`. `/app/admin/knowledge/sections` is the Russian-first UI for `KnowledgeSpace` policy under the product term `Раздел базы знаний`: title/code/description/status/default visibility plus publication/import/RAG flags are saved through existing `GET|POST /api/web/knowledge/spaces`; article counts come from existing `GET /api/web/knowledge/items`; allowed material types use existing `KnowledgeSpace.allowed_item_types`; requester portal/support workspace exposure and article length recommendation are stored in `KnowledgeSpace.metadata` as product flags, preserving existing metadata keys on save. `show_in_requester_portal` is read by requester portal home/collections/article detail, `show_in_support_workspace` is read by `/app/knowledge`, and `article_length_recommendation` is read by Studio as an authoring hint. Backend validation rejects non-requester-safe portal exposure, explicit empty material types, disabling section RAG while active articles force `ai_rag_policy=allowed`, and setting article `ai_rag_policy=allowed` in a section where RAG is disabled. The section list loads batch `GET /api/web/admin/knowledge/audience-rules?subject_type=space` to show audience summaries without raw target ids, while section audience preview/save uses existing `GET|PUT /api/web/admin/knowledge/audience-rules` and `POST /api/web/admin/knowledge/audience-rules/preview` with `subject_type=space` and the selected `space_id`. Normal UI must not expose raw JSON or UUID-first workflows.

- 2026-06-15 Knowledge Import Alignment K6: start with `webapp/src/features/knowledge/import-wizard-page.tsx`, `webapp/src/features/knowledge/import-wizard.test.tsx`, `server/knowledge/ingestion_service.py`, `server/web_api/knowledge_handlers.py`, `server/app/repos/knowledge_repo.py`, `server/tests/test_knowledge_import_api.py`, `server/docs/KNOWLEDGE_PLATFORM.md` and `PLANS.md`. `/app/admin/knowledge/import` requires a `Раздел базы знаний`, previews detected title, section, safe draft visibility, inherited audience, inherited RAG policy and auto-segmentation decision, creates `support_internal` drafts with `metadata.import_mode=safe_draft` and default `metadata.ai_rag_policy=inherit`, rejects explicit `ai_rag_policy=allowed` when target section `allow_rag=false`, forces auto segmentation for imports with 800+ words, and opens `/app/admin/knowledge/studio?item=<created_item_id>` after create. Import must not publish or create requester-visible content by accident; requester-safe exposure remains a later explicit Studio/publish decision.

- 2026-06-15 Knowledge Settings Consistency K7: start with `webapp/src/features/knowledge/search-settings-page.tsx`, `webapp/src/features/knowledge/search-settings-page.test.tsx`, `webapp/src/features/knowledge/ai-settings-page.tsx`, `webapp/src/features/knowledge/ai-settings-page.test.tsx`, `webapp/src/features/knowledge/api.ts`, `server/knowledge/search_settings_service.py`, `server/ai/provider_registry.py`, `server/docs/KNOWLEDGE_PLATFORM.md` and `PLANS.md`. `/app/admin/knowledge/search-settings` explains Keyword/Full-text/Vector/RAG modes, RAG article eligibility, and visibility/audience filtering before ranking, snippets and citations. `/app/admin/knowledge/ai` shows provider health, RAG policy blocks, requester-safe cloud policy, security-restricted local policy and redacted audit posture. These settings pages are explanatory/operational surfaces only; they do not bypass section policy, article `ai_rag_policy`, audience rules or requester-safe projection.

- 2026-06-15 Knowledge Graph Simplification K8: start with `webapp/src/features/knowledge/article-related-graph-panel.tsx`, `webapp/src/features/knowledge/authoring/EditorMetadataStep.tsx`, `webapp/src/features/knowledge/graph-editor/GraphWorkbenchLayout.tsx`, `webapp/src/pages/admin/knowledge-studio-page.test.tsx`, `webapp/src/features/knowledge/graph-studio.test.tsx`, `webapp/src/features/knowledge/api.ts` and `server/knowledge/graph_service.py`. `/app/admin/knowledge/studio` shows a simple `Связанные статьи` panel that reads graph search/neighborhood opportunistically and never blocks save when graph is empty or unavailable. `/app/admin/knowledge/graph` remains an advanced workbench for related articles, duplicates, supersedes, known error/workaround and service/article relations. Do not embed graph editing into the normal Studio save flow.

- 2026-06-15 Knowledge K3 helpdesk binding: start with `server/knowledge/binding_surfaces.py`, `server/web_api/knowledge_handlers.py::handle_web_knowledge_item_bindings`, `server/routes.py`, `server/app/repos/knowledge_repo.py`, `server/knowledge/search_service.py`, `server/knowledge/suggestion_service.py`, `server/knowledge/retrieval_service.py`, `webapp/src/features/knowledge/article-helpdesk-binding-panel.tsx`, `webapp/src/features/knowledge/api.ts`, `webapp/src/features/knowledge/authoring/EditorMetadataStep.tsx`, `server/tests/test_knowledge_api.py`, `server/tests/test_knowledge_suggestions.py`, `server/tests/test_knowledge_hybrid_retrieval.py` and `webapp/src/pages/admin/knowledge-studio-page.test.tsx`. Studio settings exposes `Связь с обращениями` instead of raw bindings, loads Service Catalog choices from `/api/service-catalog/current`, and uses `GET|POST /api/web/knowledge/items/{item_id_or_slug}/bindings` plus `PATCH|DELETE /api/web/knowledge/items/{item_id_or_slug}/bindings/{binding_id}` to read/save/edit/remove existing-article service/offering/request-template links. Duplicate same-dimension binding saves update the existing row instead of creating a second binding. Binding `metadata.surfaces` is an intentional eligibility contract: suggestions/search/retrieval normalize aliases, legacy empty surfaces allow all surfaces, and explicit surfaces must include the current consumer such as `requester_pre_submit`, `support_ticket_workspace` or `ai_rag`. Default UI must not render `binding_id`. Suggestion ranking still uses service/offering/request-template binding context before audience filtering/projection, so denied requester audiences must receive no hidden titles, snippets or counts.

- 2026-06-10/11 Stage 3 user consent layer: start with `server/consent/service.py`, `server/consent/operation_consent.py`, `server/app/repos/user_consent_repo.py`, migration `109_user_consent_requests`, `server/web_api/requester_handlers.py`, `server/web_api/registry_handlers.py`, `server/web_api/support_handlers.py`, `server/tools/handlers.py`, `server/api/operations.py`, `server/remote_assist/service.py`, `webapp/src/pages/requester/index.tsx`, `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/user_consent_dialog.py`, `server/tests/test_user_consent_api.py`, `server/tests/test_operation_retry.py` and `server/tests/test_tools_async_response_contract.py`. `UserConsentRequest` is the canonical browser/agent consent row with requester person/binding/account-session scope, one active pending consent per `subject_type + subject_id`, CAS decision semantics and safe redacted action payloads. `create_request()` is race-safe against partial-unique pending-subject collisions and returns the existing pending row instead of surfacing a duplicate-key 500. Browser APIs are under `/api/web/requester/consents*` and resolve the web user to `RegistryPerson`; agent GUI APIs are under `/api/registry/agent/consents*`, where the agent token identifies the device and `X-Account-Session-Id` plus `X-Account-Session-Token` identifies the requester. Consent-required support tools, legacy `/api/tools/run` and operation retry create `UserConsentRequest` in the same transaction as `Operation(status='waiting_consent')`; consent request payloads preserve sensitive parameter keys with `<redacted>` values for scoped approval context, while `tool_call_started` / retry ticket events drop sensitive keys entirely before writing timeline JSON. Missing requester scope is rejected instead of leaving orphan waiting operations. Approved operation/tool/diagnostic consents call the existing `OperationService.approve_consent()` path once; denied consents call `deny_consent()`. Remote Assist creates `subject_type=remote_assist` consent first and now rejects tickets without requester scope; requester consent list/detail/approve responses are consent-only and do not expose ICE/SDP/signaling/agent/viewer secrets; after approve the server sends `remote_assist.request` with `consent_status=approved`, and the agent performs only the technical signaling-token approve step. `pc_agent/ui_bridge/event_bus.py` keeps unexpired Remote Assist request events replayable by `session_id` so a late GUI/SSE subscriber still starts the technical approval path. Expired, revoked-binding or revoked-session consent decisions do not start operations.

- 2026-06-10 Browser auth/pairing hardening: start with `server/auth/middleware.py`, `server/config.py`, `server/registry/browser_pairing_service.py`, `server/web_api/registry_handlers.py`, `server/docs/SECURITY_AND_AUTH.md`, `server/tests/test_web_session_api.py` and `server/tests/test_registration_api.py`. Cookie-auth unsafe browser requests on web-session bridge paths require same-origin `Origin` or `Referer` unless `WEB_CSRF_SAME_ORIGIN_ENABLED=false`; allowed origins include the current host, reverse-proxy public host from `Forwarded` / `X-Forwarded-Proto` + `X-Forwarded-Host`, `SERVER_PUBLIC_BASE_URL`, and `WEB_CSRF_TRUSTED_ORIGINS`. Missing/mismatched origin returns 403 while bearer UI tokens and agent tokens are not treated as web-session cookie auth. Direct browser pairing GET expires stale rows, returns no device facts for expired/consumed/superseded/unknown pairings and rate-limits direct pairing-id probes.

- 2026-06-15 Agent GUI browser-pairing handoff: start with `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/server_api.py`, `pc_agent/tests/test_account_gate.py`, `server/web_api/registry_handlers.py`, `server/registry/browser_pairing_service.py`, `server/tests/test_registration_api.py`, `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md`, `server/docs/SECURITY_AND_AUTH.md` and `pc_agent/docs/CODEMAP.md`. `main_window.py` must open `/app/device/login` and `/app/device/register` on the HTTPS web boundary for non-local legacy `http://<host>:8666/api` agent API settings, so browser confirmation keeps the secure `pc_client_web_session` cookie while the agent still polls `/api/registry/agent/browser-pairings/{pairing_id}` with its machine token and receives the one-time account `session_token` only on agent pickup. Browser-pairing web APIs accept web-session roles `admin`, `support` and `user`, but login confirmation must still resolve to a registry person with an active primary/shared/responsible binding for the pairing device.

- 2026-06-08/10/16 Authenticated requester workspace: start with `server/requester/identity_service.py`, `server/web_api/requester_handlers.py`, `server/access_control/catalog.py`, `server/routes.py`, `server/tests/test_requester_workspace_api.py`, `webapp/src/pages/requester/index.tsx`, `webapp/src/features/requester/api.ts`, `webapp/src/features/requester/types.ts`, `webapp/src/app/navigation.tsx`, `webapp/src/features/auth/workspace-access.ts` and `webapp/src/app/router.tsx`. Browser requester routes live under `/app/requester` and APIs under `/api/web/requester/*`; the server resolves the registry person from the web session and active device bindings/account sessions. `GET /profile` returns a requester-safe read-only profile detail with person fields, identity aliases without `metadata_json`/`normalized_identifier`, owned devices, active bindings, pending claims and a profile edit policy. `GET /devices` lists safe owned devices, and `GET /devices/{device_id}` returns requester-safe device detail with relationship/status, safe asset facts, open ticket count, available actions and recent owned tickets while returning 404 for foreign devices. Ticket bootstrap/list/detail/device detail are scoped by requester person, active binding and account session, so one shared `device_id` can be visible to multiple users without exposing another user's tickets. Owned-device ticket preview/create must reject browser-supplied foreign `device_id`; no-device ticket preview/create is allowed only when the web user resolves to a registry person, uses a server-owned placeholder `device_id`, stores `requester_account_mode=browser_no_device`, `request_context=no_device` and no binding id, then remains visible through `requester_person_id`. `create_flow.py` persists non-primary shared/responsible confirmed bindings for browser requester create only after the requester wrapper has already validated ownership and set `requester_account.validation=web_requester_identity_resolved`; arbitrary legacy payloads remain unverified. Create validates Service Catalog/request form payload through the same runtime/create pipeline as public intake, stores explicit catalog/reporting fields on create, and the React requester form uses authenticated safe `POST /api/web/requester/tickets/preview` before submit when a catalog offering is selected. On-behalf requester creation is form-policy gated: `GET /api/web/requester/on-behalf/people` searches affected employees only within `on_behalf_policy.allowed_scope`, and preview/create revalidate `ticket_context.affected_person_id`, `on_behalf_reason` and exact-search lookup before `create_flow.py` stores `ticket_context_v1`; diagnostics target the affected person's primary agent, not the creator's selected device. Authenticated requester create also reuses requester-safe knowledge suggestions through `POST /api/knowledge/suggest`, records feedback through `/api/knowledge/feedback`, and stores sanitized `knowledge_attempts` plus `ticket_created_after_view` metrics when a ticket is created after a viewed/failed suggestion. `GET /tickets/{ticket_id}` returns requester-safe messages/events, `POST /tickets/{ticket_id}/message` writes owned-ticket public requester chat messages, accepts `attachment_refs` from same-session `/api/upload`, permits attachment-only messages, and returns/render attachment descriptors for download through `/api/artifacts/{artifact_id}/download`; owned-ticket lifecycle actions are `POST /tickets/{ticket_id}/close|feedback|reopen` using the same requester ownership resolver before delegating to workflow/quality services.
- 2026-06-09/11 Stage 2B public ticket claim: `POST /api/web/requester/tickets/claim-public` verifies the existing public access code with `tickets.public_access.verify_public_access_code()`, requires the logged-in requester to resolve to a `RegistryPerson`, updates `tickets.requester_id` / `requester_person_id`, clears the public `unbound` flag, writes `requester_ticket_claimed` audit without storing the code, and `/app/requester` exposes the claim form before opening the claimed ticket. Unlinked web users receive `REQUESTER_IDENTITY_REQUIRED` and the UI directs them to admin account linking instead of creating a web-login-only ticket claim.

- 2026-05-23 Device registration account sessions: start with `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md`, `server/registry/account_session_service.py`, `server/tickets/account_access_service.py`, `server/tickets/create_flow.py`, `server/tickets/handlers.py`, `server/uploads/handlers.py`, `server/tools/handlers.py`, `server/websocket/agent_handshake.py`, `pc_agent/core/account_session.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/automation_controller.py`, `pc_agent/ui_gui/server_api.py`, `webapp/src/features/admin/device-inventory-panel.tsx` and `webapp/src/pages/tickets/list-page.tsx`. Agent requester actions require a server-valid account session; verified-other-account sessions are admin-approved and see only their own tickets; confirmed bindings retain historical owner visibility; registration-pending is server-issued; create preview, automation create-ticket, local automation ticket tool actions (`ticket.tool.run`, `ticket.capture_screenshot`, `ticket.capture_video`), artifact upload/download, ticket list/detail/message/read/close and handshake must not bypass the account-session boundary. The agent Account page is the read-only server-session center with logout, expiry/status/warning fields, and pending other-account request polling; support UI shows the `Аккаунт обращения` context and warning for other-account tickets.
- 2026-06-14 Support detail account-session projection: start with `server/app/api/serializers.py`, `server/web_api/dto/support.py`, `server/web_api/support_handlers.py`, `server/tests/test_web_support_api.py::test_build_support_detail_payload_projects_requester_account_context`, `webapp/src/features/queues/api.ts`, `webapp/src/features/queues/support-workspace-mappers.ts` and `webapp/src/features/queues/support-workspace-mappers.test.ts`. `_ticket_payload()` is the source for `requester_registration_status`, `requester_account_session_id`, `requester_account_mode`, `requester_account_context` and `requester_account_warning`; `SupportTicketDetail` and aggregate `/api/web/support/tickets/{ticket_id}/workspace` must carry those fields so `/app/tickets` can render `Обращение создано с другого аккаунта на зарегистрированном устройстве.` for verified-other-account tickets.
- 2026-06-13/14 Registry Visibility Foundation: start with `PLANS.md`, `server/docs/REGISTRY_VISIBILITY_FOUNDATION.md`, `server/docs/REGISTRY_MANAGEMENT_CENTER.md`, `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md`, `server/docs/KNOWLEDGE_PLATFORM.md`, `server/docs/KNOWLEDGE_OPERATIONS.md`, migrations `120` and `121`, `server/registry/effective_identity_service.py`, `server/registry/audience_contracts.py`, `server/registry/audience_group_service.py`, `server/registry/account_state_service.py`, `server/registry/account_session_service.py`, `server/registry/registration_service.py`, `server/registry/registration_form_service.py`, `server/registry/policy_service.py`, `server/web_api/registry_handlers.py`, `server/routes.py`, `server/tests/test_registry_effective_identity_service.py`, `server/tests/test_registry_audience_groups.py`, `server/tests/test_knowledge_access_service.py`, `server/access_control/*`, `server/knowledge/contracts.py`, `server/knowledge/access_service.py`, `server/knowledge/visibility.py`, `server/knowledge/search_service.py`, `server/knowledge/retrieval_service.py`, `server/knowledge/portal_service.py`, `server/web_api/knowledge_handlers.py`, `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/features/admin/registry/*`, `pc_agent/core/account_session.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/main_window.py` and `pc_agent/ui_gui/server_api.py`. Keep departments as organization, access groups as RBAC/queue permissions and audience groups as content/service targeting; UI-login links stay verified `registry_person_identities(provider='ui_login')`; `device_user_bindings` stays authoritative; account-session identity must validate through `AccountSessionService`; Phase 1 admin read/explain APIs live under `/api/web/admin/registry/identity/effective`, `/identity/person/{person_id}/audience` and `/identity/session/{session_id}/explain`; Phase 2 audience-group APIs live under `/api/web/admin/registry/audience-groups*` and must not grant RBAC permissions; Phase 5 starts with `knowledge_audience_rules`, `KnowledgeAccessService`, `KnowledgeAudienceRulesService`, admin-only audience-rule/explain routes plus HTTP search/suggestion/article-detail, portal home/space/tag/action, Ask/RAG/vector retrieval and support ticket knowledge-suggestions enforcement; live anti-leak evidence is recorded by `scripts/knowledge_audience_live_smoke.py` under `artifacts/browser_live_validation/phase5-knowledge-audience-20260614145845/live-smoke-report.json`; Phase 6 authoring UI evidence is under `artifacts/browser_live_validation/phase6-knowledge-visibility-8d29b879-20260614-153908/`; Phase 7 repeatable HTTP/DB smoke starts with `scripts/registry_visibility_live_smoke.py`, while final Phase 7 still requires real agent GUI/UIA and browser evidence.
- 2026-06-14 Registry Visibility Foundation Phase 5 admin API slice: start with `server/knowledge/audience_rules_service.py`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_audience_rules.py`, `server/knowledge/access_service.py` and `server/registry/effective_identity_service.py`. Admin-only routes are `GET/PUT /api/web/admin/knowledge/audience-rules`, `POST /api/web/admin/knowledge/audience-rules/preview` and `GET /api/web/admin/knowledge/access/explain`; list/replace writes `knowledge_audience_rules`, preview/explain use Registry effective audience plus `KnowledgeAccessService`, preview supports `subject_type=item` and `subject_type=space`, `department` is exact current-department matching while `department_tree` is path/subtree matching, active Registry audience groups resolve as `{audience_group_id, code}`, support is explicitly denied from admin rule management, and later slices add search/suggestion/article/portal plus Ask/RAG/vector retrieval enforcement.
- 2026-06-14 Registry Visibility Foundation Phase 6 authoring UI slice: start with `webapp/src/features/knowledge/article-visibility-panel.tsx`, `webapp/src/features/knowledge/article-visibility-model.ts`, `webapp/src/features/knowledge/article-visibility-panel.test.tsx`, `webapp/src/features/knowledge/authoring/EditorMetadataStep.tsx`, `webapp/src/features/knowledge/api.ts` and `webapp/src/pages/admin/knowledge-studio-page.test.tsx`. `/app/admin/knowledge/studio` settings embeds the Russian-first `Аудитория` panel for selected articles: it lists saved item audience rules, lets authors add person/department/department_tree/location/access_group/audience_group/role/service allow rules from Registry/Admin lookups, estimates matched Registry people, warns about broad/empty/internal visibility, calls admin preview/explain endpoints, and saves rules through `PUT /api/web/admin/knowledge/audience-rules` with a reason. Browser/live-smoke evidence for commit `8d29b879` is recorded under `artifacts/browser_live_validation/phase6-knowledge-visibility-8d29b879-20260614-153908/`.
- 2026-06-14 Registry Visibility Foundation Phase 7 smoke harness: start with `scripts/registry_visibility_live_smoke.py`, `scripts/test_registry_visibility_live_smoke.py`, `server/registry/account_session_service.py`, `server/tickets/account_access_service.py`, `server/registry/effective_identity_service.py`, `server/knowledge/search_service.py`, `server/knowledge/suggestion_service.py`, `server/knowledge/ask_service.py` and `server/web_api/support_handlers.py`. The helper seeds deterministic Registry/Knowledge data and writes a sanitized report under `artifacts/registry-visibility-foundation-YYYYMMDD/`; it verifies confirmed-binding, verified-other-account, registration-pending and revoked-session HTTP/DB boundaries plus service-level account-session Knowledge audience and live support suggestions. `registration_pending` sessions are polling/gate state only: normal agent ticket create must return `ACCOUNT_ACCESS_DENIED`, while existing pending-status ticket view/list paths remain scoped. It deliberately reports `real_agent_gui.status=not_collected`; do not use it as final Phase 7 signoff without same-run real agent UIA/browser evidence.
- 2026-06-14 Registry Visibility Foundation Phase 8 operability: start with `server/registry/service.py`, `server/registry/admin_operations_service.py`, `server/web_api/registry_handlers.py`, `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/features/admin/registry/registry-quality-tab.tsx`, `webapp/src/features/admin/registry/registry-import-dialog.tsx`, `scripts/registry_visibility_phase8_live_signoff.py`, `server/tests/test_registry_quality_remediation.py`, `server/tests/test_registry_import_export.py` and `webapp/src/features/admin/registry/registry-quality-tab.test.tsx`. Registry quality now generates actionable issues for unlinked active UI users, active people attached to archived department/location rows, active bindings to inactive people, empty audience groups, invalid Knowledge audience-rule targets and requester-visible Knowledge items scoped to zero resolvable Registry users. Safe CSV export includes audience groups, audience group members and Knowledge audience rules; import preview/apply supports audience groups and members only, writes `registry_import_applied`, and still does not import direct device bindings or account sessions. The Phase 8 live API/DB signoff helper seeds controlled invalid states, checks `/api/web/admin/registry`, export formula escaping, import preview/apply and quality override/timeline evidence, and writes a sanitized report under `artifacts/registry-visibility-foundation-YYYYMMDD/`. Canonical Phase 8 signoff for commit `6004e541` is `artifacts/registry-visibility-foundation-20260614/registry-visibility-phase8-live-signoff-phase8-6004e541-20260614190013.json`; browser quality/import evidence is under `artifacts/browser_live_validation/phase8-registry-operability-6004e541-20260614-190013/`.
- 2026-06-14 Registry admin localization/help pass: start with `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/features/admin/registry/registry-utils.ts`, `webapp/src/features/admin/registry/registry-devices-tab.tsx`, `registry-people-tab.tsx`, `registry-bindings-tab.tsx`, `registry-account-sessions-tab.tsx`, `registry-detail-drawer.tsx`, `registry-import-dialog.tsx`, `registry-quality-tab.tsx`, `registry-policies-tab.tsx`, `registry-requests-tab.tsx`, `registry-access-groups-tab.tsx` and `registry-audience-groups-tab.tsx`. `/app/admin/registry` is Russian-first across tabs, tables, dialogs and the detail drawer; each tab now has operator-facing helper text, main actions have native `title` tooltips, and status/relationship/account-session/source/actor labels are centralized in `registry-utils.ts`. Technical enum/API/table names may remain English as diagnostic fallbacks, but normal visible operator copy should stay Russian-first and mojibake-free. Local guard checks are the focused Registry vitest suite, `pnpm --dir webapp exec tsc --noEmit`, `pnpm --dir webapp build`, `python scripts/verify_workspace.py` and `git diff --check`; canonical browser evidence for commit `17674dea` is under `artifacts/browser_live_validation/registry-localization-17674dea-20260614-165326/`.
- 2026-06-14 Registry Visibility Foundation Phase 5 search API enforcement: start with `server/web_api/knowledge_handlers.py`, `server/knowledge/search_service.py`, `server/registry/effective_identity_service.py`, `server/tests/test_knowledge_api.py::test_public_search_applies_registry_audience_rules_before_projection` and `server/tests/test_knowledge_search.py::test_knowledge_search_applies_audience_rules_before_projection`. `/api/knowledge/search` and `/api/web/knowledge/search` resolve Registry effective audience from the authenticated actor and filter audience-scoped candidates before returning requester-safe projections; later slices add suggestion, article-detail, portal and Ask/RAG/vector retrieval enforcement.
- 2026-06-14 Registry Visibility Foundation Phase 5 suggestion API enforcement: start with `server/knowledge/suggestion_service.py`, `server/web_api/knowledge_handlers.py`, `server/knowledge/search_service.py`, `server/registry/effective_identity_service.py` and `server/tests/test_knowledge_api.py::test_public_suggestions_apply_registry_audience_rules_before_projection`. `/api/knowledge/suggest` now passes effective audience into both normal and fallback search calls; later slices add article-detail, portal and Ask/RAG/vector retrieval enforcement.
- 2026-06-14 Registry Visibility Foundation Phase 5 article-detail enforcement: start with `server/knowledge/portal_service.py`, `server/web_api/knowledge_handlers.py`, `server/registry/effective_identity_service.py` and `server/tests/test_knowledge_portal.py::test_knowledge_article_detail_applies_audience_rules_before_body_projection`. `/api/knowledge/articles/{slug}` resolves effective audience and denies audience-scoped requester articles before body/segment projection; later slices add portal and Ask/RAG/vector retrieval enforcement.
- 2026-06-14 Registry Visibility Foundation Phase 5 portal list enforcement: start with `server/knowledge/portal_service.py`, `server/web_api/knowledge_handlers.py`, `server/registry/effective_identity_service.py` and `server/tests/test_knowledge_portal.py::test_knowledge_portal_home_applies_audience_rules_before_article_projection` / `::test_knowledge_portal_collections_apply_audience_rules_before_article_projection`. `/api/knowledge/portal/home`, `/api/knowledge/portal/spaces/{space_code}` and `/api/knowledge/portal/tags/{tag}` resolve requester effective audience and filter items through `KnowledgeAccessService` before article summaries, featured/popular/recent lists or collection payloads are projected; later slices add action and Ask/RAG/vector retrieval enforcement.
- 2026-06-14 Registry Visibility Foundation Phase 5 portal action enforcement: start with `server/web_api/knowledge_handlers.py`, `server/knowledge/portal_service.py`, `server/registry/effective_identity_service.py` and `server/tests/test_knowledge_portal.py::test_knowledge_portal_actions_apply_audience_rules_before_writes`. Requester portal `POST /api/knowledge/articles/{slug}/feedback`, `POST /api/knowledge/articles/{slug}/correction-request` and `POST|DELETE /api/knowledge/articles/{slug}/bookmark` resolve requester effective audience before loading article context; denied audience-scoped articles return 404 and do not write feedback, correction or bookmark rows.
- 2026-06-14 Registry Visibility Foundation Phase 5 Ask/RAG/vector retrieval enforcement: start with `server/knowledge/retrieval_service.py`, `server/knowledge/ask_service.py`, `server/web_api/knowledge_handlers.py`, `server/registry/effective_identity_service.py` and `server/tests/test_knowledge_ask.py::test_public_knowledge_ask_applies_audience_rules_before_vector_retrieval_projection`. `/api/knowledge/ask`, `/api/web/knowledge/ask`, `/api/web/knowledge/ask/preview`, `/api/web/knowledge/retrieve` and `/api/web/knowledge/search/preview` resolve Registry effective audience and filter keyword/segment/vector retrieval candidates through `KnowledgeAccessService` before ordering, rerank, citations and answer prompt construction.
- 2026-06-14 Registry Visibility Foundation Phase 5 support ticket knowledge-suggestions enforcement: start with `server/web_api/support_handlers.py`, `server/knowledge/suggestion_service.py`, `server/knowledge/search_service.py`, `server/registry/effective_identity_service.py`, `server/tests/test_web_support_api.py::test_web_support_ticket_knowledge_suggestions_apply_requester_audience_rules` and live helper `scripts/knowledge_audience_live_smoke.py`. `/api/web/support/tickets/{ticket_id}/knowledge-suggestions` and aggregate `/workspace` resolve the ticket requester audience for requester-safe P2 suggestions, while keeping `support_internal` P2 runbooks from the support context; audience-scoped articles for another department must be absent from article ids, titles and payload text. Live evidence report `artifacts/browser_live_validation/phase5-knowledge-audience-20260614145845/live-smoke-report.json` confirms requester search/suggest/Ask service checks, live support suggestions and admin explain denial; Phase 6 authoring UI remains next.
- 2026-06-13 Registry Visibility Foundation Phase 3 UI slice: start with `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/pages/admin/registry-page.test.tsx`, `webapp/src/features/admin/api.ts`, `webapp/src/features/admin/registry/registry-audience-groups-tab.tsx`, `registry-access-groups-tab.tsx`, `registry-bulk-action-dialog.tsx`, `registry-link-ui-user-dialog.tsx`, `registry-reason-dialog.tsx`, `registry-bind-person-dialog.tsx` and `registry-quality-tab.tsx`. `/app/admin/registry` now includes `Группы доступа · P1` as a read-only summary over `/api/web/admin/access/summary` with the canonical editor remaining `/app/admin/access`, plus `Аудитории · P1` over `/api/web/admin/registry/audience-groups*`; audience member save requires preview plus reason. The known Registry `window.prompt` paths are regression targets and should remain replaced by Russian-first dialogs with searchable pickers, preview-before-apply where available and normalized result reports. Live browser evidence on `https://192.168.100.17:9443/admin` is still required after this tab change before Phase 3 exit.
- 2026-06-13 Registry Visibility Foundation Phase 4 registration hardening: start with `PLANS.md`, `server/docs/REGISTRY_VISIBILITY_FOUNDATION.md`, `server/registry/registration_service.py`, `server/registry/registration_form_service.py`, `server/registry/policy_service.py`, `server/tests/test_registry_registration_policy.py`, `server/tests/test_device_registration_service.py`, `server/tests/test_registration_api.py`, `server/tests/test_account_session_service.py`, `webapp/src/features/admin/registry/registry-requests-tab.tsx`, `webapp/src/features/admin/registry/registry-policies-tab.tsx`, `webapp/src/pages/admin/registry-page.test.tsx` and `pc_agent/ui_gui/account_gate.py`. Required-existing department/location modes use registry pickers, reject missing/free-text/invalid ids and are guarded by `test_registry_registration_policy.py`; approval now applies validated claim `department_id` / `location_id` to verified existing people before derived asset/inventory sync. Phase 4 UI slices add the registration approval diff in `/app/admin/registry` -> `Заявки` and first-class `Режим подразделения` / `Режим локации` controls in `Политики · P1` over the existing Registry policy endpoints. Phase 4 exit still requires real connected-agent registration/account-session scenarios.
- 2026-06-14 Registry Visibility Foundation Phase 4 browser registration strict payload: start with `webapp/src/pages/device-pairing/index.tsx`, `webapp/src/pages/device-pairing/api.ts`, `webapp/src/pages/device-pairing/device-pairing-page.test.tsx`, `server/auth/middleware.py`, `server/registry/browser_pairing_service.py`, `server/web_api/registry_handlers.py`, `server/registry/registration_service.py`, `server/tests/test_registration_api.py::test_registration_pairing_confirmation_accepts_required_registry_ids`, `server/tests/test_web_session_api.py::test_web_session_cookie_auth_bridges_react_workbench_paths` and `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md`. `/app/device/register` reuses authenticated `GET /api/registry/options`, which must accept the same web-session cookie as the device-pairing page without broadening cookie auth to all agent registry endpoints, renders department/location selects when registry options exist, and sends only selected `department_id` / `location_id` in `POST /api/web/registry/browser-pairings/{pairing_id}/registration/confirm`; browser confirmation must not accept browser-supplied person, binding, account-session or token fields, and strict validation remains centralized in `RegistrationService`.
- 2026-05-28 Tool routing isolation: start with `server/tools/handlers.py`, `server/tools/service.py`, `server/app/repos/ticket_events_repo.py`, `server/tests/test_tools_run_device_binding.py` and `server/tests/test_tools_async_response_contract.py`. Legacy direct `/api/tools/run` must validate that the requested `ticket_id` belongs to the requested `device_id` before policy checks, operation creation or `device_outbox` dispatch; mismatches return `UNKNOWN_TICKET` or `DEVICE_MISMATCH` and must not create `tool_call_started`, operation rows or agent commands.
- 2026-05-28 `screen.record` Windows live fallback: start with `pc_agent/modules/impl/screen.py`, `pc_agent/tests/test_builtin_modules_screen_system.py` and `pc_agent/docs/CODEMAP.md`. The built-in screen recorder captures bounded ffmpeg stderr, first tries rawvideo stdin, then falls back to a temporary PNG frame sequence when the live Windows agent's raw pipe exits before frames are accepted; fallback success still returns the normal `screen_recording` artifact, includes `encoder_mode=image_sequence_fallback` plus the raw encoder error in observations, and cleans the temporary frame directory. The packaged `pc_agent/modules_packages/screen/module.py` copy is still a legacy implementation until the module-package sync pass.
- 2026-05-28 P2 public/requester ticket projection hardening: start with `server/tickets/visibility_policy.py`, `server/tickets/requester_timeline.py`, `server/tickets/handlers.py`, `server/docs/TICKET_SYSTEM.md` and `server/tests/test_requester_timeline_projection.py`. Public-token/requester `/api/tickets/{ticket_id}` responses use an explicit allowlist for ticket fields and requester-safe event/message serializers; they must not expose `queue_id`, `device_id`, `requester_id`, `assignee_id`, raw `custom_fields`, routing/priority/SLA policy internals, raw event payloads, token/session material or public access code hashes/codes in timeline metadata/text. Public access system notices project as neutral `ticket_access_notice`, not the internal `ticket_public_access_code` event kind.
- 2026-05-13 service desk P0 contract hardening: ticket statuses are canonicalized only through `server/tickets/statuses.py` (`new`, `queued`, `assigned`, `in_progress`, `waiting_on_user`, `waiting_on_internal_team`, `waiting_on_vendor`, `waiting_on_approval`, `scheduled`, `resolved`, `closed`, `canceled`); `triaged` is a legacy input/backfill alias only. Migration `081_ticket_contract_hardening` enforces `ck_tickets_status_canonical`, non-empty `requester_id`, SLA-bearing priority and deterministic ticket-event ordering. Public queue endpoints return sanitized projection only, reject numeric `queue_id`, expose no internal queue names, and Policy Health lives in `server/tickets/policy_health_service.py`, `server/web_api/policy_health_handlers.py`, `/api/web/admin/helpdesk/policy-health*` and `/app/admin/policy-health`; simulation is a dry-run over the real routing/priority/SLA/OLA/approval/closure/visibility/diagnostic resolvers.
- 2026-05-14 P1/P1.1 Service Catalog: start with `server/docs/SERVICE_CATALOG.md`, `server/app/repos/service_catalog_repo.py`, `server/tickets/service_catalog_defaults.py`, `server/tickets/service_catalog_runtime.py`, `server/tickets/service_catalog_preview.py`, `server/tickets/service_catalog_publication.py`, `server/web_api/service_catalog_handlers.py`, `scripts/seed_service_catalog.py`, `webapp/src/features/service-catalog/*`, `webapp/src/pages/help/index.tsx`, and `pc_agent/ui_gui/chat_panel.py`. Migration `082` adds `helpdesk_services`, `helpdesk_service_offerings`, `helpdesk_service_catalog_audit` and explicit `tickets.catalog_*`/`service_code`/`offering_code` reporting fields. Admin UI route is `/app/admin/service-catalog` with structured service/offering editors and Advanced JSON only as fallback; safe requester/agent catalog API is `/api/service-catalog/current`, safe runtime preview is `POST /api/service-catalog/preview`, ticket create/preview may infer catalog selection from `form_key` only when exactly one published offering maps to that template, fallback is `other.unknown` (`Другое / Не знаю`), and baseline setup is `python scripts/seed_service_catalog.py --dry-run|--force`.
- 2026-06-02 Agent Service Catalog wizard delivery: start with `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/ticket_create_wizard_widgets.py`, `pc_agent/ui_gui/automation_controller.py`, `pc_agent/ui_bridge/api_server.py`, `scripts/live_agent_uia_create_ticket.py` and `pc_agent/tests/test_chat_panel_helpers.py`. The agent create wizard refreshes on both form-pack and service-catalog changes; published Studio offerings should render as `TicketTypeCard:<service.offering>` UIA cards without reopening the GUI. `/ui/automation/status` returns HTTP 200 `status=unavailable` until the GUI automation provider is attached, and attached GUI status reports `has_account_session` rather than a legacy active-profile flag. Automation `ticket.create`/`ticket.open` must start detail polling with `TICKET_DETAIL_POLL_INTERVAL_MS`, not a fresh `QTimer.interval()` value, or the GUI can spin at interval 0. The UIA create-ticket live probe is fail-fast: if the requested published card is absent, it must stop instead of creating a legacy fallback ticket.
- 2026-05-15 P2.1 Knowledge acceptance hardening: start with `server/docs/KNOWLEDGE_PLATFORM.md`, `server/knowledge/*`, `server/app/repos/knowledge_repo.py`, `server/web_api/knowledge_handlers.py`, migrations `083_knowledge_platform` and `084_knowledge_acceptance_constraints`, `webapp/src/features/knowledge/*`, `/app/admin/knowledge`, `/app/knowledge`, `/app/help`, and `pc_agent/ui_gui/server_api.py` / `chat_panel.py`. Shared safe APIs are auth-whitelisted for the public `/app/help` deflection flow: `POST /api/knowledge/search|suggest|feedback`; admin/support management APIs are under `/api/web/knowledge/*`. Requester/agent/public suggestions are ACL-filtered before results, support/auditor direct reads do not expose `admin_internal` or `security_restricted`, admin publish selects an explicit version id instead of requiring `current_version_id`, stale passport drafts require acknowledgement, deflection/not-helpful feedback is stored in `knowledge_feedback_events`, failed self-service attempts are attached as safe `knowledge_attempts`, and existing `/api/tickets/{id}/kb_links` remains compatible.
- 2026-05-15 P2.2 Knowledge Operations: start with `server/docs/KNOWLEDGE_OPERATIONS.md`, `server/knowledge/content_pack_service.py`, `server/knowledge/content_templates.py`, `server/knowledge/content_lint.py`, `server/knowledge/review_task_service.py`, `server/knowledge/quality_service.py`, `server/knowledge/gap_service.py`, `server/knowledge/search_analytics_service.py`, `scripts/seed_knowledge_content.py`, `content_packs/knowledge/`, migrations `085_knowledge_operations` and `086_knowledge_ops_models`, `webapp/src/features/knowledge/knowledge-admin-panel.tsx`, and [server/docs/KNOWLEDGE_PLATFORM.md](../server/docs/KNOWLEDGE_PLATFORM.md). Admin operations include idempotent content packs (`--dry-run`, unchanged skip, conflict, explicit `--force`, retire), content templates/lint, first-class review tasks, quality score, persisted Service Catalog gap findings, search analytics and rollout policies. Requester/agent deflection honors rollout policies before search, including min/max suggestions, no-suggestions/API-unavailable behavior, known-error hiding and safe quality/freshness labels; support/admin/auditor and support workspace remain operational.
- Knowledge Platform vNext: начинать с `PLANS.md`, `server/docs/KNOWLEDGE_VNEXT_ARCHITECTURE.md`, `server/docs/KNOWLEDGE_PLATFORM.md`, `server/docs/KNOWLEDGE_OPERATIONS.md`, `server/knowledge/*`, `server/ai/*`, `server/web_api/knowledge_handlers.py`, `server/web_api/knowledge_ai_handlers.py`, `server/app/repos/knowledge_repo.py`, `webapp/src/features/knowledge/*`, `webapp/src/pages/kb/search-page.tsx`, `webapp/src/pages/kb/ask-page.tsx`, `webapp/src/pages/admin/knowledge-page.tsx`, `webapp/src/pages/admin/knowledge-ai-page.tsx`, `webapp/src/pages/admin/knowledge-search-settings-page.tsx`, `webapp/src/pages/knowledge/index.tsx`, `webapp/src/app/navigation.tsx` и `webapp/src/app/router.tsx`. AI provider settings foundation lives in `server/ai/provider_registry.py`, `server/ai/openrouter_client.py`, migration `110_ai_provider_settings`, admin API routes under `/api/web/knowledge/ai/*`, React route `/app/admin/knowledge/ai`, and tests `server/tests/test_ai_provider_settings.py` / `server/tests/test_ai_openrouter_client.py` / `server/tests/test_knowledge_ai_api.py` / `webapp/src/features/knowledge/ai-settings-page.test.tsx`; raw AI keys are represented only by secret refs and masked state. AI-off search settings foundation lives in `server/knowledge/search_settings_service.py`, migration `111_knowledge_search_settings`, `GET/POST /api/web/knowledge/search-settings`, authenticated `POST /api/web/knowledge/search`, public-compatible `POST /api/knowledge/search`, React route `/app/admin/knowledge/search-settings`, requester route `/app/kb/search`, and tests `server/tests/test_knowledge_search_settings.py` / `webapp/src/features/knowledge/search-settings-page.test.tsx` / `webapp/src/pages/kb/search-page.test.tsx`; default search must work without AI providers, embeddings or vector indexes. Article segmentation backend/UI lives in `server/knowledge/segmentation_service.py`, migration `112_knowledge_article_segments`, routes `GET/POST /api/web/knowledge/items/{item_id_or_slug}/segments`, `POST /api/web/knowledge/items/{item_id_or_slug}/segments/auto`, `POST /api/web/knowledge/items/{item_id_or_slug}/segments/revalidate`, `POST /api/web/knowledge/items/{item_id_or_slug}/segments/ai-proposals`, `PATCH|DELETE /api/web/knowledge/segments/{segment_id}`, `POST /api/web/knowledge/segments/{segment_id}/approve`, `POST /api/web/knowledge/segments/{segment_id}/reject`, `GET|POST /api/web/knowledge/segmentation-profiles`, `webapp/src/features/knowledge/article-segmentation-panel.tsx`, and tests `server/tests/test_knowledge_segments.py` / `server/tests/test_knowledge_search_segments.py` / `webapp/src/features/knowledge/article-segmentation-panel.test.tsx`; active segment title/text/keywords must affect AI-off search without enabling vector/RAG, version revalidation must create exact-remapped or stale target-version copies, AI proposals must remain draft until approve, and segmentation lifecycle emits Observer-visible `agent_runtime_audit` rows. Весь новый Knowledge vNext product UI и safe display errors должны быть Russian-first; routes/API fields/enums/observer event codes/metric names/model task codes остаются на английском, а UI tests должны проверять репрезентативные русские labels и отсутствие mojibake в visible text.
- 2026-06-12 Knowledge Portal Phase 7: start with `server/knowledge/portal_service.py`, `server/web_api/knowledge_handlers.py`, migration `114_knowledge_portal_persistence`, `server/routes.py`, `server/auth/middleware.py`, `webapp/src/pages/kb/home-page.tsx`, `webapp/src/pages/kb/article-page.tsx`, `webapp/src/pages/kb/collection-page.tsx`, `webapp/src/features/knowledge/api.ts`, `server/tests/test_knowledge_portal.py` and KB page tests. Public-compatible routes are `GET /api/knowledge/portal/home`, `GET /api/knowledge/portal/spaces/{space_code}`, `GET /api/knowledge/portal/tags/{tag}`, `GET /api/knowledge/articles/{slug}`, `POST /api/knowledge/articles/{slug}/feedback`, `POST /api/knowledge/articles/{slug}/correction-request` and `POST|DELETE /api/knowledge/articles/{slug}/bookmark`; every route forces requester ACL before returning or writing data. Article reads persist `knowledge_article_views`; correction requests persist `knowledge_correction_requests` with the user-entered comment; bookmark add/remove upserts `knowledge_user_bookmarks`; `knowledge_article_subscriptions` exists for later subscription UI. Portal home ranks `popular_articles` and `featured_articles` from persisted view/bookmark/helpful/correction signals, falling back to recent articles only when no signals exist. `/api/knowledge/*` accepts the standard web-session cookie so `/app/kb` requester users can load portal APIs, and unsafe cookie-auth writes remain covered by the shared same-origin check.
- 2026-06-12/15 Knowledge Authoring Studio: start with `webapp/src/features/knowledge/authoring-studio-page.tsx`, `webapp/src/pages/admin/knowledge-studio-page.tsx`, `webapp/src/features/knowledge/article-segmentation-panel.tsx`, `webapp/src/features/knowledge/api.ts`, `server/knowledge/editor_history_service.py`, migration `115_knowledge_editor_history`, `server/web_api/knowledge_handlers.py`, `server/routes.py` and `webapp/src/pages/admin/knowledge-studio-page.test.tsx`. `/app/admin/knowledge/studio` is now the simplified Russian-first authoring surface with draft/article browser, new draft creation, TipTap editor, template insertion, structured Markdown block insertion (`callout`, table, code block, checklist), requester-safe preview, one `Сохранить статью` action, persisted `История редактора`, and confirmed `История версий` rollback. Default UI hides review lifecycle actions and manual segmentation; segment markup remains under advanced tools. `GET /api/web/knowledge/items/{item_id_or_slug}/editor-history` returns admin/support/auditor-safe `knowledge_article_editor_events` plus `knowledge_version_diff_cache` summaries without article body, raw metadata, source refs or ticket/device fields.
- 2026-06-12 Knowledge Graph Studio Phase 9 layout/CRUD slice: start with `server/knowledge/graph_service.py`, migration `116_knowledge_graph_layouts`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_api.py`, `webapp/src/features/knowledge/graph-studio-page.tsx`, `webapp/src/pages/admin/knowledge-graph-page.tsx`, `webapp/src/features/knowledge/api.ts`, `webapp/src/features/knowledge/graph-studio.test.tsx`, `webapp/src/app/router.tsx` and `webapp/src/app/navigation.tsx`. `/app/admin/knowledge/graph` is a Russian-first SVG graph studio over graph APIs: `GET/POST/PATCH/DELETE /api/web/knowledge/graph/nodes`, `GET /api/web/knowledge/graph/search`, `GET /api/web/knowledge/graph/nodes/{node_id_or_stable_key}/neighborhood?depth=2`, `GET/POST/PATCH/DELETE /api/web/knowledge/graph/edges` and `GET/POST /api/web/knowledge/graph/layouts/{scope}`. It supports node search, visual neighborhood canvas, inspector, manual node creation, selected-node label edit, edge creation, edge/node archive and persisted layout save/load.
- 2026-06-12 Knowledge Graph AI proposal lifecycle: start with `server/knowledge/ai_proposal_service.py`, migration `117_knowledge_ai_proposals`, `server/app/db/models.py`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_api.py`, `webapp/src/features/knowledge/api.ts` and `webapp/src/features/knowledge/graph-studio-page.tsx`. Proposal APIs are `GET|POST /api/web/knowledge/ai/proposals` and `POST /api/web/knowledge/ai/proposals/{proposal_id}/review`; admin/support can create/review, auditor is read-only, payloads are sanitized before storage/response, graph node/edge approvals apply through `KnowledgeGraphService`, and create/review emits Observer-visible `agent_runtime_audit` rows with source `knowledge_ai_proposals` and event types `knowledge.ai_proposal.created|approved|rejected|commented`.
- 2026-06-12 Knowledge Import/Ingestion Phase 10 first slices: start with `server/knowledge/ingestion_service.py`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_import_api.py`, `webapp/src/features/knowledge/import-wizard-page.tsx`, `webapp/src/pages/admin/knowledge-import-page.tsx`, `webapp/src/features/knowledge/import-wizard.test.tsx`, `webapp/src/features/knowledge/api.ts`, `webapp/src/app/navigation.tsx` and `webapp/src/app/router.tsx`. `/app/admin/knowledge/import` previews text/markdown/html sources and creates AI-off review drafts through `POST /api/web/knowledge/import/preview` and `POST /api/web/knowledge/import/create-drafts`. The parser detects markdown H1 title, H2-H6 sections, plain-text paragraphs and stripped HTML. The backend preview path now also supports safe base64 DOCX/PDF uploads with a 5 MiB limit; URL/Git source kinds are fail-closed by default with `remote_import_blocked`. Remote URL/Git import requires `KNOWLEDGE_REMOTE_IMPORT_ENABLED=true` plus exact or wildcard `KNOWLEDGE_REMOTE_IMPORT_ALLOWED_HOSTS`; URL import uses HTTPS, blocks credentials/redirects and returns only safe `remote_source`, while Git import clones an allowlisted HTTPS repo into a temp directory and reads only markdown/text/html files within configured byte/file limits. The import wizard now exposes source-kind controls for text/markdown/html/DOCX/PDF/URL/Git, file-to-base64 upload, remote policy messaging, auto-segmentation toggle and segmentation profile selection. `create-drafts` can run existing non-AI auto segmentation when `auto_segment_after_import=true`, using `segmentation_profile_code` or `default-auto`, and returns the segmentation job/segments. When global search settings have `vector_enabled=true`, `create-drafts` also runs the existing item indexing path for the created `item_id`/`version_id` and returns safe `indexing` job/stats/embedding metadata without raw vectors; with vectors disabled it returns `indexing.enabled=false`. When `ai_enrichment_enabled=true`, `create-drafts` creates governed pending `summary`, `tags`, `glossary_term`, `graph_node` and `duplicate` proposals through `KnowledgeAiProposalService`; proposal review uses the existing `/api/web/knowledge/ai/proposals/{proposal_id}/review` lifecycle and does not echo raw import body or source secrets. Import jobs are readable through `GET /api/web/knowledge/import/jobs` and `GET /api/web/knowledge/import/jobs/{job_id}` with knowledge-space ACL filtering; compatibility ingestion routes remain available. Preview/create/failure paths emit redacted Observer-visible `knowledge.import.*` events with source `knowledge_import`.
- 2026-06-12 Support Knowledge Workspace Phase 11 first slice: start with `webapp/src/features/knowledge/support-workspace-page.tsx`, `webapp/src/features/knowledge/support-workspace-page.test.tsx`, `webapp/src/pages/knowledge/index.tsx`, `webapp/src/app/router.tsx` and `webapp/src/features/knowledge/api.ts`. `/app/knowledge` is now a dedicated support workspace instead of `KnowledgeAdminPanel mode="support"`; `/app/knowledge/articles/:itemId` opens the same workspace with a selected article/runbook/known-error. The first slice reuses `GET /api/web/knowledge/items` and `GET /api/web/knowledge/items/{item_id_or_slug}/versions`, adds fast local search, requester-safe/support-internal/type filters, current-version detail and guarded `Copy-safe answer`.
- 2026-06-12 Support Knowledge Workspace Phase 11 ticket actions: start with `webapp/src/features/knowledge/support-workspace-page.tsx`, `webapp/src/features/knowledge/api.ts`, `server/routes.py`, `server/tickets/handlers.py`, `server/web_api/knowledge_handlers.py`, `server/tests/test_ticket_knowledge_links_compat.py` and `server/tests/test_knowledge_feedback.py`. `/app/knowledge` preserves optional `ticket_id` query context, links selected articles through web-session alias `POST /api/web/support/tickets/{ticket_id}/kb_links` with `source=knowledge_support_workspace`, and records `support_used` / `weak_article_reported` through `POST /api/knowledge/feedback` with `surface=support_workspace`. The legacy `/api/tickets/{ticket_id}/kb_links` route remains compatible for token/public-token clients. Observer rows use source `knowledge_support` and events `knowledge.support.ticket_linked`, `knowledge.support.article_used` and `knowledge.support.weak_article_reported`; audit details must stay id-only and redacted. Requester attempts and draft-from-resolution remain Phase 11 follow-up.
- 2026-06-12 Knowledge Ops Dashboard Phase 12 first slice: start with `server/knowledge/ops_summary_service.py`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_ops_summary.py`, `webapp/src/pages/admin/knowledge-page.tsx`, `webapp/src/features/knowledge/ops-dashboard-panel.tsx`, `webapp/src/features/knowledge/ops-dashboard-panel.test.tsx` and `webapp/src/features/knowledge/api.ts`. `GET /api/web/knowledge/ops/summary` aggregates coverage, quality, search/RAG, indexing, AI, graph, review and Observer-backed active knowledge degradations. `/app/admin/knowledge` is the Knowledge Operations Center: health summary plus quick transitions to Studio, Import, Graph, Search Settings, Indexing and AI. Governance/editor controls live on their dedicated routes instead of the ops landing page. Live trigger coverage for failed AI health, failed indexing, successful segmentation and successful ask remains Phase 12 follow-up.
- 2026-06-12 Knowledge RAG/Search Eval Phase 13: start with `server/knowledge/evaluation.py`, `server/tests/test_knowledge_rag_eval.py`, `server/knowledge/search_service.py`, `server/knowledge/ask_service.py`, `server/knowledge/retrieval_service.py`, `server/knowledge/segmentation_service.py` and `server/ai/provider_registry.py`. `KnowledgeEvalRecorder` captures repeatable test metrics for `top_k_recall`, `citation_precision`, `no_answer_correctness`, `acl_leakage_count`, `fallback_count`, `latency_ms` and `provider_failure_count`. The controlled suite seeds requester/support/admin/security knowledge, manual segments, known-error/workaround, Service Catalog binding, JSONB vectors and rerank config, then verifies keyword/body recall, requester/support ACL safety, no-evidence/provider-unavailable Ask fallback, requester-safe citation item ids, vector recall/fallback, rerank ordering, archived item exclusion and current-version-only search.
- 2026-06-12 Knowledge Metadata Model Phase 14: start with `server/knowledge/metadata_service.py`, `server/knowledge/quality_service.py`, migrations `118_knowledge_metadata_model` and `119_knowledge_quality_model_uniqueness`, `server/web_api/knowledge_handlers.py`, `server/routes.py`, `server/tests/test_knowledge_metadata_model.py`, `webapp/src/features/knowledge/api.ts`, `webapp/src/features/knowledge/ops-dashboard-panel.tsx` and `webapp/src/features/knowledge/ops-dashboard-panel.test.tsx`. APIs are `GET /api/web/knowledge/metadata`, `POST /api/web/knowledge/taxonomy`, `POST /api/web/knowledge/properties`, `GET|PUT /api/web/knowledge/items/{item_id_or_slug}/metadata`, `GET|POST /api/web/knowledge/items/{item_id_or_slug}/applicability` and `POST /api/web/knowledge/quality-models`. Admin/support mutate, taxonomy upsert checks requested term visibility, item metadata update checks assigned taxonomy term visibility, auditor/support reads filter assigned taxonomy terms by actor-visible visibility, and requester/public APIs remain backward-compatible without metadata bundle, applicability internals or quality model weights. `GET /api/web/knowledge/metadata` returns full visible management rows plus active/total `summary` counts; the Ops panel uses active counts and Russian-first dashboard/metadata labels. Quality scoring now adds optional model-weighted `properties`, `taxonomy` and `applicability` dimensions.
- 2026-06-12 Knowledge vNext Phase 3 completion: dedicated authoring route is `webapp/src/pages/admin/knowledge-studio-page.tsx` at `/app/admin/knowledge/studio`; segment index sync is `POST /api/web/knowledge/items/{item_id_or_slug}/segments/index-sync` in `server/knowledge/segmentation_service.py`, writing active `full_text_enabled` retrieval segments into `knowledge_chunks` with `metadata_json.source=article_segment`, `segment_id`, and `embedding_status=pending|disabled`, then emitting `knowledge.segmentation.index_synced`. Real embedding generation remains Phase 4.
- 2026-06-12 Knowledge vNext Phase 4 indexing: start with `server/knowledge/embedding_service.py`, `server/knowledge/vector_search_service.py`, migration `113_knowledge_embeddings_indexing`, `server/web_api/knowledge_handlers.py`, `webapp/src/features/knowledge/indexing-page.tsx`, `webapp/src/pages/admin/knowledge-indexing-page.tsx`, route `/app/admin/knowledge/indexing`, and APIs `GET /api/web/knowledge/indexing/status`, `GET|POST /api/web/knowledge/indexing/jobs`, `POST /api/web/knowledge/indexing/reindex-item`, `POST /api/web/knowledge/indexing/reindex-segment`, `POST /api/web/knowledge/indexing/reindex-space` and `POST /api/web/knowledge/indexing/reindex-all`. Generic `POST /api/web/knowledge/indexing/jobs` dispatches `scope_type=item|segment|space|all`. Embeddings are optional: vector-disabled settings or AI policy blocks create `disabled` rows and `knowledge.embedding.policy_blocked`, missing provider/key creates redacted `failed` state and `knowledge.embedding.provider_unavailable`, successful OpenRouter-compatible transport stores raw vectors only in DB and returns no `embedding_vector` to web clients. Vector retrieval uses ACL-first JSONB cosine fallback when `vector_enabled=true` and a safe numeric `query_vector` is supplied; pgvector/rerank are later hybrid retrieval work. Tests: `server/tests/test_knowledge_embeddings.py`, `server/tests/test_knowledge_vector_search.py` and `webapp/src/features/knowledge/indexing-page.test.tsx`.
- 2026-06-12 Knowledge vNext Phase 5 retrieval: start with `server/knowledge/retrieval_service.py`, `server/knowledge/search_service.py`, `server/web_api/knowledge_handlers.py`, `webapp/src/features/knowledge/api.ts`, `webapp/src/features/knowledge/search-settings-page.tsx`, `webapp/src/pages/kb/search-page.tsx`, and APIs `POST /api/web/knowledge/retrieve` / `POST /api/web/knowledge/search/preview` / `POST /api/knowledge/search`. Retrieval applies ACL before scoring, merges keyword/manual segment/binding/optional JSONB-vector candidates, returns `score_parts`, `source_mode`, snippets and citations for admin/support, hides diagnostics for requester-safe projections, records search analytics and emits `knowledge.retrieval.executed` / `knowledge.retrieval.zero_results`. Optional OpenRouter-compatible rerank requires explicit settings/profile/provider/policy/secret plus injected transport; success emits `knowledge.retrieval.rerank_used`, failure returns pre-rerank ordering and emits `knowledge.retrieval.rerank_failed_fallback`. `/app/admin/knowledge/search-settings` preview renders source modes, citations and score breakdown. `/app/kb/search` is the protected requester portal search over the public-compatible endpoint and must stay Russian-first without admin diagnostics. Existing public `POST /api/knowledge/search` remains backward-compatible. Tests: `server/tests/test_knowledge_hybrid_retrieval.py`, `webapp/src/features/knowledge/api.test.ts`, `webapp/src/features/knowledge/search-settings-page.test.tsx` and `webapp/src/pages/kb/search-page.test.tsx`.
- 2026-06-12 Knowledge vNext Phase 6 Ask: start with `server/knowledge/ask_service.py`, `server/web_api/knowledge_handlers.py`, `webapp/src/pages/kb/ask-page.tsx`, `webapp/src/features/knowledge/api.ts`, `webapp/src/pages/requester/index.tsx`, and APIs `POST /api/knowledge/ask`, `POST /api/web/knowledge/ask`, `POST /api/web/knowledge/ask/preview`. Ask is off by default unless `rag_answer_enabled=true` and `effective_mode=rag_answer`; disabled/provider-unavailable/no-evidence states return Russian fallback messages plus retrieval results. Enabled Ask uses retrieval citations, `answer` model profile, enabled OpenRouter provider, `ai_policy_profiles.answer_allowed`, env-backed secret refs and injected transport. Public-compatible `/api/knowledge/ask` is requester-safe even when legacy test/auth context carries another role. Ask blocks out-of-range citation markers and critical operational claims without a valid `[1]..[N]` marker, returning `not_enough_evidence` with retrieval fallback and blocked `ai_request_audit` evidence instead of showing the answer. The requester Ask page reuses article feedback/correction endpoints for `Ответ полезен`, `Ответ не помог` and `Предложить исправление` against the first safe result slug for both answered and AI-off fallback results; feedback metadata includes `source=knowledge_ask`, answer status, audit id, effective mode, primary item/version/chunk/segment, score and citation count. The create-ticket CTA `/app/requester/new` stores a safe session draft in `pc_client.knowledge_ask.ticket_context`; `/app/requester/new` consumes it once to prefill title/description and submit existing `knowledge_attempts` with `result=ticket_created_after_view`, so the requester ticket create API records the downstream analytics event without a new Ask-specific backend contract. `/app/knowledge` includes support Ask debug over `/api/web/knowledge/ask/preview`, rendering status/effective mode/audit id plus chunk, segment, source mode and score breakdown diagnostics that remain hidden from requester `/app/kb/ask`. Observer events are `knowledge.rag.ai_disabled`, `knowledge.rag.provider_unavailable`, `knowledge.rag.not_enough_evidence`, `knowledge.rag.policy_blocked` and `knowledge.rag.answer_generated`; AI calls write redacted `ai_request_audit`. Tests: `server/tests/test_knowledge_ask.py`, `webapp/src/pages/kb/ask-page.test.tsx`, `webapp/src/pages/requester/index.test.tsx`, `webapp/src/features/knowledge/support-workspace-page.test.tsx` and `webapp/src/features/knowledge/api.test.ts`.
- 2026-05-17 P3/P3.1 Experience & Quality Loop: start with `server/docs/QUALITY_LOOP.md`, `server/quality/*`, `server/app/services/quality_snapshot_scheduler.py`, `server/web_api/quality_handlers.py`, migrations `088_quality_loop` and `089_quality_loop_production_hardening`, `webapp/src/features/quality/*`, `webapp/src/pages/requester-ticket/index.tsx`, `webapp/src/pages/tickets/list-page.tsx` and `/app/admin/quality`. Requester/public CSAT and reopen use structured `/api/tickets/{id}/feedback|reopen` or `/public_api/tickets/{id}/feedback|reopen`; support/admin/auditor use `/api/web/quality/*`. Analytics are aggregate-only with no requester PII; QA reviews and improvement actions are internal; latest feedback is DB-enforced and snapshots are scheduled daily/weekly with dashboard `last_computed_at`.
- 2026-05-17 P4/P4.1 Problem Management / RCA: start with `server/docs/PROBLEM_MANAGEMENT.md`, `server/problem/*`, `server/app/services/problem_candidate_scheduler.py`, `server/web_api/problem_handlers.py`, migrations `090_problem_management_rca` and `091_problem_management_production_hardening`, `webapp/src/features/problems/*`, `webapp/src/pages/admin/problems-page.tsx`, `/app/admin/problems` and the support ticket Quality tab. Support/admin APIs are `/api/web/problems*`, `/api/web/problem-candidates*` and `/api/web/problem-scanner*`; they cover scheduled/manual scanner runs, candidate scan/convert/merge, deterministic dedup/cooldown, problem SLO/aging, problem lifecycle, ticket links, RCA create/approve, known-error/workaround Knowledge drafts and aggregate no-PII problem metrics. Requester/public users have no P4 problem/RCA/scanner API; requester-safe known errors/workarounds must still go through Knowledge Platform review/lint/publish.
- 2026-05-18 P5 Change Enablement: start with `server/docs/CHANGE_ENABLEMENT.md`, `server/change/*`, `server/app/repos/change_repo.py`, `server/web_api/change_handlers.py`, migration `092_change_enablement`, `webapp/src/features/changes/*`, `webapp/src/pages/admin/changes-page.tsx`, `/app/admin/changes` and `/app/admin/problems`. Support/admin APIs are `/api/web/changes*`, `/api/web/change-windows` and `/api/web/change-policies`; they cover change requests, standard/normal/emergency types, standard preapproval catalog policy, risk/impact, plans, rollback, approvals, one-off/recurring maintenance and blackout windows, overlap detection, implementation tasks, emergency retrospective, PIR, problem/improvement-action linkage and aggregate no-PII metrics including failure/rollback rates and lead time. P5 does not auto-execute changes and requester/public users have no direct change API.
- 2026-05-19 / 2026-05-31 P0 Request Studio / Ticket Workbench: start with `webapp/src/pages/tickets/list-page.tsx`, `webapp/src/pages/tickets/components/ticket-device-agent-panel.tsx`, `webapp/src/pages/tickets/tool-availability.ts`, `webapp/src/pages/admin/request-template-studio-page.tsx`, `webapp/src/features/request-template-studio/studio-model.ts`, `webapp/src/features/request-template-studio/draft-model.ts`, `webapp/src/features/request-template-studio/create-request-wizard.tsx`, `webapp/src/features/request-template-studio/form-field-editor.tsx`, `webapp/src/features/request-template-studio/process-editors.tsx`, `webapp/src/features/request-template-studio/readiness.ts`, `webapp/src/features/request-template-studio/request-studio-shell.tsx`, `webapp/src/features/request-template-studio/request-item-list.tsx`, `webapp/src/features/request-template-studio/process-map.tsx`, `webapp/src/features/request-template-studio/block-inspector.tsx`, `webapp/src/features/request-template-studio/form-preview-panel.tsx`, `webapp/src/features/request-template-studio/simulation-panel.tsx`, `webapp/src/features/request-template-studio/readiness-panel.tsx`, `webapp/src/features/forms-builder/*`, `webapp/src/features/service-catalog/*`, `webapp/src/features/policy-health/*`, `/app/tickets`, `/app/admin/request-template-studio`, `/app/admin/forms`, `/app/admin/service-catalog` and `/app/admin/policy-health`. `/app/admin/request-template-studio` is now visually `Студия обращений`, the primary no-code admin path for configuring a user-facing request type: раздел -> тип обращения -> форма пользователя -> профиль обработки -> маршрут -> сроки -> согласование -> закрытие -> уведомления -> проверка -> публикация. Studio owns the basic draft-editing UX and persists through existing safe draft contracts (`POST /api/web/admin/forms/save-draft` plus `POST /api/web/admin/service-catalog/offerings/save-draft`) without changing DB/API contracts; Forms Builder, Service Catalog and Policy Health remain expert surfaces behind links/dropdowns. Basic mode hides raw JSON, internal ids and raw policy refs; advanced/expert modes can reveal technical details. Studio deep links preserve `service` / `offering` / `template` context, simulation sends catalog context as top-level `service_code`/`offering_code`/`offering_full_code`, and publication readiness groups blockers, recommendations and ready items in human language. Direct publish from Studio now uses the safe publish contract: validate draft, show preview, require confirmation token, then publish guarded backend changes. Ticket automation groups unavailable tools behind one offline banner and compact unavailable section, and selected ticket workspace embeds compact device/agent/inventory context from the aggregate workspace DTO.
- 2026-05-31 Request Studio stabilization: `studio-model.ts` now builds a draft-aware `workingItem` by overlaying `studioDraft` on the selected catalog/template item or by creating a virtual draft item for the wizard path. The page, process map, selected block inspector, readiness panel, preview and simulation payload must consume `workingItem`; basic mode shows the focused block editor selected on the process map instead of rendering every editor at once.
- 2026-06-01 Request setup UX polish: `/app/admin/request-template-studio` remains the primary no-code setup UX for request types. Profile detection is based on stable `processProfile`/`ticket_type`/template and offering codes instead of mojibake labels; visibility policy refs are resolved from active registry policies before draft save; save/check status distinguishes unsaved changes, saved draft, completed check and stale check. `/app/admin/service-catalog`, `/app/admin/forms` and `/app/admin/policy-health` are expert surfaces with Studio CTAs, test/retired filtering and collapsed expert JSON by default. Studio publish now uses typed safe endpoints `POST /api/web/admin/request-studio/validate-draft`, `/publish-preview` and `/publish`: backend validates the current draft, returns a confirmation token, then publishes form schema, request template and catalog offering in one guarded transaction.
- 2026-06-01 Request Studio publish hardening: preview now issues one-time HMAC/nonce confirmation tokens (`rs1.<payload>.<signature>`) stored only as hashes in `request_studio_publish_tokens`, bound to draft hash, actor id/role and `request_studio.publish`, with TTL from `REQUEST_STUDIO_CONFIRMATION_TTL_SECONDS`. Publish rejects missing, malformed, expired, reused, wrong-actor or wrong-draft tokens, revalidates before mutation and marks the token used only after successful guarded publication. Preview also returns create/update/noop/blocked diffs for form schema, request template, offering and service.
- 2026-05-19 Operator Command Center: `/app/support` is the Russian localized support action board, while `/app/tickets` and `/app/tickets/{ticket_id}` remain the guided ticket queue/workbench. Backend contract is `GET /api/web/support/command-center` in `server/web_api/support_handlers.py` with typed DTOs in `server/web_api/dto/support.py` and aggregation in `server/support/operator_command_center.py`. Query params include `scope`, `queue`, `assignee`, `query`, `limit_per_section`, `window_hours`, `sla_risk_minutes`, `ola_risk_minutes` and `include_debug`. Section keys are `new_unassigned`, `operator_action`, `unread_user_messages`, `sla_risk`, `ola_risk`, `pending_approval`, `pending_consent`, `failed_operation`, `agent_offline_active`, `diagnostics_recommended`, `closure_blocked`, `similar_tickets_spike`; data comes from compact support queue rows plus batched approvals/operations/devices/diagnostic sessions/evidence/passports and deterministic recent-ticket grouping, without fake counters. `/app/tickets?smart_view=...` accepts Command Center aliases for supported queue slices, `/app/tickets?search=...` opens filtered queue mode for similar spikes, and opening a ticket workspace calls the web-session alias `POST /api/web/support/tickets/{ticket_id}/read` for the existing staff read cursor. `unread_user_messages` is based on `support_unread_user_messages` only; `support_pending_user_messages` remains an action/needs-reply signal and must not keep a ticket in the unread section after staff opens it. Command Center requests log `[web_support_command_center.metrics]` with duration, candidate count, non-empty section counts and batched source counts; primary UI masks historical mojibake/junk title/requester text with Russian fallbacks instead of modifying stored data blindly.
- 2026-05-24 Support Action Center: `/app/support` is a task-first operator workspace implemented in `webapp/src/pages/support/command-center-page.tsx` with frontend projection in `webapp/src/features/operator-command-center/task-projection.ts`. It reuses the existing `GET /api/web/support/command-center` payload and `GET /api/web/support/workspace/summary`; no backend task endpoint is required for Phase 1. The page shows Action Inbox, read-only Kanban, left work filters/queues and right Ticket Briefing over consolidated support action tasks.
- 2026-05-22 Ticket workbench composer attachments: start with `webapp/src/pages/tickets/list-page.tsx`, `webapp/src/features/queues/api.ts`, `server/web_api/support_handlers.py`, `server/docs/CHAT_MESSAGE_CONTRACT.md` and `server/docs/CODEMAP.md`. `/app/tickets/:ticketId` keeps the `Ещё` action menu in the ticket header, makes the header and composer vertically resizable, uses a one-line autoresizing reply field, and uploads support attachments through `POST /api/upload` before sending `attachment_refs` to `POST /api/web/support/tickets/{ticket_id}/messages`; attachment-only support messages are valid and timeline DTOs return resolved artifact descriptors.
- 2026-05-21 P1 Approval/Consent Center: `/app/support/approvals` is the support/admin read workspace for pending approvals and consent requests. Backend contract is `GET /api/web/support/approvals` in `server/web_api/approval_handlers.py`, DTOs live in `server/web_api/dto/approvals.py`, and aggregation lives in `server/approvals/service.py`. Supported item kinds are `ticket_approval`, `change_approval`, `risky_tool_consent`, `remote_assist_consent`, `closure_approval` and `policy_override`; first cut reads real `ticket_approvals`, `change_approvals`, `operations.status=waiting_consent`, `remote_access_sessions` pending consent and closure-like ticket approvals. Policy overrides return zero until a first-class pending override source exists. The center is read-only except link actions to ticket/change/device contexts, rejects requester-role access, and must not expose Remote Assist tokens/ICE/SDP or operation raw params.
- 2026-05-29 OBS1 Operational Integrity Observer: start with `server/observer/integrity_service.py`, `server/observer/checks/*`, `server/app/repos/observer_integrity_repo.py`, migration `105`, `server/web_api/observer_integrity_handlers.py`, `server/tech/snapshot.py`, `server/device_operations/service.py`, `webapp/src/pages/admin/observer-page.tsx`, `webapp/src/pages/admin/tech-page.tsx`, `webapp/src/pages/admin/device-operations-page.tsx`, and `docs/runbooks/observer_*.md`. Typed APIs are `GET /api/web/admin/observer/integrity` and `POST /api/web/admin/observer/integrity/scan`. Events are deduped/resolvable/suppressible runtime invariants, not generic monitoring; known contamination is exact-scope in `observer_known_contamination`.
- 2026-05-29 OBS1 follow-up: Protocol V3 outbox ingest now writes redacted durable `agent_runtime_audit.event_type='outbox_ack_persisted'` rows before ACK flush with `audit_contract_version=2`, `outbox_id`, `trace_id`, event type, persistence kind and proof (`persisted_event_id`, `duplicate` with `duplicate_proof`, or `documented_noop`). `observer.protocol_integrity` raises `protocol_ack_without_persistence` when a v2 ACK audit lacks proof, treats bare legacy `persisted=true` as insufficient, and resolves the old `protocol_ack_audit_gap` only when good v2 audit exists. Handshake toolset refresh compares the reported hash to the pre-upsert device hash/snapshot ref, so changed toolsets enqueue safe `list_tools` instead of leaving `devices.current_toolset_hash` ahead of `device_toolset_snapshots`; if the same device already has queued/sent/accepted/running `list_tools`, handshake skips enqueue and does not advance `last_toolset_refresh_at`.
- 2026-05-21 P1 Device Operations: `/app/admin/device-operations/:deviceId` and query fallback `/app/admin/device-operations?device_id=...` are the primary device/agent operations workspace. Typed endpoint is `GET /api/web/admin/device-operations/{device_id}` (or query fallback) in `server/web_api/device_operations_handlers.py`, DTOs live in `server/web_api/dto/device_operations.py`, and aggregation lives in `server/device_operations/service.py`. The read model composes real device card, inventory/binding/refresh run, agent version/update, module desired/actual reconcile, outbox, recent operations, Observer traces, provisioning/auth and Remote Assist state without loading full history or exposing raw inventory JSON by default. Requester-role access must remain forbidden; existing Inventory, Device Card, Agent Updates, Modules, Observer, tickets and Remote Assist workflows stay source-of-truth/expert surfaces. Current UI links into Device Operations from Inventory, Device Card and the ticket device/offline banner.
- 2026-05-21 Admin Tech Panel v2.1: `/app/admin/tech` is the Pilot Readiness & Runtime Health panel. It reads typed `GET /api/web/admin/tech/snapshot` from `server/tech/snapshot.py` plus keeps legacy aliases `/api/web/admin/tech/overview|alerts|logs|agents/audit|users/audit|operations/stuck`. `GET /api/web/admin/tech/locate?q=...` is the read-only quick locator for ticket id/code, device id, hostname, operation id, UUID or string trace id (length >= 8) and problem logs; it returns grouped root-cause diagnosis plus safe links to tickets, `/app/admin/operations/{operation_id}`, Device Operations, Observer, approvals, inventory and agent updates without exposing secrets. Operation detail UI uses web-session alias `GET /api/web/admin/operations/{operation_id}` for admin/support/auditor and does not expose retry/cancel actions. Tabs: Обзор, Безопасность, Runtime, База данных, Агенты, Операции, Логи и сигналы, Релиз и smoke. Readiness gates cover DB persistence/reachability, auth fallback, query-token policy and recent attempt count, HTTPS/WSS policy, session-cookie flags, agent connection policy, migrations marker, backup/restore drill markers, inventory scheduler health including duplicate-task signal, agent baseline including below-baseline devices and business smoke marker. Agent baseline only counts real post-handshake devices with numeric `agent_version`; `protocol_version=pending` stubs and non-numeric canaries are cleanup/provisioning data quality, not below-version agents. Marker evidence paths are `TECH_RELEASE_STATUS_PATH`, `TECH_BUSINESS_SMOKE_STATUS_PATH`, `TECH_RESTORE_DRILL_STATUS_PATH` and `TECH_BACKUP_STATUS_PATH`; writers live in `scripts/release_server_to_remote.py`, `scripts/business_smoke.py`, `scripts/write_restore_drill_marker.py` and `scripts/write_backup_status_marker.py`. `release_server_to_remote.py` writes Alembic current/head after remote migration when available, writes `--release-status-path /var/chat_bot/pc_client/...` directly on the remote host over SSH, and its remote smoke gate supports HTTPS stands through `REMOTE_SMOKE_BASE_URL` / `REMOTE_SMOKE_INSECURE_TLS` or `--smoke-base-url` / `--smoke-insecure-tls`. `business_smoke.py` can optionally verify secure cookie flags, use `--insecure-tls` / `BUSINESS_SMOKE_INSECURE_TLS=true` for the current self-signed stand certificate, run Playwright mixed-content/WSS checks, create an explicit test ticket and enqueue `inventory.collect`. The panel stays read-only: refresh, locator and safe links only; `/app/admin/observer` remains the dedicated Observer workbench.
- 2026-06-06 AI Integration / MCP panel: `/app/admin/ai-integration` is the read-only admin area for Codex/MCP integration. It reads `GET /api/web/admin/ai-integration/mcp`, shows `helpdesk-server-debug` manifest, DB health, Context Index freshness, latest persisted server runtime snapshot and the reload-after-deploy instruction. The live server writes `server_runtime_snapshots` through `server/observer/runtime_snapshot_writer.py`; MCP `observer_runtime_status` reads fresh persisted rows and `observer_presence_snapshot` can include bounded aggregate or device-specific `live_ws_evidence` from those snapshots.
- 2026-05-21 stand profile defaults for pilot scripts: `scripts/manage_remote_stack.py`, `scripts/run_remote_migrations.py`, `scripts/release_server_to_remote.py` and `webapp/scripts/remote-browser-signoff.mjs` keep the current stand as fallback but can be pointed at another stand through `PC_CLIENT_REMOTE`, `PC_CLIENT_REMOTE_ROOT`, `PC_CLIENT_REMOTE_SERVER_PYTHON`, `PC_CLIENT_SSH_KEY`, `PC_CLIENT_BROWSER_BASE_URL`, `REMOTE_SMOKE_BASE_URL` and `REMOTE_SMOKE_INSECURE_TLS`. Browser signoff now defaults to the HTTPS pilot origin instead of legacy `http://...:8666`.
- 2026-05-21 pilot release gate/runbook: `docs/PILOT_RELEASE_GATE.md` lists the evidence markers, pilot flags, business smoke/browser signoff commands, stand profile env, GitHub protected-branch expectations and 72-hour soak criteria. GitHub branch protection is an external repository setting; local code/docs can define the gate but cannot enforce it without admin configuration.
- 2026-05-18 Tool Output Presentation Schema/Builder v1: start with `pc_agent/core/registry.py`, `pc_agent/core/orchestrator.py`, `pc_agent/modules/impl/system.py`, `pc_agent/modules_packages/agent_recipe_runner/module.py`, `server/diagnostics/capability_registry.py`, `server/diagnostics/agent_recipes.py`, `server/diagnostics/presentation_overrides.py`, migration `093_tool_presentation_overrides`, `server/web_api/support_handlers.py`, `server/utils/module_manifest.py`, `server/utils/module_builder.py`, `webapp/src/components/module-result/module-result-renderer.tsx`, `webapp/src/components/module-result/presentation-builder.tsx`, `webapp/src/components/module-result/schema-path-picker.ts`, `webapp/src/features/capabilities/capability-detail-drawer.tsx`, `webapp/src/features/diagnostics/diagnostic-center-panel.tsx`, `webapp/src/features/queues/support-workspace-mappers.ts`, `webapp/src/pages/tickets/list-page.tsx` and `pc_agent/docs/MODULES.md`. `presentation_schema` is top-level next to `params_schema`, `output_schema` and `output_contract`; module defaults remain immutable, `list_tools` must preserve `output_contract` and `presentation_schema` from the registry spec for server capability projection, `system.collect.output_schema` declares nested `sections.*` properties used by the path picker and generated live-preview sample, server overrides are managed through `GET|PUT|DELETE /api/web/tool-presentations?tool_id=...`, capability APIs expose `effective_presentation_schema` plus source flags, support timeline `tool_call_result` rows expose bounded renderable `result_payload` unwrapped from ToolResponse `result.output` plus effective presentation schema for real completed tool results, rendering is declarative-only with standard blocks (`field_grid`, `metric_cards`, `table`, `checklist`, `timeline`, `artifact_list`, `raw_json`) and raw JSON fallback, and none of this changes Protocol V3 or `ToolResponse`.
- 2026-05-19 Inventory v3 / Lightweight CMDB: start with `server/inventory/service.py`, `server/inventory/scheduler.py`, `server/web_api/admin_inventory_handlers.py`, migration `096_inventory_v3_lightweight_cmdb`, `webapp/src/pages/admin/inventory-page.tsx` and `webapp/src/features/admin/device-inventory-panel.tsx`. v3 keeps `shared/builtin_tool_descriptors.py` as the single descriptor source for `inventory.collect`, moves inventory web handlers out of the large admin handler module, adds binding history, CSV dry-run/apply import, binding CSV export, inventory CSV export, fleet dashboard aggregation, refresh run visibility and DeviceInventoryPanel stale/history sections. CSV export escapes formula-leading cells; the scope remains endpoint inventory/passport data, not employee monitoring or accounting CMDB.
- 2026-05-19 Inventory v4 / Workplace Registration / Presence v1: start with `pc_agent/modules/impl/presence.py`, `shared/builtin_tool_descriptors.py`, `server/inventory/service.py`, `server/presence/service.py`, `server/web_api/admin_inventory_handlers.py`, migration `097_inventory_v4_registration_presence`, `webapp/src/pages/admin/inventory-page.tsx` and `webapp/src/features/admin/device-inventory-panel.tsx`. v4 adds selected/stale/missing/department/building bulk inventory refresh with operation tracking, XLSX export, attention/report APIs, agent-profile binding suggestions requiring IT apply/ignore, and `presence.collect` with coarse active/idle/locked state plus daily counters. Presence and registration do not collect screenshots, keystrokes, browser history, document/clipboard/message contents or auto-overwrite confirmed bindings.
- 2026-05-22 Device Registration Claims / authoritative user binding: start with migration `098_device_registration`, `server/registry/registration_service.py`, `server/app/repos/registration_repo.py`, `server/registry/service.py`, `server/web_api/registry_handlers.py`, `server/routes.py`, `server/websocket/agent_handshake.py`, `server/tickets/create_flow.py`, `pc_agent/core/user_profile.py`, `pc_agent/ws_agent.py`, `pc_agent/ui_gui/server_api.py`, `webapp/src/pages/admin/registry-page.tsx` and `webapp/src/features/admin/api.ts`. Agent self-reported profiles now create/update registration claims and suggestions only; admin-approved active `device_user_bindings` are the authoritative device-person link and the only flow that denormalizes `registry_assets.assigned_person_id` and ticket requester registration context. Ticket create checks active primary binding before ingesting requester_profile, so a confirmed binding wins over later self-reported profile data and does not create a new conflict claim. Agent endpoints live under `/api/registry/agent/*`; admin review endpoints live under `/api/web/admin/registry/registrations*`, `/bindings/{binding_id}/revoke`, and `/devices/{device_id}/registration-timeline`; real HTTP agent tokens resolve to `actor_role=agent` with `actor_id=device_id`, user claim confirmation is identity-scoped through normalized person identities, verified identities are not overwritten by self-reported profiles, `/api/registry/options` stays a lightweight picker query, and handshake ACK includes a non-blocking `payload.registration` summary.
- 2026-05-23/2026-06-08 Agent registration/account sessions use server-issued requester identity sessions. Start with migrations `098_device_registration`, `099_device_account_sessions`, `100_ticket_account_sessions`, `108_device_browser_pairings`, `server/registry/account_session_service.py`, `server/registry/browser_pairing_service.py`, `server/registry/account_state_service.py`, `server/tickets/account_access_service.py`, `server/tickets/create_flow.py`, `server/tickets/handlers.py`, `pc_agent/core/account_session.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/server_api.py` and `webapp/src/pages/device-pairing/*`. Registration form uses the shared dynamic form renderer and submits to `/api/registry/agent/profile`; after claim creation the agent creates a server `registration_pending` session only for pending-gate state/polling, not as a ticket-workspace login. Agent browser pairing uses `POST /api/registry/agent/browser-pairings` and `GET /api/registry/agent/browser-pairings/{pairing_id}` with DB-backed hashes for pairing token/code, opens `/app/device/login` or `/app/device/register`, users can enter a manual code at `/app/device/pair` through `POST /api/web/registry/browser-pairings/lookup`, and web-authenticated users confirm through `/api/web/registry/browser-pairings/{pairing_id}/login/confirm|registration/confirm`; browser confirmation never receives the account session token, and the one-time token is returned only to the polling agent. Agent ticket create/list/detail/message/read/upload/close require a valid confirmed-binding or verified-other-account server account session and are filtered by account session, not just by device id. GUI and `/ui/automation/run ticket.create` must both pass selected Service Catalog refs plus the account session into `/api/tickets/create`. GUI validation of stored account sessions calls `/api/registry/agent/account-sessions/{session_id}/validate` with the session token in the POST body or account headers, never in query parameters. Admin can list/revoke global or device account sessions under `/api/web/admin/registry/account-sessions`, `/api/web/admin/registry/devices/{device_id}/account-sessions` and `/api/web/admin/registry/account-sessions/{session_id}/revoke`; binding revoke/owner transfer now actively revokes dependent sessions instead of relying only on validation-time invalidation. `/app/admin/device` shows those sessions. Other-account tickets preserve `requester_account_warning=ticket_created_from_other_account_on_registered_device` for support visibility.
- 2026-06-16 Agent release `3.1.67` supersedes `3.1.66` for final strict browser-only registration cleanup: no normal-GUI local requester profile manager/sidebar, no `requester_profiles.json` path/writes or `has_active_profile` gate in `ChatPanel`, no GUI profile registry sync, no `profile.upsert` / `profile.select` automation actions, and `/ui/automation/run ticket.create` relies on the server-issued account session boundary instead of a local profile precheck; publish it as a new upload/rollout target and do not overwrite `3.1.66` artifacts.
- 2026-06-16 Agent release `3.1.66` supersedes `3.1.65` for strict browser-only web-first agent registration: no normal GUI local registration page/buttons/confirm controls, Account page labels `Сессия` / `Привязка` / `Заявка на привязку`; publish it as a new upload/rollout target and do not overwrite `3.1.65` artifacts.
- 2026-06-16 Agent release `3.1.65` supersedes `3.1.64` as the priority Windows stable refresh from current HEAD: publish it as a new upload/rollout target and do not overwrite `3.1.64` artifacts.
- 2026-06-15 Agent release `3.1.64` supersedes `3.1.63` for terminal other-account request recovery: when the server returns `rejected`, `expired` or `canceled` for a pending other-account login request, the GUI clears the local pending gate and falls back to current account-state. Do not overwrite `3.1.63`; publish target-specific artifacts as new upload/rollout targets.
- 2026-06-15 Agent release `3.1.63` supersedes `3.1.62` for the GUI browser-pairing handoff fix: non-local legacy `http://<host>:8666/api` settings open `/app/device/login` and `/app/device/register` on the HTTPS web boundary, preserving secure web-session cookie confirmation while the agent still polls the machine-token pairing endpoint. Do not overwrite `3.1.62`; publish target-specific artifacts as new upload/rollout targets.
- 2026-06-13 Agent release `3.1.62` supersedes `3.1.61` for post-startup-hardening GUI/runtime fixes: unified user consent prompts, replay-safe Remote Assist request delivery to the GUI, account-gate recovery after revoked requester sessions, transient WSS/proxy retry behavior, command cancellation/restart recovery, account-session propagation through GUI automation, and service-catalog wizard automation hardening. Do not overwrite `3.1.61`; publish Windows and Linux artifacts as new upload/rollout targets.
- 2026-05-26 Agent release `3.1.61` supersedes `3.1.60` for GUI startup failure handling: Qt/PySide6 import failures now stop GUI startup instead of falling back to `--no-gui`, non-GUI API imports do not load Qt, and launcher entrypoints stop after repeated immediate crashes when rollback is unavailable. Do not overwrite `3.1.60`; publish Windows and Linux artifacts as new upload/rollout targets.
- 2026-05-26 Agent release `3.1.60` is the first compiled rollout target that includes the full requester registration/account-session gate from the 2026-05-23/25 registration work. Do not overwrite `3.1.59`; publish Windows and Linux artifacts as new upload/rollout targets.
- 2026-05-24/2026-06-10 Registry Management Center: `/app/admin/registry` is an action-first workspace over `GET /api/web/admin/registry` with P0 device/user/binding/session/claim operations plus P1 locations, departments and policies. Manual bind/transfer/shared/responsible operations remain under `RegistrationService`; manual bind may satisfy a matching pending `agent_profile` claim, but the created binding remains an admin-manual binding (`source=admin_manual`) and the timeline keeps `admin_binding_created`. Locations/departments CRUD+archive+merge, policies, people merge, bulk actions, CSV export/import and generic timelines are under `RegistryAdminOperationsService` / `RegistryPolicyService`; transfer, merge, bulk and registry import operations expose preview/dry-run endpoints before apply. People merge moves identities, bindings, sessions, account login requests, claims, tickets, asset owner and inventory rows to the master person and marks the duplicate merged. `GET /api/web/admin/registry` now includes safe `ui_users` account status/link fields, and `POST /api/web/admin/registry/ui-users/{user_login}/link-person` explicitly links a UI login to a RegistryPerson by creating a verified `ui_login` identity while refusing collisions. Registry policy APIs expose defaults, changed-from-defaults, validation ranges, dangerous-setting warnings, `requires_restart`, preview and reset-to-defaults; patch/reset require a reason and write `policy_changed`. Transfer owner, people/location/department merge, bulk actions and import apply return normalized operation reports with `operation_id`, `status`, `summary`, `items`, `events` and `report_url`, while keeping legacy domain fields where existing UI paths need them. Bulk/import UI can show failed rows, copy errors and export formula-safe CSV result reports. Registry import supports only people, locations, departments and device inventory mapping, returns a `preview_id`, requires the matching `preview_id` for apply, blocks row errors/duplicates before apply, writes `registry_import_applied` with an operation id and itemized row result, and intentionally does not import bindings. Generated data-quality issues carry stable `issue_key` values; ignore/snooze/resolve persist in `registry_quality_issue_overrides`, hide active overrides from the issue list and write `quality_issue_*` audit events; recomputed quality issues disappear when the underlying fix removes the root cause. Registration claim payloads include `user_confirmed_at`; the Requests tab disables ordinary approve for `pending_user_confirmation`/terminal claims and routes manual bypass through explicit admin override with reason. The generic timeline endpoint supports device/person/binding/account_session/claim and normalizes admin, registration and account events into source, actor, reason, related entities and changes for the drawer, including transfer owner before/after changes and related revoked sessions; person/identity admin mutations write explicit audit events. The service source of truth remains `device_user_bindings`; UI actions must not mutate `registry_assets.assigned_person_id` directly. For the full live workflow smoke, run `python scripts/registry_workflow_smoke.py --base-url https://192.168.100.17:9443 --insecure-tls` on the Linux stand after quick deploy.
- 2026-05-12 command lifecycle guard: `server/websocket/agent_services.py` keeps `command_ack` / `command_result` DB transitions independent from optional ticket/chat helper imports. `DB_AVAILABLE` now describes the operations/outbox lifecycle path, while `TICKET_CREATE_AVAILABLE` gates only agent-initiated `chat_raise` ticket creation.
- 2026-05-27 P1 operation recovery: start with `pc_agent/ws_agent.py`,
  `pc_agent/core/database.py`, `server/websocket/agent_services.py`,
  `server/websocket/command_result_components.py`,
  `server/docs/COMMAND_RESULT_LIFECYCLE.md`, `pc_agent/docs/DATABASE.md` and
  both Protocol V3 docs. Agent terminal `command_result` payloads are durable
  in `pending_command_results` until `command_result_ack`; stale previous
  runtime `seen_commands.in_progress` rows become terminal `AGENT_RESTARTED`
  recovery results on startup/reconnect. Late terminal results replayed after
  watchdog timeout reconcile the original operation unless a retry/replacement
  exists; timeline payloads mark `late_result=true` and preserve the previous
  `timed_out` state for audit, while successful reconciliation clears current
  operation error fields.
- 2026-05-13 diag logs runtime path guard: `pc_agent/modules/impl/diag_logs.py` resolves `diag.logs.collect` app preset sources from the current runtime data root (`PC_AGENT_DATA_DIR` or OS default) instead of the config-loader singleton, so release/pytest runs do not reuse stale temp log roots across agent scenarios.
- 2026-05-11 diagnostic capability foundation: module manifests now accept additive `execution`, `deployment`, `safety`, `readiness`, `evidence` and `artifacts` blocks; old managed ZIP tools default to `agent_managed_module`, builtin tools default to `agent_builtin`. Server projection lives in `server/diagnostics/` with `GET /api/diagnostics/capabilities`, `GET /api/tickets/{ticket_id}/diagnostics/capabilities` and `POST /api/tickets/{ticket_id}/diagnostics/capabilities/{capability_id}/run`; agent tools still execute through existing `ToolExecutionService.run_tool`, while `server_connector`, `observer_query`, `remote_assist` and `manual` route through server-side providers instead of ordinary agent tool execution.
- 2026-05-12 diagnostic provider config persistence: migration `075` adds provider/config/capability snapshot tables for diagnostic providers while runtime descriptors remain computed from manifests/providers. Admin-safe config APIs are `GET /api/diagnostics/providers/configs`, `GET /api/diagnostics/providers/configs/{provider_id}` and `PUT /api/diagnostics/providers/configs/{provider_id}` with web-session aliases under `/api/web/admin/diagnostics/providers/configs*`; config secrets are redacted before persistence/API output, credential rows store secret refs, and ticket readiness now consumes persisted integration config, credential readiness and mappings for server connectors such as Zabbix.
- 2026-05-12 diagnostic readiness hardening: `GET /api/tickets/{ticket_id}/diagnostics/capabilities` keeps legacy `readiness` statuses but adds stable `reason_code` and explicit action ids (`install`, `run`, `configure_integration`, `add_credentials`, `request_consent`, `open_remote_assist`, `create_manual_evidence`). Dependency/preflight dicts can supply their own `reason_code`; integration and permission reasons stay generic so config/credential details are not leaked.
- 2026-05-12 diagnostic execution router hardening: `POST /api/tickets/{ticket_id}/diagnostics/capabilities/{capability_id}/run` now computes readiness before dispatch, returns `409 CAPABILITY_NOT_READY` for blocked capabilities, accepts `idempotency_key` / `X-Idempotency-Key` and `timeout_ms`, and normalizes responses with `execution_target`, `execution_kind`, provider metadata and idempotency/timeout fields. Agent targets still call `ToolExecutionService.run_tool`; non-agent targets stay behind server-side provider boundaries.
- 2026-05-12 server_builtin diagnostic runner: capability registry now includes `server.dns.resolve` and `server.http.request`. They execute on the Maria server, create `operations` rows with `kind=server_capability`, move `queued -> running -> succeeded/failed`, return `operation_id` / `poll_url`, support idempotency and timeout metadata, map evidence previews, and never write `device_outbox`.
- 2026-05-12 Zabbix diagnostic server connector: `zabbix.problems.lookup`, `zabbix.host.health` and `zabbix.item.history` now use `server/diagnostics/providers/zabbix_provider.py` for bounded Zabbix JSON-RPC calls from the server. Ticket capability runs inject persisted provider config, mapping and ready credential refs into the connector route; result payloads map to monitoring evidence and do not return raw credential material.
- 2026-05-12 observer diagnostic query capabilities: `observer.ticket.summary` and `observer.trace.bundle` now run through `server/diagnostics/providers/observer_provider.py`, reuse existing observer overlay services, return compact support-facing output contracts plus `observer.summary` / `observer.trace_bundle` evidence previews, and stay read-only `observer_query` targets that never call `ToolExecutionService.run_tool` or enqueue DeviceOutbox rows.
- 2026-05-12 Remote Assist diagnostic capabilities: `remote_assist.request_view`, `remote_assist.request_control` and `remote_assist.session.summary` are session capabilities in `server/diagnostics/providers/remote_assist_provider.py`. They route through existing `RemoteAssistService` consent/signaling, use policy/RBAC/active-session readiness, return normalized session envelopes and map to passport-eligible `remote_assist.session` evidence previews instead of ordinary `run_tool`.
- 2026-05-12 manual diagnostic capabilities: `manual.visual_check`, `manual.vendor_response`, `manual.operator_note` and `manual.customer_confirmation` now run through `server/diagnostics/providers/manual_provider.py`. They require `diagnostics.create_manual_evidence`, create normalized `diagnostic_evidence`, write `diagnostic_manual_evidence_created` ticket events, expose `manual.evidence` output contracts and link to passport only through the selected diagnostic evidence bridge.
- 2026-05-12 diagnostic evidence persistence phase 9: migration `076` adds `diagnostic_session_capabilities` and `diagnostic_artifact_links`. `DiagnosticProjectionService.project_capability_result()` turns non-agent capability operation/session/query results into persistent `diagnostic_evidence`, artifact links and optional session capability snapshots; `normalize_tool_result_to_evidence_values()` is the production mapper behind legacy evidence previews. Ticket capability run persists evidence for server/observer/remote results, while agent `run_tool` and manual provider persistence remain unchanged.
- 2026-05-12 diagnostic capability observability phase 12: `server/diagnostics/observability.py` records redacted lifecycle runtime-audit rows for capability runs (`capability_run_started`, terminal success/failure/blocked and evidence-linked events). Observer projects them as `root_kind=capability_run`, `server_connector_query`, `observer_query`, `manual_evidence` or `remote_assist`; audit details include duration/result/readiness/provider-error/evidence metrics while redacting integration config, credential refs, sensitive query params and evidence output payloads.
- 2026-05-12 capability-backed playbooks phase 10: admin playbook catalog now includes non-agent diagnostic capabilities and draft saves preserve `capability_id`, `execution_target`, provider and evidence metadata in `required_capabilities`. Existing agent tool steps still use module auto-install preflight plus `run_tool`; non-agent playbook steps route through `CapabilityExecutionRouter`, write terminal `playbook_step_run` output/error JSON, optionally project evidence for real ticket context, and do not enqueue ordinary DeviceOutbox `run_tool` commands.
- 2026-05-12 Diagnostic Center UI phase 11: `/app/tickets/:ticketId` now renders a dedicated `Диагностика` tab from `webapp/src/features/diagnostics/diagnostic-center-panel.tsx`. It consumes ticket-scoped capability readiness/run APIs through web-session aliases under `/api/web/support/tickets/{ticket_id}/diagnostics/*` (capabilities, sessions, evidence, findings, bundle, profile-run and passport attach), filters capabilities by execution target/domain/perspective/provider, renders arbitrary capability `params_schema` through `webapp/src/features/diagnostics/params-schema.ts` + shared `SchemaParamEditor`, runs diagnostic capabilities through the router with selected params, shows RBAC/readiness-specific disabled-state copy, creates manual evidence, evaluates findings, builds diagnostic bundles and attaches selected diagnostic evidence to the passport while leaving the legacy tools/playbooks sidebar intact. `/app/admin/modules#diagnostic-provider-configs` now renders `DiagnosticProviderConfigPanel` over `/api/web/admin/diagnostics/providers/configs*` for server connector setup such as Zabbix.
- 2026-05-13 Capability Studio MVP: `/app/admin/capabilities` is a new top-level admin UI over the existing diagnostics capability registry. It uses `GET /api/web/admin/capabilities` and `/api/web/admin/capabilities/provider-configs*` aliases over the existing diagnostics handlers, shows catalog/providers/read-only evidence mapping/readiness lab/SDK Modules tabs, and keeps Modules Workbench as the ZIP/SDK authoring surface.
- 2026-05-13 Agent Recipe Runner production slice: `execution_target=agent_recipe` is a first-class diagnostic capability target backed by a protected managed module `agent_recipe_runner`. Recipes persist in `diagnostic_capabilities` / `diagnostic_capability_versions` plus `agent_recipe_versions`, runner primitives live in `agent_recipe_primitives`, test audit rows live in `agent_recipe_test_runs`, and the server enqueues Protocol V3 `run_recipe` commands instead of ordinary `run_tool`; agent-side `run_recipe` handling starts in `pc_agent/core/orchestrator.py`, delegates through `pc_agent/core/recipe_runner_bridge.py`, and must normalize bridge dict payloads to `ToolResponse` before common command-result post-processing. Ticket-bound missing/outdated runner handling lives in `server/diagnostics/runtime_dependencies.py`: it records `operation_dependencies`, selects the compatible preferred runner version, uses `device_desired_modules` + `server/modules/reconcile.py`, and resumes the parent recipe operation after `module_state_changed`, `tools_changed` or dependency operation terminal events. Preferred assignment for the protected runner uses the module preflight/server-harness gate instead of the normal Windows `run_tool` live-test gate because the runner has no support-visible tools. Start from `server/diagnostics/recipe_execution_service.py`, `server/diagnostics/agent_recipes_repo.py`, `pc_agent/core/recipe_runner_bridge.py`, `pc_agent/modules_packages/agent_recipe_runner/`, and `server/docs/AGENT_RECIPE_RUNNER.md`.
- 2026-05-11 diagnostic layer MVP: diagnostics are now a separate ticket layer, not a ticket status. Persistent tables `diagnostic_sessions`, `diagnostic_steps`, `diagnostic_evidence`, `diagnostic_findings` and `diagnostic_bundles` normalize operations, diag logs artifacts, observer root traces, remote assist sessions and manual checks. Start from `server/diagnostics/service.py`, `projection.py`, `findings.py`, `bundle.py`, `sessions.py`, `profile_runner.py`, `passport_bridge.py`, `server/app/repos/diagnostics_repo.py`; endpoints include `GET /api/tickets/{ticket_id}/diagnostics/overview`, typed aliases under `GET|POST|PATCH /api/web/support/tickets/{ticket_id}/diagnostics/*` for the React support workspace, sessions/evidence/findings/bundle/profile routes, `POST /api/tickets/{ticket_id}/diagnostics/run-profile`, `POST /api/tickets/{ticket_id}/diagnostics/passport/attach-selected`, and `/app/tickets` shows a unified `Диагностика` tab in the tools workspace with profile/finding/bundle/passport actions.
- Local launcher/runtime work now starts from `pc_agent/launcher_portable_main.py`, `pc_agent/ws_agent.py`, `pc_agent/ui_bridge/api_server.py`, `pc_agent/ui_gui/main.py`, `pc_agent/ui_gui/main_window.py`, and `pc_agent/ui_gui/automation_controller.py`.
- Recommended-update diagnostics and local update state are surfaced through `pc_agent/ws_agent.py`, `pc_agent/ui_bridge/api_server.py`, `pc_agent/ui_gui/main_window.py`, and `pc_agent/docs/AGENT_UPDATE_WORKFLOW.md`.
- Agent-side observer correlation for tech drilldown now uses `pc_agent/core/action_trace.py` plus the `search_action_trace` RPC in `pc_agent/ws_agent.py`; admin tech trace detail can request compact agent actions via `include_agent_actions=1`, span materialization is opt-in with `sync_agent_actions=1`, the React workbench keeps fast trace detail separate from bundle action RPC, the server bounds large `details` payloads before sync/render, the agent stores server `trace_id` in orchestrator/module action rows, and the agent now emits guaranteed module-level execution breakdown (`module.resolve`, `module.execute`) even when a tool module has no custom runtime audit hooks.
- New module authoring canon: every new `BaseCollector` tool must emit observer breadcrumbs through `trace_span(...)` / `trace_event(...)`; generated workbench modules now scaffold this automatically in `server/utils/module_builder.py`, а `server/utils/module_observer_contract.py` + `python scripts/verify_workspace.py` теперь валят CI, если mandatory `tool.entry` span отсутствует.
- Live observer canary suite для опасных flow лежит в `python scripts/run_observer_canary_suite.py`: consent approve/deny/timeout, module install/update/remove, retry exhaustion, agent disconnect during operation, ws ack/nack/replay, source-coverage probes for `module_reconcile` / `playbook_run` / `web_auth` / `observer_runtime`, stable build registry checks for `windows_amd64` and `linux_alt_x86_64`, plus JSON/Markdown coverage reports.
- 2026-05-11 observer projection hardening: `server/observer/service.py` batches PostgreSQL writes to `observer_spans` and `observer_span_links` below the asyncpg bind-parameter limit, so large ticket-root traces with thousands of events no longer fail with `number of query arguments cannot exceed 32767`.
- Local Windows launcher canary flow still goes through `python scripts/manage_local_agent.py start <name> --launcher`, with release artifacts built by `python pc_agent/build_windows_release_v2.py`. For real GUI/UIA live evidence use `python scripts/live_agent_uia_state_probe.py --instance <name> --expect-connected --expect-account ...`; it requires `pywinauto==0.6.9`, uses `backend="uia"`, writes bounded JSON evidence, and relies on stable agent accessibility names rather than OCR or coordinates.
- Server/control runtime wrappers (`scripts/run_server.py`, `scripts/run_control_plane.py`) now always bootstrap repo-root import visibility for shared packages like `shared.redaction`; when debugging Linux boot failures, treat those wrappers as canonical entrypoints instead of ad-hoc `python server/server.py`.

## 2026-04-22 update hardening

- Current launcher-based update debugging starts from `pc_agent/core/orchestrator.py`, `pc_agent/launcher/installer.py`, `pc_agent/launcher/launcher_main.py`, `pc_agent/ws_agent.py`, and `pc_agent/ui_gui/main_window.py`.
- Agent-side update scheduling is now single-flight: an existing `updates/pending_update.json` blocks overlapping update requests, while a repeated request with the same `operation_id` is treated as idempotent.
- Runtime/UI status now surfaces both `pending_update_*` and `update_request_*` through `GET /ui/agent/status`, so the local GUI can show `requesting -> requested -> pending_restart` instead of looking hung after `POST /ui/agent/update`.
- Launcher publish now uses staging + backup/restore, verify returns diagnostic output, `tar.gz` extraction restores POSIX mode bits and rejects links, and both launcher entrypoints roll back after repeated immediate crash of the newly switched version.
- 2026-05-04 agent release check: Windows stable `3.1.29` supersedes `3.1.28` for the follow-up agent GUI fixes: mandatory diagnostic-consent hint, clearer resolution confirmation card, visible diagnostics/SLA summaries, support-in-work fallback when no personal assignee is set, and Russian system event text. Upload and rollout must target a new `3.1.29` build, not overwrite existing artifacts.
- 2026-05-09 agent release check: Windows stable `3.1.31` supersedes `3.1.30` for requester timeline projection support in the desktop GUI. Upload and rollout must target a new `3.1.31` build, not overwrite existing artifacts.
- 2026-05-09 agent release check: Windows stable `3.1.32` supersedes `3.1.31` for requester diagnostic-result timeline wording: cards show only `Выполнена диагностика` and do not add resource-specific filler text. Upload and rollout must target a new `3.1.32` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.34` supersedes `3.1.33` for Remote Assist WebRTC startup: the release package includes `aiortc`/ICE/media dependencies and the GUI reports WebRTC startup failures to the backend. Upload and rollout must target a new `3.1.34` build, not overwrite existing artifacts. `3.1.33` remains the prior Remote Assist runtime fix for command handling and retained/topmost consent dialogs.
- 2026-05-10 agent release check: Windows stable `3.1.35` supersedes `3.1.34` for Remote Assist interactive-control testing: Windows input injection now uses `SendInput`, Linux uses the `pynput` backend, and viewer end returns the operator to the ticket. Upload and rollout must target a new `3.1.35` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.36` supersedes `3.1.35` for Remote Assist connection hardening: browser offers now use an explicit recvonly video transceiver, viewer/agent WebRTC timeouts close transport and mark failed sessions, the agent banner says "connecting" until ICE is connected, and screen capture reuses its `mss` context to reduce failed-session CPU. Upload and rollout must target a new `3.1.36` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.37` supersedes `3.1.36` for idle CPU/WMI reduction: Windows fingerprint collection avoids default PowerShell/WMI calls, WMI baseboard lookup is opt-in through `PC_AGENT_ENABLE_WMI_FINGERPRINT=1`, and GUI idle update checks reuse cache longer. Upload and rollout must target a new `3.1.37` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.38` supersedes `3.1.37` for launcher disk retention and focused-window CPU reduction: successful launcher updates keep only current/previous version directories, downloaded ZIP retention is reduced, and large Qt soft shadows are disabled by default unless `PC_AGENT_ENABLE_GUI_SHADOWS=1`. Upload and rollout must target a new `3.1.38` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.39` supersedes `3.1.38` for deeper focused-window CPU reduction on Windows/RDP/Proxmox graphics stacks: frameless resize cursor updates are deduplicated and unchanged ticket detail polls skip UI rebuilds before repaint. Upload and rollout must target a new `3.1.39` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.40` supersedes `3.1.39` for field GUI CPU diagnostics: the agent logs `[gui-profiler]` samples with process CPU, Qt event counters, hot receivers, focus/active widgets and top thread CPU deltas; disable with `PC_AGENT_GUI_PROFILER=0` after diagnosis. Upload and rollout must target a new `3.1.40` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.41` supersedes `3.1.40` for remote GUI CPU log retrieval: `diag.logs.collect` app preset includes the agent runtime logs directory so `[gui-profiler]` samples can be collected as an artifact. Upload and rollout must target a new `3.1.41` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.42` supersedes `3.1.41` for making `diag_logs` a core enabled module, so `diag.logs.collect` is loaded into the agent registry after upgrade. Upload and rollout must target a new `3.1.42` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.43` supersedes `3.1.42` for focused-window GUI CPU spikes: `ActionTraceRecorder` no longer rescans `action_trace.jsonl` on every trace record, avoiding 10-second ticket-list poll stalls in the qasync GUI thread. Upload and rollout must target a new `3.1.43` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.45` supersedes `3.1.44` for Remote Assist quality/control/clipboard: requested quality profiles are honored by capture, interactive control auto-enables after the initial mode consent, drag/wheel/shortcuts are supported, and text clipboard auto-sync is available behind policy. Upload and rollout must target a new `3.1.45` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.46` supersedes `3.1.45` for Remote Assist clipboard/control fixes: `clipboard_enable` / `clipboard_disable` are routed to the clipboard bridge, data-channel accept/reject decisions are logged without clipboard contents, and Windows input injection attaches to the foreground input queue before `SendInput`. Upload and rollout must target a new `3.1.46` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.47` supersedes `3.1.46` for Remote Assist WSS trust on the HTTPS stand: agent-side signaling loads Windows Trusted Root/CA stores and optional `PC_AGENT_TLS_CA_FILE` roots. Upload and rollout must target a new `3.1.47` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.48` supersedes `3.1.47` for Remote Assist video/control field fixes: screen capture uses a timeout-backed dedicated worker with visible fallback frames instead of freezing indefinitely, the viewer detects stalled video frames, and Windows mouse injection sets the OS cursor position before clicks/wheel events for RDP/virtual display reliability. Upload and rollout must target a new `3.1.48` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.49` supersedes `3.1.48` for Remote Assist input/session lifecycle fixes: browser clicks now send explicit single/double-click commands, the agent processes control messages through a sequential queue, user-side session end is pushed to the operator viewer, and support requests default to the smoother 1280x720@15fps profile. Normal interactive control still cannot drive elevated Windows surfaces such as Task Manager; that belongs to the separate elevated/admin mode.
- 2026-05-10 agent release check: Windows stable `3.1.50` supersedes `3.1.49` for Remote Assist elevated/admin control: `elevated_admin` launches a UAC-visible helper through `pc_agent.exe --remote-assist-elevated-helper`, sends input over a one-time-token loopback channel, and keeps the helper session-scoped with no server connectivity or unattended start. Upload and rollout must target a new `3.1.50` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.52` supersedes `3.1.51` for Remote Assist default capabilities: support requests now preselect clipboard auto-sync for screen-sharing sessions and file transfer for all permitted sessions, while the active viewer exposes a "Повысить права" button that ends the normal session and opens a separate `elevated_admin` consent flow. Upload and rollout must target a new `3.1.52` build, not overwrite existing artifacts.
- 2026-05-10 agent release check: Windows stable `3.1.51` supersedes `3.1.50` for Remote Assist operator-to-device file transfer: approved sessions can open a policy-gated `file-transfer` data channel, send files from the viewer by button, drag/drop or browser file-paste, and save them on the device under `Downloads\Maria Remote Assist` without sending file bytes through the server. Upload and rollout must target a new `3.1.51` build, not overwrite existing artifacts.
- 2026-05-11 agent release check: Windows stable `3.1.54` supersedes `3.1.53` for Remote Assist elevated/admin stability: the UAC helper uses timeout-safe raw socket reads, so idle elevated sessions no longer crash with `OSError: cannot read from timed out object`. Upload and rollout must target a new `3.1.54` build, not overwrite existing artifacts.
- 2026-05-11 agent release check: Windows stable `3.1.55` supersedes `3.1.54` as the Remote Assist runtime-module bootstrap: `main_window.py` now creates sessions through `pc_agent.remote_assist.runtime_host`, which loads installed `remote_assist_runtime` from `modules_store` and falls back to the bundled runtime if the module is absent or invalid. Upload and rollout must target a new `3.1.55` build, not overwrite existing artifacts.
- 2026-05-11 agent release check: Windows stable `3.1.56` supersedes `3.1.55` for Remote Assist elevated/admin helper diagnostics and stale-helper proof: helper launch/client logs include `AGENT_VERSION`, PID, executable and port, and the helper treats `OSError: cannot read from timed out object` as an idle timeout instead of crashing with a traceback. Managed `remote_assist_runtime@1.0.2` requires agent `>=3.1.56`; upload and rollout must target new artifacts, not overwrite `3.1.55` / `remote_assist_runtime@1.0.1`.
- 2026-05-22 agent release check: stable `3.1.57` supersedes `3.1.56` for local settings module visibility on Windows and Linux: `GET /ui/settings` exposes effective/core/configured module lists and the GUI shows always-on `inventory` / `presence` apart from installed module packages. Upload and rollout must target new `windows_amd64` and `linux_alt_x86_64` artifacts, not overwrite `3.1.56`.
- 2026-05-10 Remote Assist post-MVP foundation: mode policy lives in `server/remote_assist/policy.py`, quality/session media options in `server/remote_assist/media.py`, session capabilities in `server/remote_assist/features.py`, coturn REST-style ICE credentials in `server/remote_assist/ice.py`, and new RBAC permissions are `remote_assist.control`, `remote_assist.file_transfer`, `remote_assist.clipboard`, `remote_assist.elevated`, `remote_assist.unattended`. Interactive control and text clipboard auto-sync use WebRTC data channel `control` with agent-side validation in `pc_agent/remote_assist/input_controller.py` and `pc_agent/remote_assist/clipboard.py`; operator-to-device file transfer uses data channel `file-transfer` with chunk validation in `pc_agent/remote_assist/file_transfer.py`; default config keeps post-MVP modes/features disabled unless explicitly enabled.
- 2026-05-05/2026-05-06 support workspace P2.1/P2.9: `GET /api/web/support/tickets/{ticket_id}/knowledge-suggestions` still prefers manual KB links, exposes safe requester-portal `requester_attempts`, and uses catalog fallback from `server/tickets/knowledge_provider.py` with a JSON-backed source at `server/tickets/knowledge_catalog.json` for common incidents such as HTTP 502, DNS, printer offline, account access and offline agent; the same payload is embedded in aggregate `/workspace`.
- 2026-05-06 support workspace P2.2: `GET /api/web/support/tickets/{ticket_id}/timeline?filter=all|messages|internal|diagnostics|history` exposes standalone typed timeline filtering through the same normalized support timeline serializer used by detail/workspace payloads; `/app/tickets` uses it for tab-specific timeline refreshes with aggregate fallback.
- 2026-05-06 support workspace P2.3-P2.5: support timeline diagnostic rows now extract structured steps from nested result/check payloads and preserve optional `details`; `SupportTicketRegistrySnapshot` includes requester phone/email/source plus location floor from registry people/locations; `/app/tickets` has a localStorage-backed light/dark theme toggle on the workspace shell.
- 2026-05-06 support workspace P3.1: `GET /api/web/support/tickets/{ticket_id}/tools` now returns manifest-derived `domain`, `tool_kind`, derived `required_permission`, `allowed_roles` and compact `policy_labels` on `SupportToolItem`; `/app/tickets` shows permission/role labels in the tools panel without changing tool run behavior.
- 2026-05-06 support workspace P3.2: latest operation snapshots and diagnostic timeline rows now carry read-only lifecycle hints (`duration_ms`, retry counters, `retryable`, error code/category, trace id and details URL) from existing operation columns/event payloads; `/app/tickets` renders them as compact chips without adding retry actions.
- 2026-05-06 support workspace P3.3: `GET /api/web/support/tickets/{ticket_id}/knowledge-suggestions` now includes source diagnostics for manual KB links, catalog matches and similar tickets (`ai_summary.confidence/source_count`, provider/version, source counts, query signals and per-source match reasons); `/app/tickets` renders provider/confidence/signal chips in the knowledge tab while keeping AI beta source-visible and non-authoritative.
- 2026-05-06 support workspace P3.4: aggregate `GET /api/web/support/tickets/{ticket_id}/workspace` now embeds compact `closure_plan` derived from existing `actions.closure_requirements`; `/app/tickets` shows a central "Перед закрытием" panel with closure blockers, evidence candidate counts and passport navigation while backend workflow/closure guards remain authoritative.
- 2026-05-07 support workspace P4/P5/P6: `/app/tickets` closure blockers now expose real evidence candidate linking, manual evidence creation and worklog entry actions through typed passport/worklog APIs, including `POST /api/web/support/tickets/{ticket_id}/worklogs` for httpOnly web-session auth; latest operation cards expose typed action affordances (`can_cancel`, `can_retry`, action URLs and disabled reasons), safe details through `/api/operations/{operation_id}`, support-browser cancel through `POST /api/web/support/operations/{operation_id}/cancel`, and policy-aware retry through `POST /api/operations/{operation_id}/retry` or ticket-scoped `POST /api/tickets/{ticket_id}/operations/{operation_id}/retry`. Retry validates ticket/device/auth/policy/consent/replay context, creates a new operation with `retry_of_operation_id`, and writes `operation_retried` into the timeline. `server/tickets/knowledge_provider.py` has a token-overlap catalog index for reordered incident phrases plus provider diagnostics (`provider_status`, `external_provider_status`, `fallback_reason`, `catalog_entry_count`, `query_tokens`), and `SupportTicketRegistrySnapshot` carries service owner queue/source data from `RegistryRepo.get_service()`.
- 2026-05-07 observer P8.1/P8.2: support lifecycle events in `server/web_api/support_handlers.py` now rely on `TicketEventsRepo` ticket-root trace resolution instead of passing ad-hoc random trace ids; operation-bound retry/tool events keep `operation_id` so they resolve to operation traces. `GET /api/web/support/tickets/{ticket_id}` now embeds compact observer diagnostics for `/app/tickets`: root trace URL/status/kind, health label, latest error label/stage/time, top ticket-local signature, compact related/active/error traces and recent occurrences.
- 2026-05-07 observer P8.4: `SupportTicketOperationSnapshot` now carries trace relation metadata for `/app/tickets` operation cards: `ticket_root`, `operation_child`, `retry_child`, `playbook_child` or `unknown`, plus operation/root trace URLs and retry lineage (`retry_of_operation_id`, `retry_source_trace_id`). The UI labels these as `Root trace тикета`, `Трасса операции`, `Повтор операции` or `Трасса playbook` and links directly into `/app/admin/observer`.
- 2026-05-07 observer P8.5: `/app/tickets` context sidebar now renders a compact Observer diagnostic card from the typed detail payload instead of exposing observer as raw counters only. It shows health, root trace, latest error, top signature, compact trace rows, quiet no-trace state and direct `/app/admin/observer` links while keeping raw ids and summary endpoint secondary.
- 2026-05-07 observer P8.6: `/app/admin/observer?trace_id=...` opens the traces tab, requests typed traces with `trace_id`, selects the requested trace and loads detail/bundle for it. `ticket_id` and `operation_id` URL params are also accepted by the workbench traces query for precise support/admin handoff.
- 2026-05-08 support workspace P9.1: `server/web_api/support_handlers.py` hides clearly internal/test navigation artifacts (`Stage...`, `Codex OLA...`, `Live...`, `...Test...`) from `/api/web/support/queue` queue counts and `/api/web/support/workspace/summary` queues/custom smart views while preserving the actual accessible ticket rows and search results. This is sidebar data hygiene for `/app/tickets`, not a permission or routing change.
- 2026-05-08 support workspace P10.2: `/app/tickets` has first-class reversible workspace cleanup controls. `POST /api/web/support/tickets/{ticket_id}/hide|unhide` stores global support visibility in `tickets.custom_fields.support_workspace_visibility`, `POST /api/web/support/tickets/{ticket_id}/archive|unarchive` reuses `tickets.archived_at` and is admin-only, and `POST /api/web/support/workspace/cleanup-noise` hides obvious live/stage/test rows. `/api/web/support/queue` excludes hidden/archived rows by default, accepts `include_archived=1` only when the UI `Показывать архив` toggle is on, and keeps `include_hidden=1` as an explicit backend/debug escape hatch. Admin-only hard purge is separate: `POST /api/web/admin/tickets/purge/preview` reports related counts/blockers and `POST /api/web/admin/tickets/purge` requires `confirm=true`, blocks active operations/Remote Assist and parent tickets with non-selected children, then removes ticket-owned FK rows through cascades plus non-FK events/operations/artifacts/observer rows.
- 2026-05-08/2026-06-11 support workspace P10.3: policy-aware operation retry now supports consent-required tools. `POST /api/operations/{operation_id}/retry` creates a linked `waiting_consent` retry operation with `retry_requires_consent`, `consent_state`, `consent_action_url`, redacted `tool_call_started` replay params and redacted `operation_retry_consent_requested`; no agent dispatch happens until `/api/operations/{retry_operation_id}/approve`. `/app/tickets` shows consent-aware retry labels from operation policy labels.
- 2026-05-08 support workspace P12: aggregate `GET /api/web/support/tickets/{ticket_id}/workspace` now includes `ticket.first_response_at` and stops the compact first-response SLA timer once the first response is satisfied, so `/app/tickets` no longer keeps the first-response countdown as the primary next action after a public support reply. The React workspace force-refetches selected workspace/timeline/queue after sending, short-polls while selected-ticket operations are active, hardens Observer trace links to `/app/admin/observer?trace_id=...`, and opens an explicit tools/playbooks launcher instead of auto-running the first diagnostic.
- 2026-05-09 support workspace P14: `/app/tickets` now subscribes the selected ticket through the typed realtime bridge (`GET /api/web/realtime/bootstrap` -> `/ws_ui`) and invalidates workspace, typed timeline, queue and evidence-candidate queries on `ticket_event_committed` / `operation_updated`; it still keeps bounded selected-ticket fallback polling and the manual refresh button for degraded/reconnect scenarios. The central timeline uses `GET /api/web/support/tickets/{ticket_id}/timeline?filter=all`, and the endpoint returns the latest `limit` matching support events after a larger prefetch so fresh server-side operation results are not hidden behind long agent history.
- 2026-05-11 support workspace queue actions: `POST /api/web/support/queue/mass-action` executes permission-safe bulk assign, queue, priority, internal-note, diagnostics and mass-problem linking over the same accessible queue scope as `GET /api/web/support/queue`; `/app/tickets` Queue Mode uses it from the mass-actions menu. Operator/shared Queue Mode views and column sets are now persisted in PostgreSQL table `support_queue_saved_views` through `GET/POST/PUT/DELETE /api/web/support/queue/saved-views`, with personal, queue-shared and global scopes and queue membership filtering.
- 2026-05-09 Remote Assist MVP: start from `server/remote_assist/service.py`, `server/remote_assist/signaling.py`, `server/app/repos/remote_access_repo.py`, `pc_agent/ws_agent.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/remote_assist/*`, `pc_agent/ui_gui/user_consent_dialog.py`, and `webapp/src/features/remote-assist/*`. User consent is canonical `UserConsentRequest`; browser and agent GUI use the same `consent_id`, and the Protocol V3 command `remote_assist.request` is sent only after approval with `consent_status=approved`. WebRTC signaling uses `/ws/remote-assist/{session_id}` with short-lived operator/agent tokens. Agent-side WSS certificate trust is in `pc_agent/remote_assist/tls.py`; for stand HTTPS use Windows Trusted Root or `PC_AGENT_TLS_CA_FILE`. Runtime-module bootstrap starts at `pc_agent/remote_assist/runtime_host.py` and managed package source `pc_agent/modules_packages/remote_assist_runtime`.
- 2026-05-10 Remote Assist field-debug note: if `remote_access_events` show `offer_received` and `answer_received` but no `ice_connected`/`session_started`, the request/consent/signaling path works and the failure is in ICE/media connectivity. Check `ice_servers` first; an empty ICE config means only direct host candidates are available and Windows firewall/NAT can block video. Current viewer and agent code must fail and stop capture after timeout instead of leaving the session in `starting`.

## 2026-04-28 local agent Qt GUI redesign

- Local agent GUI visual system now starts from `pc_agent/ui_gui/theme.py`: centralized light/dark tokens, main shell QSS, chat/list QSS, requester helpdesk objectName/dynamic-property QSS via `requester_helpdesk_stylesheet()` and `refresh_qss_state()`, logo path and SVG icon asset helpers. Requester-facing helpdesk status/date/header/next-action/timeline/info-panel view models live in `pc_agent/ui_gui/ticket_view_models.py`, feed `pc_agent/ui_gui/ticket_format.py`, and render ticket detail widgets (`TicketHeaderWidget`, compact `NextActionCard`, `TicketRightInfoPanel`, `TimelineItemWidget`, `TicketComposerWidget`) from `pc_agent/ui_gui/ticket_detail_widgets.py`; resolved tickets expose confirm/reject solution actions in the header dropdown and use existing close/reject handlers in `ChatPanel`; attachment events render as requester-safe timeline cards with file name/size and an open button without raw URLs; mapped `user_message` and `support_message` events render as dedicated requester/specialist chat bubbles instead of centered system cards; the visible ticket detail layout must not include the legacy left meta panel or raw access-code/link strip; create-ticket wizard progress/type/confirmation/result widgets live in `pc_agent/ui_gui/ticket_create_wizard_widgets.py`, while legacy template selector/list widgets remain hidden compatibility state, dynamic form fields are shown on scrollable Step 2/Описание, and long Step 3/Подтверждение content is also scrollable.
- 2026-05-08 desktop requester cleanup: `NextActionCard` must not duplicate SLA dates; the single visible ticket-deadline block is `TicketRightInfoPanel` with visual `SlaProgressBar` rows. Missing device/inventory data must not render an `Офлайн` device card; show device facts only when real device data exists. The sidebar agent-status/update card remains the visible agent status/version/update surface; the topbar does not duplicate it.
- Main Qt Widgets shell lives in `pc_agent/ui_gui/main_window.py` + `pc_agent/ui_gui/window_chrome.py`: Maria Agent sidebar, dashboard summary, custom cross-platform frameless title bar with window controls, navigation, profile/status cards, protected-connection footer, runtime/update status and settings theme switch stay backed by existing `ui_bridge` state. The settings module block reads `GET /ui/settings` effective/core/configured module lists and shows built-in core modules (`system`, `screen`, `diag_logs`, `inventory`, `presence`) separately from `modules_store` packages. The sidebar auto-collapses to an icon rail for focused ticket/create-ticket workspaces and expands for dashboard/tickets/profile/settings or via its toggle button.
- Ticket list visuals live in `pc_agent/ui_gui/chat_panel.py` + `pc_agent/ui_gui/tickets_list_model.py`: real `TicketsListModel` data, search/filter chips, card delegate, unread counters and double-click/open-chat flow.
- GUI line-icons live in `pc_agent/ui_gui/assets/icons/`; keep new icons and theme object names reflected in `pc_agent/docs/CODEMAP.md` and `scripts/navigation_catalog.py`.
- 2026-04-30 localization cleanup: user-visible agent GUI strings in ticket creation, update status and local lifecycle messages must stay Russian and mojibake-free; requester-facing deadline wording should say what will happen for the user ("вам должны ответить до", "решение ожидается до") instead of exposing raw SLA labels.
- 2026-05-03 helpdesk historical data cleanup inventory starts from `python scripts/helpdesk_data_cleanup.py`: it is read-only by default, scans live tickets/requester names/module descriptions/tool descriptions for mojibake, `???` placeholders and token-like values, writes UTF-8 JSON/Markdown reports under `artifacts/diagnostics/`, redacts token-like samples, and keeps actual mutation disabled until deterministic cleanup rules are reviewed.

## 2026-04-24 inventory cleanup and notifications

- Typed admin inventory now exposes device identity source and duplicate warnings via `GET /api/web/admin/devices`, and safe offline `env_uuid` duplicate cleanup goes through `POST /api/web/admin/devices/cleanup_env_duplicates`; the cleanup archives device rows through `DevicesRepo.archive_device()`, so active tokens and pending runtime work are revoked/cancelled instead of hard-deleted. The selected agent details panel also has a manual archive action backed by web-session alias `DELETE /api/web/admin/devices/{device_id}`, which delegates to the same soft-delete/archive handler.
- `/app/admin/inventory` shows identity source badges (`windows_machine_guid`, `linux_machine_id`, `env_uuid`) and duplicate cleanup actions for old `ADMIN-2`-style test records. `/app/admin/access` is the RBAC start point for users, role defaults, access groups, permission grants, queue grants, audit and effective access. `/app/admin/settings` reuses the settings workspace with the `Уведомления` tab for in-app notification preferences, recent notifications and tech alerts.
- RBAC is now enforced server-side for typed support writes and settings writes, not only in React: ticket status/comments/passport/playbook/tool actions call `server/access_control/service.py::can()`, tool execution also checks low/high risk permissions, and `/app/settings` splits write UX/API guards into `settings.manage_queues` and `settings.manage_routing` with `required_permission` denial payloads.
- Notification APIs used by React now go through typed web aliases (`/api/web/notifications`, `/api/web/notifications/preferences`, `/api/web/notifications/unread_count`) instead of direct `/api/notifications*` calls; preferences `GET/POST` responses are plain JSON fields (`mute_internal`, `muted_event_types`, `suppress_self`, plus `actor_id` on save), never raw ORM rows.
- Device provisioning no longer relies on a hard "2 active tokens" limit. The agent sends `device_fingerprint` from `pc_agent/core/device_fingerprint.py`; the server compares hardware hashes in `auth/device_fingerprint.py`, tolerates one changed component, blocks clear mismatches as `DEVICE_FINGERPRINT_MISMATCH`, and rotates old active tokens when a new one is issued.
- `/app/admin/inventory` now has a fleet token panel backed by admin-only `GET /api/web/admin/device-tokens` plus per-device `GET /api/web/admin/devices/{device_id}/tokens` and `POST /api/web/admin/devices/{device_id}/tokens/revoke`; legacy `/api/devices/{device_id}/tokens*` handlers are also admin-only, revoke is scoped by `token_hash + device_id`, token rows expose device/hostname plus ISO `created_at`/`last_used_at` values for live browser rendering, and tech alerts also include `inventory_env_uuid_duplicates` and `inventory_devices_without_location`.
- `/app/admin/inventory` now presents a low-noise agent console: metrics, table, right-side details, tabs for agents / connection requests / tokens / rollout, and the existing manual connection-request flow is available through web-session aliases (`/api/web/admin/connection_policy`, `/api/web/admin/connection_requests*`) without opening legacy admin. The shell notification bell also counts pending connection requests alongside ticket notification unread count and links directly to `?panel=requests`.
- `/app/admin/agent-updates` is the React Agent Updates registry over web-session aliases `/api/web/admin/agent-builds*` and `/api/web/admin/agent-updates/rollout-policy` backed by the existing agent build/rollout handlers: upload/download/delete builds, assign/clear target rollout policy, and show selected-device current version / target rollout / recommended build / last update result. Inventory and device pages deep-link into it with `device` and `target` query params.
- Manual provisioning is idempotent around approval races: if an agent heartbeat posts `POST /api/connection_request` after admin approve but before `GET /api/connection_request/status`, the server does not create a second pending approval prompt; approve no longer stores a raw token, valid status polling generates and delivers the token once, delivery marking is scoped to `request_id + device_id + approved`, deprecated raw-token repo helpers are no-op, and legacy pending rows without `poll_secret_hash` are rejected with `POLL_SECRET_MISSING`.
- A fresh no-token `POST /api/connection_request` without matching `request_id + poll_secret` must get a new pending request with fresh polling credentials; an undelivered older approval is reserved only for the original poller.
- A missing `server_config.connection_policy` now defaults to `manual`; `accept_all` remains available only when explicitly configured or when `ALLOW_INSECURE_DEV_DEFAULTS=true` for local insecure dev.
- Connection request token exhaustion is now explicit: server returns `TOKEN_LIMIT_EXCEEDED` on 429 and marks the pending request metadata; the agent writes `connection_request_error.json`, emits `connection_rejected` with the server message, and GUI auth avoids persisting a permanent local reject flag for this transient block.
- Auth/provisioning/runtime lifecycle is observer-searchable even without an operation: `agent_runtime_audit` rows project into synthetic traces with `root_kind=device_provisioning`, `root_kind=agent_auth`, or `root_kind=agent_runtime`. For Codex/API debugging start with `/api/admin/tech/observer/search?q=connection_request`, `q=invalid_token`, or `/api/admin/tech/diagnostics/bundle?q=connection_request`.

## 2026-04-26 test and CI layering

- Canonical testing rules live in `docs/TESTING_RULES.md`.
- Root pytest collection is part of the baseline: agent runtime and tests import through the `pc_agent.*` package namespace so server, agent and scripts tests can be collected together without top-level `core` / `modules` collisions. Managed external modules may still rely on legacy loader aliases.
- `scripts/run_ci_suite.py` now runs workspace verify, webapp bundle, `webapp_unit_tests`, `webapp_fixture_e2e`, server pytest split into `server_pytest_no_db`, domain DB/API layers (`server_pytest_db_knowledge`, `server_pytest_db_tickets`, `server_pytest_db_observer_diagnostics`, `server_pytest_db_agent_runtime`, `server_pytest_db_web_api`), `server_pytest_agent_ws`, and `pc_agent_pytest`; each server layer runs with `-vv --durations=80`, a 45 minute step timeout, the configured idle timeout, and `PC_CLIENT_PYTEST_WATCHDOG_SECONDS=120`. Use `--layer <name>` for one layer and `--keep-test-db` to preserve isolated DBs after a failure.
- Every local commit must be pushed to GitHub `origin` immediately; local commit and GitHub push are one checkpoint. Routine dev-branch pushes do not require full CI or a separate strict secret scan, but still require intentional staging and `git diff --cached`.
- Deploy/release scripts use `--gate full` by default and require a green CI artifact for the current commit, so Codex must use explicit `--gate quick` for staging/iteration on the Linux stand. Full CI/full gate are important final release checkpoints, but Codex runs them only after explicit user request or confirmation; at the end of a change block, remind the user and ask whether to run them now if the plan is being delivered in parts.
- Frozen release candidate rule: run targeted tests/quick gate while iterating, run full CI only after the final candidate commit, then do not commit before full-gate release. Use `python scripts/release_candidate_preflight.py` before full gate to verify `summary.commit == HEAD`, `status == green`, the webapp bundle artifact and release-relevant dirty workspace state; generated `artifacts/*` do not block this preflight.
- `server/tests/conftest.py` auto-marks tests that use `test_agent` as `agent_ws`/`integration` and prints all Python thread stacks when a watched test exceeds the watchdog threshold.

## 2026-04-26 diagnostic playbooks

- 2026-04-27 self-healing pass: playbooks now save manifest `pc_client.playbook.self_healing.v2`, `server/playbooks/tool_catalog.py` normalizes atomic command manifests, admin catalog merges static diagnostic starters with preferred server module tools, and `server/app/services/playbook_engine.py` runs module auto-install preflight before tool-backed steps with `stage=module_install` / `stage=capability_gate` failures; after a successful DB-backed preflight/install, the immediate `run_tool` enqueue does not wait for inventory/toolset snapshot convergence.
- 2026-04-27 canvas builder pass: `/app/admin/playbooks` is a low-code grid canvas backed by the same typed playbook catalog/save API. Operators drag atomic module commands from the palette, move blocks on the grid, choose the command inside each module-like block, and edit presets/params/output contracts in the right inspector. Canvas positions are UI-only; save order is derived top-to-bottom for the existing runtime.
- Support/tool UI preset selection now carries concrete preset params, while `server/web_api/support_handlers.py` re-expands presets before dispatch and honors tool metadata `requires_consent` by creating `waiting_consent` operations without sending the command. `/app/admin/playbooks` shows module/install/platform/min-agent metadata, output contract status path/values, and quick condition templates generated from `condition_hints`; saved `required_tools` keep `output_schema` separate from `output_contract`.
- Low-code diagnostic scenarios now live in `server/playbooks/catalog.py`, `server/playbooks/form_triggers.py`, existing `server/app/services/playbook_engine.py`, typed admin handlers `GET /api/web/admin/playbooks/catalog` + `POST /api/web/admin/playbooks/save`, and React `/app/admin/playbooks` (`webapp/src/features/playbooks/*`).
- Ticket-bound playbook launch now starts from typed support routes `GET /api/web/support/tickets/{ticket_id}/playbooks` and `POST /api/web/support/tickets/{ticket_id}/playbooks/run`; `/app/tickets/:ticketId` renders the automation panel with readiness, required tools, missing tools/required params, recent ticket playbook runs, ticket-scoped operations and the effective request-template diagnostic policy summary before starting `trigger_type=support_ticket` runs. The run route blocks `PLAYBOOK_PREFLIGHT_BLOCKED` when a published playbook references tools absent from both the agent/builtin toolset and server module registry, or when required block params such as `network.ping.target` are not filled.
- Request forms can start diagnostics on ticket creation via `playbook_triggers`; `server/tickets/create_flow.py` and public requester flow `server/tickets/public_ticket_handlers.py` build a structured facts package from `request_form_data` / `request_form_summary`, start the latest published playbook version idempotently, and `server/web_api/support_handlers.py` exposes `playbook_started` in the typed support detail timeline for `/app/tickets/:ticketId`.
- Request-template `diagnostic_policy` runtime lives in `server/tickets/diagnostic_policy.py`: ticket creation can auto-run `suggested_playbooks` only when `auto_run.enabled` passes priority/online-agent/requester-consent/high-risk-tool consent safety gates, command-result terminal operations are classified into `custom_fields.diagnostic_result` / `diagnostics`, `reroute_by_result` can hand off the ticket to a queue with routing events while preserving the workflow status, and passport generation materializes terminal operations into idempotent `diagnostic_result` evidence rows when `attach_results.as_evidence=true`.
- The diagnostic/remediation boundary is explicit: the builder publishes only `diagnostic` blocks (`system.collect`, `ip_address.get_ip`, `diag.logs.collect` in the initial catalog); `remediation` blocks are reserved for a confirmed action flow.
- Canonical doc: `server/docs/DIAGNOSTIC_PLAYBOOKS.md`.

## 2026-04-16 intake forms

- Typed requester intake now lives in `server/tickets/form_catalog.py`, `server/tickets/form_pack_handlers.py`, `server/help.js`, `server/admin_ticket_forms_builder.js`, `server/web_api/admin_handlers.py`, `webapp/src/features/forms-builder/forms-builder-panel.tsx`, and `pc_agent/ui_gui/chat_panel.py`.
- Observer trace overlay now lives in `server/observer/service.py`, `server/observer/runtime.py`, `server/tech/handlers.py`, `server/app/db/models.py`, `server/websocket/agent_services.py`, and `pc_agent/core/action_trace.py`; observer v2/v3 adds canonical ticket-root traces, first-class degradation queries (`duration > N`, timeout/retry/slow rate, `root_kind` flow filtering), synthetic operation-less runtime-audit traces for auth/provisioning/runtime lifecycle, pushed `agent_observer_events` telemetry, `playbook_run` step spans, `module_reconcile`, `web_auth` and `observer_runtime` root kinds, automatic historical backfill, full client-update tracing from request to launcher apply, and isolated short-lived projection sessions so admin/support observer polling does not hold idle DB transactions while waiting on per-trace locks.
- Canonical observer docs now live in `server/docs/OBSERVER_LAYER.md` and `server/docs/OBSERVER_AUTHORING_RULES.md`; quick diagnostics use legacy-compatible `/api/admin/tech/*` endpoints for Codex/prod debugging, while React uses typed aliases under `/api/web/admin/observer/*`: quick, traces, trace-detail, diagnostics bundle, signatures, degradations, runtime, settings and traces rebuild. Support/ticket observer summary uses `GET /api/tickets/{ticket_id}/observer`. That ticket summary must report counts over the full ticket trace set and expose both global `occurrences_count` and ticket-local `ticket_occurrences_count`. Canonical operator trace UX is the `/support` workspace drawer tab `Трасса`, while `/app/admin/observer` now owns the typed observer workbench: quick overview, server-side trace search, diagnostic bundle, signatures, degradations, runtime/settings, global mode without a selected `device_id`, trace detail with `include_agent_actions=1`, evidence-source counters from `attrs_json.source_counts`, diagnostics bundle counters, compact agent-action rows, and explicit detail/bundle error states.
- Admin observer trace rows should lead with `ticket_code` / `display_title` / `display_subtitle` and device/operation labels, while UUID-like trace, ticket, operation and device ids remain secondary copyable diagnostics. Trace search is expected to find familiar ticket numbers such as `T-000520` via projected trace `attrs_json`.
- Both `/api/web/admin/observer/traces/{trace_id}` and the compatibility alias `/api/web/admin/observer/trace-detail/{trace_id}` must return the typed enriched observer detail payload; React detail headers rely on that enrichment to avoid falling back to raw `ticket_id`.
- 2026-05-09 observer explainability: typed admin trace detail includes `explanation` with launch source (`manual`, form autorun, diagnostic policy, playbook, retry, system), actor label, tool/module/preset labels, human diagnosis, launch path, next actions and agent status. Stage spans carry `stage_label`, `stage_state`, `stage_note` and `is_failure_stage`; `operation.stage.queued` must not be rendered as the root error when a later terminal `failed` stage explains the failure.
- Browser polling for `/admin` tech and `/support` workspace should stay single-flight: if you touch `server/admin.js` or `server/support.js`, keep observer refresh non-overlapping so stale tabs cannot avalanche `observer/quick` and ticket-summary requests into the DB pool.
- The legacy admin `/admin` shell still has a dedicated `Конструктор форм` tab for versioned request-form packs, served by `/api/ticket_forms/current`, `/public_api/ticket_forms/current`, and `/api/ticket_forms/packs/*`.
- The new `/app/admin` workspace now also exposes a typed `Конструктор форм заявок` through `GET /api/web/admin/forms/current`, compatibility `POST /api/web/admin/forms/save`, explicit lifecycle endpoints `POST /api/web/admin/forms/save-draft`, `POST /api/web/admin/forms/validate`, `POST /api/web/admin/forms/publish`, `PATCH /api/web/admin/forms/preferred`, and route preview `POST /api/web/admin/forms/route-preview`; lifecycle orchestration lives in `server/tickets/form_lifecycle_service.py`, business preflight rules live in `server/tickets/form_business_validation.py`, the React panel edits forms/fields directly, can add/edit server-driven priority questions and controlled process field roles from server-provided `field_role_options`, preserves canonical `*_policy_ref` fields plus preview/alias metadata and opt-in `on_behalf_policy`, shows policy refs as the primary template contract with active-template impact hints, keeps inline policy JSON under explicit `Advanced inline policy JSON`, saves explicit draft rows in `form_builder_drafts`, returns structured preflight warnings/errors from `/validate` for queues/SLA/OLA/playbooks/policy refs/preview samples/field aliases/duplicate singleton roles/unmapped diagnostic inputs/incompatible approval subjects/incompatible closure evidence, mirrors role conflicts inline near affected fields, and publishes into the same `request_forms` catalog only when the admin chooses Publish/Preferred and blocking business errors are absent.
- 2026-04-30/2026-05-03 visual request-template constructor lives in `webapp/src/features/forms-builder/forms-builder-panel.tsx`: `/app/admin/forms` shows the full chain `template -> form -> workflow -> priority -> сроки -> routing -> approvals -> diagnostics -> closure -> visibility -> notifications -> passport` plus an explicit `Карта экранов мастера` for target-model screens `Основное`, `Классификация`, `Форма`, `Процесс`, `Приоритет`, `Роутинг`, `SLA / OLA`, `Согласования`, `Диагностика`, `Закрытие`, `Видимость / Уведомления`, `Паспорт / Отчётность`; it edits policy blocks and saves `diagnostic_policy` / `notification_policy` / `reporting_policy` / `ola_policy` / `routing_policy` / `approval_policy` / `closure_policy` / `visibility_policy` into the same versioned `request_forms` pack. The deadlines step now has structured OLA P0-P3 target controls plus dispatcher-compatible `breach_actions` recipient/channel controls for SLA/OLA escalation, the routing step has structured controls for default queue, first-match condition values, target queue, priority boost and reroute guard, the approvals step has structured controls for approver source, form-field approver, mode, reminder/escalation timing and rejection/passport flags, the diagnostics step has structured controls for suggested playbooks, auto-run priorities, requester/high-risk consent, evidence attachment and reroute-by-result, the closure step has structured controls for resolution summaries, worklog/evidence requirements, requester confirmation, autoclose, negative feedback and resolution codes, the visibility step has structured controls plus requester/support preview for public status mapping, requester-hidden fields and requester-visible metadata, the notifications step has structured controls plus recipient/channel preview for event recipients and web/email/Telegram/VK Teams channels, and the passport step has structured controls for required sections, evidence/action package, export-hidden sections, report tags, official-passport requirement and knowledge draft hints before advanced JSON.
- 2026-04-30/2026-05-03 standalone helpdesk model registry starts in migrations `063`, `064`, `065`, `066`, `067` and `068`, `server/app/repos/helpdesk_policy_repo.py`, `server/tickets/helpdesk_policy_runtime.py`, `server/tickets/request_template_submission.py`, `GET /api/web/admin/helpdesk-model/policies`, `POST /api/web/admin/helpdesk-model/ticket-types/publish|deactivate|rollback`, `POST /api/web/admin/helpdesk-model/form-schemas/publish`, `POST /api/web/admin/helpdesk-model/request-templates/publish-from-form`, `POST /api/web/admin/helpdesk-model/request-templates/republish-legacy-forms`, `POST /api/web/admin/helpdesk-model/policies/publish`, `POST /api/web/admin/helpdesk-model/policies/diff`, `POST /api/web/admin/helpdesk-model/policies/deactivate`, `POST /api/web/admin/helpdesk-model/policies/rollback` and `POST /api/web/admin/helpdesk-model/smart-views/publish`: the registry publishes versioned `ticket_types`, `form_schemas`/`form_fields`/`form_conditions`, request templates, policies and smart views. `ticket_types` now hold process defaults and flags; request-template publication inherits active ticket-type defaults when available while keeping legacy unknown-type fallback. The legacy republish endpoint reads the active `request_forms` pack, backfills matching `form_schemas`/`request_templates`/policy rows, skips unchanged forms by fingerprint, and `GET /api/web/admin/helpdesk-model/policies` reports request-template data-quality gaps for missing workflow/priority/routing/SLA/closure links. The forms builder can publish the selected visual template into first-class `form_schemas`, `request_templates`, priority/SLA/OLA/routing/approval/closure/diagnostic/visibility/notification/reporting policy tables; direct policy editors can publish all those policy versions plus smart views with a publication impact preview for affected active request templates and ticket-type defaults, while raw JSON, version diff, soft-deactivate and rollback live behind the explicit `Расширенный JSON и версии` mode; audit rows are written for publication/deactivate/rollback, request-template policy refs (`*_policy_code`, plus legacy `sla_policy_id`) now drive effective template resolution ahead of inline form JSON, ticket creation stores `policy_refs`, `effective_policy_sources` and `effective_policy_snapshots`, `/api/tickets/create`, `/api/tickets/create/preview` and public create can validate an active standalone registry template/schema by `request_template_key` even when no legacy form-pack entry exists, existing-ticket lifecycle runtime can re-resolve active referenced policy codes without mutating historical snapshots, active custom smart views are executable in `GET /api/web/support/queue?smart_view=...`, smart-view publication validates supported filter keys, field paths, sort directions and display columns before writing a registry version, the smart-view editor has structured controls for excluded statuses, due-window hours, due fields, primary sort and columns before advanced JSON, standalone `sla_policies.config.targets` can calculate first-response/resolution due dates without a legacy `ticket_sla_policies` id, standalone `sla_policy` lifecycle conditions now control start/pause/resume/stop and warning-before events with policy-aware payloads, common live aliases such as `waiting_user`, `waiting_approval`, `ticket_resolved` and `ticket_closed` are normalized by `server/tickets/sla_service.py`, workflow SLA/OLA hooks pass target statuses into pause checks and configured transition triggers such as `requester_replied`/`vendor_replied` into resume checks, standalone `ola_policy` now controls queue ack/processing targets, start/stop/pause/resume conditions and breach-action payloads with legacy queue-target fallback, OLA processing stop accepts legacy list aliases such as `ticket_resolved` / `ticket_closed`, and reporting policies govern passport sections, evidence/action packages, export visibility and report tags.
- 2026-05-03 request-template publication contract: `AdminFormsSaveFormRequest` accepts structured `sla_policy` and `reporting_policy`, and `_serialize_admin_form_request()` forwards them to `POST /api/web/admin/helpdesk-model/request-templates/publish-from-form`; it also preserves `multi_select` options, so the visual constructor/API path can publish SLA, reporting and extended-option fields in the same transaction as the template.
- Public `/help` and the local agent ticket dialog now submit `form_key`, `request_template_key`, `form_payload`, and `ticket_type`; the agent caches the latest form pack in its local data root, preserves request-template/schema/policy version metadata (`request_template_version`, `form_schema_id`/`form_schema_version`, `*_policy_code`, `policy_refs`), renders priority fields from the selected template when present, supports extended intake fields including `multi_select`, keeps fixed priority controls only as fallback for legacy packs, uses `Обращение: <шаблон>` as the creation title, and refreshes when the server reports a newer version or the normalized policy metadata changes. The agent creation wizard calls authenticated `POST /api/tickets/create/preview` to show effective queue, approval, diagnostic consent and requester-facing response/resolution timing before submit, with a local metadata fallback if the preview endpoint is unavailable, blocks silent diagnostic auto-run skips when requester-device consent is required but not checked, shows that consent as an explicit required user action, and sends normalized `diagnostic_consent` when granted. Ticket detail shows user-readable diagnostics, SLA/deadline status and support assignment fallback instead of raw event names; an open create wizard refreshes from the updated cached pack when `ChatPanel.ticketFormPackChanged` fires. `/api/tickets/create`, `/api/tickets/create/preview` and public create accept `request_template_key` as a first-class alias for legacy `form_key` so old packs and new request-template-aware agent payloads both reach the same validation/runtime path.
- Old preferred `request_forms` packs are normalized with the standard priority question fields (`impact_scope`, `work_continuity`, `business_importance`, `critical_service`, `public_service`) so the server UI, `/help`, and the local agent use the same configurable priority facts before the next manual republish; custom priority-policy field keys keep their own fields/roles and do not receive roles for absent default keys. Injected default priority fields are optional when absent from an old `form_key` submission, so legacy clients can still send only the fields that existed in the original pack.
- Agent request forms now resolve `user/department/location/device/service_picker` fields through authenticated `GET /api/registry/options`; `date`/`datetime` fields use native Qt calendar/time controls, `file` fields can be selected/replaced/cleared, store `{path, filename}` in `form_payload` and feed the same attachment upload path used by wizard-level files. The agent normalizes field-level `visibility`/`process_mapping` metadata and hides requester-invisible internal process fields such as `ticket_type`, priority/SLA/OLA/routing/workflow and policy selectors unless the template explicitly exposes them to the requester. The wizard now has searchable request-template selection, a compact selected-template card with category/badges/next steps, inline required-field errors from `required_message`, attachment rows with file sizes, pre-submit missing/oversized attachment checks, exact server preview due dates/suggested diagnostics when available, and a non-blocking warning when server preview is temporarily unavailable. After creation, the agent shows a structured requester-facing result panel with access code, next action, response/resolution/expected due dates, open/add-message/create-another actions, and a process summary that prefers requester-safe `requester_view`/`public_view`, passport and user-result blocks before legacy flat ticket fields, without raw SLA/OLA/internal wording.
- 2026-05-03 agent live smoke: `server/tickets/form_catalog.py` now carries `form_schema_id`, `form_schema_version`, `request_template_version`, `workflow_profile_id`, `*_policy_code` and `policy_refs` from versioned `request_forms` into `custom_fields.request_template`, preserving legacy integer versions and standalone registry semver values such as `1.0.x`; `server/tickets/request_template_submission.py` lets agent/public `request_template_key` creates use active standalone-registry `request_templates` directly when the form is no longer present in the legacy pack and snapshots the active request-template/form-schema versions.
- 2026-05-11 form runtime audit snapshot: `server/tickets/request_template_submission.py` now returns `resolved_from`, resolved pack/template/schema keys and versions for both `legacy_pack` and `standalone_registry` paths. Ticket create stores `custom_fields.request_form` plus `custom_fields.request_template.computed` with priority, routing source, queue id/code/name and matched routing rule after routing side effects; create-preview returns the same source/computed blocks without creating a ticket. Requester/public visibility hides `custom_fields.request_template` by default so effective policy snapshots and inline policy JSON stay internal.
- 2026-05-11 form lifecycle validation fix: `server/tickets/form_lifecycle_service.py` normalizes `base_version` lookup results from `TicketFormPack` ORM rows into pack JSON before business preflight, so `/api/web/admin/forms/validate` can compare field-key changes against the active published version without a live 500.

## 2026-04-23 form-aware routing

- 2026-04-29 process-model slice: request forms are now request templates. `ticket_type` is selected from the template, not from requester input, and maps to server workflow profiles in `server/tickets/workflow_profiles.py` (`incident`, `service_request`, `access_request`, `change_request`, `consultation`). Template context now preserves `category_id`, `service_id`, `subcategory_id`, `default_queue_id`, `sla_policy_id`, field roles and policy JSON including routing/approval/closure/visibility/notification policies; `server/tickets/routing_service.py` now executes `request_template.routing_policy.rules` before global routing rules, then template default queue and fallback.
- 2026-04-30/2026-05-03 configurable workflow slice: workflow profiles are stored in `ServerConfig` key `ticket.workflow_profiles` with code defaults as fallback. `PUT /api/web/settings/workflow_profiles` saves process type labels, required fields, flags, suggested path and status transition maps, returns typed `diff`, and audits before/after JSON. Structured transition entries can define `allowed_roles`, `required_fields`, `required_comment` or `required_comment_type`, `require_approval`, `require_evidence`, `log_fields`, `trigger`, `auto` and `actions.notify`/`actions.sla`/`actions.approval`; `server/tickets/workflow_service.py` enforces gates, keeps the status invariant that `assigned` requires both `queue_id` and `assignee_id`, executes safe SLA/OLA/approval hooks where applicable using the configured transition trigger for pause/resume, passes the target status into pause checks before the final `ticket.status` write, records no-op/skipped high-risk actions in `workflow_transition_action_results`, and copies configured audit facts into `workflow_transition_log_fields`. Requester public replies use configured `trigger=requester_replied` auto transitions before the legacy `assigned` fallback and write `workflow_trigger` into audit payloads. `/app/settings` edits these profiles through a visual transition rule constructor while preserving JSON compatibility, and `/app/admin/forms` now prefers active registry `ticket_settings.ticket_types` for request-template `ticket_type` selection before falling back to workflow profiles.
- Old published `request_forms` packs that predate `ticket_type` are backfilled by `server/tickets/form_catalog.py` from form key/request_kind (`breakage`, `printer`, `network`, `site_system`, `mail_issue` -> `incident`; `access`, `new_account` -> `access_request`; standard software/hardware requests -> `service_request`), and the React forms builder mirrors that fallback before saving.
- 2026-04-29/2026-05-02 priority/SLA/OLA slice: deterministic priority calculation lives in `server/tickets/priority_policy.py`. Form `priority_policy` maps submitted facts into `impact`, `urgency`, `importance`, `computed_priority`, `manual_priority`, `effective_priority`, `priority_source`, `priority_reason` and `priority_decision`; it now supports configured `input_fields`, named/raw `matrix` rows and columns, live request-template aliases such as `affected_scope=company` and `work_continuity=blocked`, list `modifiers` with `condition`/`action`, `manual_override` role/reason checks using the server-authenticated actor role from create/create-preview/public create while always blocking requester/public/user manual priority, requester-safe create-preview explanations and optional `priority_overridden` audit events. `server/tickets/sla_service.py` resolves SLA targets by process priority P0..P3 with legacy P1..P4 fallback, uses `server/tickets/calendar_engine.py` for second-precision due dates when an SLA policy points to `ticket_business_calendars` / business hours, evaluates standalone `start_conditions`, `pause_conditions`, `resume_conditions` and `stop_conditions`, and emits policy-aware SLA lifecycle events; `server/app/services/ticket_sla_watchdog.py` emits configured `sla_warning` before SLA breach, dispatches SLA warning/breach `breach_actions` through `server/tickets/policy_action_dispatcher.py`, and also calls `server/tickets/ola_service.py::check_ola_breaches`. `server/tickets/ola_service.py` starts queue OLA from `priority_class`, resolves effective `ola_policy`, evaluates OLA start/ack-stop/processing-stop/pause/resume conditions, accepts legacy processing stop aliases such as `ticket_resolved` / `ticket_closed`, stores `custom_fields.ola_runtime`, emits OLA lifecycle/breach events with `breach_actions`, dispatches those breach_actions through `server/tickets/policy_action_dispatcher.py`, and falls back to legacy `ticket_queue_ola_targets`; OLA settings must accept process priorities including P0. `server/tickets/form_catalog.py` preserves priority-policy fact keys from submitted payload even when they are agent-collected helper facts rather than visible form fields. Typed `GET /api/web/settings` accepts both object-shaped and list-shaped calendar JSON for historical `weekly_hours_json` / `holidays_json` rows, so old calendar data no longer collapses the settings payload.
- 2026-04-29 operator-card slice: typed support detail now exposes ticket process fields, priority decision and SLA deadlines for `/app/tickets/:ticketId`; the React detail page renders an operational seven-question ticket card before the workbench.
- Ticket routing now understands form-derived context from `server/tickets/routing_service.py`: `ticket_type`, `request_kind`, `custom_fields`, `request_form_data`, and normalized `request_form_*` metadata are available for live ticket creation and existing preview. Template `routing_policy` rules are first-match by `priority_order` and can set queue, assignee, priority boost/minimum priority, SLA override, approval policy, tags and suggested playbook metadata; loop guards live in `routing_lock`, `do_not_reroute_if_assignee_locked` and `max_auto_reroutes`.
- `tickets.ticket_type` is `varchar(64)` as of migration `061`, so form `request_kind` slugs used by routing preview and real ticket creation share the same practical length budget instead of failing during DB insert.
- The typed settings payload `GET /api/web/settings` now includes both `routing_builder` and `ticket_settings.request_templates` catalogs assembled from the current preferred form pack, so `/app/settings` can reason about request templates, process bindings and routing fields without hand-written JSON.
- Settings capabilities are intentionally split: `settings.view` opens the page, `settings.manage_queues` enables queue/member/OLA saves, and `settings.manage_routing` enables routing/SLA/calendar/resolution saves; OLA target editors use process priorities P0..P3 while backend keeps legacy P4 compatibility; denied actions should show the server-provided reason instead of falling back to one broad read-only mode.
- Command-result side effects for `install_module_package`, `list_installed_modules` and `list_tools` live in `server/websocket/command_result_components.py`; `install_module_package` immediately syncs the installed module into `device_modules`, and `list_tools` snapshots device ORM scalar fields before async writes to keep auto-install follow-up toolset refreshes free of `greenlet_spawn` warnings.
- The typed admin forms boundary now exposes both `POST /api/web/admin/forms/route-preview` and `POST /api/web/admin/forms/process-preview`; `/app/admin/forms` can submit a draft form plus sample answers and see either the matched queue/rule or the fuller ticket process preview with priority, SLA/OLA, approval, diagnostics, closure and notification plan before publishing.
- The typed support detail payload `GET /api/web/support/tickets/{ticket_id}` now includes normalized `request_form` data for the `/app/support` sidebar block `Данные формы`.
- The typed support detail payload `GET /api/web/support/tickets/{ticket_id}` also exposes support-workspace timeline normalization: every returned timeline row includes `event_category`, `event_label`, `event_details`, and diagnostic `operation_steps` when present. The support timeline includes chat, tool/playbook, status, assignee, queue, priority, SLA, OLA, approval and passport/evidence lifecycle events instead of filtering detail down to chat/tool rows only.
- The lightweight `GET /api/web/support/workspace/summary` returns row-free support workspace counts: `views.needs_action`, `views.sla_risk`, `views.unassigned`, `views.requester_replied`, queue counts, raw `smart_view_counts` and `smart_view_options`.
- The aggregate `GET /api/web/support/tickets/{ticket_id}/workspace` returns detail, tools, playbooks, passport, compact `sla_ola`, compact `passport_readiness` and the first-class knowledge-suggestions payload in one typed response, so `/app/tickets` can load the selected ticket workspace without separate detail/tools/playbooks/passport/knowledge waterfalls. `GET /api/web/support/tickets/{ticket_id}/knowledge-suggestions` exposes the same KB-link-backed articles, JSON-backed catalog matches, similar tickets, safe requester-portal attempts, conservative source-visible AI beta summary and P3.3 source diagnostics as a standalone contract; provider/search code is in `server/tickets/knowledge_provider.py`.
- The support knowledge provider now builds a small token index over `server/tickets/knowledge_catalog.json`, so phrase order differences such as "gateway 502" vs "502 gateway" still produce source-visible catalog suggestions without changing the API shape.
- Typed support ticket mutation aliases now live at `POST /api/web/support/tickets/{ticket_id}/assign`, `/queue`, `/priority` and `/reroute`. They delegate to the same assignment, queue/routing, priority/SLA and OLA services as the legacy `/api/tickets/{ticket_id}/*` routes, but return `SuccessResponse<SupportTicketMutationActionResult>` and typed RBAC denial payloads for `/app/tickets`.
- Reports request-kind labels are no longer hardcoded: `server/web_api/reports_handlers.py` resolves them from the current preferred request-form catalog.
- Ticket work visibility now starts from `server/tickets/statuses.py`, `server/tickets/visibility_policy.py`, `server/tickets/workflow_service.py`, `server/app/api/serializers.py`, and migrations `056`/`057`: internal statuses are `new/queued/assigned/in_progress/waiting_on_* /scheduled/resolved/closed/canceled`, requester-facing statuses are mapped separately, request-template/effective registry `visibility_policy.public_status_mapping` controls `public_status_label`, and requester payloads redact configured/default hidden fields such as OLA internals, raw diagnostics, root cause, assignment internals and dotted nested paths like `custom_fields.*` or `passport.sections.*` while support payloads keep the internal process view.
- Ticket snapshot relation payloads in `GET /api/tickets/{ticket_id}/snapshot` are JSON-safe DTOs: `watchers`, `links`, `kb_links` and `worklogs` are serialized as plain dicts before visibility projection, so support views can inspect relation/worklog context without leaking those hidden fields to requester snapshots.
- Request-template/effective registry `notification_policy` is executed in `server/tickets/notification_service.py`: canonical event blocks such as `on_created`, `on_assigned`, `on_requester_replied`, `on_sla_breach`, approval events and diagnostic completion select requester, assignee, queue and watcher recipients before user notification preferences apply; enabled external channels such as `email`, `telegram`, `vk_teams` and provider-defined names are delivered through the injectable provider interface in `server/tickets/notification_channels.py`, checked against optional provider `available_channels`, and audited as sent/skipped ticket events without blocking in-app notification creation. Policy-owned escalation actions that should live outside raw SLA/OLA event payloads go through `server/tickets/policy_action_dispatcher.py`: SLA `sla_warning` / `sla_breached` actions and OLA `breach_actions.notify_queue_lead` create recipient notifications, `policy_action_dispatched` audit rows and optional external delivery audits idempotently by source event/action/recipient. Backend smart views live in `server/tickets/smart_views.py` and are exposed through typed support queue `GET /api/web/support/queue?smart_view=...` with built-in options for `sla_risk`, `ola_risk`, `unassigned`, requester replies, stale waits, waiting approval, diagnostics failed and `mass_incident_candidates` / "Похожие массовые обращения"; `summary.smart_view_counts` returns counts for each smart-view option, and React support/tickets pages render the OLA risk slice as an operational queue filter. Active published custom smart views from `smart_views` are appended to options and matched by their validated saved JSON filter; selected custom views also apply their saved sort before scope/status/query slicing.
- Request-template/effective registry `approval_policy` is executed in `server/tickets/approval_policy.py` during workflow transitions. `required=true` allows entering `waiting_on_approval`; create-flow now detects required approval after routing/SLA/OLA startup and moves new tickets into that waiting status before auto-assign or queue-status transitions, while `workflow_service.py` prefers `custom_fields.request_template.workflow_profile_id` over raw custom `ticket_type` when selecting the process profile. Approval entry creates active `ticket_approvals` from configured approver sources (`explicit_user`, `form_field`, `service_owner` with `requester_manager` fallback, `queue_lead`, `security_role`, `group`) without duplicating existing requested approvals; `approval_mode=all` requires every approval to be approved, while `approval_mode=sequential` creates only the first approver as `requested` and later approvers as `pending`; typed support decision route `POST /api/web/support/tickets/{ticket_id}/approvals/{approval_id}/decision` persists approve/reject decisions only for the current requested approver, advances the next sequential approval, emits `approval_approved` / `approval_rejected` audit events and executes notification-policy aliases `on_approval_approved` / `on_approval_rejected`; `timeout.due_in` / `reminder_after` / `escalate_after` are processed by the SLA/OLA watchdog as `approval_reminder_due`, `approval_escalated` and `approval_timed_out` events, and `require_comment_on_reject=true` blocks both approval decision reject calls and rejected-transition actions without a reason/public/internal comment; execution statuses such as `assigned`, `in_progress`, `scheduled` and `resolved` remain blocked until the required approved decision exists, and rejected approvals return `APPROVAL_POLICY_BLOCKED` through typed support status actions. Typed support detail `GET /api/web/support/tickets/{ticket_id}` exposes `ticket.approval_summary` plus `actions.approval`, and `/app/tickets/:ticketId` renders current approvers, due/reminder/escalation timestamps, reject-comment guard hints and runtime reminder/escalation state while requester-facing public status labels still come from visibility policy without leaking internal approver groups.
- Ticket resolution passports now live in `server/tickets/passport_service.py`, `server/tickets/evidence_service.py`, `server/app/repos/ticket_passport_repo.py`, migrations `059` and `069`, typed support handlers `GET/POST/PATCH /api/web/support/tickets/{ticket_id}/passport*`, `GET /passport/evidence-candidates`, `POST /passport/evidence/link`, `PATCH /passport/evidence/{evidence_id}`, and the React `/app/tickets/:ticketId` tab `Паспорт` plus `/app/tickets/:ticketId/passport/print`; governed tickets with `evidence_required=true` cannot move to `resolved` without accepted evidence or `evidence_ref`. Evidence rows carry source/fact/verification/export metadata and are idempotent by ticket/source/fact; candidates are collected from ticket-scoped operations, playbook context, artifacts, worklogs, approvals, chat messages and observer traces; passport generation uses ticket-scoped operations and playbook-context operations only, never unrelated device-wide fallback operations as accepted facts. Request-template/effective registry `closure_policy` is executed in `server/tickets/closure_policy.py` during workflow resolution and supports nested `before_resolved`, `evidence`, `requester_confirmation` and `allowed_resolution_codes`: it can require resolution code, public/internal summaries, worklog, priority evidence, operation log after module/playbook use, approved approval evidence, requester confirmation, policy-driven `auto_close_after_days` through `server/app/services/ticket_auto_close_watchdog.py` and `reopen_on_negative_feedback` behavior; rejected/archived/superseded evidence does not satisfy closure. Typed support detail exposes `actions.closure_requirements`, including `passport_missing:<fact>` rows with fact key, candidate count and recommended actions plus `official_passport_stale` rows with `stale_reasons`/`current_source_counts` when official-passport requirements block closure, and `/app/tickets/:ticketId` shows the missing close checklist before applying `resolved`. Effective `reporting_policy` drives required facts, evidence packages, report tags and `export_preview`, but support passport payloads keep the full internal/operator section set for the workspace; the print/PDF page filters sections through `export_preview.visible_sections` / `hidden_sections` so internal sections remain support-visible but export-hidden. Passport payloads include actionable `requirements.missing_facts` with source candidate previews/counts, `export_preview`, `knowledge_draft_hints`, `source_payload.source_counts`, `source_payload.current_source_counts` and `stale=true` after new evidence or countable source changes appear after generation. The React passport tab renders stale warnings, readable missing-fact sources/actions/candidates, evidence dossier rows, candidate linking, manual evidence creation and editable result/operator sections, while the print/PDF page includes an evidence appendix with source refs and export/verification metadata; closure blocks official dossier completion only when the active reporting policy explicitly sets `require_official_passport=true`.
- `/app/tickets` reuses those passport evidence APIs from the central closure guidance panel: missing evidence blockers can link a candidate, create manual evidence, or add a worklog through typed `POST /api/web/support/tickets/{ticket_id}/worklogs` before the backend closure guard is retried.
- 2026-05-05 passport/evidence backend update: reporting-policy publication now validates supported passport sections and `required_evidence_types`, `/app/admin/forms` exposes the `evidence` evidence-type mapping, passport missing-fact satisfaction respects those required evidence types, unbound requester/agent attachments sent through ticket chat are claimed to the ticket and become artifact evidence candidates, and passport evidence add/link/update audit events include `trace_id`, `source_ref`, `section_key`, verification/export metadata and `observer_provenance`.
- Official-passport closure gates accept user/internal result facts supplied by the current `resolved` transition and do not block on reporting-only `automated_checks` / `approvals` sections when no module run or approval policy applies to the ticket.
- `/app/settings` now has a dedicated `Тикеты` tab backed by `GET /api/web/settings.ticket_settings`: lifecycle/status mapping, requester-facing statuses, `next_action_owner`, governance flags, passport/evidence guard state, operational ticket flags, a visible future service-desk chain (`request_template -> ticket_type/workflow_profile -> priority -> routing -> SLA/OLA -> observer`), editable workflow profiles, read-only planned L1/L2/L3 support-line model and priority model; sibling tabs still own queues, routing, SLA, calendars and resolution codes.

## 2026-04-15 module workbench focus

- For module authoring in the admin UI, start with `server/admin_modules_workbench.js`, `server/admin_modules_workbench.html`, `server/modules/handlers.py`, and `server/modules/workbench_service.py`.
- The module workbench now supports template-driven tool creation, inline validation, validate-before-publish preview, preferred-version assignment, rollout-policy settings for preferred versions, ZIP archive import into the registry, archive-to-code decomposition, and delete-from-registry actions from the module list.
- The admin `Модули` page is split into inner tabs: `Разработка модулей`, `Список модулей`, `Редактор модулей`, and `Модули на устройствах`; the authoring flow inside the first tab is a 4-step wizard.
- Everyday module authoring no longer starts from raw JSON: platforms are selected from supported values, requirements are entered line-by-line, and params/output schemas can be assembled from validated blueprint rows before publish.
- The server validate endpoint for the workbench is `POST /api/modules/workbench/validate`; API clients should prefer the headless authoring trio `GET /api/modules/authoring/catalog`, `POST /api/modules/authoring/validate`, and `POST /api/modules/authoring/publish`. Validate/save/upload must expose `validation_json.server_harness` as the mandatory server-side harness result before publish.
- Server-side module harness runs `pc_agent/scripts/smoke_check_module.py --ignore-platform-check`, so a Linux server can validate Windows-targeted packages structurally without blocking publish only because `manifest.platforms` excludes Linux.
- Windows-targeted modules (`win32` / `windows*`) publish after the server harness but cannot become preferred until `POST /api/modules/{module_name}/{version}/live_tests` records a passed Windows agent run with a compatible agent version; UI should surface the `WINDOWS_LIVE_TEST_REQUIRED_BEFORE_PREFERRED` warning before promotion.
- Published module live testing from React starts with `GET /api/web/admin/modules/workbench/{module_name}/{version}/live_test_candidates?platform=win32|linux` and runs through `POST /api/web/admin/modules/workbench/{module_name}/{version}/live_tests`, while legacy `/api/modules/*` routes remain compatibility aliases. A selected run creates `module_live_test` observer traces; blocked Windows preferred rollout creates `module_preferred_gate` and returns `observer_trace_id`. Module live tests are not ticket-bound; do not pass synthetic `ticket_id` values to the agent run, correlate them by `trace_id`/operation ids. For real agent command results, derive operation ids from top-level response fields or `payload.meta.request_id`.
- New playbook-ready modules should use the module workbench `Playbook decision contract` controls or send `output_contract` through the headless API so the playbook builder can branch on explicit status values instead of raw command text.
- In the typed React UI, the active module constructor is `webapp/src/features/modules/modules-panel.tsx`; keep its payload builder and API preview aligned with the typed `/api/web/admin/modules/workbench/*` endpoints.
- Preferred-version rollout settings now live in `server/app/repos/module_rollout_repo.py` and are exposed via `GET/PATCH /api/modules/rollout_settings`; in `installed_devices` mode a preferred-version change rewrites desired state, triggers reconcile for matching devices, and then refreshes inventory/toolset so the admin UI converges.
- Agent Recipe Runner fleet rollout is a separate persisted workflow: `server/diagnostics/runner_rollout.py` plus `runner_rollout_plans`, `runner_rollout_waves`, `runner_rollout_targets`, `runner_rollout_events`. Admin APIs are under `/api/web/admin/capabilities/runner-rollout*`; the Capability Studio Providers tab shows canary/wave/rollback controls. Delivery still uses `device_desired_modules` and `modules.reconcile.reconcile_device`.
- After agent-side module lifecycle changes, `server/websocket/outbox_ingest_components.py` now treats `tools_changed` and `module_state_changed` as convergence signals and queues a debounced `list_tools`, so auto-install/reconcile updates `device_toolset_snapshots` without waiting for reconnect or manual Sync Modules.
- `server/tools/service.py` also queues `list_installed_modules` and `list_tools` after a successful auto-install before `run_tool`, covering no-op/repeated installs where the agent may not emit a fresh `tools_changed`.

# QUICK_LOOKUP

Короткий канонический навигатор по проекту `pc_client`.

## С чего начинать

1. Для нетривиальной задачи сначала запустите `python scripts/task_intake.py`.
2. Если задача описана словами, используйте `python scripts/task_intake.py --task "<описание>"`.
3. Если уже важен diff, используйте `python scripts/task_intake.py` без аргументов.
4. Перед правками откройте `docs/CODEX_WORKFLOW.md` и выберите режим работы: Explore / Debug / Plan / Execute / Feature / Contract / Verify / Commit / Deploy.
5. Затем откройте `docs/ARCHITECTURE_BOUNDARIES.md`, определите ownership zone и проверьте, не меняется ли contract surface.
6. Для быстрого retrieval после intake используйте `python scripts/build_context_pack.py --topic "<описание>"` и `python scripts/search_context_index.py "<символ route error-code concept>"`; если индекс устарел или search печатает stale-warning, `python scripts/build_context_index.py --force`.
7. Если нужна точечная локальная навигация после retrieval:
   - `python scripts/agent_find.py "<pattern>" --dir server|pc_agent`
   - `python scripts/diff_context.py`
8. Для длинной задачи синхронно ведите `PLANS.md`.
9. Для Live validation, debug, Protocol V3, browser/UIA, account-session, operation lifecycle, module runtime или runtime-control bugfix задач дополнительно откройте `docs/LIVE_TESTING_DEBUG_RULES.md`.

Routing-логика живёт в:

- `scripts/task_intake.py`
- `scripts/navigation_catalog.py`
- `scripts/build_context_index.py`
- `scripts/search_context_index.py`
- `scripts/build_context_pack.py`
- `scripts/docs_inventory.py`

Этот файл остаётся human-facing индексом и не должен дублировать весь routing.

## Canonical docs only

Источник истины для структуры и workflow:

- `AGENTS.md`
- `docs/README.md`
- `docs/CODEX_WORKFLOW.md`
- `docs/QUICK_LOOKUP.md`
- `docs/ARCHITECTURE_BOUNDARIES.md`
- `docs/CONTEXT_INDEX.md`
- `docs/LIVE_TESTING_DEBUG_RULES.md`
- `server/docs/CODEMAP.md`
- `pc_agent/docs/CODEMAP.md`
- protocol/auth/runtime/update docs рядом с кодом

Historical docs больше не канон:

- `docs/archive/TICKET_CRM_GAP_ANALYSIS.md`
- `docs/archive/TICKET_AND_AGENT_UPDATE_ROADMAP.md`
- `docs/archive/BOTTLENECKS_AND_RISKS.md`

Их архивные копии лежат в `docs/archive/`.

## Truth baseline

Минимальный baseline перед commit:

- `python scripts/verify_workspace.py`
- `python -m pytest --collect-only -q`
- `python -m pytest server/tests/ ...`
- `python -m pytest pc_agent/tests/ ...`

Если задача затрагивает release/deploy:

- `python scripts/release_server_to_remote.py --gate quick` for staging iteration
- `python scripts/run_ci_suite.py`, `python scripts/release_candidate_preflight.py` and `python scripts/release_server_to_remote.py --gate full` only after explicit user request or confirmation for the final frozen release checkpoint

После любого локального commit:

- `git push -u origin <current-branch>` для первого push ветки
- `git push` для следующих commit на ветке

Если задача затрагивает `webapp/` или frontend bundle pipeline:

- `python scripts/bootstrap_web_toolchain.py`
- после release живой signoff нового `/app/*`: `pnpm --dir webapp run check:remote:webapp -- --base-url https://192.168.100.17:9443`
- preflight перед включением default-route switch: `python scripts/check_webapp_cutover.py --json`
- operational checklist для полного переключения: `docs/WEBAPP_CUTOVER_CHECKLIST.md`

Если задача про UI/UX/accessibility audit, admin UI или заметную визуальную переработку:

- routing должен попадать в тему `New web workspaces / typed web boundary`;
- для `webapp/` сначала `python scripts/bootstrap_web_toolchain.py`;
- живую проверку делать через MCP на `https://192.168.100.17:9443/admin`.

Если менялся веб:

- browser verification через MCP на `https://192.168.100.17:9443/admin`

Test DB env vars:

- `TEST_DATABASE_ADMIN_URL`
- `TEST_DATABASE_URL`
- `PC_CLIENT_ALLOW_SHARED_TEST_DB`

Windows note:

- On Windows, if `TEST_DATABASE_URL` and `TEST_DATABASE_ADMIN_URL` are not set, server DB-backed pytest opens the local SSH tunnel and defaults to an isolated `pc_support_test_<domain>_<pid_or_worker>_<short_hash>` database through the admin DB.
- In that default mode the harness opens a local SSH tunnel to PostgreSQL using `C:\Users\admin-2\.ssh\pc_client_altserver_ed25519`.
- The first isolated Windows DB-backed run can take several minutes because it creates a fresh database and applies the full Alembic chain through the SSH tunnel.
- Shared `pc_support_test` is now an explicit fallback/debug mode: set `PC_CLIENT_ALLOW_SHARED_TEST_DB=1`, or let the Windows default fallback use it only if the admin DB is unavailable. On Windows, explicit shared debug mode uses the same local SSH tunnel. In shared-DB fallback mode the harness terminates stale `pc_support_test` backends before `TRUNCATE` and uses a short lock timeout, so leaked sessions fail fast instead of hanging the suite.
- For websocket-heavy pytest on Windows, `server/tests/conftest.py` now forces `WindowsSelectorEventLoopPolicy`, so the old trailing `unexpected connection_lost() call` noise is no longer a completion criterion.
- For custom isolated ephemeral test DBs from Windows, set `TEST_DATABASE_ADMIN_URL` explicitly.

Knowledge Studio K2 note:

- `/app/admin/knowledge/studio` is now the simplified article editor: default UI has one primary `Сохранить статью` action, hides review/version/segmentation mechanics, keeps rollback in `История версий`, and uses `KnowledgeRepo.publish_item` reviewer autofill when `KNOWLEDGE_REVIEW_REQUIRED=false`.

## 2026-04-28 Protocol hardening notes

- `server/websocket/agent_services.py` now marks an `outbox_id` as runtime-deduped only after a terminal non-retryable outcome or ACK. Retryable `outbox_nack` outcomes must remain retryable by the same `outbox_id`.
- `server/websocket/outbox_ingest_components.py` treats missing `trace_id` as an envelope validation error and returns a typed validation NACK with a fallback server trace id instead of silently skipping ACK/NACK.
- `server/websocket/protocol.py` registers sync `send_ws_command(..., wait_for_result=True)` waiters before waking device dispatch, and copies caller params before consuming internal `_operation_id`.
- `server/tools/service.py` also copies caller params before building `run_tool` command params, so retries/logging/audit code can safely reuse the original dict.
- `pc_agent/ws_agent.py` handshake diagnostics must log the current `PROTOCOL_VERSION` (`ws_ticket_v3`), not legacy `ws_mcp_v1`.
- `pc_agent/ws_agent.py` classifies only explicit auth handshake rejects (`4003`, invalid-token, token-required) as reprovision triggers; transient WSS/proxy handshakes such as HTTP `502` must stay in reconnect/backoff and must not terminate the agent process.
- Agent device lifecycle events: `pc_agent/ws_agent.py` must pass the canonical `device_id` into `AgentOrchestrator`; `pc_agent/core/orchestrator.py` emits `module_state_changed` and hash-gated `tools_changed`; `pc_agent/core/sender.py` must not serialize the local SQLite compatibility `ticket_id=device_id` as ticket context for Protocol V3 device events.

## Fast map

| Topic | Open first | Then |
|------|------------|------|
| Codex workflow / dirty worktree / commit / deploy | `docs/CODEX_WORKFLOW.md`, `AGENTS.md`, `docs/ARCHITECTURE_BOUNDARIES.md` | Use before work to choose Explore, Debug, Plan, Execute, Feature, Contract, Verify, Commit or Deploy mode and follow script-first commands |
| Live testing / debugging rules | `docs/LIVE_TESTING_DEBUG_RULES.md`, `docs/TESTING_RULES.md`, `PLANS.md` | Mandatory for Live validation and bugfix work: evidence before fix, no mixed validation modes, browser/UIA requirements, account-session checks, clean-run markers, contamination labels and status consistency |
| Architecture boundaries / contract impact | `docs/ARCHITECTURE_BOUNDARIES.md`, `AGENTS.md`, `docs/QUICK_LOOKUP.md` | Use before edits to classify a change as local / boundary / cross-cutting / release-control and to choose neighboring docs/tests |
| Context index / retrieval | `docs/CONTEXT_INDEX.md`, `scripts/build_context_index.py`, `scripts/search_context_index.py`, `scripts/build_context_pack.py` | Build with `python scripts/build_context_index.py --force`; search with `python scripts/search_context_index.py "<query>"`; use `--profile debug|contract|route|test|web` for targeted ranking; `build_context_pack.py` includes top context-index hits after `task_intake`; use as retrieval, not as a replacement for CODEMAP/boundaries |
| Protocol V3 / handshake | `server/websocket/agent_handshake.py`, `server/websocket/agent_services.py`, `server/state_manager.py`, `server/app/repos/device_outbox_repo.py`, `pc_agent/ws_agent.py`, `pc_agent/core/sender.py`, `pc_agent/ws_agent_runtime_helpers.py` | `server/docs/PROTOCOL_V3.md`, `pc_agent/docs/PROTOCOL_V3.md` |
| Remote Assist / WebRTC | `server/remote_assist/service.py`, `server/remote_assist/handlers.py`, `server/remote_assist/signaling.py`, `server/remote_assist/policy.py`, `server/remote_assist/ice.py`, `server/app/repos/remote_access_repo.py`, `pc_agent/ws_agent.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/remote_assist/*`, `pc_agent/modules_packages/remote_assist_runtime/*`, `pc_agent/ui_gui/remote_assist_dialog.py`, `webapp/src/features/remote-assist/*`, `webapp/src/pages/tickets/list-page.tsx` | `server/docs/CODEMAP.md`, `pc_agent/docs/CODEMAP.md`, `server/docs/PROTOCOL_V3.md`, `pc_agent/docs/PROTOCOL_V3.md`, `PLANS.md`; runtime module host is `pc_agent/remote_assist/runtime_host.py`, first managed module is `remote_assist_runtime@1.0.0`; file transfer uses WebRTC data channel `file-transfer`, browser button/drag-drop/file-paste entry points, and saves on the device under `Downloads\Maria Remote Assist` |
| New web workspaces / typed web boundary | `webapp/src/main.tsx`, `webapp/src/app/router.tsx`, `webapp/src/app/routes/lazy-pages.tsx`, `webapp/src/app/layouts/app-shell.tsx`, `webapp/src/app/navigation.tsx`, `webapp/src/components/shell/*`, `webapp/src/components/ui/*`, `webapp/src/features/auth/session-provider.tsx`, `webapp/src/features/auth/login-page.tsx`, `webapp/src/features/auth/workspace-access.ts`, `webapp/src/features/access-control/*`, `webapp/src/features/agent-updates/*`, `webapp/src/features/capabilities/*`, `webapp/src/features/requester/*`, `webapp/src/features/queues/api.ts`, `webapp/src/features/reports/api.ts`, `webapp/src/features/settings/api.ts`, `webapp/src/features/playbooks/*`, `webapp/src/pages/help/index.tsx`, `webapp/src/pages/requester-ticket/index.tsx`, `webapp/src/pages/tickets/*`, `webapp/src/pages/reports/index.tsx`, `webapp/src/pages/knowledge/index.tsx`, `webapp/src/pages/settings/index.tsx`, `webapp/src/pages/admin/*`, `webapp/playwright.config.ts`, `webapp/scripts/remote-browser-signoff.mjs`, `webapp/package.json`, `webapp/tests/support-workspace.spec.ts`, `webapp/tests/admin-workspace.spec.ts`, `webapp/tests/fixtures/support_fixture_server.py`, `server/access_control/*`, `server/app/repos/access_control_repo.py`, `server/web_api/access_handlers.py`, `server/web_api/session_handlers.py`, `server/web_api/support_handlers.py`, `server/web_api/admin_handlers.py`, `server/web_api/reports_handlers.py`, `server/web_api/settings_handlers.py`, `server/web_api/realtime_handlers.py`, `server/web_api/dto/reports.py`, `server/web_api/dto/settings.py`, `server/static_pages/webapp_assets.py`, `server/static_pages/handlers.py`, `server/config.py`, `server/routes.py`, `server/websocket/ui_handler.py` | `docs/superpowers/specs/2026-04-20-admin-support-web-rearchitecture-design.md`, `docs/superpowers/specs/2026-04-22-admin-support-unified-workspace-style-design.md`, `server/docs/SECURITY_AND_AUTH.md`, `server/docs/OBSERVER_LAYER.md`, `server/docs/REQUEST_FORM_BUILDER.md`, `server/docs/DIAGNOSTIC_PLAYBOOKS.md`; route pages are lazy-loaded from `webapp/src/app/routes/lazy-pages.tsx`; current route model is SaaS-style and workspace-first: `/app/support` is the Support Work Center, `/app/admin` is the Admin Center, `/app/tickets`, `/app/tickets/:ticketId`, `/app/tickets/:ticketId/passport/print`, `/app/reports`, `/app/knowledge`, `/app/settings`, `/app/admin/inventory`, `/app/admin/device`, `/app/admin/device-operations`, `/app/admin/device-operations/:deviceId`, `/app/admin/agent-updates`, `/app/admin/access`, `/app/admin/registry`, `/app/admin/modules`, `/app/admin/capabilities`, `/app/admin/forms`, `/app/admin/request-template-studio`, `/app/admin/service-catalog`, `/app/admin/policy-health`, `/app/admin/knowledge`, `/app/admin/knowledge/metadata`, `/app/admin/quality`, `/app/admin/problems`, `/app/admin/changes`, `/app/admin/playbooks`, `/app/admin/observer`, `/app/admin/settings`, plus public requester `/app/help`, `/app/ticket` and `/app/ticket/:ticketId`; `webapp/src/app/navigation.tsx` is the single source for workspace/domain groups, permission-filtered visible navigation, active route matching, local admin domain tabs and workspace switch fallbacks; support ticket detail includes the `Паспорт` tab and typed passport API integration; support, admin, access-control, reports and settings pages read real typed `/api/web/*` payloads instead of frontend mock data, while public requester pages intentionally use `/public_api/*` for anonymous intake/code auth; `/app/admin/capabilities` is Capability Studio over diagnostics capability/provider aliases with catalog, providers, evidence mapping, readiness lab and SDK Modules link; `/app/admin/agent-updates` uses `/api/web/admin/agent-builds*` and `/api/web/admin/agent-updates/rollout-policy` web-session aliases backed by the existing build/rollout handlers for registry/upload/download/delete and target preferred rollout assignment; `/app/admin/access` is the Access Control Center backed by typed `/api/web/admin/access/*` with group creation, permission/member/queue grants, effective access and admin-only UI password reset; `/app/admin/playbooks` is the low-code diagnostic playbook builder backed by typed `/api/web/admin/playbooks/*`; `/app/admin/observer` is the full observer workbench with quick/traces/signatures/degradations/runtime tabs, global mode without a selected device, and trace detail backed by `include_agent_actions=1`; the left rail shows only the active workspace, admin pages are grouped into collapsible domains, related route-level admin pages get local domain tabs, workspace switching/logout live in the topbar, and the visual system is driven by Tailwind v4 tokens plus shared `Button`/`Badge`/`Card`/`Tabs`/`SearchField` primitives |
| Web-first registration / requester profile | `PLANS.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`, `webapp/src/features/auth/register-page.tsx`, `webapp/src/features/auth/register-page.test.tsx`, `webapp/src/features/auth/api.ts`, `webapp/src/features/auth/login-page.tsx`, `webapp/src/app/router.tsx`, `server/web_api/session_handlers.py`, `server/web_api/dto/session.py`, `server/config.py`, `server/routes.py`, `server/tests/test_web_session_api.py`, `webapp/src/pages/device-pairing/index.tsx`, `webapp/src/pages/device-pairing/device-pairing-page.test.tsx`, `webapp/src/pages/requester/index.tsx`, `webapp/src/pages/requester/index.test.tsx`, `webapp/src/features/requester/api.ts`, `webapp/src/features/requester/types.ts`, `webapp/src/features/auth/workspace-access.ts`, `webapp/src/features/admin/registry/registry-profile-schema-tab.tsx`, `webapp/src/features/admin/registry/registry-people-tab.tsx`, `webapp/src/features/admin/registry/registry-person-edit-dialog.tsx`, `server/web_api/requester_handlers.py`, `server/requester/identity_service.py`, `server/registry/profile_schema_service.py`, `server/registry/browser_pairing_service.py`, `server/registry/registration_service.py`, `server/registry/service.py`, `server/web_api/registry_handlers.py`, `server/tests/test_registration_api.py`, `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_registry_quality_remediation.py`, `server/tests/test_registry_web_api.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/main_window.py` | `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`; browser-first requester onboarding keeps account creation separate from registry-backed profile setup. R2 account creation is `/app/register` + feature-flagged `POST /api/web/session/register`: login/password/repeat password and optional device-link code only, role `user`, no auto-login/cookie, redirect to `/app/login?registered=1`, and optional code validation without active binding. R3 setup uses `/app/requester/profile/setup` and writes the caller-owned registry person. R4 device linking is owned by `/app/requester/devices`: manual code lookup and direct `pairing_id` preview reuse browser-pairing APIs, `registration/confirm` returns `REQUESTER_PROFILE_INCOMPLETE` until setup is complete, and successful confirmation creates an admin-reviewed claim from the resolved `RegistryPerson` profile snapshot. R6 profile schema uses admin-only `/api/web/admin/registry/profile-schema*` for controlled optional/custom fields; requester APIs expose only safe render metadata and submit values under `custom_fields`. R7 registry production context projects focused person/department/service/asset context from existing registry rows/metadata into the admin snapshot and lets admin person edit only controlled work-context fields. R8 request forms consume server-resolved requester context, store `requester_context_snapshot`/routing aliases on ticket create and prefill requester cabinet form fields from safe profile/device facts. Normal requester UI stays free of raw `pairing`/`binding`/`claim`/`session`/`registry person` terms and backend enum values. |
| Registry objects / device registration | `server/registry/service.py`, `server/registry/registration_service.py`, `server/registry/registration_form_service.py`, `server/registry/account_state_service.py`, `server/app/repos/registration_repo.py`, `server/web_api/registry_handlers.py`, `server/websocket/agent_handshake.py`, `server/tickets/create_flow.py`, `pc_agent/core/user_profile.py`, `pc_agent/core/account_session.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/dynamic_form_widget.py`, `pc_agent/ui_gui/server_api.py`, `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/features/admin/device-inventory-panel.tsx`, `webapp/src/features/admin/api.ts` | `server/docs/CODEMAP.md`, `pc_agent/docs/CODEMAP.md`; self-reported agent profile creates a controlled `device_registration_claim`, never direct `registry_assets.assigned_person_id`, and one open `agent_profile` claim per device/source is reused so logout/resubmit cannot create duplicate pending claims. Agent auth is limited to its own UUID device id; admin/support submit for explicit existing devices; user claim confirmation is identity-scoped. Admin approval enforces user confirmation unless explicit override, is idempotent for the same device/person, writes `device_user_bindings`, registration events and inventory binding history, and revoke clears or re-syncs derived asset/inventory state. Manual admin bind satisfies matching pending claims without turning the binding into a `registration_claim` approval flow, or supersedes stale open claims for the same device. Ticket create checks active primary binding before ingesting requester profile, then enriches `requester_person_id`, `requester_binding_id`, `requester_registration_status` and `asset_id`; explicit agent `requester_account` can mark other-account tickets without creating registration claims or changing active binding, while `registration_pending` is retained for pending gate state/polling and no longer opens the normal Qt ticket workspace. Handshake adds non-fatal `payload.registration`; `/api/registry/agent/registration-form` remains compatibility API surface for older agents; `/api/registry/agent/account-state` feeds the agent account gate; `main_window.py` uses account entry mode so the main shell is hidden before account selection, registration remains in the account-gate browser handoff (pairing code/direct link only), entry settings have a back action, and the post-login Account page is a server-session summary with logout instead of the legacy profile editor; `/app/admin/registry` and `/app/admin/device` expose pending/conflict/stale/active registration state and active binding revoke actions. Key tests: `server/tests/test_device_registration_service.py`, `server/tests/test_registration_api.py`, `server/tests/test_ticket_registration_enrichment.py`, `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_handshake_registration_payload.py`, `pc_agent/tests/test_user_profile_manager.py`, `pc_agent/tests/test_registration_status.py`, `pc_agent/tests/test_dynamic_form_widget.py`, `pc_agent/tests/test_account_session_manager.py`, `pc_agent/tests/test_account_gate.py`. |
| Registry objects / Реестры | `server/registry/service.py`, `server/registry/registration_service.py`, `server/registry/account_session_service.py`, `server/registry/admin_operations_service.py`, `server/registry/policy_service.py`, `server/app/repos/registry_repo.py`, `server/app/repos/registration_repo.py`, `server/app/repos/account_session_repo.py`, `server/web_api/registry_handlers.py`, `server/websocket/agent_handshake.py`, `server/tickets/create_flow.py`, `server/web_api/support_handlers.py`, `server/web_api/dto/support.py`, `scripts/registry_workflow_smoke.py`, `pc_agent/core/user_profile.py`, `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/server_api.py`, `webapp/src/pages/admin/registry-page.tsx`, `webapp/src/pages/admin/registry-page.test.tsx`, `webapp/src/features/admin/api.ts`, `webapp/src/features/admin/registry/*` | `server/docs/CODEMAP.md`, `server/docs/REGISTRY_MANAGEMENT_CENTER.md`, `server/docs/REGISTRY_VISIBILITY_FOUNDATION.md`, `pc_agent/docs/CODEMAP.md`; lightweight registry for people, departments, buildings/rooms, PC/printer assets, services and vendors. Agent handshake auto-creates PC assets and now returns non-blocking registration status; requester profile sync creates registration claims/suggestions instead of directly assigning an owner; approved active `device_user_bindings` drive `registry_assets.assigned_person_id` and ticket requester registration fields. `/app/admin/registry` is the Registry Management Center: dashboard, devices, people/identities, bindings, requests, account sessions, locations/departments CRUD, cautious policy editor, people merge, bulk actions, CSV export/import, timelines, data-quality actions and the Phase 3 `Аудитории · P1` audience-group editor. The audience editor loads RBAC groups from `/api/web/admin/access/summary` and exposes `member_type=access_group` as a targeting fact only. Manual bind/transfer/shared/responsible operations go through `RegistrationService`; claim approval revokes matching `registration_pending` account sessions; binding revoke/transfer revokes dependent account sessions; CMDB/policy/merge/bulk/import/audience operations write `registry_admin_events`; policy patch/reset write `policy_changed` and expose defaults, effective preview, validation ranges, dangerous-setting warnings and restart metadata; person/identity CRUD writes explicit admin events; safe registry import uses preview/apply with a required matching `preview_id` for people, locations, departments, device inventory mapping, audience groups and audience-group members, never direct binding or account-session import; Registry export also includes audience groups, audience-group members and Knowledge audience rules; generated quality issues use stable `issue_key` values and now include empty audience groups plus invalid/zero-user Knowledge visibility rules, with `registry_quality_issue_overrides` for ignore/snooze/resolve; the drawer timeline normalizes admin, registration and account events for device/person/binding/session/claim with actor, reason, changes and affected entity ids; dangerous transfer/merge/bulk/import and audience member saves have preview/reason UI paths where applicable. The current React registry prompt-regression guard is `webapp/src/pages/admin/registry-page.test.tsx`; normal operator flows should use dialogs/pickers instead of `window.prompt`. `scripts/registry_workflow_smoke.py` runs the live A-F workflow smoke against the Linux stand and checks API plus DB invariants. |
| Primary diagnostic target resolver | `server/registry/primary_agent_resolver.py`, `server/tickets/ticket_context.py`, `server/registry/policy_service.py`, `server/app/repos/registration_repo.py`, `server/app/repos/registry_repo.py`, `server/tests/test_primary_agent_resolver.py`, `server/tests/test_ticket_context_builder.py` | Resolves a `RegistryPerson` diagnostic target from active device bindings: one active `primary_user` wins, a single shared/responsible binding is used only when `diagnostic_target.allow_single_active_binding_fallback=true`, ambiguous or missing targets return safe reason codes, and resolved payloads include only diagnostic-safe device/binding/hostname/online/last-seen/agent-version evidence without secrets or raw metadata. `TicketContextBuilder` wraps that resolver during ticket create to snapshot creator, affected person and target aliases in `custom_fields.ticket_context`. |
| Registry picker/options and agent registration/account form | `server/web_api/registry_handlers.py`, `server/registry/registration_form_service.py`, `server/registry/account_state_service.py`, `server/registry/account_session_service.py`, `server/routes.py`, `pc_agent/core/account_session.py`, `pc_agent/ui_gui/server_api.py`, `pc_agent/ui_gui/dynamic_form_widget.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/main_window.py`, `webapp/src/pages/device-pairing/*` | `GET /api/registry/options` is authenticated for `admin/support/user/agent` and feeds agent request-form picker fields without exposing the admin-only full registry snapshot. `GET /api/registry/agent/registration-form` is authenticated for `admin/support/agent`, returns side-effect-free `agent_device_registration` form schema plus current registration status/options, and remains compatibility API surface for older agents; the normal GUI no longer renders it as a local registration form. `GET /api/registry/agent/account-state` is side-effect-free, user-forbidden, includes server-side account sessions and pending other-account login requests, and drives the GUI first screen/account gate; pre-login entry mode must show only account selection, browser registration handoff or settings, not the normal ticket workspace shell. Browser pairings are created/polled by the agent endpoints, manual code lookup is `POST /api/web/registry/browser-pairings/lookup` behind `/app/device/pair`, and confirmation is web-authenticated `/api/web/registry/browser-pairings/*`; `/app/device/login` and `/app/device/register` show safe device facts, never session tokens. Server-side account-session payloads must expose confirmed person display/full/email/phone/login both in `session.person` and top-level compatibility fields; confirmed sessions support active primary/shared/responsible bindings, validation rechecks binding/base-binding activity, and approved other-account sessions require a server token that the agent gets once from the approved request; GUI validation sends that token in a POST body or account headers, not in URL/query parameters. `AccountSessionManager` enriches already saved local sessions from fresh account-state and now matches local sessions by server session id rather than binding-only heuristics. When validation returns `ACCOUNT_SESSION_*` invalidation such as revoke/expired/inactive binding, `main_window.py` clears local session and ticket state and returns to account gate. Other-account login is not local/instant: the agent creates `/api/registry/agent/account-login-requests` for any active registered binding (primary preferred, then responsible/shared), admin approves through `/api/web/admin/registry/account-login-requests/*`, then the gate can select the resulting `verified_other_account` server session. Unknown/error states show only refresh/settings until account-state succeeds. GUI startup validates stored machine tokens through account-state, deactivates stale local tokens on HTTP auth failure and shows friendly auth messages instead of raw JSON. |
| Tool execution / operations | `server/tools/service.py`, `server/tools/handlers.py`, `server/app/services/operation_service.py`, `pc_agent/core/orchestrator.py`, `pc_agent/core/orchestrator_collect_helpers.py`, `pc_agent/core/orchestrator_job_helpers.py` | `server/docs/TOOL_CALL_STARTED_INVARIANT.md`, `pc_agent/docs/CODEMAP.md` |
| Observer traces / tech drilldown | `server/observer/service.py`, `server/observer/runtime.py`, `server/tech/handlers.py`, `server/app/db/models.py`, `server/websocket/agent_services.py`, `pc_agent/core/action_trace.py`, `pc_agent/core/orchestrator.py` | `server/docs/OBSERVER_LAYER.md`, `server/docs/OBSERVER_AUTHORING_RULES.md`, `server/docs/CODEMAP.md`, Codex skill `pc-client-observer-diagnostics` |
| MCP read-only server debug | `mcp_helpdesk_server/server.py`, `mcp_helpdesk_server/bootstrap.py`, `mcp_helpdesk_server/tools/*`, `server/observer/debug_facade.py`, `server/observer/runtime_snapshot_writer.py`, `server/web_api/ai_integration_handlers.py`, `docs/MCP_SERVER_DEBUG.md`, `docs/mcp/helpdesk-server-debug.manifest.json` | `helpdesk-server-debug` is a stdio `debug_readonly` MCP server for Codex diagnostics over DB/service/context layers only. It must not proxy HTTP `/api/*`, call `run_tool`, write `DeviceOutbox`, make approvals/consent decisions, use WS RPC, or return raw secrets. The observer debug facade applies an extra MCP output redaction layer for public ticket access-code fields and text snippets. Runtime status reads fresh persisted `server_runtime_snapshots`; presence output includes persisted snapshots, DB `Device.last_seen_at` / `last_handshake_at` evidence and bounded aggregate or device-specific `live_ws_evidence` from the latest fresh server runtime snapshot when available. Admin status UI is `/app/admin/ai-integration` over `GET /api/web/admin/ai-integration/mcp`. |
| Agent updates / recommended version / rollout policy | `server/agents/agent_builds_handlers.py`, `server/app/repos/agent_rollout_repo.py`, `server/routes.py`, `pc_agent/ws_agent.py`, `pc_agent/ui_bridge/api_server.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/pyinstaller_agent_win.spec`, `pc_agent/pyinstaller_agent_win_release.spec`, `pc_agent/pyinstaller_agent_linux.spec` | `server/docs/AGENT_UPDATES_API.md`, `pc_agent/docs/AGENT_RUNTIME_ALWAYS_ON.md`, `pc_agent/docs/AGENT_UPDATE_WORKFLOW.md`; 3.1.67 is the current priority Windows stable refresh target for the final browser-only registration cleanup, 3.1.66 is strict browser-only agent registration and localized Account page technical labels, 3.1.65 is the prior priority Windows stable refresh, 3.1.64 is terminal other-account request recovery, 3.1.63 is GUI browser-pairing handoff, 3.1.62 is post-startup-hardening GUI/runtime, 3.1.61 is GUI startup failure handling, 3.1.60 is the compiled registration/account-session rollout target, 3.1.59 publishes `inventory.collect` real `processes.items` and the "Процессы" presentation block, while 3.1.58 published requester auth-token refresh plus Windows core built-in packaging for `inventory.collect`/`presence.collect`; release agent specs must include core built-in modules under `pc_agent.modules.impl.*` so packaged agents expose `system.collect`, `diag.logs.collect`, `inventory.collect` and `presence.collect` |
| Modules / desired state / reconcile / workbench | `server/modules/handlers.py`, `server/modules/workbench_service.py`, `server/web_api/admin_handlers.py`, `server/tools/service.py`, `server/websocket/outbox_ingest_components.py`, `server/modules/reconcile.py`, `server/admin_modules_workbench.js`, `webapp/src/features/modules/modules-panel.tsx`, `pc_agent/core/module_manager.py`, `shared/tool_contracts.py` | `server/docs/MODULE_CREATION_GUIDE.md`, `server/docs/MODULES_API.md`, `server/docs/MODULE_AUTHORING_RULES.md`, `server/docs/REGISTRY_PUBLICATION_RULES.md`, `server/docs/RUNTIME_EXECUTION_CONTRACT.md`, `pc_agent/docs/MODULES.md`; новый typed admin surface теперь не только читает `GET /api/web/admin/modules`, но и ведёт `PATCH /api/web/admin/modules/rollout_settings` и `PATCH /api/web/admin/modules/{module_name}/preferred`, чтобы `/app/admin` умел менять preferred-version policy и preferred assignment без legacy workbench/editor shell |
| Inventory Collect / Lightweight CMDB / readable tool results | `pc_agent/modules/impl/inventory.py`, `pc_agent/modules/impl/inventory_profiles.py`, `pc_agent/modules/impl/presence.py`, `shared/builtin_tool_descriptors.py`, `server/inventory/service.py`, `server/inventory/scheduler.py`, `server/presence/service.py`, `server/web_api/admin_inventory_handlers.py`, `server/websocket/command_result_components.py`, `webapp/src/features/admin/device-inventory-panel.tsx`, `webapp/src/pages/admin/inventory-page.tsx`, `webapp/src/components/module-result/tool-result-event-card.tsx`, `webapp/src/components/module-result/module-result-renderer.tsx`, `webapp/src/pages/admin/device-page.tsx`, `webapp/src/pages/tickets/list-page.tsx`, `webapp/src/pages/tickets/components/ticket-device-agent-panel.tsx` | `pc_agent/docs/MODULES.md`, `pc_agent/docs/CODEMAP.md`, `server/docs/CODEMAP.md`; `inventory.collect` returns `device.inventory.snapshot`, `presence.collect` returns `device.presence.snapshot`, server stores inventory snapshots/bindings/history/refresh runs/bulk operations/profile suggestions/presence snapshots, `/api/web/admin/devices/{device_id}/inventory` returns latest/history/effective presentation schema/device-card slots/binding/profile suggestions/presence, selected support ticket workspace embeds compact `inventory_context` for device/agent/freshness/binding/refresh signals, `/api/web/admin/inventory/dashboard` aggregates fleet gaps and attention groups, CSV/XLSX endpoints import/export inventory/binding fields, bulk refresh uses existing `ToolExecutionService.run_tool`, and React device/ticket surfaces render tool results through shared presentation renderers with raw JSON fallback |
| Ticket flows / helpdesk | `server/tickets/handlers.py`, `server/tickets/create_flow.py`, `server/tickets/ticket_context.py`, `server/tickets/request_template_submission.py`, `server/tickets/workflow_service.py`, `server/tickets/routing_service.py`, `server/tickets/form_catalog.py`, `server/tickets/helpdesk_policy_runtime.py`, `server/tickets/priority_policy.py`, `server/tickets/sla_service.py`, `server/tickets/ola_service.py`, `server/tickets/notification_service.py`, `server/tickets/visibility_policy.py`, `server/tickets/smart_views.py`, `server/tickets/passport_service.py`, `server/app/repos/ticket_passport_repo.py`, `server/web_api/support_handlers.py`, `server/web_api/settings_handlers.py` | `server/docs/TICKET_SYSTEM.md`, `server/docs/CODEMAP.md`, `server/docs/REQUEST_FORM_BUILDER.md`; паспорт решения хранит versioned official dossier, evidence/action/approval/related-object facts, reporting-policy `missing_facts` / `export_preview`, and powers `/app/tickets/:ticketId`; request-template catalog is exposed as `ticket_settings.request_templates`, ticket creation validates legacy packs or standalone registry templates by `request_template_key`, priority/SLA/OLA policies execute matrix/lifecycle conditions/manual overrides with preview or event explanations, effective registry approval/closure/notification/visibility/reporting policies plus typed support smart views are backend-enforced before visual changes, and `on_behalf_policy` is a form-level opt-in process capability surfaced safely to settings/UI; create stores server-owned creator/affected/diagnostic target context in `custom_fields.ticket_context` plus flat aliases while preserving `requester_*` compatibility and hiding the snapshot from requester/public projection; requester/public ticket detail keeps only requester-safe request-form `custom_fields` (`request_form`, `request_form_key`, `request_form_title`, `request_form_data`, `request_form_summary` and resolver metadata) while internal priority/routing/approval/public-access snapshots stay hidden; external notification channel audit distinguishes explicit disable of a globally enabled channel from event-local `false` for an otherwise unconfigured channel |
| Auth / token bootstrap | `server/auth/`, `server/websocket/agent_handshake.py`, `pc_agent/auth/token_source.py`, `pc_agent/auth/connection_request.py`, `pc_agent/launcher_portable_main.py` | `server/docs/SECURITY_AND_AUTH.md`, `pc_agent/docs/AUTHENTICATION.md`; httpOnly cookie `pc_client_web_session` authenticates `/api/web/*`, including the typed React aliases for modules, observer, tech panel, notifications and settings. Legacy `/api/modules/*`, `/api/admin/tech/*`, `/api/admin/settings/observer`, `/api/ticket_forms/*`, `/api/upload` and `/api/artifacts/{artifact_id}/download` remain compatibility endpoints for existing webapp support/admin flows; new React data routes should still prefer typed `/api/web/*` where available. Public requester-safe knowledge endpoints `/api/knowledge/search`, `/api/knowledge/suggest`, and `/api/knowledge/feedback` are auth-whitelisted only after ACL filtering and safe projection; valid optional bearer or web-session auth is preserved for Registry audience resolution, while missing/invalid auth remains anonymous. |
- Artifact download with `ticket_id` is not an anonymous bypass: `/api/artifacts/{artifact_id}/download?ticket_id=...` still requires middleware-authenticated web session, agent token or public-ticket token, then `ArtifactService` enforces ticket/artifact visibility. Unicode download names use `Content-Disposition` with safe ASCII `filename` fallback plus RFC 5987 `filename*`.
- Agent artifact upload uses two separate boundaries: operation-bound tool artifacts with `ticket_id + operation_id` are authorized by matching the server operation to the authenticated agent device, while manual local-agent/requester attachments without `operation_id` still require requester account-session context. ToolResponse `status=partial` must remain visible as partial/failed lifecycle evidence, not full operation success.
| Knowledge Platform / deflection | `server/knowledge/*`, `server/ai/*`, `server/web_api/knowledge_handlers.py`, `server/web_api/knowledge_ai_handlers.py`, `server/app/repos/knowledge_repo.py`, `server/app/db/models.py`, `scripts/seed_knowledge_content.py`, `scripts/seed_knowledge_metadata.py`, `scripts/validate_knowledge_pack_bindings.py`, `scripts/repair_knowledge_pack_bindings.py`, `content_packs/knowledge/*`, `webapp/src/features/knowledge/*`, `webapp/src/pages/help/index.tsx`, `webapp/src/pages/requester/index.tsx`, `pc_agent/ui_gui/server_api.py`, `pc_agent/ui_gui/chat_panel.py` | `server/docs/KNOWLEDGE_PLATFORM.md`, `server/docs/KNOWLEDGE_OPERATIONS.md`, `server/docs/KNOWLEDGE_VNEXT_ARCHITECTURE.md`, `server/docs/REGISTRY_VISIBILITY_FOUNDATION.md`, `server/docs/SERVICE_CATALOG.md`, `server/docs/DATABASE.md`; validate baseline bindings with `python scripts/validate_knowledge_pack_bindings.py --strict`, repair installed pack-managed binding drift with `python scripts/repair_knowledge_pack_bindings.py --dry-run --all` then actual run, seed editable governed metadata with `python scripts/seed_knowledge_metadata.py --dry-run|--apply`, keep rollout/AI policy changes non-blocking by default for requester/agent urgent/API-unavailable/no-suggestions/AI-disabled cases, use `server/knowledge/search_settings_service.py` plus `GET/POST /api/web/knowledge/search-settings` for AI-off search controls, use `server/knowledge/metadata_service.py` plus `/app/admin/knowledge/metadata` for taxonomy/property/applicability/quality-model management gated by `knowledge.metadata.manage`, use `server/knowledge/segmentation_service.py` plus migration `112` for manual/auto article segments that boost AI-off retrieval, use `server/knowledge/access_service.py` plus migration `121` for Registry audience-rule decisions, and keep every requester/agent/public search/vector/RAG/result projection free of hidden titles, summaries, snippets, chunks, counts, diagnostics and rule ids. |
| Agent runtime / UI bridge | `pc_agent/ws_agent.py`, `pc_agent/ws_agent_runtime_helpers.py`, `pc_agent/ui_bridge/api_server.py`, `pc_agent/ui_bridge/event_bus.py`, `pc_agent/ui_gui/main.py`, `pc_agent/ui_gui/main_window.py`, `pc_agent/ui_gui/accessibility.py`, `pc_agent/ui_gui/account_gate.py`, `pc_agent/ui_gui/window_chrome.py`, `pc_agent/ui_gui/chat_panel.py`, `pc_agent/ui_gui/automation_controller.py`, `pc_agent/ui_gui/server_api.py`, `pc_agent/ui_gui/ticket_view_models.py`, `pc_agent/ui_gui/ticket_detail_widgets.py`, `pc_agent/ui_gui/ticket_create_wizard_widgets.py`, `pc_agent/ui_gui/theme.py`, `pc_agent/ui_gui/tickets_list_model.py`, `pc_agent/ui_gui/assets/icons/`, `pc_agent/core/account_session.py`, `scripts/live_agent_uia_state_probe.py`, `scripts/live_agent_uia_create_ticket.py` | `pc_agent/docs/AGENT_RUNTIME_ALWAYS_ON.md`, `pc_agent/docs/CODEMAP.md`; GUI HTTP clients must refresh the active local auth token before requester API calls so runtime reprovision/token rotation cannot leave stale Bearer headers in an already-open GUI, and GUI startup now rejects/deactivates stale local machine tokens before opening the requester account shell; requester account session is local GUI state containing a server-issued account-session id/token, separate from machine-token auth, and `main_window.py` shows `AccountGateWidget` before ticket list/create in an entry mode that hides sidebar/footer until login while keeping settings accessible; R5/R12/3.1.67 strict mode removes legacy local full-profile registration/confirm controls, local requester profile manager/sidebar state, `ChatPanel` active-profile gates and profile mutation automation from normal GUI code, while browser handoff shows a copyable pairing code and status refresh; after login the Account page is read-only server-session state with logout, not legacy profile editing; real GUI create and `/ui/automation/run` create-ticket both pass the active server account session to `create_ticket()`, and `/ui/automation/run` ticket tool actions pass the same active account session to `run_tool()`, so automation regressions exercise the same account boundary as manual requester creation/tool execution; `/ui/automation/status` returns `status=unavailable` instead of local `501` before the GUI controller is configured and reports `has_account_session` when attached, `/ui/automation/run` is a separate test surface and preserves structured downstream server `4xx` denials (`downstream_http_status`, `error_code`, `details`) instead of reporting local `500`, while true bridge exceptions still return `500`; main GUI connection/account/ticket state exposes stable UIA-readable semantic names for pywinauto live validation without leaking session tokens; the UI bridge EventBus replays unexpired `remote_assist_request` events by `session_id` for late GUI/SSE subscribers, allowing browser-approved Remote Assist to reach the technical agent start path; Service Catalog wizard cards expose stable UIA identifiers and refresh on catalog-only sync; `create_ticket()` sends only the selected account session id/token, so stale local profiles cannot override the server-selected requester identity; если в Taskbar/Alt-Tab видны лишние окна `pc_agent`/`python`, проверить top-level GUI overlays и orphan widgets в `main_window.py`, а также Qt `_q_titlebar` helper path в `window_chrome.py`; не использовать обычный `Qt.Window` для служебных controls, не показывать parentless blank widgets и не вызывать `startSystemMove()` / `startSystemResize()` |
| Release / deploy / CI | `scripts/task_intake.py`, `scripts/bootstrap_web_toolchain.py`, `scripts/check_webapp_cutover.py`, `scripts/verify_workspace.py`, `scripts/run_ci_suite.py`, `scripts/release_candidate_preflight.py`, `scripts/run_observer_canary_suite.py`, `scripts/deploy_workspace_to_remote.py`, `scripts/release_server_to_remote.py`, `webapp/scripts/remote-browser-signoff.mjs` | `AGENTS.md`, `PLANS.md`, `docs/LOCAL_WORKFLOW.md`, `docs/WEBAPP_CUTOVER_CHECKLIST.md` |

Knowledge K4/K5 note: RAG eligibility starts in `server/knowledge/rag_policy.py`, `KnowledgeRetrievalService`, `KnowledgeVectorSearchService`, `KnowledgeRepo.update_item_settings`, `/api/web/knowledge/items/{item_id_or_slug}` PATCH, `webapp/src/features/knowledge/authoring/*`, and `KnowledgeOpsSummaryService.action_queues`; `/app/admin/knowledge` is ops-only while authoring and article AI/RAG policy stay in `/app/admin/knowledge/studio`.

## When to update this file

Обновляйте `docs/QUICK_LOOKUP.md`, если меняются:

- ключевые entrypoints;
- канонический intake/test/release workflow;
- карта основных тем и стартовых файлов;
- статус canonical vs historical docs.
- observer quick diagnosis, trace APIs, dangerous-flow coverage или обязательные observer docs/skills.

## 2026-05-09 requester timeline projection

- Requester-facing ticket event text now starts from `server/tickets/requester_timeline.py`. Server serializers add `requester_timeline_text`, `requester_timeline_kind` and `requester_timeline_payload` in `server/tickets/handlers.py` and `server/web_api/support_handlers.py`; desktop Qt (`pc_agent/ui_gui/ticket_view_models.py`), React requester page and support workspace mappers must prefer these fields over raw `event_type`, status, queue, tool or observer payloads.
- Hidden from requester timeline by default: `internal_note`, `worklog_added`, `message_read`, workspace hide/archive events, external notification delivery, policy dispatch audit, internal SLA/OLA noise and raw observer/tool/internal/debug/protocol/auth/module logs. Public access-code chat messages remain system notices and do not count as first-response support replies.
- 2026-05-23/24 Auth/provisioning security hardening: start with `server/auth/handlers.py`, `server/auth/middleware.py`, `server/auth/service.py`, `server/auth/connection_request_handlers.py`, `server/auth/connection_request_service.py`, `server/auth/rate_limit.py`, `server/app/repos/auth_tokens_repo.py`, `server/app/repos/connection_requests_repo.py`, `server/app/repos/ui_users_repo.py`, `server/auth/admin_users_handlers.py`, `server/auth/password_service.py`, `server/web_api/session_handlers.py`, `server/web_api/registry_handlers.py`, `server/registry/registration_service.py`, `server/registry/account_session_service.py`, `pc_agent/auth/connection_request.py`, `server/docs/SECURITY_AND_AUTH.md`, and `pc_agent/docs/AUTHENTICATION.md`. `POST /api/login` is admin-only; manual connection-request status requires `device_id` + `request_id` + `poll_secret`; approve does not store raw tokens and valid poll generates the agent token; UI web login uses httpOnly cookie sessions; query-token auth, config fallback, and legacy `/api/ui_login` are disabled by default; role handling fails closed; `X-Forwarded-For` is trusted only for configured proxies.
