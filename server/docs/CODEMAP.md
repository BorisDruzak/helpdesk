# CODEMAP (server)

## 2026-08-09 Helpdesk segmentation boundary and ports (PR-0/PR-1)

- PR-6 is complete: in-process runtime, routes, UI, AI components,
  configuration and content packs are removed. `KnowledgePort` is external-only
  and remains fail-closed as `knowledge_unavailable` until PR-7 accepts a
  versioned adapter plus shadow-read evidence. PR-11a revision `134` removed
  the physical legacy graph; `TicketKbLink`/`ticket_kb_links` history and
  sanitized `knowledge_attempts` remain read-only historical projections.
- `server/docs/SEGMENTATION_BOUNDARIES.md` is the normative ownership map:
  Helpdesk owns tickets/ITSM processes; Endpoint Platform owns the endpoint-agent
  control plane; future Knowledge and Registry Platforms are separate domains.
- `server/docs/adr/0001-helpdesk-external-domain-ports.md` records the decision
  to consume Endpoint, Knowledge and Registry only through explicitly composed,
  versioned ports. Direct foreign-domain imports and cross-domain database FKs
  are forbidden.
- `server/docs/KNOWLEDGE_PLATFORM_API_V1.md` defines a future external
  Knowledge API contract. `KnowledgePort` remains unavailable until explicit
  composition and reports `knowledge_unavailable`.
- `server/domain_ports/knowledge.py`, `registry.py`, `endpoint.py` and
  `endpoint_modules.py` define the
  neutral, runtime-checkable dependency-injection protocols. Knowledge request
  and result DTOs are frozen, forbid extra content fields and preserve opaque
  refs exactly, subject to the documented 512-character limit. EndpointPort
  exposes only typed availability, exact device/capability, create-operation and
  exact operation-read contracts for `context.diagnostic.collect`; its frozen
  safe projections reject raw context/credentials and use opaque 128-character
  external refs. External composition defaults to unavailable and rejects
  invalid Endpoint modes/configuration. `server/endpoint_adapter/http.py`
  composes only a complete external Endpoint configuration into the four-route,
  HTTPS-only Operations API v1 transport; it has no Helpdesk database,
  WebSocket or ticket dependencies.
- `EndpointModulePort` is a separate frozen typed boundary for module catalog,
  authoring/version lifecycle and operation reads. Its default
  `ENDPOINT_MODULE_PORT_MODE=unavailable` adapter performs no HTTP, database,
  agent, ToolService or diagnostic-provider work. `external` composes only
  `server/endpoint_adapter/modules_http.py` after the existing Endpoint TLS
  configuration validates; it maps catalog/version and operation routes into
  safe projections and never retains recipes or dispatches an agent command.
  `web_api/endpoint_module_handlers.py` exposes the authenticated Helpdesk
  BFF routes under `/api/web/admin/endpoint-modules` (with `/api/admin`
  aliases) and `/api/web/support/tickets/{ticket_id}/endpoint-modules/.../run`.
  Catalog rows add only the Endpoint-projected latest version/state when that
  safe read succeeds; they never expose a recipe source. The legacy Admin
  shell's `Endpoint recipes` subtab (`admin.html`,
  `endpoint_module_workbench.js`) calls only that BFF to create declarative
  allowlisted DNS/ping/TCP recipes and apply lifecycle actions; the existing
  Python-oriented workbench remains untouched. It authorizes admin/support/auditor
  actions locally, records safe actor audit data, and queues only a local facade
  operation. Its resolver reads the
  already verified ticket mapping and never composes the diagnostic port,
  `ToolService`, DeviceOutbox or legacy WebSocket.
- Migrations `139–142` keep module facade work in the established local
  `endpoint_operation_links` lifecycle. Revision `142` adds nullable module
  identity, safe input/snapshot fields and removes the temporary staging
  table; no cross-repository FK exists. `EndpointModuleOperationReconciler`
  calls only the typed port outside its short database transactions, is gated
  by `ENDPOINT_MODULE_EXECUTION_MODE=endpoint`, and writes exactly one safe
  `endpoint.module.recipe` evidence item after remote success.
- `server/domain_ports/unavailable.py` contains side-effect-free unavailable
  adapters with no DB or HTTP work.
  `server/domain_ports/container.py::DomainPortContainer.from_config()` creates
  fresh adapters from the fail-closed external-domain mode settings and defines
  no module-level singleton.
- `server/app/services/endpoint_diagnostic_operation_service.py` creates the
  ticket-facing Endpoint diagnostic facade with a deterministic local operation
  id. `server/app/services/endpoint_operation_reconciler.py` claims durable
  links, calls only `EndpointPort` outside database transactions, projects
  safe terminal results into diagnostic evidence, and idempotently completes an
  already-terminal linked diagnostic session without another remote call.
  `server/server.py` starts its runner only for `ENDPOINT_PORT_MODE=external`
  together with `ENDPOINT_DIAGNOSTIC_EXECUTION_MODE=endpoint`, then stops it
  during shutdown.
  Its only post-commit notification is a reloaded local operation through the
  Helpdesk UI publisher, never an agent command dispatch.
- `server/diagnostics/providers/endpoint_platform.py` exposes only
  `endpoint.context.diagnostic.collect` while
  `ENDPOINT_DIAGNOSTIC_EXECUTION_MODE=endpoint`; its `endpoint_operation`
  router target invokes the injected local facade and returns HTTP-202 queued
  semantics. The endpoint-mode handler composes no ToolExecutionService or
  agent transport; readiness checks the typed Endpoint port, exact ticket
  mapping and the one external capability before execution.
- Revisions `135` and `136` add nullable ticket Endpoint device snapshots,
  local `endpoint_operation_links`, and the durable opaque external correlation
  ref. Both are forward-only; the reconciler fails historical links lacking the
  correlation ref closed rather than creating a remote operation.
- `server/domain_ports/registry_contracts.py` defines the frozen/redacted
  Registry read and command DTOs. `server/registry_adapter/local.py` is the
  PR-8 compatibility adapter and the only new Registry ORM/repository import
  site; `server/registry_adapter/http.py` is the versioned external read client
  and optional local-authoritative shadow wrapper. `REGISTRY_PORT_MODE=local|
  unavailable|external` defaults to `local`; external mode requires configured
  URL/token and otherwise fails closed. Commands remain local until PR-9
  command/auth acceptance; uncomposed local commands return
  `registry_command_not_composed` without side effects.
  until caller-owned idempotency is supported and accepted.
- `RegistryPort.requester_profile_completion()` is the narrow observer-only
  profile-gate read. `server/observer/checks/web_cabinet.py` composes its fixed
  provenance, receives only the redacted completion projection and records
  typed unavailable/invalid source degradations. It has no direct Registry ORM
  or requester-profile-schema dependency; `server/observer/integrity_service.py`
  injects the composed port into the checker.
- PR-6 retains no in-process RBAC or runtime configuration.
- `scripts/check_domain_import_boundaries.py` is the AST guard for prohibited
  legacy-domain imports and local ORM dependencies; only historical Alembic
  version files are exempt. The default invocation preserves the existing
  removed-domain enforcement. Registry enforcement is incremental and requires
  `--registry-scope`; focused coverage
  is in `server/tests/test_domain_import_boundaries.py` and
  `server/tests/test_registry_boundary.py`.
  Its `observer` Registry scope protects
  `server/observer/checks/web_cabinet.py` from future direct Registry imports.
- Port-contract coverage remains in `server/tests/test_domain_ports.py` and
  `server/tests/test_endpoint_port_contracts.py`.
- `Ticket.endpoint_device_ref` and `endpoint_device_snapshot_json` are nullable
  server-owned Endpoint mapping fields. `server/app/services/endpoint_device_reference_service.py`
  accepts only a previously verified mapping for readiness. The admin-only
  `PUT /api/admin/tickets/{ticket_id}/endpoint-device-mapping` path reads the
  exact provider device through `EndpointPort` outside a DB transaction before
  persisting the frozen `endpoint_device_snapshot_v1`; it never derives a
  mapping from `Ticket.device_id`, hostname, IP, or MAC. Its strict
  `endpoint_device_mapping_request_v1` body rejects drift, and replacements
  require the exact previous ref plus a safe reason under a row lock. Created
  and replaced mappings are committed with an actor audit event; a replay of
  the same ref makes no write only when its frozen snapshot is valid, otherwise
  it backfills that snapshot with an actor audit event. Revision `135` adds the ticket fields plus
  `endpoint_operation_links`; revision `137` adds actor-scoped caller
  idempotency fields and their partial unique index; revision `138` permits a null legacy `operations.device_id`
  only for server-originated `endpoint_operation` records and keeps all other
  operation kinds guarded by a database constraint.
  `server/app/repos/endpoint_operation_links_repo.py` contains only exact link
  lookup/create persistence and performs no HTTP, DeviceOutbox, tool, or
  WebSocket work.

## 2026-08-26 Endpoint Operations cross-platform acceptance

- The ALT and Windows real-agent staging canaries are accepted after rollback.
  They confirm that the bounded `endpoint.context.diagnostic.collect` slice
  uses the Endpoint Operations API and Gateway WSS command path across both
  platforms, with no Helpdesk legacy agent, `DeviceOutbox`, or
  `ToolService` dispatch. The redacted records are
  `docs/verification/ALT_HEADLESS_AGENT_CANARY_ACCEPTANCE.md` and
  `docs/verification/WINDOWS_HEADLESS_AGENT_CANARY_ACCEPTANCE.md`.
- This is acceptance evidence for the existing bounded transport only. The
  legacy agent runtime, legacy `/ws`, `DeviceOutbox`, consent paths, Remote
  Assist, and agent account-session lifecycle remain active ownership areas;
  none is deleted or migrated by this acceptance. The next scoped work is
  typed read-only Endpoint capabilities.

## 2026-08-10 RegistryPort Helpdesk read cutover (PR-8)

- `server/tickets/ticket_context.py` builds verified immutable requester
  reference/snapshot pairs through `RegistryPort.requester_snapshot()`.
  Creator and affected participant fields for `ticket_context_v1` come only
  from the purpose-bound `RegistryPort.ticket_participant()` projection, with
  exact opaque-ref correlation and typed fail-closed outcomes. The separate
  `PrimaryAgentResolver` dependency remains explicit deferred diagnostic-target
  debt.
- `server/web_api/requester_handlers.py` builds requester on-behalf
  `RegistryReadActor` correlation only from middleware auth and the
  server-resolved creator identity, freezes only the resolved form policy and
  calls `RegistryPort.on_behalf_candidates()` / `authorize_on_behalf()`.
  Candidate lifecycle, same-department/direct-report and exact-search policy
  evaluation belongs to the adapter. Generic `search_people()` remains
  support/admin-only. The existing `primary_agent` response decoration still
  uses `PrimaryAgentResolver` as explicit deferred resolver debt.
  `server/tickets/create_flow.py` reads redacted current registration state
  through `RegistryPort.account_status()`; an invalid projection fails closed
  instead of creating a verified requester ticket with legacy-only identity.
  A trusted registration result created inside that same ticket transaction may
  overlay only an absent/`unregistered` isolated read with safe status/action
  fields; observed active, conflict, rejected and other non-stale states retain
  authority, and no client payload or claim ID participates.
- `server/inventory/service.py` uses `RegistryPort.active_binding()` and exposes
  only requester ref/display plus typed source/status. Registry contact,
  organisation and asset fields are intentionally `null` until a future
  contract explicitly represents them.
- `server/customer_history/sources.py` projects only
  `RegistryPort.requester_history()` events. `projection_service.py` carries
  the typed Registry source state in `source_states.registry`, and
  `handlers.py` builds `RegistryReadActor` only from verified middleware auth;
  requester history is scoped to the server-resolved requester ref. There is no
  direct `DeviceUserBinding` or `DeviceAccountSession` read/fallback in this
  Helpdesk consumer.
- Customer History correlation is neutral-reference-first: valid snapshot pairs
  match exactly on `tickets.requester_external_ref`; malformed neutral fields
  fail closed, and legacy `requester_person_id`/creator/affected aliases are
  read-only only for rows without neutral fields. `tickets.requester_id` is not
  a person-history alias. Requester handlers compose the subject exclusively
  from verified middleware identity.
- `server/web_api/support_handlers.py` and
  `server/web_api/dto/support.py::SupportTicketRegistrySnapshot` no longer
  query/represent uncontracted current Registry repositories.
  The current requester projection exposes `status`, `source` and `code`; when
  Registry is unavailable/not-found it may show only a valid immutable ticket
  requester snapshot with `source=ticket_snapshot`. Partial, mismatched or
  malformed neutral ticket identity fails closed and never authorizes a legacy
  Registry fallback.
- `scripts/check_domain_import_boundaries.py --registry-scope
  requester,tickets,customer_history,inventory,web_api,tech` is the incremental AST
  boundary. It guards all new ticket modules and the named migration paths, and
  permits only the exact known symbol debt recorded in its allowance ledger.
  Repository-wide Registry enforcement is deferred while richer operations are
  absent from the frozen Task-3 contract.
- Deferred consumers are requester profile/schema/identity reads,
  primary diagnostic-target resolution, and richer
  inventory/contact/organisation/asset/service
  projections. Exact shared-binding and account-session revalidation plus
  Registry ingestion/commands remain in `create_flow.py` to preserve requester
  isolation and command behavior until binding-specific/idempotent port
  operations exist.
- Focused gate:
  `python -m pytest server/tests/test_domain_import_boundaries.py server/tests/test_registry_boundary.py server/tests/test_requester_workspace_api.py server/tests/test_web_support_api.py -q --tb=short`.

## 2026-08-10 RegistryPort rich read contracts (PR-8)

- `server/domain_ports/registry_contracts.py` adds frozen, redacted requester
  profile, directory person/search, device context and requester history DTOs.
  They contain opaque references and bounded display/status labels only; local
  contacts, identity aliases, account-session IDs, asset IDs, serial numbers and
  ORM metadata are deliberately not serializable through the port.
- Its purpose-bound `TicketParticipantProjection` is the sole contact-field
  exception: it carries only the existing ticket-context person/display/full/
  email and department/location opaque refs plus provenance, never local
  identities, bindings, assets, sessions or policy metadata.
- `RegistryReadActor` carries only trusted composition-time actor context.
  `LocalRegistryAdapter.audience_projection()`, requester profile and history
  require it; directory search is support/admin-only and audience resolution
  passes actor id/role to `EffectiveIdentityService` for authoritative
  access-group evaluation. Requester actors may read only their matching opaque
  requester reference.
- `RegistryInvalidProjection` separates malformed authoritative data from
  `RegistryNotFound` and `RegistryUnavailable`. Local reads use an adapter-owned
  independent session when supplied an async application session, preserving
  the caller-owned unit of work after failure while logging only an
  operation-level safe diagnostic. Contract maxima are directory 50 and
  audience/history 100.
- `server/tests/test_registry_port_rich_projections.py` covers redaction,
  trusted directory visibility, invalid outcomes, collection bounds, unavailable
  behavior and savepoint isolation. Customer History consumes the requester-
  history projection; other rich consumer migrations remain separate tasks.

## 2026-08-10 Registry command/eligibility acceptance contract (PR-9 prerequisite)

- `server/docs/REGISTRY_PLATFORM_COMMANDS_V1.md` is the minimum versioned
  external command/eligibility contract for UI-login eligibility, registration
  and binding lifecycle, account sessions, browser pairing and other-account
  approval. It requires HTTPS service authentication, verified actor context,
  stable idempotency/replay semantics, correlation and typed fail-closed
  unavailable/unauthorized/uncertain outcomes.
- It is documentation only: no Helpdesk route, adapter command transport,
  feature flag, migration or authority transfer is introduced. Commands remain
  local/off until per-operation external service acceptance evidence exists.
  `ui_users`, web sessions/tokens, RBAC, tickets and consent stay
  Helpdesk-owned; Registry is not permitted to mutate or authorize them.
- `server/docs/REGISTRY_PLATFORM_API_V1.md` remains the read-only external
  API/shadow contract and links to the command prerequisite. Focused contract
  coverage is `server/tests/test_registry_commands_api_docs.py`.

## 2026-08-10 Knowledge/AI schema retirement (PR-11a)

- `server/app/db/migrations/versions/20260810_134_retire_local_knowledge_ai_schema.py`
  is the forward-only, static historical removal of the 45 retired local
  Knowledge/AI tables, including `ticket_knowledge_links` and
  `problem_known_error_links`. It imports neither active ORM metadata nor the
  Registry retirement manifest; rollback is only application rollback plus a
  verified PostgreSQL backup restore.
- `server/tests/test_knowledge_schema_retirement.py` owns a fresh private DB
  `133 -> 134 -> head` Alembic clone check. Its `migration_clone` marker
  suppresses only global head/cleanup setup because the test itself creates,
  migrates and tears down the isolated database; it rejects shared DB names.
- `scripts/registry_retirement_manifest.py` retains historical Knowledge names
  for audit provenance but its future evidence target is Registry-only after
  revision 134.

## 2026-08-10 Registry retirement preflight (PR-11 prerequisite)

- `scripts/registry_retirement_manifest.py` is the reviewed no-DB declaration
  of retire/retain tables, retained-column detachments and a deterministic
  reverse-FK order derived from `server/app/db/models.py`. It rejects missing,
  duplicate or child-after-parent targets. `scripts/audit_db_cleanup_schema.py`
  incorporates that manifest validation in its strict schema audit.
- `scripts/rehearse_registry_retirement.py` is a no-write readiness gate. It
  blocks local Registry runtime, `LocalRegistryAdapter`, local config mode and
  direct Registry/Registration repository imports. Its v1 evidence bundle
  requires a trusted external public-key/KMS verifier for canonical redacted
  acceptance/backup/restore/catalog/maintenance proof; evidence cannot provide
  its own verifier or trust material. `--require-ready` derives immutable Git
  `HEAD`, requires an explicit expected environment, enforces a 24-hour
  attestation lifetime/five-minute future skew and strict backup → restore →
  catalog → maintenance → attestation chronology.
- `server/tests/test_registry_retirement_preflight.py` covers protected actual
  UI/session/RBAC table names, FK ordering and fail-closed local/evidence gates.
  PR-11 remains future-only: no migration, DDL or database connection exists in
  this task, and rollback remains application rollback plus verified restore.

## 2026-08-10 Helpdesk requester reference persistence (PR-2)

- `server/tickets/ticket_context.py` creates immutable `RequesterRef` /
  `RequesterSnapshot` values from `RegistryPort.requester_snapshot()` and
  validates neutral values read from stored ticket/consent rows. Snapshots
  contain only the opaque person reference and a display name.
- `server/tickets/create_flow.py` dual-writes neutral requester values with the
  temporary legacy columns only after validated account-session, active or
  confirmed binding, or verified web-identity resolution. Request payload
  profile/account fields cannot create a neutral reference or snapshot.
- `server/tickets/account_access_service.py`,
  `server/requester/identity_service.py` and `server/consent/service.py` read
  neutral requester references first. Legacy person/binding/session fields are
  compatibility fallbacks only for rows without neutral state. The neutral ref
  and snapshot must both exist, identify the same opaque requester and contain a
  valid display name; partial, mismatched or invalid neutral rows fail closed.
  New verified writes fail instead of silently downgrading to legacy-only state.
  Agent consent access explicitly branches by validated account mode:
  `registration_pending` and `verified_other_account` require an exact non-empty
  session id plus requester identity, while `confirmed_binding` keeps exact-
  session precedence and neutral/legacy identity paths. Identity-less sessions
  fail closed.
- `server/consent/operation_consent.py` inherits the validated neutral requester
  state from the ticket. The snapshot is historical display data only:
  authorization still uses verified identity, active binding and account-session
  checks. `server/web_api/requester_handlers.py` passes only the external
  requester reference produced by the server resolver into consent list/detail/
  decision calls. An unverified exact `ui_login` may list only its pending
  registration claim unless an active binding traces to an approved source claim
  for the same normalized login, person and device.
- Regression coverage is in
  `server/tests/test_ticket_registration_enrichment.py`,
  `server/tests/test_ticket_account_access.py`,
  `server/tests/test_user_consent_api.py` and
  `server/tests/test_requester_workspace_api.py`.

Карта кода `pc_client/server`. Пути указаны относительно корня репозитория (например `server/routes.py`).

---

### Truth baseline

- Корневой pytest-контур: `pytest.ini` (markers `unit`, `integration`, `manual`, `no_db`).
- Server harness: `server/tests/conftest.py`, `server/tests/README.md`.
- CI runner: `scripts/run_ci_suite.py`; isolated temp checkout runner: `scripts/run_ci_in_temp_workspace.py`.
- Канонические test/CI env vars: `TEST_DATABASE_ADMIN_URL`, `TEST_DATABASE_URL`, `PC_CLIENT_ALLOW_SHARED_TEST_DB`, `PC_CLIENT_TEST_DB_TEMPLATE`, `PC_CLIENT_TEST_DB_TEMPLATE_KEEP`, `PC_CLIENT_TEST_CLEANUP_AUDIT`.
- Windows default: if `TEST_DATABASE_URL` and `TEST_DATABASE_ADMIN_URL` are not set, DB-backed server pytest opens the local SSH tunnel and creates an isolated `pc_support_test_<domain>_<pid_or_worker>_<short_hash>` database through the admin DB. Shared `pc_support_test` is explicit fallback/debug mode (`PC_CLIENT_ALLOW_SHARED_TEST_DB=1`) or automatic fallback only when the admin DB is unavailable; shared fallback terminates stale test backends before cleanup, applies a short `lock_timeout`, and is not valid for the full DB/API gate.
- For websocket-heavy pytest on Windows, `server/tests/conftest.py` also switches the harness to `WindowsSelectorEventLoopPolicy`, which removes the old Proactor-only `unexpected connection_lost() call` noise during teardown.

| Сценарий | Открыть сначала | Затем |
|------|------------------|-------|
| Handshake / Protocol V3 | `server/websocket/agent_handshake.py` | `server/docs/PROTOCOL_V3.md`, `pc_agent/docs/PROTOCOL_V3.md` |
| Outbox multi-instance prerequisites | `quality/outbox_multi_instance_prerequisites.json`, `quality/active_risks.json`, `server/tests/test_outbox_multi_instance_prerequisites.py` | `server/websocket/device_outbox_sender.py`, `server/app/repos/device_outbox_repo.py`, `server/docs/COMMAND_RESULT_LIFECYCLE.md`; horizontal DeviceOutbox dispatch is blocked until TD-015 prerequisites pass |
| `run_tool` / consent | `server/tools/service.py` | `server/app/services/operation_service.py`, `server/web_api/requester_handlers.py`, `server/web_api/registry_handlers.py`, `server/api/operations.py`, `server/docs/TOOL_CALL_STARTED_INVARIANT.md`; `ToolExecutionService` is the canonical facade for direct run_tool and approved-consent resume, while `OperationService.approve_consent()` only transitions the pre-created operation to `queued` and records the decision |
| Remote Assist / WebRTC | `server/remote_assist/service.py` | `server/remote_assist/handlers.py`, `server/remote_assist/signaling.py`, `server/remote_assist/policy.py`, `server/remote_assist/ice.py`, `server/app/repos/remote_access_repo.py`, `server/docs/PROTOCOL_V3.md`; agent runtime-module bootstrap lives in `pc_agent/remote_assist/runtime_host.py` and managed package `pc_agent/modules_packages/remote_assist_runtime` |
| Тикеты / очередь / чат | `server/tickets/handlers.py` | `server/tickets/create_flow.py`, `server/tickets/ticket_context.py`, `server/tickets/workflow_service.py`, `server/docs/TICKET_SYSTEM.md` |
| Customer History / LLM preview | `server/customer_history/projection_service.py`, `server/customer_history/context_builder.py` | `server/customer_history/handlers.py`, `server/customer_history/redaction.py`, `server/routes.py`, `server/docs/TICKET_SYSTEM.md`; role-safe support/requester/admin history routes and bounded redacted LLM preview |
| Registration / account sessions | `server/registry/account_session_service.py`, `server/registry/primary_agent_resolver.py`, `server/registry/browser_pairing_service.py`, `server/registry/effective_identity_service.py`, `server/registry/audience_contracts.py`, `server/registry/audience_group_service.py` | `server/docs/REGISTRATION_ACCOUNT_SESSIONS.md`, `server/docs/REGISTRY_VISIBILITY_FOUNDATION.md`, `server/tickets/account_access_service.py`, `server/uploads/handlers.py`, `server/web_api/registry_handlers.py`, `server/tests/test_primary_agent_resolver.py`, `server/tests/test_registry_effective_identity_service.py`, `server/tests/test_registry_audience_groups.py`; browser registration pairing stays `confirmed` until agent pickup can deliver a confirmed-binding account session; approved other-account login requests store the one-time session token in durable encrypted request metadata and agent poll claims delivery atomically |
| Web-first product contract / Phase A | `PLANS.md`, `docs/WEB_FIRST_REGISTRATION_UX_CONTRACT.md`, `scripts/test_web_first_registration_localization.py` | `docs/LIVE_TESTING_DEBUG_RULES.md`, `docs/TESTING_RULES.md`, `server/docs/TICKET_SYSTEM.md`; Phase A freezes requester/account/profile/device/binding/primary-agent terms, `/app/requester` as the canonical requester workspace, browser-host non-target semantics, route proof surfaces and the normal UI raw-id guard before broad live testing |
| Web-first Phase E data pack | `test_data_packs/web_first_phase_e.json`, `test_data_packs/critical_behavior_v1.json`, `scripts/collect_phase_e_vm_snapshot.py`, `scripts/check_phase_e_vm_snapshot.py`, `scripts/check_phase_e_live_gate.py` | `server/tests/test_phase_e_test_data_pack.py`, `server/tests/test_critical_behavior_data_pack.py`, `scripts/test_phase_e_vm_snapshot_collect.py`, `scripts/test_phase_e_vm_snapshot_check.py`, `scripts/test_phase_e_live_gate.py`, `PLANS.md`, `docs/LIVE_TESTING_DEBUG_RULES.md`; no-db guard for pre-broad-live users, form-pack, Windows/Linux VM-agent prerequisites and validation matrix, plus read-only collector/checker scripts for a `web_first_phase_e_vm_snapshot_v1`; collector supports `--database-url <stand-db-url>` when the local DB default is not the live stand; live-gate checker rejects DB/API-only broad-live pass claims without passing VM check and required `real_browser` artifacts; `critical_behavior_v1.json` maps Workstream D critical domains to owners, invariants, existing automated test refs, live scenarios and required API/DB/Observer/live-manifest evidence; not proof of live VM cleanliness until real snapshot evidence passes |
| Observer retention/query plans | `server/app/db/models.py`, `server/app/db/migrations/versions/20260624_128_observer_query_plan_indexes.py`, `server/observer/service.py`, `server/observer/runtime.py` | `server/tests/test_observer_query_plan_no_db.py`, `server/docs/OBSERVER_LAYER.md`, `PLANS.md`; no-DB contract for retention cleanup, trace search filters, trace detail/span filters and ticket signature stats composite indexes |
| Модули / reconcile | `server/modules/service.py`, `server/modules/handlers.py`, `server/modules/workbench_service.py` | `server/websocket/modules_sync.py`, `server/docs/MODULE_CREATION_GUIDE.md`, `server/docs/MODULES_API.md` |
| Admin / support / ticket UI and production control | `server/admin.js`, `server/admin_modules_workbench.js`, `server/support.js`, `server/ticket.js`, `server/web_shared.js`, `server/control_plane.py`, `server/runtime_control.py`, `scripts/manage_remote_stack.py` | `server/static_pages/`, `server/docs/SECURITY_AND_AUTH.md`, `server/docs/RUNBOOK_HELPDESK_ENDPOINT_HOST.md`; current bootstrap browser endpoint is `http://192.168.100.19:8080/admin`, while production management uses root-system `helpdesk-server.service` and `helpdesk-control.service` rather than user services |

---

## 1. Точки входа

| Файл | Назначение |
|------|------------|
Requester create note: `/app/requester/new` now renders one category/form surface instead of a separate review stepper. The form-level "Создать обращение" action runs local required/format validation, authenticated safe preview and create in order; category, dynamic field, on-behalf and backend `details.fields` errors render inline before navigation, while preview blockers stay on the form and prevent the create request.
Requester Observer note: requester preview/create success and block paths write redacted web-cabinet Observer traces in `server/web_api/requester_handlers.py`. Blocked no-agent and on-behalf-denied paths use `ticket_preview_blocked` / `ticket_create_blocked` with safe error codes (`REQUESTER_AGENT_REQUIRED`, `ON_BEHALF_SCOPE_DENIED`) and form/action flags only. `auth.middleware.request_id_middleware` gives each HTTP request a server-generated `server_request_id`, web actor-context helpers pass it to `server/observer/web_event_writer.py`, and requester/support chat pass `message_id` as `idempotency_key` so distinct same-ticket actions do not collapse while retries keep a durable logical key. The BEH-201 API + DB + Observer matrix is in `server/tests/test_requester_workspace_api.py`.

| `webapp/src/features/requester/consent-card.tsx` | Shared requester consent card used by `/app/requester` and `/app/requester/tickets/:ticketCode`. It distinguishes diagnostics, screen view, remote control and administrative access, explains action/scope/reason/request/requester/expiry, masks id-like tokens in free text, keeps consent/session/subject/device identifiers out of visible and aria text, locks duplicate card decisions, and posts only through existing requester consent approve/deny APIs backed by `UserConsentService` ownership, idempotency and audit checks. |
| `webapp/src/features/requester/labels.ts`, `webapp/src/features/requester/dynamic-form/index.tsx`, `webapp/src/features/requester/profile-runtime/index.tsx`, `webapp/src/pages/requester/new-request-workflow.tsx`, `webapp/src/pages/requester/tickets-workflow.tsx`, `webapp/src/pages/requester/profile-workflow.ts`, `webapp/src/pages/requester/devices-workflow.ts`, `scripts/test_web_first_registration_localization.py` | Phase L requester localization/accessibility runtime. Shared labels centralize Russian device/readiness/status/next-action/error fallbacks and avoid raw identifier fallbacks. Dynamic form and profile controls use requester-visible Russian labels for accessible names instead of field keys, expose field errors with `aria-invalid`, and accept refs so `/app/requester/profile/setup` and `/app/requester/new` can focus the first missing field before API calls. `scripts/test_web_first_registration_localization.py` includes the split requester pages/workflows/runtimes in the UTF-8/mojibake guard and keeps baseline-contract files out of normal UI raw-id scanning. Browser evidence lives in `webapp/artifacts/requester-phase-l-a11y-responsive-check.mjs`. |
| `server/web_api/requester_handlers.py::handle_web_requester_ticket_claim_public`, `webapp/src/pages/requester/tickets-page.tsx` | Stage 2B public ticket claim-to-account. Authenticated requester `POST /api/web/requester/tickets/claim-public` verifies the existing public access code, requires the web actor to resolve to a `RegistryPerson`, then attaches the ticket with an atomic conditional `UPDATE` on `tickets.requester_person_id` so concurrent different-person claims are single-winner. The winning transaction clears `custom_fields.public_access.unbound`, writes one `requester_ticket_claimed` audit without the code and refreshes the requester list through the dedicated tickets route; the losing transaction returns `PUBLIC_TICKET_ALREADY_CLAIMED`/409 and writes no event. If no requester person is linked, the API returns `REQUESTER_IDENTITY_REQUIRED` and the UI shows an admin-link guidance message. Regression coverage: `server/tests/test_requester_workspace_api.py::test_requester_public_ticket_claim_is_single_winner_under_race`. |
| `server/tickets/public_ticket_handlers.py::handle_public_ticket_authorize`, `server/auth/rate_limit.py` | PUBLIC-AUTH-034 public code-to-session exchange. `POST /public_api/tickets/{ticket_id}/authorize` uses the shared auth rate limiter before `verify_public_access_code()`: 5 attempts per 300 seconds per `client_ip:ticket_id`, returning `RATE_LIMITED`/429 before a public session token can be minted. Regression coverage: `server/tests/test_ticket_device_binding.py::test_public_ticket_authorize_rate_limits_invalid_code_attempts`. |
| `server/server.py` | Запуск aiohttp, startup/shutdown, watchdog/scheduler; поднимает background `ObserverRefreshRuntime` для incremental trace projection и historical backfill ticket-root traces, plus one idempotent startup-owned `InventoryRefreshRuntime` for periodic `inventory.collect` dispatch through the existing tool execution path; `housekeeping_cleanup_task()` only cleans runtime caches/outbox repair and must not create schedulers; strict runtime profiles re-raise DB initialization failures instead of continuing in memory; перед настройкой loguru принудительно включает UTF-8 для stdout/stderr на Windows, чтобы консоль и логи не превращались в mojibake; bootstrap-ит repo root в `sys.path`, чтобы sibling-пакет `shared/*` был доступен в canonical runtime wrappers; legacy `server_old.py` удалён из активного runtime tree |
| `server/control_plane.py` | Отдельный aiohttp control-plane на порту `8667`: status/logs/download/actions для server runtime, auth/CORS/audit и переживание `stop/restart` основного сервера; так же bootstrap-ит repo root в `sys.path` для shared runtime helpers |
| `server/routes.py` | Регистрация всех HTTP и WS маршрутов, включая shell-страницы `/login`, `/admin`, `/support`, `/help`, `/ticket`, новые React SPA routes `/app` + `/app/{tail}`, built asset endpoints `/assets/*` и `/favicon.svg`, legacy session endpoint `GET /api/ui_session`, typed web boundary `/api/web/session|support|admin|reports|settings|realtime/*` включая feature-flagged `POST /api/web/session/register`, requester profile gate route `PUT /api/web/requester/profile`, admin profile-schema routes `GET|PUT /api/web/admin/registry/profile-schema` and `POST /api/web/admin/registry/profile-schema/preview`, registry UI-account disable route `POST /api/web/admin/registry/ui-users/{user_login}/disable`, controlled `/help` -> `/app/help` and `/ticket/{id}` -> `/app/ticket/{id}` cutover, typed observer aliases `/api/web/admin/observer/*`, typed notifications `/api/web/notifications*`, typed tech aliases `/api/web/admin/tech/snapshot|locate|overview|alerts|logs|agents/audit|users/audit|operations/stuck`, typed access-control aliases `GET /api/web/admin/access/catalog|summary|effective|audit`, `POST /api/web/admin/access/users/{user_login}/password`, `POST/PATCH /api/web/admin/access/groups*` and `PUT /api/web/admin/access/groups/{id}/permissions|members|queues`, typed module workbench aliases `/api/web/admin/modules/workbench/*`, typed Capability Studio aliases `GET /api/web/admin/capabilities`, `/api/web/admin/capabilities/provider-configs*` and runner rollout `/api/web/admin/capabilities/runner-rollout*`, device update recommendation `GET /api/devices/{device_id}/agent/update_recommendation`, module rollout settings `GET/PATCH /api/modules/rollout_settings`, typed reports/settings routes `GET /api/web/reports/summary` + `GET /api/web/settings`, configurable workflow save `PUT /api/web/settings/workflow_profiles`, typed inventory cleanup `POST /api/web/admin/devices/cleanup_env_duplicates`, typed device inventory routes `GET /api/web/admin/devices/{device_id}/inventory` + `POST /api/web/admin/devices/{device_id}/inventory/collect`, lightweight inventory v4 routes `GET /api/web/admin/inventory/dashboard`, `POST /api/web/admin/inventory/bindings/import`, `GET /api/web/admin/inventory/bindings/export.csv`, `GET /api/web/admin/inventory/export.csv|export.xlsx`, `POST /api/web/admin/inventory/bulk-refresh`, `GET /api/web/admin/inventory/bulk-operations`, `GET /api/web/admin/inventory/reports*`, `GET /api/web/admin/inventory/refresh-runs`, `GET /api/web/admin/devices/{device_id}/binding/history`, profile/suggestion routes under `/api/web/admin/devices/{device_id}/profiles|binding-suggestions*` and presence routes `GET|POST /api/web/admin/devices/{device_id}/presence*`, typed token panel routes `GET /api/web/admin/device-tokens`, `GET /api/web/admin/devices/{device_id}/tokens` + `POST /api/web/admin/devices/{device_id}/tokens/revoke`, typed forms endpoints `GET /api/web/admin/forms/current`, compatibility `POST /api/web/admin/forms/save`, lifecycle `POST /api/web/admin/forms/save-draft|validate|publish`, `PATCH /api/web/admin/forms/preferred`, `POST /api/web/admin/forms/route-preview|process-preview`, typed Request Studio safe publish endpoints `GET /api/web/admin/request-studio/capabilities`, `POST /api/web/admin/request-studio/validate-draft|publish-preview|publish`, typed helpdesk-model ticket type endpoints `POST /api/web/admin/helpdesk-model/ticket-types/publish|deactivate|rollback`, typed registry picker endpoint `GET /api/registry/options`, typed playbook builder endpoints `GET /api/web/admin/playbooks/catalog` + `POST /api/web/admin/playbooks/save`, typed support Queue Mode saved-view endpoints `GET/POST/PUT/DELETE /api/web/support/queue/saved-views`, typed admin ticket purge endpoints `POST /api/web/admin/tickets/purge/preview|/purge`, typed passport routes `GET/POST/PATCH /api/web/support/tickets/{ticket_id}/passport*`, а также `/api/web/settings/*` alias-маршруты поверх legacy admin-config handlers для очередей, членов очередей, OLA, routing, SLA, calendars, resolution codes и audit |
| `server/registry/service.py` | Лёгкий registry/CMDB слой: auto-ingest ПК из agent handshake, requester profile ingestion теперь передает self-reported данные в controlled registration claim flow вместо прямого `registry_assets.assigned_person_id`, registry snapshot для `/api/web/admin/registry` включает management-center summary, device summaries, people identity/device/session counts, all binding rows, account sessions, login requests, pending/conflict claims with `user_confirmed_at`, unregistered devices, focused production context for people/departments/services/assets and actionable data-quality signals; generated quality issues carry stable `issue_key` values, now include missing person department/location, missing asset owner/responsible and pending department confirmation, and are filtered through `registry_quality_issue_overrides` for ignore/snooze/resolve state |
| `server/registry/profile_schema_service.py` | Controlled requester profile schema service. Defaults protect system fields (`full_name`, login identity, department, location, phone contact requirement, active device links), expose optional `internal_extension`, allow admin overrides only for visible/required/help/validation on known fields, constrain custom fields to `registry_people.metadata_json.profile_custom_fields.<key>`, persist config in `registry_admin_policies(policy_key=requester_profile_schema)`, write `registry_admin_events(event_type=profile_schema_updated)`, validate requester `custom_fields`, compute schema-aware missing fields, and keep admin storage targets out of requester-safe projections. |
| `server/registry/registration_service.py` | Workflow регистрации пользователя за устройством: создает/дедуплицирует `device_registration_claims`, registry people/identities и suggestions, подтверждает user/admin review, детектирует конфликты, требует user confirmation перед обычным admin approve, поддерживает explicit admin override, идемпотентно переиспользует active binding для того же device/person and reuses one open `agent_profile` claim per device/source so logout/resubmit cannot create parallel pending claims, создает authoritative `device_user_bindings`, синхронизирует derived owner в `registry_assets`/`device_inventory_bindings`, пишет registration events и inventory binding history при approve/transfer/revoke, отдает registration status/timeline для handshake, API и UI; admin P0 helpers `bind_person_to_device`, `transfer_owner`, `add_shared_user`, `assign_responsible` создают service-backed admin bindings, manual bind can mark a matching pending `agent_profile` claim approved while preserving `device_user_bindings.source=admin_manual` and `admin_binding_created` timeline semantics, supersede other open agent claims for the same device, and revoke dependent account sessions on binding revoke/transfer |
| `server/registry/account_state_service.py` | Side-effect-free read model for the agent account gate: projects confirmed active bindings, pending registration claim, active server account sessions including `registration_pending`, and non-stale pending other-account login requests into login candidates/actions, returns `can_register` / `can_login_confirmed_binding` / `can_request_other_account_login`, and never creates claims or mutates registry assets |
| `server/registry/account_session_service.py` | Server-side requester account-session workflow for the agent GUI: creates verified sessions for active confirmed device bindings including primary/shared/responsible relationships, creates PA8 GUI password-login `confirmed_binding` sessions with `verification_method=gui_password` only after server auth, verified `ui_login` resolution and active current-device binding check, creates server-issued `registration_pending` sessions for pending claims so the agent can poll registration state without resubmitting, revokes matching pending sessions when the registration claim is approved, creates admin-reviewed other-account login requests from any active registered binding (primary preferred, then responsible/shared) without changing device registration, cancels pending other-account requests when their base binding is transferred/revoked or found stale during poll/approve, approves/rejects valid requests into token-protected `verified_other_account` sessions with one-time agent token pickup, validates session id/token for ticket create/list/detail/message/upload/read boundaries, rechecks current binding/base-binding/claim activity so revokes or claim terminal states invalidate dependent sessions, supports agent logout and admin revoke, and preserves phone/reason/base registered-owner context for ticket warnings |
| `server/registry/password_reset_service.py` | Web-account password-reset request workflow: creates a generic public pending request from `/api/web/session/password-reset-requests` without account-enumeration leakage, lists pending requests for `/app/admin/registry`, and lets an admin complete a request by setting a new UI-user password through the existing password policy/hash path while recording completion metadata. |
| `server/registry/primary_agent_resolver.py` | Safe diagnostic target resolver for a `RegistryPerson`: prefers one active `primary_user` binding, can fall back to one active shared/responsible binding only when `diagnostic_target.allow_single_active_binding_fallback=true`, reports `primary_device_missing` or `ambiguous_primary_device`, and returns device/binding/hostname/online/last-seen/agent-version evidence without token, capability or raw metadata leakage |
| `server/app/repos/registry_repo.py` | SQLAlchemy repo для `registry_*` таблиц: departments, locations, vendors, services, people, assets; upsert idempotency по device/requester/location, service lookup for support workspace service owner/source enrichment, и ticket counts by device |
| `server/app/repos/registration_repo.py` | SQLAlchemy repo для `registry_person_identities`, `device_registration_claims`, `device_user_bindings` и `device_registration_events`; нормализует user identifiers, ищет active/all primary/shared bindings, не переassign-ит существующие identities между людьми silently, управляет claim status, revoke/stale binding и audit events |
| `server/app/repos/ticket_passport_repo.py` | Repo для `ticket_resolution_passports`, `ticket_evidence_items`, `ticket_action_log`, `ticket_approvals`, `ticket_related_objects`: latest passport, versioned create/update, generated action replacement, idempotent evidence append by source/fact и related-object sync для official resolution dossier |
| `server/app/repos/helpdesk_policy_repo.py`, `server/tickets/helpdesk_policy_runtime.py`, `server/tickets/policy_health_service.py` | Standalone versioned helpdesk model registry: publishes versioned `ticket_types`, `form_schemas`/`form_fields`/`form_conditions`, `request_templates`, priority/SLA/OLA/routing/approval/closure/diagnostic/visibility/notification/reporting policies, smart views and audit rows; reporting policy publication validates supported passport sections and `required_evidence_types`; `ticket_types` carry default workflow/profile/policy references and feature flags that `request_templates` inherit during publication with legacy unknown-type fallback; visual template publication now materializes `request_template.form_schema_id` as a first-class active form schema while keeping `ticket_form_packs` compatibility; field `process_mapping.roles` normalizes to existing `field_roles`, and schema fields preserve validation/process mapping/visibility metadata; resolves effective policy config through inheritance `system -> ticket_type -> category -> request_template`, and `resolve_effective_request_template()` prefers explicit template policy refs (`*_policy_code`, plus `sla_policy_id` compatibility) over inline legacy form JSON; supports policy and ticket-type version lookup, JSON config diff, soft deactivate and rollback-as-new-version with audit events; ticket creation overlays effective registry policies into `custom_fields.request_template` with `policy_refs`, `effective_policy_sources` and `effective_policy_snapshots`, while existing-ticket lifecycle helpers can re-resolve active referenced policy code without mutating the historical snapshot; Policy Health evaluates active/published templates for routing/SLA/OLA/approval/closure/visibility/notification/diagnostic/reporting health, invalid refs and conflicts; its simulate endpoint is dry-run but runtime-equivalent, using the real routing, priority, SLA, OLA, approval, closure, visibility and diagnostic resolvers against an unsaved ticket context; catalog-aware simulation expects `service_code`, `offering_code` and `offering_full_code` at top level, with `custom_fields` only as legacy/non-canonical context; typed admin routes are `GET /api/web/admin/helpdesk-model/policies`, `POST /api/web/admin/helpdesk-model/ticket-types/publish|deactivate|rollback`, `POST /api/web/admin/helpdesk-model/form-schemas/publish`, `POST /api/web/admin/helpdesk-model/request-templates/publish-from-form`, `POST /api/web/admin/helpdesk-model/request-templates/republish-legacy-forms`, `POST /api/web/admin/helpdesk-model/policies/publish`, `POST /api/web/admin/helpdesk-model/policies/diff`, `POST /api/web/admin/helpdesk-model/policies/deactivate`, `POST /api/web/admin/helpdesk-model/policies/rollback`, `POST /api/web/admin/helpdesk-model/smart-views/publish`, `GET /api/web/admin/helpdesk/policy-health`, `GET /api/web/admin/helpdesk/policy-health/{template_code}` and `POST /api/web/admin/helpdesk/policy-health/simulate`; the legacy republish route backfills active `request_forms` into registry rows and skips unchanged forms by fingerprint, while the registry payload exposes data-quality issues for active templates missing workflow/priority/routing/SLA/closure links; smart-view publication validates supported filter keys, field paths, sort directions and display columns before writing a registry version, and active custom smart views are consumed by typed support queue runtime with their saved filter and sort definitions |
| `server/app/repos/service_catalog_repo.py`, `server/tickets/service_catalog_contract.py`, `server/tickets/service_catalog_defaults.py`, `server/tickets/service_catalog_runtime.py`, `server/tickets/service_catalog_preview.py`, `server/tickets/service_catalog_publication.py`, `server/tickets/request_studio_publication.py`, `server/web_api/service_catalog_handlers.py`, `server/web_api/request_studio_handlers.py`, `server/web_api/dto/request_studio.py`, `scripts/seed_service_catalog.py` | P1/P1.1 Service Catalog process layer. `helpdesk_services` and `helpdesk_service_offerings` are requester-facing catalog objects linked optionally to CMDB `registry_services`, not replacements for registry services or legacy `tickets.service_id`. Runtime create/preview accepts `service_code`, `offering_code`, `offering_full_code`, legacy `request_template_key`, and `form_key` as a unique-template inference fallback; it resolves published/visible catalog selections, applies service/offering policy refs in inheritance order `system -> ticket_type -> category -> service -> offering -> request_template`, stores explicit ticket catalog/reporting fields plus `custom_fields.service_catalog`, and keeps form-only legacy flow when no unambiguous offering exists. `service_catalog_defaults.py` owns the safe `other.unknown` fallback and seed defaults. `service_catalog_preview.py` exposes requester-safe runtime-backed preview through `POST /api/service-catalog/preview` without inserts/events/approvals/diagnostics. Publication gates validate owner/queue/template/request type/visibility and run runtime simulation before publish, then write `helpdesk_service_catalog_audit`. Routes include admin/auditor `/api/web/admin/service-catalog*` and requester/agent-safe `/api/service-catalog/*`; Policy Health also evaluates services/offerings and simulation accepts service/offering input. React `/app/admin/request-template-studio` is the primary no-code setup surface over this layer and the forms/policy registry: `webapp/src/features/request-template-studio/draft-model.ts`, `create-request-wizard.tsx`, `form-field-editor.tsx` and `process-editors.tsx` build a mutable Studio draft, persist form edits with `/api/web/admin/forms/save-draft`, persist offering/title/visibility/policy refs with `/api/web/admin/service-catalog/offerings/save-draft`, hide raw refs in basic mode, and keep Forms Builder, Service Catalog and Policy Health as expert-only surfaces. `webapp/src/features/requester/dynamic-form` is reused by Forms Builder process preview and Studio publish preflight so constructor-authored fields, options, `visible_when`, arrays and blocked `file` publication match requester runtime before backend safe publish; `/public_api/ticket_forms/current?pack_key=request_forms` appends active standalone registry templates to the preferred legacy pack using the same registry-to-form resolver so published Studio templates are selectable by requester create flows. Direct publish from Studio uses typed safe endpoints; publish preview issues one-time HMAC/nonce tokens stored only as hashes in `request_studio_publish_tokens`, returns create/update/noop/blocked object diffs, and publish revalidates the same draft before guarded mutation. |
| `server/quality/*`, `server/app/services/quality_snapshot_scheduler.py`, `server/web_api/quality_handlers.py`, `server/app/db/migrations/versions/20260517_0900_088_quality_loop.py`, `server/app/db/migrations/versions/20260517_1500_089_quality_loop_production_hardening.py`, `server/docs/QUALITY_LOOP.md` | P3/P3.1 Experience & Quality Loop. Migration `088` adds structured `ticket_feedback`, `ticket_reopen_events`, `ticket_quality_reviews`, `ticket_quality_review_comments`, `continuous_improvement_actions`, `service_quality_snapshots` and `quality_policies`; migration `089` enforces one latest feedback row per ticket with a partial unique index. Requester/public endpoints save CSAT and structured reopen reasons for resolved/closed tickets; support/admin/auditor APIs under `/api/web/quality/*` expose internal QA reviews, improvement actions, aggregate service/offering analytics, snapshot `last_computed_at`, and policy preview/save; support ticket detail includes a requester-hidden `quality` payload. Analytics are aggregate-only and avoid requester PII. |
| `server/change/*`, `server/app/repos/change_repo.py`, `server/web_api/change_handlers.py`, `server/app/db/migrations/versions/20260518_0900_092_change_enablement.py`, `server/docs/CHANGE_ENABLEMENT.md` | P5 Change Enablement. Migration `092` adds first-class `changes`, versioned risk assessments/plans, approval rows, change windows, affected objects, tasks, PIR records, policies and activity audit, plus nullable `continuous_improvement_actions.change_id`. Support/admin/auditor APIs under `/api/web/changes*`, `/api/web/change-windows` and `/api/web/change-policies` expose change list/detail, create from problem/improvement action, lifecycle transitions, risk/plan/approval/schedule/task/PIR operations and aggregate no-PII change metrics. Calendar hardening detects same-service/offering overlaps, supports simple recurring blackout/maintenance RRULEs, and metrics report failure/rollback rates, lead time and emergency retrospective overdue counts. Requester/public change APIs are intentionally absent; P5 does not execute changes automatically or replace Problem Management/Improvement Actions. |
| `server/app/repos/form_drafts_repo.py` | Draft storage repo for the React request-form lifecycle: reads/upserts explicit `form_builder_drafts` rows and marks drafts published after a pack version is created, while the compatibility `ticket_form_packs` registry remains the published source for `/help`, public create and agent catalogs |
| `server/tickets/form_lifecycle_service.py` | Typed request-form builder lifecycle service: serializes admin draft payloads, including canonical `*_policy_ref` fields and preflight metadata, into normalized `request_forms` packs, saves explicit drafts, builds DB-backed business preflight context, publishes new pack versions with optional preferred switch only after blocking business errors are absent, and updates preferred for rollback/promotion while `server/web_api/admin_handlers.py` stays a thin HTTP boundary |
| `server/tickets/form_business_validation.py` | Business preflight validator for request-form packs: returns structured errors/warnings/summary for missing conditional fields, missing queues/SLA/OLA/playbooks/policy refs, non diagnostic-safe playbooks, approval source gaps, closure evidence gaps, missing preview samples, missing help text, weak public titles and field-key changes without alias/migration notes |
| `server/tickets/form_process_preview.py` | Side-effect-free process preview for the request-form builder: validates a draft form plus sample answers, computes priority, template/global routing decision, SLA/OLA targets, approval requirement, diagnostics, closure, visibility, notifications and business validation report for `POST /api/web/admin/forms/process-preview` |
| `server/tickets/request_template_submission.py` | Shared create/preview/public-create submission resolver: validates legacy `request_forms` packs first, then falls back to active standalone helpdesk registry `request_template` + `form_schema` when `request_template_key` points at the versioned model registry, preserving schema fields, visibility/process mapping and effective policy context before ticket creation side effects run; returns `resolved_from`, resolved pack/template/schema keys and versions so runtime snapshots can explain whether a ticket came from `legacy_pack` or `standalone_registry` |
| `server/tickets/requester_policy.py` | Policy-owned requester ticket action projection used by requester DTOs and lifecycle handlers. It computes message, attachment, solution-confirmation, rating and reopen capabilities from ticket status, workflow/requester action policy, closure policy, feedback window and latest-feedback state; latest positive feedback blocks requester reopen while latest low CSAT keeps it available. `annotate_requester_ticket_policy_state()` keeps list/detail projections aligned with mutation guards. |
| `server/tickets/diagnostic_policy.py` | Executable request-template diagnostic policy helper: normalizes requester diagnostic-consent payloads for authenticated and public ticket creation, reads `custom_fields.request_template.diagnostic_policy` / legacy `diagnostics`, selects safe `auto_run` suggested playbooks through the resolved diagnostic target with priority/target-online/requester-consent/high-risk-tool consent gates, writes `target_agent_offline` / `target_device_missing` / `target_device_ambiguous` skip evidence, classifies terminal operation/result payloads into `custom_fields.diagnostic_result`, executes `reroute_by_result` queue handoff idempotently per operation without changing `ticket.status`, and materializes terminal diagnostic operations into `ticket_evidence_items` when `attach_results.as_evidence=true` for passport/closure evidence |
| `server/diagnostics/service.py`, `server/diagnostics/projection.py`, `server/diagnostics/evidence.py`, `server/diagnostics/sessions.py`, `server/diagnostics/findings.py`, `server/diagnostics/bundle.py`, `server/diagnostics/profile_runner.py`, `server/diagnostics/passport_bridge.py`, `server/diagnostics/provider_config.py`, `server/diagnostics/providers/observer_provider.py`, `server/diagnostics/providers/remote_assist_provider.py`, `server/diagnostics/providers/manual_provider.py`, `server/app/repos/diagnostics_repo.py`, `server/app/repos/diagnostic_provider_config_repo.py` | Persistent Diagnostic Layer over tickets: read-only overview, session/step lifecycle, normalized `diagnostic_evidence` projection from operations, `diag.logs.collect` artifacts, observer root traces, remote assist sessions and manual checks, deterministic finding rules, JSON diagnostic bundle creation, profile-run orchestration, selected evidence promotion into existing passport evidence rows, persisted provider config lifecycle for server connectors, server-side observer query capabilities `observer.ticket.summary` / `observer.trace.bundle` with compact evidence previews, Remote Assist session capabilities `remote_assist.request_view` / `remote_assist.request_control` / `remote_assist.session.summary` routed through existing consent/signaling services, and manual capabilities `manual.visual_check` / `manual.vendor_response` / `manual.operator_note` / `manual.customer_confirmation` that create `diagnostic_evidence` plus `diagnostic_manual_evidence_created` ticket audit events before optional passport selection. Migration `076` adds `diagnostic_session_capabilities` and `diagnostic_artifact_links`; `normalize_tool_result_to_evidence_values()` is the production result-to-evidence mapper, and `project_capability_result()` persists non-agent capability operation/session/query results with provider/capability/version/source/trace/artifact/actor/redaction provenance plus optional session capability snapshots. Diagnostics stay independent from `tickets.status` and reuse existing tools/playbooks/observer/remote assist instead of replacing their runtimes. Routes include `GET /api/tickets/{ticket_id}/diagnostics/overview`, `GET /api/web/support/tickets/{ticket_id}/diagnostics/overview`, sessions/evidence/findings/bundle/profile endpoints, web-session support aliases under `/api/web/support/tickets/{ticket_id}/diagnostics/*`, `POST /api/tickets/{ticket_id}/diagnostics/run-profile`, `POST /api/tickets/{ticket_id}/diagnostics/passport/attach-selected`, `/api/diagnostics/profiles`, ticket capability run routes, admin provider config routes `/api/diagnostics/providers/configs*`, and web-session aliases `/api/web/admin/diagnostics/providers/configs*`. |
| `server/diagnostics/agent_recipes.py`, `server/diagnostics/agent_recipes_repo.py`, `server/diagnostics/recipe_execution_service.py`, `server/diagnostics/runtime_dependencies.py`, `server/diagnostics/runner_rollout.py`, `server/docs/AGENT_RECIPE_RUNNER.md` | Declarative Agent Recipe Runner foundation. `agent_recipe` is a first-class execution target backed by persisted recipe versions in `agent_recipe_versions`, primitive catalogs in `agent_recipe_primitives`, test audit rows in `agent_recipe_test_runs`, and existing `diagnostic_capabilities` / `diagnostic_capability_versions` for identity/contracts. Recipe descriptors preserve composite `presentation_schema`, and built-in primitive descriptors expose simple result presentation schemas for DNS, TCP, HTTP, file, process and service checks. `RecipeExecutionService` creates `operations.kind=agent_recipe`, enqueues Protocol V3 `run_recipe` in `device_outbox`, and terminal command results are projected to `diagnostic_evidence`. If the protected runner is missing/outdated, `runtime_dependencies.py` persists `operation_dependencies`, selects the compatible preferred runner version, uses desired modules + reconcile for ticket-bound install/upgrade, and resumes the parent operation after readiness becomes available. `runner_rollout.py` is the admin fleet workflow for `agent_recipe_runner` canary/waves/rollback backed by `runner_rollout_*` tables while still delivering through desired modules + reconcile. Admin authoring routes live under `/api/web/admin/agent-recipes*`; rollout routes live under `/api/web/admin/capabilities/runner-rollout*`. |
| `server/tickets/ticket_context.py` | Server-owned `ticket_context_v1` builder, validator and projection helpers. On create/preview, requester handlers and `create_flow.py` resolve creator, affected person, requester context, form/policy refs and diagnostic target from verified requester identity plus Registry primary-agent bindings, store `custom_fields.ticket_context` with canonical sections (`created_at`, `on_behalf`, `requester_context`, `diagnostic_target`, `form`, `policy_refs`, `redaction`) and preserve flat aliases (`created_on_behalf`, `creator_person_id`, `affected_person_id`, `target_device_id`, `target_agent_status`, `diagnostic_target_source`) without trusting client-supplied target ids. Requester projection redacts ids/policy/source codes; support projection keeps operator context. |
| `server/tickets/diagnostic_target.py` | Shared runtime resolver for diagnostic/module execution target. Request-form auto-run, diagnostic-policy auto-run, support manual tool/playbook launch and `/api/tools/run` ticket-device guard use it to prefer `ticket_context_v1.diagnostic_target.device_id`, then flat `target_device_id`, over legacy `ticket.device_id`, falling back to legacy only for tickets without context and preserving safe `diagnostic_target` evidence for UI/API responses. |
| `server/tickets/visibility_policy.py` | Executable request-template/effective registry visibility policy helper: resolves `public_status` / `public_status_label`, exposes requester/support visibility metadata, and redacts requester-hidden top-level and dotted-path fields from serialized ticket/passport-like payloads |
| `server/tickets/notification_service.py`, `server/tickets/notification_channels.py`, `server/tickets/policy_action_dispatcher.py` | Notification and policy-action delivery layer: legacy queue/assignee/requester/watchers in-app delivery plus executable request-template/effective registry `notification_policy` event blocks (`on_created`, `on_assigned`, `on_sla_breach`, etc.) before user notification preferences; typed `GET/POST /api/web/notifications/preferences` is implemented in `server/tickets/handlers.py` and returns plain JSON preference fields instead of ORM rows; enabled external channels (`email`, `telegram`, `vk_teams`, provider-defined names) go through an injectable provider interface, are checked against optional provider `available_channels`, and write non-blocking delivery/skip audit events for sent, explicit disable of a globally enabled channel, unavailable and missing-provider outcomes while event-local `false` for a channel that was not globally enabled is treated as no-op config; `policy_action_dispatcher.py` executes SLA/OLA/workflow-owned escalation actions outside raw event payloads, resolves recipients such as queue leads, treats `escalate_to_queue_lead` as a queue-lead recipient action, creates `TicketNotification` rows, and writes idempotent `policy_action_dispatched` / `external_notification_delivery` audit events |
| `server/tickets/evidence_service.py` | Ticket evidence source service: collects ticket-scoped operation/playbook/artifact/worklog/approval/chat/observer-trace candidates, rejects unrelated device-wide operations as accepted sources, links candidates into structured `ticket_evidence_items`, and updates verify/reject/archive evidence status with source/fact/verification metadata. |
| `server/tickets/purge_service.py` | Admin-only irreversible ticket purge service for `POST /api/web/admin/tickets/purge/preview` and `/purge`: normalizes bounded ticket id batches, reports related FK/non-FK counts, blocks active operations, active Remote Assist and parent tickets with non-selected children, deletes non-FK ticket-owned rows/files explicitly, and deletes `tickets` last so FK cascades own the remaining cleanup. |
| `server/tickets/closure_policy.py` | Executable resolution gate for request-template/effective closure policy: validates resolution code, public/internal summaries, worklog, accepted evidence, module operation logs, approval evidence and official passport requirements; rejected/archived/superseded evidence does not satisfy closure, official passport closure treats current transition summaries as pending facts without blocking on reporting-only `automated_checks`/`approvals` sections when no module run or approval policy applies, and support-facing closure requirements include `passport_missing:<fact>` rows with fact key/recommended actions/candidate metadata |
| `server/tickets/smart_views.py` | Backend saved-view catalog, publication validator and matcher for support queue slices such as `sla_risk`, `ola_risk`, `unassigned`, requester replies, stale waits, waiting approval, diagnostics failed and `mass_incident_candidates` / "Похожие массовые обращения"; validates custom filter keys, safe field paths, sort directions and display columns before registry publication, then evaluates active custom smart-view JSON filters from the standalone registry for `GET /api/web/support/queue?smart_view=...`, whose typed response exposes `summary.smart_view_counts` for built-in and published views while `server/web_api/support_handlers.py` applies the selected custom view's saved sort before scope/status/query slicing |
| `server/tickets/sla_service.py` | Policy-aware SLA runtime: starts first-response/resolution timers from legacy policies or standalone `request_template.sla_policy`, evaluates start/pause/resume/stop conditions against triggers and optional workflow target status, normalizes live aliases such as `waiting_user`, `waiting_approval`, `ticket_resolved` and `ticket_closed`, emits SLA lifecycle/warning/breach events, and exposes policy metadata plus pause fields for ticket snapshots |
| `server/tickets/ola_service.py` | Policy-aware OLA runtime: resolves effective `request_template.ola_policy`, starts queue ack/processing timers from standalone targets or legacy `ticket_queue_ola_targets`, evaluates start/ack-stop/processing-stop/pause/resume conditions including legacy list aliases such as `ticket_resolved` / `ticket_closed`, stores source tracking in `custom_fields.ola_runtime`, emits OLA lifecycle/breach events, dispatches configured breach actions through `policy_action_dispatcher.py`, and exposes OLA policy metadata in ticket snapshots |
| `server/config.py` | Конфигурация, feature flags, таймауты SLA/operations/playbook |
| `server/runtime_control.py` | Канонический runtime-control слой для `systemctl`/`journalctl`, control-plane state, smoke/status/log filtering и unit-level lifecycle |

### Observer docs

- `server/docs/OBSERVER_LAYER.md` — полная модель observer-слоя, runtime, APIs, UI и быстрый путь диагностики.
- `server/docs/OBSERVER_AUTHORING_RULES.md` — правила, как добавлять новый dangerous flow или instrumentation без деградации observer.

---

## 2. Структура каталогов (ключевые файлы)

### 2.1 WebSocket
| Файл | Назначение |
|------|------------|
| `server/websocket/agent_handler.py` | WS агентов `/ws`: transport-loop + wiring `AgentMessageRouter`; compare-safe disconnect cleanup и `outbox_items_batch` routing |
| `server/websocket/agent_services.py` | `AgentMessageRouter` + сервисы handshake/ack/result/outbox/agent-command; batched outbox ingest reuses the per-item pipeline and flushes ACK/NACK once per WS batch; runtime outbox dedupe marks ids only after ACK/non-retryable terminal outcomes, so retryable NACKs can be retried with the same `outbox_id`; ACK/NACK decisions write redacted durable v2 `agent_runtime_audit` proof for Observer ACK/persistence correlation |
| `server/websocket/agent_handshake.py` | Полная логика `handle_handshake` (Protocol V3 auth/register/capabilities), canonical `machine_id`/`install_id` metadata, controlled reprovision токена на уже известный `device_id`, runtime `connection_id` per successful handshake и supersede-close старого websocket того же `device_id`; toolset refresh compares the handshake hash against the pre-upsert device hash/snapshot ref so changed toolsets enqueue `list_tools`; startup handshake can enqueue `agent_handshake_auto_update` when an older installed agent is below the newer assigned rollout and no active/same-failed update blocks it |
| `server/websocket/agent_command_result.py` | Thin compatibility wrapper (deprecated-internal) |
| `server/websocket/command_result_components.py`, `server/websocket/agent_services.py` | Компоненты command_result pipeline: normalizer/lifecycle/future/artifact/event publisher + side effects для `install_module_package`, `list_installed_modules`, `list_tools` and `inventory.collect` and `presence.collect` (module install inventory sync, toolset snapshots, `device_inventory_snapshots` persistence and `device_presence_snapshots` persistence); future resolver сначала смотрит state-level waiter registry, terminal lifecycle для операций с `playbook_run_id` продвигает playbook step/run через `advance_after_terminal`, and ToolResponse `status=partial` is preserved as partial in ticket events while the operation lifecycle becomes terminal failed rather than successful |
| `server/websocket/agent_outbox_ingest.py` | Thin compatibility wrapper (deprecated-internal) |
| `server/websocket/outbox_ingest_components.py` | Компоненты outbox pipeline: envelope validator, persistence c requester-name enrichment для `chat_message`, ACK/NACK decision, post-commit publish; missing `trace_id` is a validation NACK with fallback server trace id; `module_state_changed` здесь синхронизирует `device_modules` из snapshot и затем запускает reconcile; `tools_changed` / `module_state_changed` ставят debounced `list_tools`, чтобы toolset snapshots сходились после agent-side module lifecycle events, and both events trigger idempotent Agent Recipe Runner dependency resume checks |
| `server/websocket/job_event_persistence.py` | Best-effort `persist_job_event` в `job_events` |
| `server/websocket/contexts.py` | Контексты `AgentConnectionContext`, `EnvelopeContext` |
| `server/websocket/ui_handler.py` | WS UI `/ws_ui`: `ui_hello`, `run_tool`, ticket/device/chat subscriptions; новый web-layer теперь допускает auth не только по raw `token` в `ui_hello`, но и по httpOnly cookie `pc_client_web_session`, а live-only подписки умеют `skip_catchup` для React realtime bridge |
| `server/websocket/protocol.py` | Отправка ACK/NACK/command, trace_id; ticket-bound `send_ws_command` теперь пытается закрепить canonical `observer_root_trace_id` тикета до создания operation/outbox entry, copies caller params before consuming `_operation_id`, а sync `wait_for_result=True` registers the waiter before device dispatch wakeup in `StateManager.pending_command_futures` для reconnect-safe ожидания `command_result` |
| `server/websocket/device_outbox_sender.py` | Dispatch runtime: `poll` и `sharded` (`DeviceReadyQueue`, shard workers, DB lease claim, reconcile); send failures теперь синхронизируют `operations.retry_count`, пишут runtime audit (`command_retry_scheduled` / `command_delivery_failed`) и завершают operation явным `DELIVERY_RETRY_EXHAUSTED`, когда outbox исчерпал retries |
| `server/app/repos/device_outbox_repo.py` | Репозиторий server-side command outbox; pending drain order теперь lane-aware (`cancel_operation` -> update -> control/health -> FIFO по `created_at`) |
| `server/websocket/validator.py` | Валидация событий, device binding |
| `server/websocket/modules_sync.py` | Синхронизация модулей с UI/агентом |
| `server/websocket/ui_publisher.py` | Публикация событий в UI |
| `server/websocket/command_result_parser.py` | Разбор command_result |

### 2.2 API (HTTP)
| Файл | Назначение |
|------|------------|
| `server/api/events.py` | Replay: ticket_events, device_events, ticket_messages |
| `server/api/commands.py` | send_command, check_functions, smoke_run |
| `server/api/operations.py` | Lifecycle операций, consent/cancel/retry; retry endpoints revalidate ticket/device/auth/policy/replay context, create linked operations by `retry_of_operation_id`, and create `waiting_consent` retries for consent-required tools without dispatching before approve |
| `server/api/admin.py` | admin_run_tool и др. |
| `server/api/protocol.py` | Endpoint протокола |
| `server/web_api/` | Typed web-facing boundary для нового `webapp/`: `session_handlers.py`, `support_handlers.py`, `admin_handlers.py`, `reports_handlers.py`, `settings_handlers.py`, `realtime_handlers.py` и DTO-модели в `dto/*`; `session_handlers.py` обслуживает `POST /api/web/session/login`, feature-flagged account-only `POST /api/web/session/register`, `POST /api/web/session/logout`, `GET /api/web/session/me` через httpOnly cookie flow; registration creates role `user`, validates password/repeat/duplicate login and optional registration device-link code, but does not set a cookie or create an active device binding, `realtime_handlers.py` отдаёт typed transport bootstrap `GET /api/web/realtime/bootstrap` для `ws_ui` bridge (session-cookie auth, `ui_hello`, channel contracts `support.queue` / `ticket.stream` / `admin.devices` / `tech.feed`), `support_handlers.py` уже даёт queue/detail/action routes `GET /api/web/support/bootstrap`, `GET /api/web/support/workspace/summary`, `GET /api/web/support/queue`, `GET /api/web/support/tickets/{ticket_id}`, aggregate `GET /api/web/support/tickets/{ticket_id}/workspace`, `GET /api/web/support/tickets/{ticket_id}/tools`, `POST /api/web/support/tickets/{ticket_id}/messages`, `POST /api/web/support/tickets/{ticket_id}/status`, typed mutation aliases `POST /api/web/support/tickets/{ticket_id}/assign|queue|priority|reroute`, `POST /api/web/support/tickets/{ticket_id}/tools/run` и typed queue counters для SaaS-style ticket list, а detail payload теперь нормализует `request_form_summary` в отдельный блок `request_form` для `/app/support`; aggregate workspace additionally returns selected-ticket-only compact `inventory_context` (device/agent/latest inventory freshness/binding/refresh signals) to avoid queue-row inventory fanout; support write routes enforce effective RBAC through `can()` for `ticket.status.change`, `ticket.queue.change`, `ticket.assign`, public/internal comments, passport management, ticket playbook launch and tool launch, including `module.tool.run.low_risk` / `module.tool.run.high_risk`; support operation snapshots expose observer trace relation metadata (`ticket_root`, `operation_child`, `retry_child`, `playbook_child`, `unknown`) plus root/operation trace URLs and retry lineage for `/app/tickets`; `admin_handlers.py` теперь кроме bootstrap обслуживает `GET /api/web/admin/devices` с typed inventory устройств, identity source summary, hostname duplicate warnings, cleanup candidate counters, status/query filters и rollout summary, `POST /api/web/admin/devices/cleanup_env_duplicates` для безопасного archive env_uuid-дублей через `DevicesRepo.archive_device()`, `GET /api/web/admin/modules` с family-level registry overview, preferred-version rollout settings, validation/preflight labels и file-availability summary, `GET /api/web/admin/forms/current` + `POST /api/web/admin/forms/save` для typed request-form catalog builder, `POST /api/web/admin/forms/route-preview` для preview form-aware routing and `POST /api/web/admin/forms/process-preview` для full side-effect-free ticket process preview, `GET /api/web/admin/helpdesk-model/policies`, `POST /api/web/admin/helpdesk-model/request-templates/publish-from-form` and `POST /api/web/admin/helpdesk-model/request-templates/republish-legacy-forms` для standalone реестра шаблонов/политик и migration/backfill старого `request_forms`, and `GET/POST /api/web/admin/playbooks/*` для low-code диагностических плейбуков, а также admin modules action endpoints `PATCH /api/web/admin/modules/rollout_settings` и `PATCH /api/web/admin/modules/{module_name}/preferred`, чтобы React-панели `Реестр модулей`, `Конструктор форм заявок` и `Плейбуки` могли работать без legacy shell; device-scoped update boundary остаётся `GET /api/web/admin/devices/{device_id}/updates` и `POST /api/web/admin/devices/{device_id}/updates/run`, включая Linux target/version surfacing, а typed observer surfaces `GET /api/web/admin/observer/quick`, `GET /api/web/admin/observer/traces` и `GET /api/web/admin/observer/traces/{trace_id}` продолжают кормить device-scoped quick summary, список трасс и drilldown span/error detail в `/app/admin` без прямой зависимости React от legacy `/api/admin/tech/traces*`; `reports_handlers.py` агрегирует реальные helpdesk-метрики для `GET /api/web/reports/summary` (backlog, SLA, reopen rate, daily trend, request kinds, top queues/requesters, recent tickets) и теперь подписывает `request_kind` по текущему preferred form pack, а `settings_handlers.py` собирает typed settings snapshot для `GET /api/web/settings`, включая split capabilities `settings.view`, `settings.manage_queues`, `settings.manage_routing`, `routing_builder` catalog из текущих форм и `ticket_settings` lifecycle/governance/operational flags для вкладки `/app/settings` → `Тикеты`, после чего web shell пишет обратно через `/api/web/settings/*` alias-маршруты в существующий admin-config backend (`admin_config_handlers.py`) без фронтенд-моков; typed detail payload для support включает chat + `tool_call_started` / `tool_call_result` timeline entries, observer root trace metadata и device-bound tool inventory/run surface для `/app/support`, а bootstrap-ответы рекламируют observer capability links и forms-builder feature для `/app/admin`; legacy `/api/tickets/create` and public create accept `request_template_key` as a request-template-aware alias for `form_key` |
| `server/web_api/registry_handlers.py` | Typed registry endpoints: `GET /api/web/admin/registry` для страницы `/app/admin/registry`, `POST /api/registry/profile` для agent GUI profile sync, authenticated `GET /api/registry/options` для компактных picker-каталогов agent/user/support/admin без доступа к admin-only snapshot, `GET /api/registry/agent/registration-form` для side-effect-free schema/field формы регистрации агента, `GET /api/registry/agent/account-state` for the non-modal agent account gate, server-side requester account-session endpoints (`/api/registry/agent/account-sessions/*`, `/api/registry/agent/account-login-requests/*`, `/api/web/admin/registry/account-login-requests/*`), DB-backed agent browser pairing endpoints `POST /api/registry/agent/browser-pairings` + `GET /api/registry/agent/browser-pairings/{pairing_id}`, web-authenticated manual-code lookup `POST /api/web/registry/browser-pairings/lookup` with minimal redirect metadata plus best-effort `requester_web` Observer trace `source=device_linking` / `event_type=browser_pairing_lookup_succeeded`, and web-authenticated browser confirmation endpoints `GET /api/web/registry/browser-pairings/{pairing_id}` plus `POST /api/web/registry/browser-pairings/{pairing_id}/login/confirm|registration/confirm`; registration confirm writes `event_type=device_link_request_created`, admin claim approve/reject writes `event_type=device_link_request_approved|device_link_request_rejected`, admin owner transfer writes `event_type=device_link_owner_transferred`, and admin bind-person/revoke/shared/responsible writes `source=registry_binding` with `event_type=registry_binding_created|registry_binding_revoked` using normalized route patterns and no raw pairing id/code/token, claim id, binding id, session id, login request id, rejection reason, transfer reason or registry binding reason in Observer payloads. Admin Registry Management Center actions include `/api/web/admin/registry/devices/{device_id}/bind-person|transfer-owner|shared-users|responsible`, read-only preview endpoints for transfer-owner, people/location/department merge and bulk operations, global `/api/web/admin/registry/account-sessions`, account-session revoke/timeline, person create/update including controlled work-context metadata (`position`, `workplace_label`, `internal_extension`, `manager_person_id`), people merge, identity add/verify/remove, locations/departments CRUD/archive/merge, policies, bulk actions, CSV export, import preview/apply with required matching `preview_id`, quality issue ignore/snooze/resolve, and generic timeline endpoints; dangerous apply responses expose normalized `operation_id`/`status`/`summary`/`items`/`events` reports for transfer, merge, bulk and import while keeping legacy domain fields; person/identity/quality mutations write `registry_admin_events` with before/after payloads for controlled person metadata, and the generic timeline endpoint normalizes admin, registration and account events for device/person/binding/session/claim drawers with source, actor, reason, related entities and changes; account-state is admin/support/agent only, validates existing device id and forbids user role |
| `scripts/registry_workflow_smoke.py` | Live Linux-stand Registry Management Center workflow smoke. Issues short-lived admin/agent tokens through `AuthService`, drives admin/agent HTTP APIs for person identity, bind, shared/responsible, transfer owner, people merge and location/department merge flows, then verifies `device_user_bindings`, derived `registry_assets`/`device_inventory_bindings`, registration/admin/account events, account-session revocation and ticket account-access invariants through the database. |
| `server/registry/registration_form_service.py` | Code-defined fallback form contract `agent_device_registration` plus lightweight registry options payload for agent registration. GET формы не создаёт registration claim и не ходит через ticket create flow; submit/confirm остаются в `/api/registry/agent/profile` и `/api/registry/agent/claims/{claim_id}/confirm`. |
| `server/device_operations/service.py`, `server/web_api/device_operations_handlers.py`, `server/web_api/dto/device_operations.py` | P1 Device Operations read model: `GET /api/web/admin/device-operations/{device_id}` and query fallback compose a compact admin/support-only device workspace from real device registry/card fields, inventory latest snapshot/binding/refresh runs, agent version/update signals, module desired/actual reconcile, outbox, recent operations, Observer traces, provisioning/auth state and Remote Assist availability. The handler stays read-only and must not bypass Inventory, Agent Updates, Modules, Observer or Remote Assist domain permissions/consent workflows. |
| `server/access_control/` | Server-owned RBAC catalog and resolver для нового Access Control Center: permission codes, Russian labels, risk groups, built-in role defaults, workspace derivation, `permissions_version`, `can()` helper and effective-access calculation across role defaults + group grants. |
| `server/app/repos/access_control_repo.py` | DB-backed access groups repository: CRUD `access_groups`, membership grants, permission grants, group queue grants and append-only `access_audit`. |
| `server/web_api/access_handlers.py` | Typed admin access endpoints `GET /api/web/admin/access/catalog|summary|effective|audit`, `POST /api/web/admin/access/users/{user_login}/password`, `POST/PATCH /api/web/admin/access/groups*`, `PUT /groups/{group_id}/permissions|members|queues`; питают `/app/admin/access` пользователями из `ui_users`, очередями, access groups и effective permissions без raw JSON authoring. Password reset accepts only the new password, validates policy, hashes server-side and never returns or requires the current password. |
| `server/tech/handlers.py` | Техпанель `/api/admin/tech/*`: overview/alerts/logs, русифицированный аудит агентов/пользователей, data-quality alerts (`inventory_env_uuid_duplicates`, `inventory_devices_without_location`, stale agents). The latter aggregate is read only through `RegistryPort.inventory_quality()`; `inventory_quality.devices_without_location` stays numeric and typed Registry unavailable/invalid states are redacted in `inventory_quality.source_state`. Also includes drilldown по агенту (`/agents/{device_id}/timeline`), observer quick diagnosis (`/observer/quick`), observer correlation search (`/observer/search`) and Codex-friendly diagnostics bundle (`/diagnostics/bundle`), trace/signature/degradation search (`/traces`, `/signatures`, `/degradations`, `/traces/rebuild`, `/traces/runtime`), observer settings API (`GET/PATCH /api/admin/settings/observer`), bounded/compact agent action trace rows and opt-in `sync_agent_actions=1` materialization в first-class observer spans, быстрые диагностические actions, lifecycle тикета (`milestone_rail`, `sla_lane`, ссылки ticket/device/operation), dismiss endpoint `/api/admin/tech/dismiss`, suppression шумных UI WebSocket alert и удаление log/alert из панели |
| `server/tech/snapshot.py` | Read-only Tech Panel v2/v2.1 read model для `GET /api/web/admin/tech/snapshot`: агрегирует config policy including `APP_ENV`/legacy `PILOT_STAND_MODE`, PostgreSQL overview health, agent connection policy, devices/agents, below-baseline device list, operations/outbox, scheduler/watchdog runtime, inventory scheduler duplicate-task details, auth query-token attempt counter, audit counters, log-buffer, release/smoke/backup/restore marker files и safe React links. Readiness gates: DB persistence/reachability, auth fallback, query token, HTTPS/WSS, session cookie flags, controlled connection policy, migrations marker from release evidence, backup/restore drill markers, inventory scheduler health, agent baseline and business smoke. `APP_ENV=pilot|prod` and `PILOT_STAND_MODE=true` both make unsafe gates blocking. Agent baseline considers only real post-handshake devices with numeric `agent_version`; pending provisioning stubs and non-numeric canaries are excluded from below-version counts. Не показывает секреты и не запускает shell/restore/control-plane actions из web request. |
| `server/tech/locator.py` | Tech Panel v2.1 quick locator read model для `GET /api/web/admin/tech/locate?q=...`: ищет ticket id/code/title, device id/hostname, operation id, UUID or string Observer trace id, and bounded problem log matches; возвращает grouped root-cause diagnosis, severity/signals/context и safe links на `/app/tickets/{id}`, `/app/admin/operations/{operation_id}`, Device Operations, Observer, approvals, inventory и agent updates. Operation detail UI читает read-only web-session alias `GET /api/web/admin/operations/{operation_id}` из `server/api/operations.py`. Короткие запросы не запускают broad scans; operation/log/error payloads redacted before response. |
| `server/control_plane.py` | Внешний runtime API `/api/control/server/*`: status, full journal logs, download logs, lifecycle actions `start/stop/restart/smoke` |

### 2.2.1 Tools / единый путь run_tool
| Файл | Назначение |
|------|------------|
| `server/tools/service.py` | Канонический вход `ToolExecutionService.run_tool` (совместим с `ToolService`): pre-start event `tool_call_started`, version-aware auto-install/auto-update module pack по preferred version, фиксация desired state перед `run_tool`; после успешного auto-install ставит follow-up `list_installed_modules` + `list_tools` для convergence inventory/toolset snapshots |
| `server/tools/handlers.py` | HTTP handlers, которые делегируют run_tool в `ToolExecutionService`; `/api/tools/run` по умолчанию возвращает async `202 Accepted` + `operation_id` + `poll_url`, sync path допускается только через явный `wait=1`; agent-token requests сверяют `device_id` с `AuthContext.actor_id` до policy/dispatch и отклоняют чужой device context через 403 `DEVICE_CONTEXT_MISMATCH`; все роли сверяют `ticket_id -> device_id` до operation/device_outbox dispatch и отклоняют mismatch через `UNKNOWN_TICKET`/`DEVICE_MISMATCH` |
| `server/inventory/service.py`, `server/inventory/scheduler.py`, `server/presence/service.py`, `server/web_api/admin_inventory_handlers.py`, `device_inventory_snapshots`, `device_inventory_bindings`, `device_inventory_binding_history`, `device_inventory_refresh_policies`, `device_inventory_refresh_runs`, `device_inventory_bulk_operations`, `device_inventory_bulk_operation_items`, `device_binding_suggestions`, `device_presence_snapshots`, `device_presence_daily_summaries` | Inventory persistence/projection and lightweight CMDB: unwraps `inventory.collect` ToolResponse-style results, stores raw snapshots plus normalized summary/hash including the new bounded `processes.items` payload, resolves latest/history per device, applies `ToolPresentationOverrideService` so device-card API returns module default/effective presentation schema and `device_card_slots`, stores lightweight device binding metadata with change history, imports/exports binding CSV with formula-injection protection, exports fleet inventory CSV/XLSX, aggregates `/api/web/admin/inventory/dashboard`, builds attention/department/building reports, tracks bulk refresh operations/items, records manual/scheduled refresh attempts, resolves refresh policy, creates agent-profile binding suggestions for IT review, persists `presence.collect` snapshots/daily summaries, and dispatches inventory/presence collection only through `ToolExecutionService.run_tool`; fallback descriptors come from pure `shared/builtin_tool_descriptors.py`, not agent collector implementation imports |
| `server/diagnostics/` | Diagnostic capability foundation: `capability_registry.py` projects agent builtin/managed tools plus server builtin, server connector, observer, remote assist and manual providers and preserves top-level `presentation_schema` in capability descriptors; `presentation_overrides.py` owns server-side Tool Output Presentation Builder v1 validation/storage resolution, keeps module `presentation_schema` immutable, and projects `effective_presentation_schema`, `presentation_schema_source` and `has_presentation_override` from `tool_presentation_overrides`; `provider_config.py` persists provider integration config/credential refs, feeds readiness maps and passes internal runtime credential refs into connector runs; `readiness.py` computes ticket-scoped readiness from device/platform/module install state, desired state, dependency/preflight flags, persisted integration config, credentials, mapping, policy and RBAC context, returning backward-compatible `readiness` statuses plus stable `reason_code` and explicit action ids; `execution_router.py` returns a target-specific execution envelope, routes `agent_builtin` and `agent_managed_module` through existing `ToolExecutionService.run_tool`, routes server-builtin/observer/manual/server-connector capabilities through server providers, routes remote assist through the existing session service, and blocks not-ready capabilities before dispatch when readiness is supplied; `providers/server_builtin.py` runs `server.dns.resolve` / `server.http.request` with `operations` rows and no `device_outbox`; `providers/zabbix_provider.py` is the bounded Zabbix JSON-RPC server connector for `zabbix.problems.lookup`, `zabbix.host.health` and `zabbix.item.history`; `handlers.py` exposes `GET /api/diagnostics/capabilities`, `GET /api/tickets/{ticket_id}/diagnostics/capabilities`, `POST /api/tickets/{ticket_id}/diagnostics/capabilities/{capability_id}/run`, admin provider config routes `/api/diagnostics/providers/configs*`, web-session aliases `/api/web/admin/diagnostics/providers/configs*`, Capability Studio aliases `/api/web/admin/capabilities*`, and presentation override endpoints `GET|PUT|DELETE /api/web/tool-presentations?tool_id=...` |
| `server/app/services/operation_service.py` | Ветка consent: `approve_consent()` переводит operation `waiting_consent -> queued` и enqueue `run_tool` в `device_outbox` (исполнение после явного approve) |
| `server/websocket/protocol.py` | Транспорт: `send_ws_command(..., wait_for_result=...)` enqueue в `device_outbox` + опционально ожидание `command_result`; caller params are copied before internal `_operation_id` handling |

### 2.3 БД и репозитории
| Файл | Назначение |
|------|------------|
| `server/app/db/engine.py` | Async engine/session; DB pool tunables via `PC_CLIENT_DB_POOL_SIZE`, `PC_CLIENT_DB_MAX_OVERFLOW`, `PC_CLIENT_DB_POOL_TIMEOUT_SEC`, `PC_CLIENT_DB_POOL_RECYCLE_SEC` |
| `server/app/db/migrations/versions/*.py` | Alembic миграции |
| `server/app/repos/device_outbox_repo.py` | device_outbox (pending/sent/delivered); exposes latest outbox lookup by `operation_id` for audited policy-aware retry replay |
| `server/app/repos/device_events_repo.py` | События устройства |
| `server/app/repos/ticket_events_repo.py` | События тикета; canonical observer trace resolution для ticket-bound lifecycle events, lazy bootstrap `tickets.observer_root_trace_id` и связь server-originated событий с operation trace/root trace |
| `server/app/repos/operations_repo.py` | Операции; includes manual retry budget increment and `retry_of_operation_id` lineage |
| `server/app/repos/modules_repo.py` | Модули в БД |
| `server/app/repos/device_modules_repo.py` | Установленные модули на устройстве |
| `server/app/repos/device_desired_modules_repo.py` | Желаемое состояние модулей |
| `server/app/repos/auth_tokens_repo.py`, `server/websocket/agent_handshake.py` | Токены устройств/UI и public-session токены requester-доступа; проверка, revoke/rotation old active agent tokens on new issue, controlled rebind токена агента по canonical `machine_id` only after target-device fingerprint proof and expected original token binding, so a rejected fingerprint mismatch leaves the token bound to the original provisioning/legacy device; public ticket session verification denies `closed` / `canceled` ticket states even if the session row is still unrevoked |
| `server/app/repos/devices_repo.py` | Реестр устройств: handshake upsert, last_seen/toolset update, soft-delete/архивирование устройства с сохранением истории, отзывом token и остановкой pending runtime/provisioning; restore архивной записи очищает только archive marker и не восстанавливает token/session/runtime state |
| `server/app/repos/connection_requests_repo.py` | Запросы на подключение устройств, политика (reject_all/accept_all/manual) с безопасным default `manual` при отсутствии явной настройки |
| Остальные repos | artifacts, notifications, playbook, tickets, agents, jobs, ui_users и др. — см. `server/app/repos/`. |

### 2.4 Сервисы (app/services)
| Файл | Назначение |
|------|------------|
| `server/app/services/operation_watchdog.py` | SLA операций |
| `server/app/services/operation_service.py` | Жизненный цикл операций |
| `server/app/services/ticket_sla_watchdog.py` | SLA тикетов: warning-before, breach, reminder events; also runs OLA breach checks and approval-policy timeout/reminder/escalation processing |
| `server/app/services/ticket_auto_close_watchdog.py` | Контроль policy для resolved; закрытие только после подтверждения пользователя |
| `server/app/services/playbook_engine.py` | Движок playbook execution: создаёт runs/step_runs, запускает module auto-install preflight перед tool-backed шагами, после успешного DB-backed preflight не блокируется stale/no toolset snapshot и сразу enqueue-ит `run_tool`, пишет `stage=module_install` / `stage=capability_gate` failures и отправляет diagnostic tool calls через support actor |
| `server/playbooks/catalog.py` | Каталог диагностических low-code блоков и шаблонов сценариев (`system.collect`, `ip_address.get_ip`, `diag.logs.collect`) + нормализация draft в PlaybookStep и self-healing manifest v2; поддерживает `capability_id` и пишет `required_capabilities` поверх legacy `required_tools` |
| `server/playbooks/tool_catalog.py` | Нормализация atomic command manifests для playbook UI/runtime: source, module owner, install policy, platforms, min agent version, params/output schemas, presets, error codes |
| `server/playbooks/form_triggers.py` | Автозапуск опубликованных диагностических плейбуков при создании тикета из request-form trigger и сбор `facts_package` из ответов формы |
| `server/app/services/playbook_scheduler.py` | Планировщик playbook |
| `server/app/services/module_reconcile_scheduler.py` | Реконсиляция модулей |
| `server/app/services/artifact_service.py` | Артефакты |
| `server/observer/service.py`, `shared/redaction.py` | Trace overlay projection/search: `observer_traces`, `observer_spans`, `observer_span_links`, `observer_error_occurrences`, `observer_error_signatures`; observer v3 умеет canonical ticket-root trace, synthetic `ticket.lifecycle` span, pushed `agent_observer_events`, `playbook_run`/`playbook_step_run` spans, `module_reconcile`, `web_auth`, `observer_runtime`, materialized `agent_action` spans/links, redacted attrs/details export через non-mutating nested/custom-mapping redaction, free-text query correlation across trace/ticket/operation/device/tool/module/signature/playbook/route fields and degradation queries (`min_duration_ms`, `min_retry_count`, timeout/retry/slow rates); ticket observer summary also returns compact support-facing root trace URL/status/kind, health label, latest error, top ticket-local signature and compact related/active/error traces for `/app/tickets`; request-side `_ensure_projected`/`rebuild_traces` теперь завершают source read-транзакцию и materialize traces в отдельных short-lived projection sessions, чтобы tech/support polling не держал idle-in-transaction соединения во время trace-lock wait |
| `server/observer/runtime.py` | Background refresh для observer traces: сканирует committed source rows including agent telemetry and playbook rows, enqueue-ит hot ticket-root traces, делает исторический backfill missing projections без ручного rebuild/search, emits bounded `observer_runtime` self-health audits when degraded, and obey-ит DB-backed retention/sampling settings |
| `server/observer/runtime_snapshot_writer.py` | Startup-owned persisted runtime snapshot writer: serializes bounded aiohttp process health, connected-agent WS evidence without raw websocket objects, runtime statuses and MCP reload metadata into `server_runtime_snapshots` for read-only MCP/debug consumers. |

### 2.5 Доменные модули (handlers + service)
| Каталог/файл | Назначение |
|--------------|------------|
| `server/tickets/` | handlers, `create_flow.py`, service, assignment_service, sla_service, workflow_service, public_queue_handlers, public_ticket_handlers, admin_config_handlers и др.; `create_flow.py` держит единый DB-first путь создания для `/api/tickets/create`, WS `chat_raise` и legacy `/api/chat_raise`, accepts server-backed `requester_account.session_id` from the agent GUI account gate, validates the server-side account session against device id/status/token and current binding activity, resolves confirmed binding (including shared/responsible active bindings) and verified other-account context on the server, marks pending-registration tickets without blocking, and stores `custom_fields.requester_account_context.created_from_other_account=true` plus active registered-device binding/person context for verified other-account tickets without creating registration claims or changing active binding; browser requester create can also persist a non-primary confirmed binding only when the requester wrapper has already verified device ownership and set `requester_account.validation=web_requester_identity_resolved`; legacy full requester_account payloads remain unverified; `assignment_service.py` и `ticket_events_repo.py` считают состав очереди обязательным для назначения и автоназначения; `request_template_submission.py` is the shared form submission resolver for authenticated create, create-preview and public create, keeping legacy `request_forms` compatible while also validating standalone registry `request_template` + `form_schema` definitions by `request_template_key`; `handlers.py` also exposes authenticated side-effect-free `POST /api/tickets/create/preview`, which validates the request-template form payload, overlays effective registry policies, computes priority/routing and first-response/resolution due dates for the PC-agent wizard before ticket creation, and passes only the authenticated actor role into manual priority override gates; `form_catalog.py` treats request forms as request templates, backfills missing `ticket_type`, attaches editable default priority question fields/`field_roles`, preserves priority-policy fact keys from submitted payload, supports extended intake field types including `multi_select`, and carries template context (`category_id`, `service_id`, `subcategory_id`, `default_queue_id`, `sla_policy_id`, diagnostic/field/policy JSON including notification policy and normalized `on_behalf_policy`); authenticated and public create handlers accept `request_template_key`, enforce priority `manual_override` role/reason gates from server-side actor context, and persist normalized `diagnostic_consent` when a diagnostic policy needs requester-device consent; `routing_service.py` first executes `request_template.routing_policy.rules` over ticket/template/form facts, then global routing rules, then template default queue, then `servicedesk_l1` fallback; template routing actions can set queue/assignee, priority boost/minimum priority, SLA override, approval policy, tags and suggested-playbook metadata, with loop guards through `routing_lock`, `do_not_reroute_if_assignee_locked` and `max_auto_reroutes`; `notification_service.py` executes request-template recipient policy before notification preferences; `smart_views.py` provides backend support queue slices with Russian labels for user-facing срок risks; `handle_ticket_get` поддерживает forward catch-up (`since_event_id`) и reverse pagination (`before_event_id`, `limit`, `has_older`, `next_before_event_id`) для агентского ticket chat; `handle_ticket_get_observer_summary` отдаёт support-facing trace summary по `GET /api/tickets/{ticket_id}/observer`, где summary counts считаются по полному trace-set тикета, а signatures несут и глобальный `occurrences_count`, и ticket-local `ticket_occurrences_count`; `admin_config_handlers.py` ведёт helpdesk-admin settings API: очереди, состав очередей, routing rules, SLA policies, targets/matrix (GET/PUT), calendars, OLA targets, audit, and now enforces split settings permissions on write aliases (`settings.manage_queues` for queue/member/OLA mutations, `settings.manage_routing` for routing/SLA/calendar/resolution mutations) with typed denial payloads containing `required_permission` |
| `server/tickets/statuses.py`, `server/tickets/visibility_policy.py`, `server/tickets/notification_service.py`, `server/tickets/smart_views.py`, `server/tickets/workflow_service.py`, `server/tickets/workflow_profiles.py`, `server/tickets/priority_policy.py`, `server/tickets/sla_service.py`, `server/tickets/calendar_engine.py`, `server/tickets/approval_policy.py`, `server/tickets/closure_policy.py` | Product lifecycle: `new`, `queued`, `assigned`, `in_progress`, requester/internal/vendor/approval waits, `scheduled`, `resolved`, `closed`, `canceled`; `triaged` remains a legacy alias. `workflow_profiles.py` makes process profiles configurable and loads editable profiles from `ServerConfig` key `ticket.workflow_profiles`; `workflow_service.py` selects the profile from `custom_fields.request_template.workflow_profile_id` when present, falling back to raw `ticket_type`, then enforces saved transition maps, the built-in invariant that `assigned` status requires both `queue_id` and `assignee_id`, and structured gates for fields, actor roles, public/internal comments (`required_comment` or `required_comment_type`), approved `ticket_approvals`, attached evidence, audit `log_fields`, notification action markers, explicit SLA pause/resume/stop markers, approval action markers and `trigger`/`auto` metadata before/after a transition applies. Workflow action execution writes `workflow_transition_actions`, `workflow_transition_action_results` and `workflow_transition_log_fields`, executing safe SLA/OLA/approval hooks where applicable and recording no-op/skipped high-risk actions instead of silently ignoring them. `apply_triggered_transition()` executes configured system triggers such as `requester_replied` before legacy fallback and records `workflow_trigger` in status audit payloads. Workflow updates `next_action_owner`, `requester_status`, `status_reason`, policy-aware SLA/OLA pause/resume hooks, SLA stop and `ticket_waits`, passing the transition target status to SLA/OLA pause checks and configured transition triggers into resume checks so standalone aliases such as `waiting_user` and `vendor_replied` match before the final `ticket.status` write; `visibility_policy.py` executes request-template/effective registry public status mapping and requester/support field visibility for serialized ticket payloads without changing internal `ticket.status`, including dotted-path redaction for nested custom fields and passport sections; `notification_service.py` executes request-template/effective registry recipient selection for in-app notifications while user notification preferences remain the final filter, recognizes canonical event aliases such as create/assign/wait/reply/approval approved/rejected/diagnostic completion, and audits external channel skips when a channel is disabled, unavailable or providerless; `smart_views.py` defines built-in support work slices and evaluates active published custom smart views consumed by `/api/web/support/queue`, including OLA-risk and mass-incident candidate counters in `summary.smart_view_counts`; `priority_policy.py` calculates process priority P0..P3 from configurable impact/urgency matrix rows, list modifiers and manual override policy, stores canonical computed/manual/effective priority fields, and emits requester-safe create-preview explanations plus optional `priority_overridden` audit payloads; `sla_service.py` selects SLA targets by process priority from either legacy `ticket_sla_policies` rows or standalone `request_template.sla_policy.targets`, parses numeric JSON durations with `m`/`h`/`d` units such as `45m`/`90m`/`2h`/`1d`, calculates due dates through `calendar_engine.py` for legacy calendars or embedded standalone calendar config, evaluates configured `start_conditions`/`pause_conditions`/`resume_conditions`/`stop_conditions`, and emits policy-code/version/source SLA lifecycle events consumed by observer/reporting; `approval_policy.py` creates active `ticket_approvals` from effective approver sources when tickets enter the waiting approval status, deduplicates requested/pending approvals, applies `any_one`/`all`/`sequential` mode semantics, emits `approval_reminder_due`/`approval_escalated`/`approval_timed_out` from `approval_policy.timeout`, enforces `require_comment_on_reject`, and blocks execution statuses until the required approved decision exists; typed support route `POST /api/web/support/tickets/{ticket_id}/approvals/{approval_id}/decision` persists approve/reject decisions for the current requested approver, advances the next sequential approval, writes `approval_approved` / `approval_rejected` audit events and runs notification policy recipients; `closure_policy.py` executes request-template/effective registry nested `before_resolved`, `evidence`, `requester_confirmation` and `allowed_resolution_codes` requirements for resolution code, public/internal summaries, worklog, priority evidence, operation logs after module/playbook use, approved approval evidence and requester confirmation before `resolved`, exposes support-facing closure requirements and applies `reopen_on_negative_feedback` when requester rejects a solution; `ticket_auto_close_watchdog.py` closes resolved tickets only when closure policy stores positive `auto_close_after_days` and pending requester confirmation has expired; `web_api/settings_handlers.py` exposes and saves the service-desk model surface for `/app/settings`, including typed `ticket_settings.request_templates`, workflow profile save diff and list/object-compatible calendar JSON from historical settings rows; `ticket_to_dict` exposes requester-safe public status labels, process priority and closure/evidence fields for support/requester UI. |
| `server/tickets/passport_service.py`, `server/tickets/evidence_service.py` | Deterministic assembler and source service for `Паспорт решения`: builds versioned requester/problem/object/checks/changes/approvals/evidence/user/internal/repeat sections from ticket facts, events, ticket-scoped operations and worklog; collects/link evidence candidates from operations/playbook context/artifacts/worklog/approval/chat/observer traces; maps candidates into missing-fact source previews; stores source counts/current counts/stale reasons for evidence and source-change detection; writes `passport_generated` and evidence audit events; powers typed support passport APIs and the `official_passport_stale` closure checklist. |
| `server/tickets/visibility_policy.py`, `server/tickets/requester_timeline.py` | Requester/public safety projection: ticket payloads with public/requester visibility use an explicit allowlist and timeline serializers emit requester projection fields instead of raw event payloads, preventing queue/device/requester ids, raw custom fields, policy/routing/SLA internals, token/session fields and public access code hashes from reaching public-token callers. |
| `server/agents/` | handlers, agent_builds_handlers, service; `handlers.py` также даёт admin-only архивирование устройства с очисткой live runtime-сессии |
| `server/modules/` | handlers, service, reconcile, verification, `workbench_service.py` для реконструкции editable draft и module-family workbench payload; preferred-version auto-rollout теперь через policy может переводить существующие установки в новый desired state |
| `server/tools/` | handlers, service (каталог инструментов, manifest) |
| `server/chat/` | handlers, service |
| `server/jobs/` | handlers |
| `server/uploads/` | upload/download handlers for artifacts; operation-bound agent artifact uploads are authorized by matching `operation_id + ticket_id + device_id`, while manual agent ticket uploads still require requester account-session context; download auth accepts web-session, agent and public-ticket tokens before `ArtifactService` visibility checks, and `Content-Disposition` uses safe ASCII `filename` fallback plus RFC 5987 `filename*` for Unicode originals. |
| `server/auth/` | handlers, admin_users_handlers, connection_request_handlers (запросы на подключение устройств, включая `DEVICE_ARCHIVED`, legacy `TOKEN_LIMIT_EXCEEDED` status/error_code и новый `DEVICE_FINGERPRINT_MISMATCH`), `device_fingerprint.py` (majority-match hardware proof для machine_id), middleware, service, `agent_token_service.py`, `connection_request_service.py`, password_service; legacy `handlers.py` содержит UI login/session endpoints, `middleware.py` дополнительно читает web session cookie `pc_client_web_session` для `/api/web/*`, `/api/registry/options`, `/api/notifications*`, admin tech/module/ticket-form bridges, а `service.py` теперь поддерживает config/in-memory fallback для локального UI session flow без PostgreSQL |
| `server/playbook_handlers.py` | Старт playbook run |
| `server/static_pages/` | `cutover.py` с operational policy для default-route switch, `handlers.py` для legacy login/admin/support/ticket/public_queue/help shell-файлов и общего `web_shared.js`, плюс `webapp_assets.py` для отдачи built React bundle: `/app/*`, `/assets/*`, `/favicon.svg`; direct route `/favicon.svg` теперь обслуживается тем же public-asset handler без обязательного `match_info`, чтобы browser shell не ловил `500` на favicon при cutover |
| `webapp/src/features/request-template-studio/*` | Request Studio no-code MVP components and models. `studio-model.ts` now overlays `studioDraft` onto the selected catalog/template item into one draft-aware `workingItem`, or creates a virtual draft item for the wizard path, so ProcessMap, BlockInspector, ReadinessPanel, FormPreviewPanel and SimulationPanel consume the same current form/policy/title/visibility state. Basic mode renders the focused block editor selected on the process map; advanced/expert modes keep broader diagnostic detail. |
| `webapp/src/features/service-catalog/*`, `webapp/src/features/forms-builder/*`, `webapp/src/features/policy-health/*` | Expert request-setup surfaces. Service Catalog owns catalog lifecycle/overrides/publication checks, Forms Builder owns detailed form/policy/smart-view/version editing, and Policy Health owns diagnostics/dry-run. All three now link back to `/app/admin/request-template-studio`, hide raw JSON behind collapsed expert details in the normal path, and should not be treated as mandatory basic setup routes. |

Auth security update 2026-05-23/24: `server/auth/handlers.py` keeps `POST /api/login` as admin-only audited manual token issue; `server/auth/connection_request_handlers.py` and `server/auth/connection_request_service.py` require `request_id` + `poll_secret` for manual polling, reject legacy pending approvals without `poll_secret_hash`, generate approved agent tokens only on a valid poll, and deliver them once without storing raw poll secrets or approved tokens; `server/app/repos/connection_requests_repo.py` keeps deprecated raw-token helpers as no-op and marks delivery only for matching `request_id + device_id + approved`; `server/auth/rate_limit.py` trusts `X-Forwarded-For` only for configured trusted proxies; `server/auth/service.py`, `server/app/repos/ui_users_repo.py`, and `server/auth/admin_users_handlers.py` fail closed on invalid roles and default new UI users to `user`; `server/auth/password_service.py` enforces the password policy for new/changed passwords.

### 2.6 Утилиты (модули/manifest)
| Файл | Назначение |
|------|------------|
| `server/utils/module_manifest.py` | Manifest contract и strict validation для typed tools: canonical semantic ids, aliases, contract/dependencies/redaction/resources blocks, reserved namespace governance; additive diagnostic capability blocks `execution`, `deployment`, `safety`, `evidence`, `artifacts` with backward-compatible defaults for old managed ZIP and builtin tools |
| `server/utils/module_preflight.py` | Preflight ZIP, manifest_json, validation_json; теперь жестко применяет observer contract guard к module ZIP, чтобы новые tool methods без mandatory breadcrumbs не публиковались |
| `server/utils/module_observer_contract.py` | AST-валидатор observer contract для `BaseCollector`-модулей: требует `self.trace_span("tool.entry", ...)` в каждом `@exposed_tool`, используется и в upload preflight, и в `scripts/verify_workspace.py` |
| `server/utils/module_builder.py` | Сборка пакета для POST /api/modules/create: legacy single-tool и multi-tool module packs с canonical semantic tool ids |
| `server/utils/module_storage.py` | Хранение модулей |
| `server/utils/tool_metadata_validation.py` | Валидация метаданных инструментов |
| `server/core/policy_engine.py` | Политики доступа |
| `shared/tool_contracts.py` | Shared contract layer для server+agent: ToolMetadata, ToolExecutionEnvelope, ToolError, artifacts/dependencies/redaction/resources vocabulary |

### 2.7 UI (статика)
| Файл | Назначение |
|------|------------|
| `server/admin.html`, `server/admin.js`, `server/admin_modules_workbench.html`, `server/admin_modules_workbench.js`, `server/admin.css`, `server/web_shared.js` | Админка и shared web-shell helpers (`authHeaders`, `responseToJson`, `escapeHtml`, `parseServerDate`, `formatDate`) для admin/support/ticket; вкладка `Модули` теперь разделена на подтемы `Разработка модулей` / `Список модулей` / `Редактор модулей` / `Модули на устройствах`, observer-блок техпанели ищет traces/signatures/degradations и умеет thresholds по duration/retry, а legacy queue fallback больше не подписывает `/ws_ui` на весь видимый список тикетов, чтобы `?legacy=1` не создавал burst из сотен `subscribe_ticket` |
| `server/control_plane.py`, `server/runtime_control.py` | Control-plane и runtime orchestration для техпанели: статус сервера (включая `started_at`/`uptime` из systemd и его timezone-offset timestamps вида `+05`), корректный `stop -> stopped/inactive` без ложного `failed`, health summary main API, полные логи (по умолчанию `Info+`), подтверждённые lifecycle actions с аудитом |
| `server/login.html`, `server/login.js`, `server/login.css` | Единая legacy-страница логина: выбор целевой роли (`admin` или `support`), POST `/api/ui_login` c `expected_role`, redirect в нужный shell |
| `server/support.html`, `server/support.js`, `server/support.css` | Отдельный support workspace: двухрежимный экран `Очередь` + `Тикеты`; runtime теперь опирается на `web_shared.js` для общих auth/date/html helpers, чтобы не дублировать базовый shell-код. Переключатель режимов собран в верхние twin-cards внутри workspace: слева компактная доска очереди с summary-плитками, встроенным scope-toggle и hover-подсказкой по новым ответам пользователя, справа компактный вход в рабочий тикет без дублирования длинной мета-строки. В `Очереди` основное место отдано доске тикетов, поиск/сортировка живут отдельной полосой ниже, а поиск сохраняет фокус при каждом вводе. В `Рабочем тикете` используются режимы preview/work/observe и встроенный ticket workbench (`Трасса`, `Контекст`, `Инструменты`, `Пайплайн`); observer summary тянется через `GET /api/tickets/{ticket_id}/observer`, а support drawer должен показывать ticket-local signature counts (`ticket_occurrences_count`) отдельно от глобального `occurrences_count`. Быстрые статусы `Ждём пользователя` / `Решено` делегируются в embedded composer тикета через postMessage bridge. Dev-only тестовая учётка support для локальных/browser проверок: `op1` / `1.Abcdef` (убрать перед production) |
| `server/ticket.html`, `server/ticket.js`, `server/ticket.css` | Основная рабочая область тикета: чат, reply-to баннер/ссылки на исходное сообщение, mark-read, подтверждение решения, редактирование профиля инициатора, очередь/исполнитель по составу очереди, SLA/OLA и ручной reroute по правилам; поддерживает `embed=1` для встраивания в support workspace и использует `web_shared.js` для базовых shell helpers |
| `server/public_queue.html`, `server/public_queue.js` | Публичная очередь со ссылками на requester-вход в тикет; renders only sanitized public projection (`ticket_code`, public position/status, queue code, wait bucket) and never requester identity, internal ids, urgency/importance or raw priority |
| `server/help.html`, `server/help.js`, `server/help.css` | Публичная страница requester: создание тикета, вход по коду, чат |
| `server/static_pages/` | Обработчики legacy shell-страниц и built React-ассетов для `/app/*` |

---

## 3. Быстрый поиск (ключевое слово → файлы)

Ниже — куда смотреть в первую очередь.

- **handshake** — `websocket/agent_handshake.py`, `websocket/agent_services.py`, `websocket/validator.py`, `docs/PROTOCOL_V3.md` (включая migration rebind legacy install-based token -> canonical `machine_id`; `client_kind=diagnostic_probe` is isolated from runtime online state and device_outbox dispatch)
- **outbox_ack, outbox_nack** — `websocket/agent_services.py`, `websocket/outbox_ingest_components.py`, `websocket/protocol.py`
- **run_tool** — `websocket/ui_handler.py`, `api/admin.py`, `tools/handlers.py`
- **device_outbox, DeviceOutboxSender** — `websocket/device_outbox_sender.py`, `app/repos/device_outbox_repo.py`, `config.py` (`DEVICE_DISPATCH_*`); retry exhaustion теперь коррелируется обратно в `operations` и `agent_runtime_audit`
- **outbox multi-instance gate** — `quality/outbox_multi_instance_prerequisites.json`, `quality/active_risks.json` TD-015, `server/tests/test_outbox_multi_instance_prerequisites.py`; horizontal dispatch is blocked until prerequisites pass
- **command_result** — `websocket/agent_services.py`, `websocket/command_result_components.py`, `websocket/command_result_parser.py`, `app/repos/operations_repo.py`
- **tool_call_started** — сервер создаёт до run_tool; идемпотентность: `docs/TOOL_CALL_STARTED_INVARIANT.md`
- **device_seq, agent_seq** — тип события только по ним; `websocket/validator.py`, `app/repos/device_events_repo.py`, `app/repos/ticket_events_repo.py`
- **ticket_events, device_events** — `api/events.py`, соответствующие repos
- **модули (install, desired, reconcile, preferred version, rollout policy, UI workbench)** — `modules/handlers.py`, `modules/service.py`, `modules/reconcile.py`, `modules/workbench_service.py`, `websocket/modules_sync.py`, `websocket/outbox_ingest_components.py`, `app/repos/device_desired_modules_repo.py`, `app/repos/module_rollout_repo.py`, `app/services/module_reconcile_scheduler.py`, `utils/module_manifest.py`, `utils/module_preflight.py`, `utils/module_observer_contract.py`, `utils/module_builder.py`
- **diagnostic capabilities (projection, readiness, provider routes)** — `diagnostics/capability_registry.py`, `diagnostics/readiness.py`, `diagnostics/execution_router.py`, `diagnostics/observability.py`, `diagnostics/evidence.py`, `diagnostics/providers/*`, `diagnostics/handlers.py`, routes `GET /api/diagnostics/capabilities`, `GET /api/tickets/{ticket_id}/diagnostics/capabilities` and `POST /api/tickets/{ticket_id}/diagnostics/capabilities/{capability_id}/run`; capability runs emit redacted lifecycle runtime-audit rows for observer root kinds `capability_run`, `server_connector_query`, `observer_query`, `manual_evidence` and `remote_assist`
- **diagnostic layer (overview, sessions, evidence, findings, bundle)** — `diagnostics/service.py`, `diagnostics/projection.py`, `diagnostics/sessions.py`, `diagnostics/findings.py`, `diagnostics/bundle.py`, `diagnostics/profiles.py`, `diagnostics/profile_runner.py`, `diagnostics/passport_bridge.py`, `app/repos/diagnostics_repo.py`, migration `074`, routes `GET /api/tickets/{ticket_id}/diagnostics/overview`, `GET /api/web/support/tickets/{ticket_id}/diagnostics/overview`, sessions/evidence/findings/bundle/profile endpoints, `POST /api/tickets/{ticket_id}/diagnostics/run-profile`, `POST /api/tickets/{ticket_id}/diagnostics/passport/attach-selected`; `/app/tickets` consumes `webapp/src/features/diagnostics/api.ts`
- **playbook / diagnostic scenarios** — `playbook_handlers.py`, `playbooks/catalog.py`, `playbooks/tool_catalog.py`, `playbooks/form_triggers.py`, `tickets/diagnostic_policy.py`, `app/services/playbook_engine.py`, `app/services/playbook_scheduler.py`, `app/repos/playbook_repo.py`, `web_api/admin_handlers.py`, `webapp/src/features/playbooks/*`, `server/docs/DIAGNOSTIC_PLAYBOOKS.md`; `tool_catalog.py` is the server source for normalized `output_contract` and derived `condition_hints` used by the low-code builder, while `webapp/src/features/playbooks/playbook-builder-panel.tsx` owns the canvas UI: palette drag/drop, visual block positions, in-block command selection and inspector-side preset/params editing. Capability-backed playbook blocks now use `capability_id`/`execution_target`: agent targets keep the existing module preflight + `run_tool` path, while server/observer/remote/manual targets route through the diagnostic capability router and do not enqueue DeviceOutbox commands.
- **операции (consent, cancel, lifecycle)** — `api/operations.py`, `app/services/operation_service.py`, `app/services/operation_watchdog.py`, `app/repos/operations_repo.py`
- **observer traces / signatures / degradations** — `observer/service.py`, `observer/runtime.py`, `tech/handlers.py`, `app/db/models.py`, `websocket/agent_services.py`, `pc_agent/core/action_trace.py`, `server/docs/OBSERVER_LAYER.md`, `server/docs/OBSERVER_AUTHORING_RULES.md`
- **MCP read-only server debug** — `mcp_helpdesk_server/server.py`, `mcp_helpdesk_server/bootstrap.py`, `mcp_helpdesk_server/tools/*`, `server/observer/debug_facade.py`, `server/observer/runtime_snapshot_writer.py`, `server/web_api/ai_integration_handlers.py`, `docs/MCP_SERVER_DEBUG.md`, `docs/mcp/helpdesk-server-debug.manifest.json`; standalone stdio MCP `helpdesk-server-debug` for Codex debug access to DB health, Tech locate and Observer bundles without HTTP API proxying, `run_tool`, DeviceOutbox writes, approvals/consent decisions, WS RPC or raw-secret output. Runtime status reads fresh `server_runtime_snapshots`; admin UI status lives at `/app/admin/ai-integration` over `GET /api/web/admin/ai-integration/mcp`. MCP DB bootstrap reuses an existing app DB engine when invoked in-process and only shuts down engines it started itself.
- **тикеты (SLA, назначение, очереди, structured confirmation, public access, описание заявки)** — `tickets/handlers.py`, `tickets/assignment_service.py`, `tickets/sla_service.py`, `tickets/workflow_service.py`, `tickets/public_queue_handlers.py`, `tickets/public_ticket_handlers.py`, `tickets/public_access.py`, `auth/admin_users_handlers.py`
- **ticket snapshot / workbench payload** — `tickets/handlers.py` (`GET /api/tickets/{ticket_id}/snapshot`: relations, worklogs, watchers and ticket relations serialized as JSON-safe DTOs before visibility projection, device/provisioning/update summary, latest operations, notification counters, device_metadata, OLA-блок, а также queue_members / assignable_users / available_queues / queue_auto_assign_enabled для основной рабочей области; `GET /api/tickets/{ticket_id}/observer`: root trace, related traces, signatures, occurrences для support workspace). Новый typed support boundary в `server/web_api/support_handlers.py` агрегирует из этого ticket detail/timeline/snapshot/action payload для `/app/support`, теперь уже включая tool events в timeline, нормализованный блок `request_form` из `request_form_summary`, `actions.closure_requirements` для support-facing checklist перед `resolved`, approval summary/decision route `POST /api/web/support/tickets/{ticket_id}/approvals/{approval_id}/decision`, и отдельный typed inventory/run contract для `GET /api/web/support/tickets/{ticket_id}/tools` + `POST /api/web/support/tickets/{ticket_id}/tools/run`, чтобы React-слой не зависел от raw legacy snapshot shape и `/api/tools/*`.
- **аутентификация, RBAC, login routing** — `auth/`, `auth/agent_token_service.py`, `routes.py`, `static_pages/handlers.py`, `docs/SECURITY_AND_AUTH.md`
- **миграции БД** — `app/db/migrations/versions/`, `docs/DATABASE.md`
- **обновление агента (builds, upload, delete, update, mass, diagnostics, recommendation, global rollout policy)** — `agents/agent_builds_handlers.py`, `agents/handlers.py`, `app/repos/agent_rollout_repo.py`, `websocket/agent_handshake.py`, `docs/AGENT_UPDATES_API.md`, `../../docs/AGENT_UPDATE_CONTRACT.md`, `../../pc_agent/docs/AGENT_UPDATE_WORKFLOW.md`, маршруты `POST/GET/DELETE /api/agent_builds*`, `GET/PATCH /api/agent_updates/rollout_policy`, web-session aliases `GET/POST/DELETE /api/web/admin/agent-builds*` + `GET/PATCH /api/web/admin/agent-updates/rollout-policy`, `POST /api/devices/{id}/agent/update`, `GET /api/devices/{id}/agent/update_recommendation`, `POST /api/agents/update_bulk`, `GET /api/devices/{id}/agent/update_diagnostics`, а новый typed admin boundary использует тот же flow через `GET /api/web/admin/devices/{device_id}/updates` + `POST /api/web/admin/devices/{device_id}/updates/run`
  - Инвариант recommendation: `assigned_rollout` — source of truth; любой version mismatch с rollout actionable, включая controlled rollback на более старую release-версию.
  - Инвариант cleanup: rollout-assigned build нельзя удалять, пока он назначен source of truth для target.
- **tech observability / tech panel** — `tech/handlers.py`, `tech/snapshot.py`, `tech/locator.py`, `api/operations.py`, `observer/service.py`, `observer/runtime.py`, `tech/runtime_audit.py`, `tech/log_buffer.py`, `auth/middleware.py`, `control_plane.py`, `runtime_control.py`, таблицы `agent_runtime_audit` + `observer_*`, маршруты `/api/admin/tech/*`, typed web aliases `/api/web/admin/tech/snapshot|locate|overview|alerts|logs|agents/audit|users/audit|operations/stuck` + `/api/web/admin/operations/{operation_id}`, React routes `/app/admin/tech` and read-only `/app/admin/operations/{operation_id}`, scripts `business_smoke.py`, `write_restore_drill_marker.py`, `write_backup_status_marker.py`, `release_candidate_preflight.py`, `release_server_to_remote.py`, and `/api/control/server/*` (overview, readiness snapshot, quick locator, operation detail link, alerts, runtime health, full journal logs, lifecycle actions, traces/signatures/runtime/degradations, audits). `tech/handlers.py` receives the active-PC-without-location aggregate only through `RegistryPort.inventory_quality()`; its numeric overview field fails closed to zero and carries a redacted typed source state. Data sources for snapshot/locator include config flags, PostgreSQL models, operations/outbox, audit counters, observer traces, inventory scheduler runtime and marker files `TECH_RELEASE_STATUS_PATH`, `TECH_BUSINESS_SMOKE_STATUS_PATH`, `TECH_RESTORE_DRILL_STATUS_PATH`, `TECH_BACKUP_STATUS_PATH`; release marker collects Alembic current/head after remote migrations when available and writes remote-root marker paths on the remote host over SSH. Full release preflight/gate additionally requires a passing `pc_client.live_release_summary.v1` at `artifacts/live/release-summary.json` for the exact candidate commit/environment. Remote server smoke goes through `scripts/runtime_stack.py` -> `server/runtime_control.py` and supports HTTPS pilot stands via `REMOTE_SMOKE_BASE_URL` / `REMOTE_SMOKE_INSECURE_TLS` or explicit `manage_remote_stack.py smoke server --base-url ... --insecure-tls`; `business_smoke.py` uses `--insecure-tls` or `BUSINESS_SMOKE_INSECURE_TLS=true` for the same self-signed stand case. Stand profile env for release/control/migration/browser helpers is `PC_CLIENT_REMOTE`, `PC_CLIENT_REMOTE_ROOT`, `PC_CLIENT_REMOTE_SERVER_PYTHON`, `PC_CLIENT_SSH_KEY`, `PC_CLIENT_BROWSER_BASE_URL`.
  - Observer queries now accept first-class filters `root_kind`, `min_duration_ms`, `min_retry_count`, `min_timeout_rate`, `min_retry_rate`, `min_slow_rate`, plus canonical ids (`trace_id`, `ticket_id`, `operation_id`, `device_id`).
  - `root_kind=agent_update` is the canonical way to isolate risky client-update traces and degradation groups from normal tool traffic.
  - Operation-less `agent_runtime_audit` rows are projected as synthetic observer traces for auth/provisioning/runtime/diagnostic-capability drilldown. Use `root_kind=device_provisioning` for connection-request/token-delivery/fingerprint issues, `root_kind=agent_auth` for invalid/revoked token and handshake failures, `root_kind=agent_runtime` for lifecycle/offline events, and diagnostic root kinds `capability_run`, `server_connector_query`, `observer_query`, `manual_evidence`, `remote_assist` for capability lifecycle audits.
- **device provisioning/update summary API** — `agents/handlers.py` (`GET /api/devices`, `GET /api/devices/{device_id}` возвращают `provisioning_summary`, `update_summary` и `identity_summary`)

---

## 4. Потоки выполнения

- **Agent → Server (Protocol V3):** агент подключается к `/ws`, handshake → outbox_item (ticket_event/device_event) → outbox_ack/outbox_nack.
- **Server → Agent:** команда в device_outbox → DeviceOutboxSender → WS → при command_result outbox помечается delivered/failed.
- **UI → Server:** `/ws_ui`, ui_hello → run_tool/API → операции и события в UI.
- **Helpdesk ticket flow:** создание тикета → template/global routing/SLA/OLA → если effective approval policy requires approval, create-flow переводит новый тикет в `waiting_on_approval` и создаёт approval requests до auto-assign/queue-status переходов → иначе попытка auto-assign только внутри состава целевой очереди и только если у очереди включено автоназначение → canonical status remains `new`, `queued` or `assigned` until an operator starts active work; legacy `triaged` is accepted only as an input/backfill alias and is never stored; form-aware routing теперь строит условия не только по базовым полям тикета, но и по `ticket_type`, `request_kind`, `custom_fields`, `request_form_data.<field>` и `request_form_*` metadata из typed intake-форм; request-template `routing_policy` выполняется перед глобальными rules и пишет `routing_decision`/`routing_applied` с источником, rule metadata и action payload; если форма содержит enabled `playbook_triggers`, create-flow запускает опубликованный diagnostic playbook с facts package по данным формы и пишет `playbook_started` в тикет; `request_template.diagnostic_policy.auto_run` can also start `suggested_playbooks` on creation only after priority, online-agent, requester-consent and high-risk-tool consent gates, otherwise it records `diagnostic_autorun_skipped`; terminal tool/diagnostic results execute `request_template.diagnostic_policy.reroute_by_result` after classification into `diagnostic_result`, writing routing events and OLA handoff hooks without treating diagnostics as a ticket status; при генерации паспорта `request_template.diagnostic_policy.attach_results.as_evidence=true` превращает terminal operation results в `diagnostic_result` evidence без изменения `ticket.status`; `helpdesk_policy_runtime.py` overlays effective registry policies at ticket creation and resolves approval/closure/notification/visibility policies again during existing-ticket lifecycle reads; `request_template.sla_policy` controls SLA start/pause/resume/stop/warning/breach events, accepts common live aliases, and workflow SLA hooks pass configured transition triggers such as `requester_replied` into resume checks; SLA warning/breach actions are dispatched by `policy_action_dispatcher.py`; `request_template.ola_policy` controls OLA start, ack stop, processing stop, pause/resume and breach events while preserving queue-target fallback, with workflow OLA hooks passing target statuses into pause checks and configured transition triggers such as `vendor_replied` into resume checks before the final status write; OLA breach actions are dispatched by `policy_action_dispatcher.py` into recipient notifications plus audit/delivery rows; `request_template.visibility_policy.public_status_mapping` задаёт requester-facing `public_status_label`, а requester payloads скрывают OLA/raw diagnostics/root cause/assignment internals; public queue handlers additionally use a separate sanitized projection without internal ids, requester identity, urgency/importance/priority, internal queue names or raw custom fields, and unauthenticated queue filters accept only `queue_code` / `public_queue_code`; `request_template.notification_policy` управляет получателями in-app notifications по событиям перед preferences-фильтрацией; typed support queue применяет backend `smart_view` срезы (`sla_risk`, `ola_risk`, `unassigned`, `waiting_approval`, etc.) серверно; ручная смена очереди ставит routing lock, а ручной reroute и сохранение профиля инициатора с флагом reroute пересчитывают очередь по правилам и дополнительно приводят назначение к составу новой очереди (старый исполнитель снимается, затем возможен auto-assign по новой очереди); лимит оператора считается только по `in_progress`; `resolved` запрашивает requester confirmation только если closure policy требует его, negative feedback either reopens to `in_progress` or keeps `resolved` by `reopen_on_negative_feedback`, and legacy tickets still fall back to `assigned`.

### 4.1 Internal WS pipelines

- **`command_result` pipeline (`AgentMessageRouter` → `CommandResultService`)**
  - normalize (`CommandResultNormalizer`)
  - lifecycle update (`OperationLifecycleService`)
  - sync wait future resolve (`CommandResultFutureResolver`)
  - artifact/result post-processing (`CommandResultArtifactHandler`)
  - side effects publish (`CommandResultEventPublisher`)
  - terminal result ACK back to the agent (`command_result_ack`) so the agent
    can remove durable `pending_command_results`; recovery results with
    `error.code=AGENT_RESTARTED` use this same path and must reconcile the
    operation plus target `device_outbox`; a duplicate command redelivered to a
    new runtime before startup replay still sends this terminal result without
    `command_ack` or tool rerun. Late terminal results replayed after
    watchdog timeout reconcile the original operation when no retry/replacement
    exists; otherwise the original timeout is preserved and linked late-result
    evidence is written for audit.
- **`outbox_item` pipeline (`AgentMessageRouter` → `OutboxIngestService`)**
  - envelope validate (`OutboxEnvelopeValidator`)
  - post-handshake guard (`OutboxGuardService`)
  - dedupe check (`OutboxDedupService`)
  - persistence (`OutboxPersistenceService`, включая enrichment `sender_display_name`/`requester_display_name` для requester-originated `chat_message`)
  - ack/nack decision (`OutboxAckDecisionService`)
  - publish after persistence (`OutboxEventPublishService`)

---

## 5. БД и миграции

- PostgreSQL. Миграции: `server/app/db/migrations/versions/`. Команда: из каталога `server` — `alembic upgrade head`. См. `docs/DATABASE.md`, `docs/COMMAND_RESULT_LIFECYCLE.md`.

---

## 6. Тесты и скрипты

- `pytest.ini` — корневой test harness и markers для всего монорепо.
- `server/tests/` — интеграционные/регрессионные тесты.
- `server/tests/conftest.py` — isolated DB-per-run, session-scoped engine, in-process agent harness.
- `server/tests/README.md` — канон по локальному server baseline и env vars.
- `scripts/bootstrap_web_toolchain.py` — каноничный bootstrap `Node.js 24 LTS + corepack + pnpm` для нового `webapp/` и frontend build/release pipeline.
- `scripts/check_webapp_cutover.py` — preflight по operational readiness нового cutover: built bundle, requested/active флаги и готовность полного default-route switch; путь workspace теперь определяется от самого repo и одинаково работает локально и на Linux.
- `scripts/helpdesk_data_cleanup.py` — read-only dry-run inventory for historical helpdesk data quality: tickets/requester names/module descriptions/tool descriptions with mojibake, `???` placeholders and token-like samples; outputs UTF-8 JSON/Markdown under `artifacts/diagnostics/` and leaves mutation disabled until reviewed cleanup rules exist.
- `scripts/ci_artifacts.py` — validates self-hosted CI summaries for release/deploy consumers; green artifacts must match the exact commit, avoid shared DB fallback and satisfy the full merge gate, so affected or `--layer` summaries cannot be used as final release evidence.
- `scripts/run_ci_suite.py` — канонический локальный/self-hosted CI run с artifact layout `artifacts/ci/<sha>/`; включает workspace verify, webapp bundle, webapp Vitest, Playwright fixture E2E with JSON retry-pass report, `test_inventory_audit`, `db_cleanup_profile_audit`, `fixture_builder_audit`, `active_risk_audit`, `observer_contamination_audit`, `branch_coverage_audit`, `mutation_smoke`, `scripts_pytest_no_db`, server pytest layers with migrated DB template setup, fixture timing summary with budget status, flaky registry summary, no-DB property/state contracts and pc_agent pytest; uses `scripts/suite_catalog.py` + `quality/test_suites.toml` for layer order, server DB/API ownership, affected source routing and DB/WS parallel grouping; `--changed-path`/`--affected-from` выбирают affected fast-gate layers and mark summaries as not satisfying the full merge gate unless every canonical layer ran green.
- `scripts/suite_catalog.py` — loader for `quality/test_suites.toml`; shared by `scripts/run_ci_suite.py` and `scripts/audit_test_inventory.py` so runner, affected-suite selection, parallel DB/WS grouping and inventory ownership use one suite catalog.
- `quality/flaky_registry.json` — tracked allowlist for CI retry-pass/flaky records; unknown Playwright fixture retry-pass records remain `summary.flaky_summary.unknown_records` and make CI red.
- `quality/fixture_builders.json` — tracked registry for schema-validated fixture/data packs such as `test_data_packs/web_first_phase_e.json` and `test_data_packs/critical_behavior_v1.json`.
- `scripts/fixture_schema_builders.py` / `scripts/audit_fixture_builders.py` — JSON Schema builders and strict audit for fixture/data pack schema drift, source-pack refs, test refs, live evidence requirements and secret-free checks before branch/mutation gates.
- `quality/active_risks.json` — tracked registry for active technical-debt risks migrated from `docs/archive/BOTTLENECKS_AND_RISKS.md`; active rows carry owner, affected contracts, source/evidence refs, escalation trigger, measurable acceptance criteria and linked tests.
- `scripts/audit_active_risks.py` — strict validator for `quality/active_risks.json`; checks required archive source coverage, active-risk gate fields, existing evidence/source paths and existing pytest node refs before branch/mutation gates.
- `quality/critical_branch_coverage.json` — tracked targeted branch coverage records for critical pure logic packages.
- `scripts/audit_branch_coverage.py` — strict validator for `quality/critical_branch_coverage.json`; checks schema, package owners, unique branch ids and existing pytest node refs before pytest layers run.
- `quality/mutation_smoke_targets.json` — tracked temp-workspace source mutants for critical pure logic.
- `scripts/run_mutation_smoke.py` — applies configured mutants to a temp workspace copy, runs focused pytest node ids, and fails on survivors or collection/infrastructure errors without modifying the real checkout.
- `server/tests/test_property_state_contracts_no_db.py` — deterministic property/state-machine coverage for redaction non-mutation/idempotency, ticket status normalization and workflow profile FSM graphs without adding a property-test dependency.
- `scripts/summarize_fixture_timings.py` — summarizes `artifacts/ci/<sha>/fixture-timings/*.jsonl` into `fixture-timings-summary.json`, including fixture/phase p50/p95/max stats, cleanup-profile breakdowns, default budget annotations and optional `--enforce-budget` failure.
- `scripts/audit_db_cleanup_profiles.py` — report-only audit for server pytest `db_cleanup` marker coverage; use `--strict` only when missing DB-backed profiles should fail a gate.
- `scripts/audit_db_cleanup_schema.py` — schema/classification cleanup drift gate for DB tables; validates `quality/db_table_classification.toml`, static pytest cleanup, dynamic reset policy and FK cleanup blockers.
- `scripts/validate_live_evidence.py` — validates `pc_client.live_evidence.v2` manifests created or completed from `scripts/live_evidence_pack.py` folders, including required preflight commit parity, expected/actual schema head parity, service/schema health status, before/after Observer integrity delta, trace/DB consistency, Observer writer/correlation health and Observer canary JSON/Markdown report coverage.
- `scripts/run_live_behavior_suite.py` — reads `test_data_packs/critical_behavior_v1.json`, selects live scenarios by surface/scenario key, runs or dry-runs requester/support browser commands through `webapp/scripts/live-browser-scenarios.mjs` or agent/operation commands through `scripts/live_agent_uia_state_probe.py` and `scripts/live_ws_v3_probe.py`, and can scaffold per-scenario blocked `pc_client.live_evidence.v2` manifests with exact release context via `--evidence-root`.
- `scripts/build_live_release_summary.py` — aggregates `test_data_packs/critical_behavior_v1.json` and completed `artifacts/live/**/manifest.json` files into one `pc_client.live_release_summary.v1` JSON/Markdown release summary with pass/fail/missing scenario coverage and release blockers, filtered by exact `--commit`, `--environment`, `--release-run-id` and `--expected-schema-head` for final release evidence.
- `scripts/audit_test_app_light_candidates.py` — report-only audit for HTTP/API pytest files that may be safe candidates for explicit `test_client_light` opt-in.
- `scripts/live_evidence_pack.py` — read-only scaffold for `artifacts/live/<run_id>/` evidence markdown checklists before non-trivial live validation/debug runs, including scenario key, release run id, commit/environment/schema preflight identity, `observer-delta.md` for baseline/scenario integrity scans and `observer-canary.md` for canary report evidence.
- `scripts/canary/endpoint_diagnostic_canary.py` — fail-closed staging-only Helpdesk↔Endpoint diagnostic canary: validates manifest, Windows-agent preflight and one hashed idempotency key; maps only through the existing admin route, executes only Helpdesk capability `endpoint.context.diagnostic.collect` through `/api/web/support/tickets/{ticket_id}/diagnostics/capabilities/.../run`, and unwraps the matching `{status, data}` support diagnostics overview response for observe/verify/report without exposing credentials or raw results.
- `webapp/scripts/live-browser-scenarios.mjs` — Playwright live-browser probe for requester/support behavior scenarios; uses real `/app/*` routes without request mocks, role-specific environment credentials, screenshot/DOM capture and console/page-error failure checks.
- `scripts/run_ci_in_temp_workspace.py` — hook-friendly runner для self-hosted CI в отдельном checkout/venv.
- `requirements-ci.txt` — минимальный CI dependency set.
- Корень проекта: `scripts/run_server.py`, `scripts/stop_server.py`, `scripts/restart_server.py`, `scripts/run_control_plane.py`, `scripts/runtime_stack.py`, `scripts/manage_remote_stack.py`, `scripts/release_server_to_remote.py`, `scripts/smoke_test.py`, `scripts/admin_run_tool.py`.
- `server/scripts/` — `run_subagents.py`, `subagent_worker_server.py`, `subagent_worker_agent.py`.

---

## 7. Инварианты

- Тип события только по `device_seq` vs `agent_seq`. `tool_call_started` создаётся на сервере до run_tool, идемпотентен. Не логировать сырой токен. Детали: `AGENTS.md`, `docs/PROTOCOL_V3.md`.

---

## 8. Когда обновлять этот CODEMAP

Каноническая карта сервера — этот файл: `server/docs/CODEMAP.md` (других CODEMAP для дерева `server/` нет).

При изменениях, затрагивающих структуру кода сервера, его **нужно** обновить:

- добавление/удаление/перенос маршрутов (routes) или ключевых обработчиков;
- новые или переименованные ключевые каталоги/файлы в `server/`;
- новые точки входа или смена назначения существующих модулей.
- если change затрагивает observer-слой, dangerous flow, trace-visible API/UI или instrumentation rules — синхронно обновлять и `server/docs/OBSERVER_LAYER.md` + `server/docs/OBSERVER_AUTHORING_RULES.md`.

Проверка: ключевые термины и файлы из раздела «Быстрый поиск» и «Структура каталогов» должны соответствовать коду.
---

## 2026-04-15 Module workbench note

- `server/modules/handlers.py` now exposes `GET/POST /api/modules/authoring/*` for headless module creation (`catalog`, `validate`, `publish`) and `POST /api/modules/workbench/validate` in addition to list/detail/save/preferred endpoints, plus `GET/PATCH /api/modules/rollout_settings` for preferred-version auto-rollout policy, `GET /api/modules/{module_name}/{version}/live_test_candidates` for lab-agent selection and `POST /api/modules/{module_name}/{version}/live_tests` for lab-agent module verification.
- Module validation now records mandatory `validation_json.server_harness`; Windows-targeted versions warn with `WINDOWS_LIVE_TEST_REQUIRED_BEFORE_PREFERRED` and cannot become preferred until `validation_json.live_tests` contains a passed Windows agent run that satisfies the module/tool `min_agent_version`.

## 2026-04-27 React inventory provisioning note

- `/app/admin/inventory` now combines the typed inventory/fleet token/rollout data from admin-only `GET /api/web/admin/devices*`, global `GET /api/web/admin/device-tokens`, per-device `GET /api/web/admin/devices/{device_id}/tokens` and legacy admin-only `/api/devices/{device_id}/tokens*` with web-session aliases for the existing manual provisioning endpoints: `GET/PATCH /api/web/admin/connection_policy` and `GET/POST /api/web/admin/connection_requests*`. Revoke handlers scope `token_hash` to the path `device_id`. The selected agent details panel can archive a test/stale agent through `DELETE /api/web/admin/devices/{device_id}`, delegating to the existing soft-delete handler that revokes tokens, rejects pending requests, cancels active operations and hides the row from active inventory. The React shell notification bell polls ticket unread count and active connection requests, then deep-links administrators to `/app/admin/inventory?panel=requests` for approve/reject. Its rollout panel and device detail quick links now open `/app/admin/agent-updates?device=...&target=...` for first-class agent build/rollout management.
- Manual connection approval is race-safe in `server/auth/connection_request_handlers.py` and `server/app/repos/connection_requests_repo.py`: a post-approval agent heartbeat no longer creates a second `pending` row, a fresh no-token POST after an undelivered approval gets new polling credentials when no active agent token exists, a no-token POST for a device with an active token returns `already_authorized=true` and clears pending rows without issuing a token, approve does not generate or store a raw token, valid status polling generates the agent token and marks the approval delivered, and legacy pending rows without `poll_secret_hash` are rejected with `POLL_SECRET_MISSING`.
- Registry active-person hardening is centralized in `server/app/repos/registration_repo.py`, `server/registry/registration_service.py`, `server/registry/account_session_service.py`, `server/requester/identity_service.py` and `server/auth/service.py`: archived/inactive/merged/disabled people are excluded from active bindings, requester identity resolution, browser registration, GUI password login and account-session validation. Web-session auth also blocks requester UI accounts linked by any `ui_login` identity to archived/inactive/deactivated/disabled people, deactivates the UI login and revokes active UI tokens; migrations `125` and `126` reconcile historical rows that predate this cascade.
- Module live-test and preferred-gate failures now materialize observer traces (`module_live_test`, `module_preferred_gate`) with spans for lab-agent selection, module install, tool run and gate failure.
- `server/modules/workbench_service.py` can reconstruct editable tool fragments from module archives via builder markers or AST analysis of `@exposed_tool` functions.
- `server/admin_modules_workbench.js` is the main entrypoint for template-driven module authoring, inline validation, output-contract/readiness controls, API preview over headless authoring endpoints, archive/source exploration, and rollout-policy controls in the admin UI.
- `webapp/src/features/modules/modules-panel.tsx` is the typed `/app/admin/modules` module constructor; it mirrors output-contract controls, calls `/api/modules/authoring/validate|publish`, and includes the published-version lab-test panel for choosing Windows/Linux lab agents before live verification.
- `webapp/src/features/diagnostics/diagnostic-center-panel.tsx` is the React ticket Diagnostic Center used by `/app/tickets/:ticketId`: it consumes diagnostics overview/capability/evidence/session/finding APIs, filters capabilities by target/domain/perspective/provider, runs capability router actions, creates manual evidence, evaluates findings, builds bundles and attaches selected evidence to passport without replacing the legacy tools/playbooks sidebar. Diagnostic results can render through `webapp/src/components/module-result/module-result-renderer.tsx` when a capability has `presentation_schema`, while keeping raw JSON as fallback. `webapp/src/features/diagnostics/provider-config-panel.tsx` is mounted under `/app/admin/modules#diagnostic-provider-configs` for web-admin server connector config over `/api/web/admin/diagnostics/providers/configs*`.

## 2026-04-16 Module page refactor

- `server/admin.html` and `server/admin.js` now treat the admin `Модули` page as an inner-tab workspace with four subviews: step-by-step authoring, module registry list, advanced editor, and device installs.
- `server/admin_modules_workbench.html` / `server/admin_modules_workbench.js` split authoring into a guided 4-step flow (`Каркас` -> `Инструменты` -> `Политики` -> `Проверка`) while keeping the advanced manifest/tool editor available as a separate view for power users.
- The workbench list can now import a ready ZIP archive, delete a published module version, and open the uploaded package directly in the editor; the authoring/editor views replace most routine JSON entry with guided controls: platform pills, line-based requirements, and validated schema blueprints for params/output before publish.

## 2026-04-16 Request form builder UX

- `server/admin_ticket_forms_builder.html` / `server/admin_ticket_forms_builder.js` are the dedicated admin UI entrypoints for the request-form catalog.
- `webapp/src/features/forms-builder/forms-builder-panel.tsx` and `webapp/src/features/forms-builder/api.ts` now mirror the same authoring flow inside `/app/admin` via typed `GET /api/web/admin/forms/current`, explicit draft/validate/publish/preferred endpoints and compatibility `POST /api/web/admin/forms/save`.
- 2026-05-11 update: `/app/admin/forms` now shows canonical request-template policy refs as primary controls, keeps inline policy JSON behind an explicit `Advanced inline policy JSON` section, and shows active request templates already using an entered policy code.
- 2026-05-11 update: request-template field roles are server-driven via `field_role_options`; canonical roles include `priority_impact`, `priority_urgency`, `priority_importance`, `diagnostic_input`, `approval_subject`, `closure_evidence`, `reporting_dimension`, `passport_fact`, `visibility_public` and `display_only`, while legacy `priority_field`/`sla_field`/`approval_field` packs still load for compatibility. Business preflight blocks duplicate singleton priority roles, unmapped diagnostic autorun inputs, incompatible approval subjects and incompatible closure evidence fields before publish; `/app/admin/forms` mirrors those role issues inline near affected fields.
- 2026-05-02 update: `server/tickets/form_catalog.py` keeps legacy `request_forms` compatibility when default priority questions are injected into old packs: old `form_key` submissions may omit those injected fields, while new UI/agent flows can still render and submit them.
- 2026-05-03/2026-05-11 update: `server/tickets/form_catalog.py` preserves schema/template/policy metadata from versioned `request_forms` (`form_schema_id`, `form_schema_version`, `request_template_version`, `workflow_profile_id`, `*_policy_code`, `policy_refs`) into `custom_fields.request_template` for `/api/tickets/create`, `/api/tickets/create/preview` and public/agent `request_template_key` submissions, keeping old integer versions compatible while accepting standalone registry semver values such as `1.0.x`; `server/tickets/request_template_submission.py` extends the same create paths to active standalone helpdesk registry templates when no legacy form-pack entry exists for the submitted `request_template_key` and now snapshots `resolved_from`, pack/template/schema keys and versions; `create_flow.py` attaches `custom_fields.request_form` and `request_template.computed` after routing side effects so support/admin can audit priority, routing source, queue id/code/name and matched rule, while requester/public visibility hides `custom_fields.request_template`; `workflow_service.py` consumes the preserved `workflow_profile_id`, and `create_flow.py` starts approval-required new tickets in the approval waiting status before auto-assign/queued transitions.
- 2026-06-16 update: request form packs accept normalized `on_behalf_policy` as an opt-in template process setting. `server/web_api/admin_handlers.py` and DTOs preserve it through `/api/web/admin/forms/*`, `server/web_api/settings_handlers.py` projects a requester-safe disabled default in `ticket_settings.request_templates[].form.on_behalf_policy`, and `/app/admin/forms` renders the process-step toggle/settings.
- 2026-06-16 update: PA6 diagnostic/module execution is target-aware. `server/tickets/diagnostic_target.py` resolves the dispatch device from `ticket_context_v1`/`target_device_id`; `server/playbooks/form_triggers.py`, `server/tickets/diagnostic_policy.py`, `server/web_api/support_handlers.py` and `server/tools/handlers.py` use that target for auto-run, support manual tool/playbook launch and `/api/tools/run` mismatch rejection. Tools/playbooks DTOs expose safe `diagnostic_target`, and `/app/tickets/:ticketId` shows the target device/affected person before manual launch.
- 2026-06-17 update: web-first Phase B ticket context helpers are active. `POST /api/web/requester/tickets/preview` returns requester-safe `ticket_context` without creating DB rows, `create_flow.py` writes internal/support `ticket_context_resolved`, requester-safe `customer_history_ticket_created` and internal `requester_ticket_create_audit`, and `resolve_ticket_diagnostic_target()` now prefers nested `diagnostic_target.device_id` over stale flat aliases. The new B3 marker/audit payloads use `source=requester_ticket_create` and safe request-context/account-mode/snapshot/diagnostic-target flags only; coverage is `server/tests/test_requester_workspace_api.py::test_requester_can_create_ticket_for_owned_device_and_not_foreign_device`.
- 2026-06-17 update: legacy/agent compatibility in `server/tickets/create_flow.py` marks create paths without `requester_account` as `requester_account_mode=agent_legacy_or_device_only`, with `requester_account_context.context_scope=limited` and `profile_completion_evidence=false`. Active bindings still provide compatibility person/binding/diagnostic context; tickets without confirmed bindings remain pending/unregistered without `ticket_context_v1`. Coverage is `server/tests/test_ticket_registration_enrichment.py`.
- 2026-06-26 ACCOUNT-033 account boundary: `server/tickets/create_flow.py` no longer resolves `requester_person_id` from declared `email`/`login` on unverified or legacy other-account payloads. Only a validated server-side `verified_other_account` session may contribute the matched person id, so unverified declared accounts preserve warning context without contaminating Customer History. Coverage is `server/tests/test_ticket_account_access.py::test_unverified_other_account_does_not_resolve_declared_person`.
- 2026-06-17 update: Phase C Customer History v1 is active for the first slice. `server/customer_history/*` provides role-safe projections, redaction and deterministic LLM preview. Routes are registered for support ticket/person history, requester ticket/history, support context-pack/preview and admin history search; `/app/tickets/:ticketId` shows compact `История клиента` and `Контекст для ассистента - preview` blocks from support detail payload. The read model now includes dedicated chat, diagnostics, compact ObserverTrace, on-behalf creator/affected, time-window and related-history context coverage while leaving Phase D web-cabinet Observer event writing/integrity checks open.
- 2026-05-02 update: `/app/admin/forms` has structured request-template approval-policy controls for approver source, optional form-field approver key, approval mode, reminder/escalation timing, reject-comment requirement and passport logging before the advanced JSON editor.
- 2026-05-02 update: `/app/admin/forms` has structured request-template closure-policy controls for resolution/public/internal summary requirements, worklog, evidence by priority, operation/approval evidence, requester confirmation, autoclose days, negative-feedback reopen behavior and resolution codes before the advanced JSON editor.
- 2026-05-02 update: `/app/admin/forms` has structured request-template diagnostic-policy controls for suggested playbooks, auto-run priorities, requester-device and high-risk-tool consent, attach-to-timeline/passport/evidence flags and reroute-by-result queues before the advanced JSON editor.
- 2026-05-02 update: `/app/admin/forms` has structured request-template visibility-policy controls for public status mapping, requester-hidden fields and requester-visible metadata before the advanced JSON editor.
- 2026-05-02 update: `/app/admin/forms` has structured request-template notification-policy controls for ticket event recipients and web/email/Telegram/VK Teams channels before the advanced JSON editor.
- 2026-05-02 update: `/app/admin/forms` has structured smart-view controls for excluded statuses, due-window hours, due fields, primary sort and columns before the advanced filter/sort JSON editor.
- 2026-05-02 update: `POST /api/web/admin/helpdesk-model/smart-views/publish` rejects unsupported smart-view filter keys, unsafe field paths, invalid sort directions and unsupported display columns before creating a registry version.
- 2026-05-02 update: `GET /api/web/support/tickets/{ticket_id}/playbooks` returns a typed diagnostic-policy summary for the ticket request template, and `/app/tickets/:ticketId` shows suggested playbooks, auto-run priority scope, consent/evidence flags and reroute-by-result queues in the support automation panel.
- 2026-05-03 update: `server/tickets/approval_policy.py` now builds typed approval summaries for support/requester-safe surfaces; `GET /api/web/support/tickets/{ticket_id}` includes `ticket.approval_summary` and `actions.approval`, and `/app/tickets/:ticketId` renders pending approvers, current action owner, due/reminder/escalation timestamps, reject-comment guard hints and runtime reminder/escalation state.
- 2026-05-08 P10.2 update: the same typed support boundary now exposes reversible workspace visibility cleanup: `POST /api/web/support/tickets/{ticket_id}/hide|unhide` stores global hidden state in `tickets.custom_fields.support_workspace_visibility`, `POST /api/web/support/tickets/{ticket_id}/archive|unarchive` reuses `tickets.archived_at` and is admin-only, and `POST /api/web/support/workspace/cleanup-noise` hides obvious live/stage/test ticket rows without deleting history. `/api/web/support/queue` excludes hidden/archived rows by default, supports explicit `include_archived=1` and backend/debug `include_hidden=1`, and `/app/tickets` renders the archive toggle, cleanup button, hidden/archive badges and `Ещё` menu controls.
- 2026-05-11 update: `POST /api/web/support/queue/mass-action` applies permission-safe bulk queue actions over the same accessible support queue view: assign self/auto, change queue, change priority with reason, add internal note, launch diagnostics and link mass-problem context. The handler returns per-ticket success/skipped/error rows and does not expand ticket visibility beyond the caller's queue/role scope. Queue Mode saved views and visible columns are DB-backed by `support_queue_saved_views` (migration `072`) and exposed through `GET/POST/PUT/DELETE /api/web/support/queue/saved-views` with personal, queue-shared and admin global scopes.
- 2026-05-19 update: Operator Command Center lives at `/app/support` and is backed by `GET /api/web/support/command-center`. HTTP parsing, requester denial, scope fallback and batched source reads are in `server/web_api/support_handlers.py`, typed DTOs are in `server/web_api/dto/support.py`, and deterministic aggregation is in `server/support/operator_command_center.py`. The endpoint reuses compact support queue rows, chat counters, SLA/OLA fields, batched `ticket_approvals`, latest operations, compact device presence from `devices.last_seen_at`, batched `diagnostic_sessions` / `diagnostic_evidence`, latest resolution passports and deterministic recent-ticket grouping; it does not load the full ticket workspace or diagnostics overview per candidate row. Query param `query` filters the candidate set before section counting, similar spike items link into `/app/tickets?search=...`, command-center smart-view aliases normalize through `server/tickets/smart_views.py`, command-center duration/candidate metrics are logged, and real zero counts are returned for unavailable signals. Ticket workspace mark-read uses `POST /api/web/support/tickets/{ticket_id}/read`, a web-session alias around the existing staff read cursor handler, so support operators do not need a legacy bearer token to clear requester-message counters.
- 2026-05-21 update: Approval/Consent Center lives at `/app/support/approvals` and is backed by `GET /api/web/support/approvals`. The handler is `server/web_api/approval_handlers.py`, typed DTOs are in `server/web_api/dto/approvals.py`, and `server/approvals/service.py` builds the read model from real `ticket_approvals`, `change_approvals`, `operations.status='waiting_consent'`, `remote_access_sessions` pending consent and closure-like ticket approvals. First cut returns only safe link actions to ticket/change/device contexts; mutating approve/reject remains in the source-domain endpoints. Requester-role access is forbidden, support `scope=all` falls back to team scope, and the payload intentionally omits Remote Assist tokens/ICE/SDP plus raw operation params/secrets. Policy override count is zero until a first-class pending override source exists. Command Center pending approval/consent section actions deep-link to `/app/support/approvals?kind=pending_approval|pending_consent`, and ticket context shows a compact link when approvals or waiting consent operations are present.
- 2026-06-10/23 update: Stage 3 user consent backend adds `user_consent_requests` through migration `109`, model `UserConsentRequest`, `server/app/repos/user_consent_repo.py` and `server/consent/service.py`. Requester browser APIs live under `/api/web/requester/consents*` and agent GUI APIs under `/api/registry/agent/consents*`; both serialize only safe redacted request data. Browser decisions resolve the web user to a `RegistryPerson`; agent decisions require the agent device token plus a valid requester account session id/token, so the machine token is never the approving identity. The service enforces one active pending consent per `subject_type + subject_id` through partial unique index `ux_user_consent_requests_pending_subject` plus savepoint/`IntegrityError` recovery in `create_request()`, CAS first-decision-wins semantics, active binding/session checks, expiry before side effects, ticket timeline events and operation side effects through the existing `OperationService.approve_consent()` / `deny_consent()` path. Pending operation consents are canceled before decision side effects if the linked operation has already reached `cancel_requested`, `canceled`, `denied`, `failed`, `succeeded` or `timed_out`; the server writes `user_consent_canceled` and skips both `ConsentDecision` and `DeviceOutbox`. Consent-required operation creation paths in support tools, legacy `/api/tools/run` and operation retry must create a canonical `UserConsentRequest` in the same transaction as `Operation(status='waiting_consent')`; missing requester scope is rejected instead of leaving an orphan waiting operation.
- 2026-06-24 update: Approved operation consent dispatch now resumes through `ToolExecutionService.resume_approved_operation()` after the approval transaction commits. `OperationService.approve_consent()` remains the CAS transition/decision writer, and `ToolExecutionService.run_tool(require_online=False)` owns `tool_call_started` creation plus deferred `device_outbox` enqueue for the pre-created operation.
- 2026-06-10 audit hardening: cookie-auth browser unsafe requests are same-origin guarded in `server/auth/middleware.py`; allowed origins include the current host, reverse-proxy public host from `Forwarded` / `X-Forwarded-Proto` + `X-Forwarded-Host`, `SERVER_PUBLIC_BASE_URL`, and `WEB_CSRF_TRUSTED_ORIGINS`, while bearer UI tokens and agent tokens are not treated as web-session cookie auth. Direct browser pairing GET uses `BrowserPairingService.get_browser_visible_pairing()`, expires stale rows before response, rate-limits probes and returns no device facts for inactive or unknown pairings.
- 2026-05-08 P10.3 update: `POST /api/operations/{operation_id}/retry` now returns `202 waiting_consent` for consent-required tools after normal auth/device/tool/policy/replay validation, writes `tool_call_started` and `operation_retry_consent_requested` with redacted replay params, and waits for `/api/operations/{retry_operation_id}/approve` before enqueuing the agent command. Support operation snapshots surface `consent:required` labels from `tool_call_started` payloads for `/app/tickets`.
- 2026-05-06 P2.2 update: standalone `GET /api/web/support/tickets/{ticket_id}/timeline?filter=all|messages|internal|diagnostics|history` reuses the same support timeline normalization and lifecycle allow-list as the detail/workspace payloads; `/app/tickets` calls it for tab-specific timeline refreshes and falls back to aggregate timeline filtering when the standalone query has not returned yet.
- 2026-05-06 P2.3-P2.5 update: support timeline diagnostic rows extract structured operation steps from nested `result.steps`, `result.checks`, diagnostics and map-shaped payloads, carrying optional step `details` into `/app/tickets` diagnostic result cards. For terminal `tool_call_result` events, `server/web_api/support_handlers.py` also exposes bounded `result_payload`, `result_presentation_schema` and `result_presentation_schema_source` so `/app/tickets` can render real completed tool output through `ModuleResultRenderer` with the effective server override/module-default schema instead of only a compact JSON preview. `SupportTicketRegistrySnapshot` now carries registry person phone/email/source and location floor so the SaaS right context sidebar can show enriched requester contacts. `/app/tickets` adds a persisted light/dark workspace theme toggle at the app-shell level.
- 2026-05-06 P3.1 update: `GET /api/web/support/tickets/{ticket_id}/tools` now includes support-safe tool policy metadata on each `SupportToolItem`: manifest `domain`, `tool_kind`, derived `required_permission`, `allowed_roles` and compact `policy_labels`. The enrichment is computed in `server/web_api/support_handlers.py` from existing `ToolMetadata` and does not change `/tools/run` behavior; `/app/tickets` renders the permission/role labels in the right-side tools panel through the existing workspace mapper.
- 2026-05-22 update: typed support messages `POST /api/web/support/tickets/{ticket_id}/messages` accept `attachment_refs` produced by `POST /api/upload`; `server/web_api/support_handlers.py` resolves artifact descriptors into chat `payload.attachments`, permits attachment-only support messages, and returns the same attachments in `SupportTicketMessage` for `/app/tickets`.
- 2026-06-09 update: authenticated requester messages `POST /api/web/requester/tickets/{ticket_ref}/message` accept `attachment_refs` produced by same-session `POST /api/upload`; `server/web_api/requester_handlers.py` resolves `{ticket_ref}` by requester-safe `ticket_code` with legacy `ticket_id` fallback behind the same ownership check, resolves artifact descriptors with the shared ticket attachment helper, permits attachment-only requester messages, keeps owned-ticket checks in `RequesterIdentityResolver`, and `/app/requester` uploads/removes pending files before sending refs and renders returned attachment download links.
- 2026-06-23 update: requester and support web chat sends use client/server `message_id` as the retry key. `server/web_api/requester_handlers.py` and `server/web_api/support_handlers.py` resolve duplicate `message_id` submissions through `TicketEventsRepo.get_chat_message_by_message_id()`, return the original event id, and skip repeated workflow/SLA/Observer/realtime side effects only when `server/tickets/chat_idempotency.py` confirms the retry payload matches the original stable chat payload; same-ID/different-payload retries return `409 MESSAGE_RETRY_PAYLOAD_CONFLICT`, and non-empty client `message_id` values longer than 120 characters return `400 INVALID_MESSAGE_ID` instead of being truncated. Migration `20260626_132_ticket_events_server_idempotency.py` adds `uq_ticket_events_server_event_id` and `uq_ticket_events_server_message_id`, and `TicketEventsRepo.add_event()` treats those unique-index conflicts as idempotent duplicates for parallel retry races. Typed support sends reject unknown `visibility` before DB access with `400 INVALID_VISIBILITY`; `server/tickets/requester_timeline.py` hides every non-public visibility value from requester projections.
- 2026-05-22 update: device/user registration is a controlled claim workflow. Migration `098_device_registration` adds `registry_person_identities`, `device_registration_claims`, `device_user_bindings`, `device_registration_events`, inventory binding registration columns and ticket requester registration columns. `POST /api/registry/agent/profile`, `GET /api/registry/agent/registration-status`, side-effect-free `GET /api/registry/agent/registration-form`, and `POST /api/registry/agent/claims/{claim_id}/confirm` are agent/user-safe with device boundary checks; admin review lives under `GET /api/web/admin/registry/registrations`, `POST /api/web/admin/registry/registrations/{claim_id}/approve|reject`, `POST /api/web/admin/registry/bindings/{binding_id}/revoke`, and `GET /api/web/admin/registry/devices/{device_id}/registration-timeline`. `agent_handshake.py` appends non-fatal `payload.registration`, and `create_flow.py` enriches new tickets from active primary bindings without blocking unregistered devices.
- 2026-05-24 update: `/app/admin/registry` is a Registry Management Center. P0 admin operations are service-backed: bind person to device, transfer owner, add shared user, assign responsible, list/revoke global account sessions, create/update people and add/verify/remove identities. P1/P2 adds functional locations/departments CRUD+archive+merge, persisted registry/account-session/ticket-visibility policy editor, people merge/dedup, bulk assign location/department and bulk account-session revoke, CSV export, safe CSV import preview/apply and lazy timeline drawer via `RegistryAdminOperationsService`, `RegistryPolicyService`, `registry_admin_policies` and `registry_admin_events`. Dangerous operations now have preview/dry-run contracts: transfer owner through `RegistrationService.preview_transfer_owner`, people/location/department merge, bulk and registry imports through `RegistryAdminOperationsService.preview_*`, with UI preview required before apply where applicable. People merge moves identities, bindings, sessions, account login requests, claims, tickets, asset owner and inventory rows to the master person and marks the duplicate merged. Registry policy APIs expose defaults, changed-from-defaults, validation ranges, policy warnings, `requires_restart` metadata, preview and reset-to-defaults; patch/reset require a reason and write `policy_changed`. Registration policies also include `department_mode` and `location_mode` (`allow_pending_request`, `optional`, `required_existing`) so agent registration forms can use strict CMDB pickers instead of free text. Transfer owner, people/location/department merge, bulk actions and import apply return a normalized apply report with `operation_id`, `status`, `summary`, `items`, `events` and `report_url`, while preserving legacy domain fields such as `binding`, `master`, `duplicate`, `bulk_operation_id` and `results` where needed. Bulk/import UI can show failed rows, copy errors and export formula-safe CSV result reports. Registry imports cover people, locations, departments, device inventory mapping, audience groups and audience-group members; direct binding and account-session imports are intentionally unsupported, preview returns `preview_id`, apply requires the matching `preview_id`, reruns validation, writes `registry_import_applied` and returns itemized row results. Generated quality issues use stable `issue_key` values; ignore/snooze/resolve persist in `registry_quality_issue_overrides`, hide active overrides from the issue list and write `quality_issue_*` events; issues also disappear automatically when the underlying registry state is fixed and the snapshot is recomputed. Timeline drawer items now normalize `registry_admin_events`, `device_registration_events` and `device_account_events` for device/person/binding/session/claim into source, actor, reason, related entity ids and changes, including transfer before/after owner changes and revoked session references; person/identity admin mutations write `person_created`, `person_updated`, `identity_added`, `identity_verified` and `identity_deleted`. `scripts/registry_workflow_smoke.py` is the live A-F workflow smoke for this layer. `device_user_bindings` remains the authoritative relation; `registry_assets.assigned_person_id` and inventory binding fields are only synced through `RegistrationService`.
- 2026-06-10/26 update: `/api/web/admin/registry` includes safe `ui_users` account/link fields for the Registry Management Center, and `POST /api/web/admin/registry/ui-users/{user_login}/link-person` links an existing UI account to a RegistryPerson through a verified `registry_person_identities(provider='ui_login')` row with collision-safe 409 behavior and registry admin event audit. Migration `131` enforces unique `lower(trim(ui_users.user_login))`; web auth/session code stores new UI logins lower-case and resolves account links through verified `ui_login` only. `/app/admin/registry` People shows linked UI accounts and has a `UI login` action.
- 2026-06-14 update: `registration_pending` account sessions are gate/polling state only for agent registration. `TicketAccountAccessService.can_create_ticket()` permits normal agent ticket create only for `confirmed_binding` and `verified_other_account`; existing pending-status ticket view/list paths remain scoped by session/person for compatibility.
- 2026-05-06 P3.2 update: `SupportTicketOperationSnapshot` and diagnostic timeline rows now expose operation lifecycle hints (`duration_ms`, retry counters, `retryable`, error code/category, trace id and `/api/operations/{operation_id}` details URL) from existing `operations` columns or event payloads. This is read-only enrichment for `/app/tickets` latest-operation and diagnostic-result chips; operation transitions and retry/run behavior remain unchanged.
- 2026-06-13 Registry Visibility Foundation Phase 3 UI slice: `/app/admin/registry` now has the `Аудитории · P1` tab backed by typed `fetch/create/update/archive/set/preview` audience-group helpers in `webapp/src/features/admin/api.ts` and `webapp/src/features/admin/registry/registry-audience-groups-tab.tsx`. The known Registry `window.prompt` paths were replaced by `registry-bulk-action-dialog.tsx`, `registry-link-ui-user-dialog.tsx`, `registry-reason-dialog.tsx` and a preselected-person bind dialog flow, so bulk device/person/session operations, quality actions and UI-login linking use Russian-first dialogs with searchable pickers, required reason, preview-before-apply where supported and normalized result reports. Guard tests live in `webapp/src/pages/admin/registry-page.test.tsx`; live browser signoff remains the Phase 3 exit gate.
- 2026-06-13 Registry access-groups Phase 3 addendum: `/app/admin/registry` also has `Группы доступа · P1` in `webapp/src/features/admin/registry/registry-access-groups-tab.tsx`. It reads `/api/web/admin/access/summary` through the existing `webapp/src/features/access-control/api.ts` helper and links to `/app/admin/access`; it intentionally does not duplicate access-group mutation UI or create a second RBAC source of truth.
- 2026-06-13 Registry Visibility Foundation Phase 4 start: `webapp/src/features/admin/registry/registry-requests-tab.tsx` now renders a registration approval diff for each claim in `/app/admin/registry` -> `Заявки` using the already loaded Registry payload. The diff shows current device/binding context, claimed person, claimed department/location labels, proposed identity, binding type and conflict/blocker before approve/reject buttons. `webapp/src/features/admin/registry/registry-policies-tab.tsx` also exposes `registration.department_mode` and `registration.location_mode` as first-class `Политики · P1` selects backed by the existing Registry policy preview/save endpoints; guard coverage is in `webapp/src/pages/admin/registry-page.test.tsx`.
- 2026-06-14 Registry browser registration strict-policy payload: `/app/device/register` now loads `/api/registry/options` department/location picker options through the narrow web-session cookie bridge in `server/auth/middleware.py` and sends selected `department_id` / `location_id` to `POST /api/web/registry/browser-pairings/{pairing_id}/registration/confirm`; `server/web_api/registry_handlers.py` passes only those optional ids into `BrowserPairingService.confirm_registration_pairing_for_web_user()`, and `RegistrationService` remains the validator for required-existing policy.
- 2026-06-15 Browser-pairing web-session role fix: `server/web_api/registry_handlers.py` uses `WEB_BROWSER_PAIRING_ROLES = ("admin", "support", "user")` for `POST /api/web/registry/browser-pairings/lookup`, `GET /api/web/registry/browser-pairings/{pairing_id}` and both confirmation endpoints. `BrowserPairingService.confirm_login_pairing_for_web_user()` remains the ownership gate, so admin/support web sessions can confirm only when their actor identity resolves to a registry person with an active primary/shared/responsible binding for the pairing device. Regression coverage is in `server/tests/test_registration_api.py::test_admin_web_session_confirms_login_pairing_for_linked_registry_identity`.
- 2026-06-20 Requester critical-defect closure: `server/requester/identity_service.py` bootstrap now projects `primary_device`, `primary_device_resolution`, runtime-derived device `online` state and ordered `next_actions[]`; `server/app/api/serializers.py` exposes requester ticket `actions`, and `server/web_api/requester_handlers.py` enforces those actions before requester messages. `webapp/src/features/requester/dynamic-form/index.tsx` is the requester value/schema validator for hidden-field omission, null `visible_when.equals`, email/url/number min-max/text length/pattern/options and condition-cycle guards. `/app/requester/devices/link` now lazy-loads `RequesterDeviceLinkPage`; direct pairing loads are idempotent per id, `active` status is connected, and `/app/requester/new?intent=device_owner_change` opens the owner-verification flow. Regression coverage is in `server/tests/test_requester_workspace_api.py`, `webapp/src/features/requester/*.test.ts`, `webapp/src/pages/requester/*.test.tsx` and `webapp/src/features/request-template-studio/*.test.ts`.
- 2026-06-20 Requester remaining-defect closure: `webapp/src/pages/requester/new-request-page.tsx` no longer selects the first visible form or `devices[0]`; category selection is explicit unless a typed system intent or confident catalog match resolves a `request_template_key`, and self/on-behalf/no-device/no-profile/manual-triage availability is computed separately. `webapp/src/pages/requester/home-page.tsx` also trusts only server `primary_device`, while `webapp/src/pages/requester/tickets-page.tsx` refreshes active detail after mutations independently from `can_attach_files` and uses server `actions.can_reopen`. `server/web_api/requester_handlers.py` now passes runtime state into `/api/web/requester/devices`, and `server/requester/identity_service.py::get_device_detail()` serializes the same runtime-derived `online=true/false/null` state as bootstrap/list. `server/tickets/form_catalog.py::validate_form_submission()` now enforces server-side dynamic field constraints for forged payloads: number min/max, text min/max length, regex, email, http/https URL, visible-field required values and option membership while still omitting hidden fields. `PageShell` defaults to `div` so the app shell owns the main landmark. Regression coverage is in `server/tests/test_requester_workspace_api.py`, `server/tests/test_ticket_form_packs.py`, `webapp/src/pages/requester/*.test.tsx`, `webapp/src/features/requester/queries.test.ts` and `webapp/src/components/ui-page/*.test.tsx`.
- 2026-06-22 Requester critical follow-up: `server/web_api/requester_handlers.py` preview/create now resolve self-mode device context through `RequesterIdentityResolver.resolve_primary_device()` and no longer treat any allowed binding as a valid primary device for normal requester forms. No-device requester tickets are stored as `tickets.device_id=NULL`; migration `127` also allows `ticket_events.device_id=NULL`, while `create_flow.py` skips device-bound registration/account-session lookups for browser no-device context and persists explicit `custom_fields.no_device` plus `primary_device_resolution`. `server/tickets/form_catalog.py` evaluates `visible_when` against multi-select values element-by-element. `server/registry/profile_schema_service.py` and `server/requester/identity_service.py` preserve requester-safe `section` / `order` profile layout metadata without exposing technical identities. Regression coverage is in `server/tests/test_requester_workspace_api.py`, `server/tests/test_ticket_form_packs.py` and `webapp/src/pages/requester/tickets-page.test.tsx`.
- 2026-06-19 Requester devices Phase J: `/app/requester/devices` and `/app/requester/devices/link` now use `webapp/src/pages/requester/devices-page.tsx` for device cards, localized activity/version/status labels, safe device detail, code lookup, direct link preview, confirm result states, incomplete-profile link availability and owner-check request handoff. The page reuses existing requester/device-pairing APIs and keeps raw pairing/binding/claim/session ids out of visible UI. Regression coverage is in `webapp/src/pages/requester/devices-page.test.tsx`, `webapp/src/app/router.test.tsx` and browser evidence under `artifacts/browser_live_validation/requester-ui-refactor-20260619/`.
- 2026-06-18 Web-first device linking R4 update: `/app/requester/devices` owns manual device-link code lookup and direct `pairing_id` preview for requester users through the existing web browser-pairing APIs. `server/web_api/registry_handlers.py` no longer blocks `registration/confirm` on requester profile completion; `BrowserPairingService.confirm_registration_pairing_for_web_user()` builds the claim profile snapshot from the resolved `RegistryPerson` when present or from the authenticated web account otherwise. `RegistryPolicyService` defaults to automatic first non-conflicting binding approval while preserving manual admin approval policy. Regression coverage is in `server/tests/test_registration_api.py`, `server/tests/test_requester_workspace_api.py`, `webapp/src/pages/requester/devices-page.test.tsx`.
- 2026-06-19 Requester profile Phase I: `/app/requester/profile` and `/app/requester/profile/setup` now use `webapp/src/pages/requester/profile-page.tsx` plus `webapp/src/features/requester/profile-runtime/index.tsx` for read/edit/setup modes, phone-or-internal-extension completion, safe custom fields, unsaved-change protection and admin-preview parity. `server/requester/identity_service.py` and `server/registry/profile_schema_service.py` project/store `internal_extension` as controlled metadata while preserving requester-safe schema projections. Regression coverage is in `server/tests/test_requester_workspace_api.py`, `webapp/src/features/requester/profile-runtime/profile-runtime.test.tsx`, `webapp/src/pages/requester/profile-page.test.tsx` and `webapp/src/pages/admin/registry-page.test.tsx`.
- 2026-06-19 Web cabinet and password-reset queue update: `/app/device/register` keeps the controlled department/location picker ids in the registration-confirm request and redirects successful confirmations to `/app/requester/devices`; the agent GUI exposes direct web-cabinet/login/password-reset links. `POST /api/web/session/password-reset-requests` creates generic pending rows in `ui_password_reset_requests`, and `/api/web/admin/registry/password-reset-requests*` lets admins list and complete them by setting a new UI-user password through `PasswordResetRequestService`. Regression coverage is in `server/tests/test_web_session_api.py`, `pc_agent/tests/test_account_gate.py`, `webapp/src/features/auth/register-page.test.tsx`, `webapp/src/pages/device-pairing/device-pairing-page.test.tsx` and `webapp/src/pages/admin/registry-page.test.tsx`.
- 2026-06-15 Web-first registration R7 production context: `server/registry/service.py` projects focused people/department/service/asset production context into the admin registry snapshot from existing rows and controlled metadata, and generated quality issues now cover missing person department/location, missing asset owner/responsible and pending department confirmation. `server/web_api/registry_handlers.py` accepts/audits the controlled admin person metadata fields `position`, `workplace_label`, `internal_extension` and `manager_person_id`. Regression coverage is in `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_registry_quality_remediation.py`, `server/tests/test_registry_web_api.py` and `webapp/src/pages/admin/registry-page.test.tsx`.
- 2026-06-15 Web-first registration R8 request-form context: `server/requester/identity_service.py` builds server-owned requester context v1 with safe profile/device/form-prefill/routing facts plus stable custom-field aliases, and `server/web_api/requester_handlers.py` recomputes that context for preview/create instead of trusting browser context hints. Ticket create stores `custom_fields.requester_context_snapshot` plus aliases such as `requester_department_id`, `requester_location_id`, `requester_device_id`, `requester_asset_id`, `requester_binding_id` and `requester_account_mode`, so routing policies can match resolved registry context. Regression coverage is in `server/tests/test_requester_workspace_api.py` and `webapp/src/pages/requester/new-request-page.test.tsx`.
- 2026-06-15 Web-first registration R10 admin moderation: `server/registry/service.py` projects schema-aware `people[].profile_completion` into `GET /api/web/admin/registry`, and `webapp/src/features/admin/registry/registry-people-tab.tsx` renders Russian profile-completion status/missing field labels in the People tab. Existing admin registry workflows cover pending claim approval/rejection, UI-user/person links, duplicate merge preview/apply, transfer-owner preview/apply, account-session revoke and timeline audit for `device`, `person`, `binding`, `account_session` and `claim`. Regression coverage is in `server/tests/test_registry_registration_snapshot.py`, `server/tests/test_registry_web_api.py`, `server/tests/test_registry_people_admin.py`, `server/tests/test_registry_admin_previews.py`, `server/tests/test_registry_timeline_admin.py`, `server/tests/test_registration_api.py` and `webapp/src/pages/admin/registry-page.test.tsx`.
- 2026-06-16 Primary Agent PA9 admin center UI: `/app/admin/registry` is now titled `Центр регистрации и привязок`; `webapp/src/features/admin/registry/registry-overview-tab.tsx` adds scenario-first queues for pending device links, ownership conflicts, users without primary agent, devices without owner, incomplete profiles, duplicate identities, active sessions tied to transferred/inactive bindings and unlinked UI accounts. The cards reuse existing claim/device/person/session/quality drawers and RBAC password reset/link-person surfaces; no duplicate registry mutation path was added. `webapp/src/pages/tickets/detail-page.tsx` renders internal on-behalf context from `SupportTicketDetail.ticket_context` and diagnostic target data, showing creator, affected user, target device, reason and diagnostic source/status. Regression coverage is in `webapp/src/features/admin/registry/registry-overview-tab.test.tsx`, `webapp/src/pages/admin/registry-page.test.tsx` and `webapp/src/pages/tickets/detail-page.test.tsx`.
- 2026-06-16 Primary Agent PA10 emergency form availability: `server/tickets/form_catalog.py` normalizes request-form `availability_policy` and compatibility booleans for `available_without_completed_profile`, `available_without_agent_binding`, `requires_manual_triage`, `contact_required` and `allowed_for_anonymous=false`. `server/web_api/requester_handlers.py` resolves this policy for requester preview/create before profile and agent-binding gates, requires reachable contact for emergency forms, blocks normal no-agent forms with `REQUESTER_AGENT_REQUIRED`, stores manual-triage/no-primary-agent diagnostic evidence and suppresses device autorun when no valid target exists. `server/tickets/routing_service.py` sends manual-triage availability-policy tickets to `servicedesk_l1` before ordinary template/default routing. Regression coverage is in `server/tests/test_ticket_form_packs.py`, `server/tests/test_requester_workspace_api.py` and `webapp/src/pages/requester/new-request-page.test.tsx`.
- 2026-06-20 requester-safe form pack projection: `server/tickets/form_pack_handlers.py` now uses `server/tickets/form_catalog.py::requester_safe_form_pack()` for `/public_api/ticket_forms/current`, so legacy preferred `request_forms` rows can remain intact for admin/versioning while public requester-facing payloads normalize the old `Каталог заявок` title to `Каталог обращений`. Regression coverage is in `server/tests/test_ticket_form_packs.py`.
- 2026-06-15 Web-first registration R12 compatibility: `server/config.py` adds `PROFILE_COMPLETION_REQUIRED=true` by default; `server/requester/identity_service.py` keeps missing profile fields visible while making `profile_completion.blocks` and requester create feature flags policy-aware, and `server/web_api/requester_handlers.py` follows those blocks for preview/create. Existing pending agent claims are projected into requester bootstrap and admin registry rows, while `BrowserPairingService.expire_stale_pairings()` and `AccountSessionService.expire_stale_sessions()` provide explicit maintenance cleanup for expired browser pairings and temporary account sessions without deleting audit state. Regression coverage is in `server/tests/test_requester_workspace_api.py`, `server/tests/test_registration_api.py`, `server/tests/test_account_session_service.py` and `webapp/src/pages/requester/new-request-page.test.tsx`.
- 2026-06-16 Primary Agent PA8 GUI password login: `server/web_api/registry_handlers.py` adds agent-only `POST /api/registry/agent/account-sessions/login`; `AccountSessionService.create_gui_password_session()` creates a `confirmed_binding` session only when server-authenticated user credentials resolve through verified `ui_login` to a person with an active binding on the current agent device. Migration `122` extends `device_account_sessions.verification_method` with `gui_password`. Wrong passwords return `INVALID_CREDENTIALS`; valid users on another device return `ACCOUNT_SESSION_DEVICE_MISMATCH` without creating a session or rebinding the device. Regression coverage is in `server/tests/test_registration_api.py`.
- 2026-05-07 P4/P5/P6.1/P6.2 update: `/app/tickets` central closure guidance now has actionable evidence/worklog controls backed by `GET /api/web/support/tickets/{ticket_id}/passport/evidence-candidates`, `POST /passport/evidence/link`, `POST /passport/evidence` and typed `POST /api/web/support/tickets/{ticket_id}/worklogs` for httpOnly web-session auth; operation cards expose typed details/cancel/retry affordances from `SupportTicketOperationSnapshot` (`can_cancel`, `can_retry`, action URLs, disabled reasons and `policy_labels`) and execute support-browser cancel through `POST /api/web/support/operations/{operation_id}/cancel` while token clients keep `/api/operations/{operation_id}/cancel`, plus policy-aware retry through `POST /api/operations/{operation_id}/retry` or ticket-scoped `POST /api/tickets/{ticket_id}/operations/{operation_id}/retry`. Retry validates ticket/device/auth/policy/consent/replay context, creates a new operation with `retry_of_operation_id`, and writes `operation_retried` into the timeline.
- The UI is now organized around one working catalog: a left navigator, a central form editor, and a right field-parameters panel with shared height and independent scrolling.
- Clicking a form opens its fields directly; clicking a field opens its parameters. The builder keeps `title` / `description` of the catalog in a hidden service section so the main flow stays focused on forms.

## 2026-04-17 Trace overlay

- `server/observer/service.py` projects a technical trace overlay over existing domain data instead of replacing ticket/problem entities.
- PostgreSQL projection writes for `observer_spans` and `observer_span_links` are chunked under asyncpg's bind-parameter ceiling; large ticket-root traces must be projected in batches, not as one giant `INSERT ... VALUES`.
- `server/observer/runtime.py` keeps hot traces fresh in the background by scanning newly committed source rows and re-projecting only changed trace ids.
- New observer storage lives in `observer_traces`, `observer_spans`, `observer_span_links`, `observer_error_occurrences`, and `observer_error_signatures` (migration `052_observer_trace_overlay`).
- Canonical observer docs: `server/docs/OBSERVER_LAYER.md` and `server/docs/OBSERVER_AUTHORING_RULES.md`.
- Tech API now exposes `GET /api/admin/tech/traces`, `GET /api/admin/tech/traces/runtime`, `GET /api/admin/tech/traces/{trace_id}`, `POST /api/admin/tech/traces/rebuild`, `GET /api/admin/tech/signatures`, `GET /api/admin/tech/signatures/{error_signature}`, `GET /api/admin/tech/observer/search`, and `GET /api/admin/tech/diagnostics/bundle`.
- The projection bridges `operations`, `ticket_events`, `device_events`, `agent_runtime_audit`, and optional agent-side `action_trace` search for drilldown near failures.

## 2026-04-19 Observer v2

- `tickets.observer_root_trace_id` (migration `053_ticket_observer_root_trace`) is the technical anchor for a full ticket lifecycle trace; server-originated ticket events resolve to this root instead of ad-hoc random trace ids.
- `server/observer/service.py` can project a ticket-root trace by `ticket_id`, even if historical source rows were written with multiple old trace ids; the resulting detail includes a synthetic `ticket.lifecycle` root span and nested operation/event spans.
- `server/observer/runtime.py` now discovers hot ticket-root traces plus missing historical projections, so archive tickets no longer rely on manual rebuild as the normal path.
- Tech API and `/admin` now expose degradation search (`GET /api/admin/tech/degradations`) alongside trace/signature drilldown, with first-class filters for `min_duration_ms`, `min_retry_count`, and `lookback_hours`.
- Tech API now also exposes quick diagnosis via `GET /api/admin/tech/observer/quick`, and support workspace gets ticket-scoped observer summary via `GET /api/tickets/{ticket_id}/observer`.
- `/admin` now shows a quick observer dashboard for operators, while `/support` gets a dedicated `Трасса` drawer tab with root trace, related traces, signatures and recent occurrences.
- `/app/admin/observer` trace detail surfaces `attrs_json.source_counts` as evidence-source counters and diagnostics bundle counters for agent actions, runtime audit, logs and related traces; `?trace_id=...` deep links from support cards select the intended trace and load its detail/bundle through typed observer queries; projected/hot traces that no longer have persisted detail return typed empty detail/bundle payloads instead of HTTP 404 so browser live signoff is not blocked by expected projection gaps; live coverage closure is checked by `scripts/run_observer_canary_suite.py` with JSON/Markdown reports that are now required by `pc_client.live_evidence.v2` manifests.
- `/api/web/admin/observer/quick|traces|traces/{trace_id}` enrich trace rows with human-readable ticket/device/operation/error context (`ticket_code`, `ticket_title`, `device_hostname`, labels and display title/subtitle), and trace search now matches projected `attrs_json` so `q=T-000520` can find the ticket-root trace without knowing the UUID-like `ticket_id`.
- `/api/web/admin/observer/traces/{trace_id}` and `/api/web/admin/observer/trace-detail/{trace_id}` now include `explanation`: launch source, actor, tool/module/preset metadata, human error diagnosis, launch path, next actions, agent status and debug refs. Missing projected detail returns `trace.status=missing` with zero spans/errors, not 404. Operation stage spans expose `stage_label`, `stage_state`, `stage_note` and `is_failure_stage` so intermediate `queued` stages do not look like separate failures when the terminal stage failed later.
- 2026-06-23 update: support message send, support status changes and support assignment changes now write best-effort web-cabinet Observer traces from `server/web_api/support_handlers.py`: `source=support_chat`, `event_type=support_message_sent`; `source=support_status`, `event_type=support_status_changed|resolution_confirmation_requested`; and `source=support_assignment`, `event_type=support_assignment_changed`. `server/web_api/dto/support.py` adds `observer.web_flow.support_chat`, `observer.web_flow.support_status` and `observer.web_flow.support_assignment` for typed support detail. Payloads are limited to visibility/status/present/confirmation/assignment flags and counts and intentionally exclude raw support message text, resolution text, root-cause text, public/internal comments, reason/comment text, actor ids, assignee ids, tokens and raw confirmation prompt content. Coverage is `server/tests/test_web_support_api.py::test_web_support_message_writes_observer_event_without_raw_text`, `server/tests/test_web_support_api.py::test_web_support_resolve_writes_status_and_confirmation_observer_events_without_raw_reason`, `server/tests/test_web_support_api.py::test_web_support_assign_writes_observer_event_without_raw_actor_or_reason` and `server/tests/test_web_support_api.py::test_web_support_internal_note_does_not_stop_frt_until_public_reply`.
- 2026-06-17 update: requester ticket create now writes diagnostic target failure traces from `server/web_api/requester_handlers.py` using `source=requester_ticket_create`, `event_type=diagnostic_target_missing|diagnostic_target_offline|diagnostic_target_ambiguous` and matching `DIAGNOSTIC_TARGET_*` error codes when `server/tickets/diagnostic_target.py::resolve_ticket_diagnostic_target()` cannot produce a dispatch target or reports an offline/ambiguous primary agent. Payloads carry only source/status/reason and boolean evidence flags, never raw requester login, token, target id or person id values. Coverage is `server/tests/test_observer_web_cabinet.py::test_web_requester_create_missing_diagnostic_target_writes_observer_event`.
- Everyday authoring should happen through the direct form -> field -> parameter flow; raw JSON preview, visibility rules, placeholders, and other power-user controls live in advanced sections instead of the main path.
- The UI now separates request-form lifecycle actions: `Сохранить черновик` writes `form_builder_drafts` without changing the active pack, `Проверить публикацию` returns a typed validation report, `Опубликовать` creates a published `request_forms` pack version, and version cards can make an existing version preferred for rollback/promotion.
- The canonical operator guide for this UI is `server/docs/REQUEST_FORM_BUILDER.md`.

## 2026-04-21 Web cutover and pytest harness cleanup

- `server/web_api/session_handlers.py` now returns `default_workspace`, `available_workspaces`, `permissions` and `permissions_version` in `GET /api/web/session/me`, and that payload is the source of truth for role-aware redirect and element-visibility logic in the new `/app/*` shell.
- `webapp/src/features/auth/workspace-access.ts`, `webapp/src/app/navigation.tsx`, `webapp/src/app/router.tsx`, `webapp/src/app/layouts/app-shell.tsx` and `webapp/src/components/shell/*` own the role-aware `/app` entrypoint, `/app/support` Work Center, `/app/admin` Admin Center, workspace access gates, permission-filtered workspace/domain navigation, local admin domain tabs, last-workspace-path switching and safe fallback redirects for sessions that only have one workspace.
- `server/static_pages/cutover.py`, `server/static_pages/handlers.py`, and `server/config.py` now prepare and enable legacy-shell cutover through `WEBAPP_CUTOVER_LOGIN_ENABLED`, `WEBAPP_CUTOVER_SUPPORT_ENABLED`, and `WEBAPP_CUTOVER_ADMIN_ENABLED`; redirect в новый shell активируется по умолчанию при наличии built bundle, а для `/support` и `/admin` дополнительно требуется включённый login cutover; explicit legacy escape remains `?legacy=1`, while `WEBAPP_CUTOVER_*=false` in `server/.env` is the rollback path.
- `server/tests/conftest.py`, `server/websocket/device_outbox_sender.py`, `server/server.py`, and `pc_agent/ws_agent.py` now close websocket-heavy test/runtime paths more gracefully; on Windows pytest also switches to `WindowsSelectorEventLoopPolicy`, which removes the non-fatal `unexpected connection_lost() call` tail noise from websocket suites.
- `webapp/scripts/remote-browser-signoff.mjs` plus `pnpm --dir webapp run check:remote:webapp -- --base-url http://example.test:8666` are now the canonical live browser signoff for the new `/app/*` shell after release: the script logs in, verifies `/app` default routing, checks `/app/admin` and `/app/support`, confirms Russian UI strings, validates raw redirects `/login|/admin|/support` and `?legacy=1` escape, and fails fast on browser console/page errors.

## 2026-04-28 Ticket-bound playbook launch

- Typed support playbook launch starts from `server/web_api/support_handlers.py`, `server/web_api/dto/support.py`, `server/routes.py`, and `server/app/services/playbook_engine.py`.
- `GET /api/web/support/tickets/{ticket_id}/playbooks` lists published playbook versions with version id, required tools, missing tools, missing required params, block count and readiness for the resolved diagnostic target device, plus recent ticket playbook runs and step errors.
- `POST /api/web/support/tickets/{ticket_id}/playbooks/run` starts the selected version with `trigger_type=support_ticket` and a ticket-bound context including `diagnostic_target` for observer correlation, but first blocks `PLAYBOOK_PREFLIGHT_BLOCKED` if the playbook references tools absent from builtin/device/server-module sources or has unresolved required params.
## 2026-05-09 requester timeline projection

- `server/tickets/requester_timeline.py` owns requester-facing projection for `ticket_events`: safe Russian `requester_timeline_text`, typed `requester_timeline_kind`, compact `requester_timeline_payload`, plus optional icon/style.
- `server/app/repos/ticket_events_repo.py::get_events()` returns UI/API timeline items in global `(created_at, id)` order. `agent_seq` remains an agent replay/idempotency coordinate and must not be the primary requester/support timeline sort key.
- `server/tickets/handlers.py` attaches those fields to requester/agent ticket event serialization and uses the same projection to filter internal/debug/noise events out of requester history.
- `server/web_api/support_handlers.py` and `server/web_api/dto/support.py` carry the same projection fields through typed support timeline DTOs so React support/requester surfaces can render the same text without raw event/status/tool payloads.
- Legacy requester page code in `server/ticket.js` must prefer `requester_timeline_text` and hide events with no requester projection in requester-facing history.

## 2026-05-09 support workspace realtime updates

- `/app/tickets` (`webapp/src/pages/tickets/list-page.tsx`) consumes the existing typed realtime bridge from `webapp/src/shared/realtime/client.ts`, subscribes only to the selected ticket, and invalidates selected workspace, active standalone timeline tabs, queue data and passport evidence candidates on `ticket_event_committed` / `operation_updated`.
- Backend producers remain the existing `server/web_api/support_handlers.py`, `server/tickets/handlers.py`, `server/tools/service.py` and websocket operation result publishers; do not add a second polling/SSE transport for this page.

## 2026-05-09 Remote Assist MVP

- Remote Assist server flow starts from `server/remote_assist/service.py`, `server/remote_assist/handlers.py`, `server/remote_assist/signaling.py`, `server/remote_assist/policy.py`, `server/remote_assist/media.py`, `server/remote_assist/features.py`, `server/remote_assist/ice.py`, `server/app/repos/remote_access_repo.py`, and routes registered in `server/routes.py`.
- Schema is added by `server/app/db/migrations/versions/20260509_1200_071_remote_assist.py`: `remote_access_sessions` owns the ticket/device/operator-bound lifecycle, and `remote_access_events` owns audit events.
- Remote Assist requests now create a canonical `UserConsentRequest` first (`subject_type=remote_assist`, `subject_id=remote_access_sessions.id`). Browser requester approval/denial and agent GUI approval/denial share that same `consent_id`; browser approval never receives agent signaling tokens, ICE secrets or SDP data.
- After the canonical consent is approved, `server/remote_assist/service.py` delivers `remote_assist.request` through the existing DeviceOutbox / Protocol V3 command path with the approved `consent_id` and `consent_status=approved`. Maria Agent then performs the technical `/api/remote-assist/{session_id}/approve` step to receive the short-lived agent signaling token. The dedicated signaling endpoint is `/ws/remote-assist/{session_id}` and validates short-lived role tokens before relaying WebRTC envelopes.
- Support-facing ticket timeline labels are projected in `server/web_api/support_handlers.py`; resolution passport summaries read `remote_access_sessions` in `server/tickets/passport_service.py`.
- Policy/config entry points are `server/access_control/catalog.py` (`remote_assist.request`, `remote_assist.view`, `remote_assist.control`, `remote_assist.file_transfer`, `remote_assist.clipboard`, `remote_assist.elevated`, `remote_assist.unattended`), `server/remote_assist/policy.py` mode gates, and `server/config.py` (`REMOTE_ASSIST_*`, ICE/TURN settings). `server/remote_assist/ice.py` generates coturn REST-style short-lived credentials from the server-only shared secret; do not log raw tokens, full SDP, or TURN static secrets.
- Post-MVP control uses WebRTC data channel `control` only for `interactive_control`/`elevated_admin` sessions. Clipboard auto-sync is allowed for screen-sharing sessions when the explicit capability, policy and `remote_assist.clipboard` permission are present; the support request modal preselects it by default for screen modes. File transfer uses a separate `file-transfer` data channel for approved policy-gated sessions and is not tied to control mode; the support request modal preselects it by default when `remote_assist.file_transfer` is present. The support viewer can send files by button, drag/drop or browser file-paste, and Maria Agent saves validated chunks locally under `Downloads\Maria Remote Assist`. Signaling accepts `control.state`/`control.error`, `clipboard.state`/`clipboard.error`, and sanitized `file.transfer`/`file.error` audit messages for ticket timeline events; mouse/keyboard, clipboard contents and file bytes stay peer-to-peer and are not written to the server.
- Field hardening after `3.1.48`: `server/remote_assist/signaling.py` keeps the in-memory peer registry as the source for best-effort `session.end` pushes when `/remote-assist/{session_id}/end` is called over HTTP, and `server/remote_assist/media.py` includes a `smooth` 1280x720@15fps profile for more responsive control sessions. Elevated Windows targets such as Task Manager remain outside normal `interactive_control` because they require the separate elevated/admin mode boundary.
- `elevated_admin` is now a control-capable mode in `server/remote_assist/policy.py`, still gated by `remote_assist.elevated`, `REMOTE_ASSIST_ELEVATED_ADMIN_ENABLED` and `REMOTE_ASSIST_ALLOWED_MODES`; default support operators include `remote_assist.elevated` so the active viewer can request the separate elevated consent flow. The server does not run elevated code; it only authorizes/audits the mode and passes `mode=elevated_admin` to Maria Agent, where the UAC helper boundary lives.
