# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8666

# Database Configuration
# PostgreSQL connection URL for async SQLAlchemy
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://chatbot:chatbot@127.0.0.1:5432/pc_client
PC_CLIENT_DB_POOL_SIZE=10
PC_CLIENT_DB_MAX_OVERFLOW=20
PC_CLIENT_DB_POOL_TIMEOUT_SEC=30
PC_CLIENT_DB_POOL_RECYCLE_SEC=1800

# Enable/disable database persistence
# Set to "false" to run without database (in-memory only)
ENABLE_DB_PERSISTENCE=true

# Logging
LOG_LEVEL=DEBUG

# Remote Assist
# JSON list of ICE server objects. Keep TURN static secrets server-side only.
# Example: [{"urls":["stun:turn.example.local:3478"]},{"urls":["turn:turn.example.local:3478?transport=udp"],"credential_mode":"time_limited_hmac","ttl_seconds":3600}]
# Coturn REST shared secret; never expose this to frontend or agents.
